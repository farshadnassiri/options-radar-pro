# دفتر کار

- ۱۴۰۵/۰۷/۰۱ (چهارم) — **R5-13: دروازهٔ ورودِ ریزمعامله، و نگهبانی که دور زدنش را می‌بندد.** خواستهٔ صاحب پروژه از تبِ خروجی فراتر رفت: «هر جا دیتای تاریخی یا ریز گرفته می‌شود — برای استراتژی، رصد، جدول، نمودار — کامل و دقیق باشد.» بررسیِ کلِ رابط نشان داد سرور از چهار دور ممیزی به این‌سو `complete` · `verified` · `shortfall` · `emptyBoth` · `throttled` را با هر پاسخ می‌فرستد، ولی **فقط همان یک تب** می‌خواندشان: «نگاه باز» `item.rows || []` برمی‌داشت، «بک‌تست» `payload.items` خام، و «بک‌تستِ سبد» یک جا `emptyBoth` را می‌خواند و جای دیگر نه. و چون پاسخِ سهمیه‌خورده **خطا ندارد** (`HTTP 200` با آرایهٔ خالی)، هیچ‌کدام نمی‌فهمید داده نیامده — نوارِ خالی «معامله‌ای نبود» خوانده می‌شد و رویش استراتژی ساخته و نمودار کشیده می‌شد. `ui/tape-intake.mjs` حالا تنها در است: `tapeVerdict` هفت حالت می‌دهد (سهمیه و خطا **مقدم** بر هر حکمِ بازار، چون دربارهٔ رسیدن حرف می‌زنند نه دربارهٔ بازار)، `tapeSummary` «قابلِ تکیه» را از «ردیف دارد» جدا می‌کند، و `tapeWarning` یک جمله برای همه. پنج تب از آن رد شدند و هر سه تبِ اولویت‌دار حکم را **نشان** هم می‌دهند، نه فقط می‌گیرند — و کشِ «نگاه باز» دیگر دادهٔ مشکوک را نگه نمی‌دارد، وگرنه یک اجرای سهمیه‌خورده تا پایان نشست تکرار می‌شد. **ولی نگهبان از خودِ تابع مهم‌تر است:** دستهٔ ۲۹۱ هر فایلی را که مستقیم `/api/trades` را صدا بزند رد می‌کند، با دو استثنای مستند. نسخهٔ اولِ همین نگهبان دو فایل را فقط به‌خاطر نامِ مسیر در یک **توضیح** گناهکار خواند؛ حالا پیش از پویش، توضیحات برداشته می‌شوند و خودِ این رفتار پنج ادعا دارد — نگهبانی که هشدارِ کاذب بدهد خیلی زود خاموش می‌شود. راست‌آزمایی: هر ۱۲ تب در مرورگرِ واقعی باز شد، `pageerror` صفر. دستهٔ ۲۹۱ با ۳۸ ادعا؛ ادعاهای پین‌شدهٔ ۰۴۱ و ۲۰۱ منتقل و **گسترده** شدند.
- ۱۴۰۵/۰۷/۰۱ (سوم) — **R5-12: یک قاعده که در دو جا دو پیاده‌سازی داشت.** اجرای دوبارهٔ همان هفته با کدِ تازه، دو اصلاح را تأیید کرد (**تأییدنشده ۸۵ → ۰** و «هر ابزار/روز مرجعِ روزانه داشت» از هشدار به ✔) ولی عددِ **ناقص ۶** تکان نخورد. علتش این بود که رواداریِ حجمِ باطل‌ها را فقط به `tapeMatchesDaily` داده بودم، و `coverageVerdict` در `core/data-export.mjs` همان مفهوم را با شرطِ برابریِ **دقیقِ** حجم جدا پیاده کرده بود — یعنی سرور شش ابزار/روز را «کامل» می‌دانست و بازبینی همان‌ها را «ناقص» می‌خواند. درسش از قبل در `NEXT.md` نوشته شده بود («عددی که در دو جا به دو راه حساب می‌شود، آزمونی می‌خواهد که همان دو راه را کنار هم بگذارد») و باز هم همان‌جا گیر کردم. حالا هر دو یک کران دارند، `dataExportBlankAudit` حجمِ باطل‌ها را می‌شمارد و حمل می‌کند، و `tradeGap`/`volumeGap` دیگر آنچه را توضیح دارد «کم‌داشته» نمی‌نویسند. **و یک اصلاحِ دوم در ابزارِ کنترل:** روزِ جاری تابلوی نهایی ندارد، پس نه در صورتِ کسرِ پوشش می‌آید نه در مخرجش — وگرنه هر خروجی‌ای که امروز را هم بگیرد محکوم به «پوششِ ناکافی» است. دستهٔ ۲۹۰ به ۲۶ ادعا رسید.
- ۱۴۰۵/۰۷/۰۱ (دوم) — **اولین هفتهٔ واقعاً کامل، و سه برچسبِ غلط که همان فایل لو داد (R5-11).** خروجیِ ۲۰۲۶۰۹۱۵ تا ۲۰۲۶۰۹۲۲: ۴۹۶ ابزار/روز، **۶۲۱٬۷۸۰ ریزمعامله**، و برای اولین بار **نیامد صفر · سهمیه صفر · خطا صفر · هیچ شکافی در زمان**. کلِ هفته داخلِ سهمیه جا شد — یعنی نرخِ ۳ در ثانیه و ترتیبِ `false`-اول کار کردند. سه برچسب ولی غلط بود، هر سه از جنسِ «فایل چیزی می‌گوید که نیست»: **(۱) شش ابزار/روزِ «ناقص» که کامل بودند** — کسریِ ادعاشده **دقیقاً** برابرِ حجمِ ردیف‌های باطلِ همان روز بود (۲=۲، ۶=۶، ۱۹۸=۱۹۸، ۵۱=۵۱، ۵=۵)، یعنی `qTotTran5J` حجمِ باطل‌ها را در خود دارد و ما درست بیرونش می‌گذاریم؛ شرطِ برابریِ **دقیقِ** حجم این را «کسری» می‌خواند. کران حالا «تا اندازهٔ ردیف‌های باطل، نه بیشتر» است، و **کمتر** بودنِ تابلو همچنان تطبیق نیست چون آن یعنی ما چیزی داریم که تابلو نمی‌شناسد. **(۲) ۶۸ ابزار/روزِ دارای داده که «خالی» خوانده شدند** — `unknown` خودش کلیدِ معتبرِ `EMPTY_STATUS` بود، پس برچسبِ «خالی، تأییدنشده» حتی با هزاران ردیف می‌نشست و `'داده آمد، تأییدنشده'` هیچ‌وقت دیده نمی‌شد. **(۳) ۸۵ ابزار/روزِ امروز که «نامعلوم» شدند** — تابلوی روزِ جاری تا تسویه نهایی نمی‌شود، ولی این فقط وقتی فهمیده می‌شد که فازِ بازار `open` باشد؛ حالا منبعِ `live` خودش می‌گوید، و برچسب به «روزِ جاری — تابلو هنوز نهایی نشده» عوض شد. دستهٔ ۲۹۰ با ۱۷ ادعا.
- ۱۴۰۵/۰۷/۰۱ — **R5-10: فازِ تدریجیِ سهمیه، و سه جایی که پرچمش گم می‌شد.** ناظر رشتهٔ **پیاپی** می‌شمرد و بستنِ سهمیه **تدریجی** است؛ در فازِ میانی هر پاسخِ موفق رشته را صفر می‌کند، پس ۵۷ ردیف «نیامد» برچسب گرفتند که قربانیِ سهمیه بودند. معیارِ سوم: **نسبت در پنجرهٔ کشویی** (۱۵ از ۲۵ مرجع‌دار)، که با یک موفقیت پاک نمی‌شود؛ و `state().suspects` ابزار/روزهایی را که **مدرکِ** حکم بودند نام می‌برد تا سرور برچسبشان را عوض کند. **پرچم سه بار در مسیر گم شد و هر سه فقط با هارنس دیده شدند** — عدد ۵۴ سه بار پشت سر هم تکان نخورد: `keepBetterTape` پرچم را مثل `attempts` دور می‌ریخت (حالا می‌چسبد، ولی فقط وقتی رکوردِ برنده بی‌ردیف باشد) · حلقهٔ توقفِ تب شرطش `if (items[pair.key]) continue` بود و همان ردیف‌های **خالیِ** پاسِ قبلی را رد می‌کرد، یعنی دقیقاً آن‌هایی که باید عوض می‌شدند · و استابِ قدیمی روی همان پورت زنده مانده بود، که نزدیک بود یک «خطای مثبتِ کاذب»ِ ناموجود گزارش کنم — **پورتِ تازه برای هر اجرا**. اجرای کنترل: ۵۲۲ تطبیق‌شده · ۰ نیامد · ۰ سهمیه، و حکمِ ابزار برای اولین بار «این فایل دادهٔ کاملِ بازه را دارد». دستهٔ ۲۸۹ با ۷۰ ادعا.
- ۱۴۰۵/۰۶/۳۱ — **سهمیهٔ بالادست: علتِ ۳۵۹ ابزار/روزِ «نیامد».** شکلِ بستنش همان چیزی بود که سه دور ممیزی را گمراه کرد: **`HTTP 200` با `{"tradeHistory":[]}`** — نه ۴۲۹، نه ۴۰۳؛ دقیقاً پاسخِ یک روزِ بی‌معامله. سه اجرای پیاپی روی همان ۳۰۰ ابزار/روز: ۲۹۴ داده و صفر خالی (۷۰۱ms) → ۱۷۸ و ۱۱۰ خالی → **صفر داده و ۳۰۰ خالی در پنج ثانیه** (۶۵ms). انباشتی است؛ درخواستِ تکیِ همان روزها کامل آمد، یعنی داده روی بورس بود. **مسیرِ تشخیص دو بار از دستم در رفت:** حکمِ «نرخ مقصر نیست» را از ۳۰ درخواست (دو ثانیه بار) گرفتم در برابر ۱٬۵۱۰ درخواستِ اجرای واقعی؛ و دستورهای بررسی را **بی هدر** نوشتم، ۴۰۳ِ فیلترِ سادهٔ curl گرفتم و «IP شما مسدود شده» تشخیص دادم — هر پنج حالتِ هدر ۲۰۰ دادند، از جمله هدرهای خودِ برنامه. پس `BLIND_STREAK = 40` اضافه شد: چهل ابزار/روزِ پیاپیِ خالی که هیچ راهی برای سنجششان نیست.

- ۱۴۰۵/۰۶/۳۱ — **دور پنجم، شش یافته.** `R5-01` شمارندهٔ تلاش دریافتِ اولیه را نمی‌شمرد، پس فایل پس از یک تلاشِ تکمیلیِ انجام‌شده می‌نوشت «هر ابزار/روز یک بار پرسیده شد» — یک شمارنده با دو قرارداد یعنی هیچ قراردادی؛ حالا هر دورِ پرسیدن یک واحد از دورِ اول، با یک نقطهٔ افزایش. `R5-02`/`R5-03` ابزارِ کنترل انتظارش را از **همان چیزی** می‌گرفت که قرار بود بسنجد (برگ‌های موجود، روزهای دیده‌شده)، پس برگِ حذف‌شده و روزِ حذف‌شده هر دو «۰ اشکال» می‌گرفتند؛ انتظار حالا فقط از برگ پوشش می‌آید و سنجش روزبه‌روز است. `R5-04` قاعدهٔ «پرچم را بین دورها عوض کن» بی‌اثر بود چون دورِ ساده به کشِ ۹۰۰ثانیه‌ایِ سرور می‌افتاد — `fresh` از `bust` جدا شد. `R5-05` کسری را سرور حساب می‌کرد و ستونِ پشتوانه را تب پر می‌کرد، پس ۳۶۲ ردیف عددِ بی‌پشتوانه داشتند؛ `chooseTape` حالا مرجعِ حکم را همراهش برمی‌گرداند. دستهٔ ۲۸۸.

- ۱۴۰۵/۰۶/۳۰ — **قفلِ دفترِ قراردادها برداشته شد** (R4-05). دکمهٔ «آماده‌سازی» هرگز فعال نمی‌شد: ساختِ دفتر روی ۱٬۳۷۴ از ۳٬۵۱۳ جست‌وجو بود و شمارِ کل هم **بالا می‌رفت** — شرطِ بازشدنِ قفل خودش در حال دور شدن بود، و پیامدش این که مسیرِ واقعیِ کلیک تا دانلود یک بار هم اجرا نشد. مرزِ تازه: **بی‌معنا قفل می‌کند، ناقص گفته می‌شود.** `exportGate` حالا `{blocking, warnings}` می‌دهد و تنها موردِ `blocking` دفترِ نسخه‌قدیمی است — که به پیمایش وابسته نیست و با بازسازی خودش باز می‌شود؛ قفلی که راهِ خروج دارد، برخلاف آن یکی. و نیمهٔ دومِ کار، که بی آن اصلاح خودش یک ایرادِ تازه بود: **آنچه قفل نمی‌کند باید نوشته شود** — وگرنه «قفل برداشته شد» به «انگار مشکلی نبود» ترجمه می‌شود. جملهٔ کنارِ دکمه و سطرِ «کم‌داشتهٔ دفتر هنگام خروجی». سه ادعای ۲۶۸ عمداً وارونه شدند. دستهٔ ۲۸۷ با ۲۰ ادعا.
- ۱۴۰۵/۰۶/۳۰ — بازآزماییِ پذیرش سه ایراد داد و دو تایش در ابزارِ کنترلِ خودم بود: `datedRow` فقط `dEven` را می‌خواند و پاسخِ درستِ `clientType` (تاریخش `recDate`) را دور می‌ریخت · ابزار فایلِ بریده از ۳۳ به ۱ ردیف را «کامل» خواند · و جدولِ پیوستهٔ سالم را رد کرد. یک معیار برای سه شکلِ خروجی، همیشه غلط است. دستهٔ ۲۸۶. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۳۰ — `tools/verify-export.mjs`: فایلِ خروجی را می‌خواند و حکم می‌دهد. شش کنترل، صفر وابستگی، و توزیعِ زمانیِ شکاف که بلوکِ پیوستهٔ خراب را جدا می‌کند. دستهٔ ۲۸۵ خواننده را با نویسندهٔ واقعی رفت‌وبرگشت می‌زند. دستهٔ ۲۸۵. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۳۰ — تلاشِ تکمیلیِ قابل پیگیری: `core/refill-queue.mjs` صف را فقط از کم‌داشته‌ها می‌سازد، مرتب بر ارزشِ جامانده، با فاصلهٔ فزاینده و سقفِ چهار تلاش. یک باگ که فقط شبیه‌سازیِ حلقه گرفتش: `keepBetterTape` شمارندهٔ `attempts` را دور می‌ریخت. دستهٔ ۲۸۴. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۳۰ — بازآزماییِ دور سوم سه ایراد داد و هر سه بسته شد: `/api/hist?kind=daily` رکوردِ تاریخِ دیگر را در جایگاهِ روزانه می‌داد · روزِ دریافت‌نشده از جدولِ پیوسته ناپدید می‌شد · صفرِ تأییدشدهٔ تابلو «مرجع نداریم» خوانده می‌شد. دستهٔ ۲۸۳. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — فایلِ واقعیِ ۲۰۲۶۰۶۲۲ تا ۲۰۲۶۰۹۲۰ باگِ نوبتِ قبل را لو داد: `GetClosingPriceDaily` برای روزِ بی‌جلسه رکوردِ آخرین جلسه را می‌دهد و `dailyExpectation` آن را مرجعِ آن روز می‌گرفت — ۸۵ ردیفِ فایل عددِ ساختگی گرفتند. `dEven` حالا سنجیده می‌شود. و مرجع یک بار از مصرف‌کننده می‌آید، نه برای هر ابزار/روز جدا. دستهٔ ۲۸۲. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — آزمونِ عملی چهار ایراد داد: توقف روی پاسخِ غیرخالیِ بریده (اهرم/۲۰۲۶۰۹۱۹: ۲٬۵۲۱ در برابر ۷٬۷۳۶)، پاک‌شدنِ دادهٔ موجود در تلاشِ دوباره، ستونِ بیرون‌ازجلسهٔ ناهماهنگ با پنجرهٔ انتخابی، و `hist/closing` بی وضعیتِ پوشش. `core/tape-choice.mjs` و `keepBetterTape` و دو ستونِ تازه. دستهٔ ۲۸۱. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — بستنِ حلقهٔ معیار پذیرشِ ۱: یکی‌شدنِ **نتیجه** کافی نبود، **وضعیتِ پوشش** هم باید یکسان برسد. نقطه‌های سنجشِ بک‌تستِ سبد (`/api/trades`) فقط `payload.rows || []` را می‌گرفتند؛ حالا که آن مسیر هر دو پرچمِ بالادست را امتحان می‌کند و `emptyBoth` می‌دهد، دور ریختنش یعنی نوشتنِ «این ابزار معامله نشد» برای ابزاری که فقط داده‌اش نرسیده — دقیقاً همان اشتباهی که بند ۱ ممیزی با نمونهٔ ۷٬۷۳۶ معاملهٔ اهرم نشانش داد. `fetchTape` حالا خطا و «خالی پس از هر دو مسیر» را جدا می‌شمارد و جملهٔ هر دو نقطهٔ مصرف آن را «تأییدنشده» می‌خواند، نه «بی‌معامله». و هر شش معیارِ عددیِ پذیرش با توابعِ واقعیِ برنامه و اعدادِ خودِ ممیزی بازتولید شدند: ۷٬۷۳۶ معامله و ۷۳٬۳۰۵٬۲۲۴ حجمِ مسیر تاریخی · ۱٬۸۰۰ ردیفِ خام به ۱٬۷۹۵ و ۶٬۶۶۰٬۴۲۹ · نوارِ یک‌ردیفی در برابر روزانهٔ هزارمعامله‌ای حکمِ «ناقص» با ۹٬۹۹۰ واحد حجمِ جامانده · پنجرهٔ ۹:۳۰ با ۱۸۰ سطلِ پیوسته که ۱۷۸ تایشان بی‌معامله و همه بی‌قیمت‌اند · ۲۰۱ و ۲۵ و ۶۱ شناسه بی گم‌شدن · ۲٬۵۰۰٬۰۰۳ ردیف در ۳ برگ با مجموعِ برابر. دستهٔ ۲۷۶ به ۲۳ ادعا رسید.
- ۱۴۰۵/۰۶/۲۹ — ساعت ورودی شد، جدول پیوسته شد، و فایلِ بزرگ دیگر بن‌بست نیست. پنجره یک **مقدار** شد که پالایه و شمع‌سازی و برگ راهنما از آن می‌خوانند؛ سطلِ خالی ردیف می‌گیرد ولی هیچ قیمتی نمی‌گیرد؛ و `splitSheets` جای پرتابِ سقفِ یک شیت را گرفت. دستهٔ ۲۸۰. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — بند ۵ و ۷: منبع جایگزینِ روزانه رکوردِ بی‌تاریخ و عکسِ پیش‌جلسه را می‌پذیرفت (اهرم/۲۰۲۶۰۹۱۹: `dEven=0`، قیمتِ دیروز)؛ `core/daily-trust.mjs` و `fallbackCovers` بستندش. و تب‌های تاریخچه و نگاه باز که `value.rows || []` می‌گرفتند، حالا خطای هر ابزار را جدا نگه می‌دارند. دستهٔ ۲۷۹. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — بند ۳: بازبینی فقط روی پاسخ‌های کاملاً خالی اجرا می‌شد، پس نوارِ یک‌ردیفی در برابر تابلوی هزارمعامله‌ای «داده آمد» می‌گرفت. حکم‌ها از سه به هفت رسید و حجم معیارِ اول شد. دستهٔ ۲۷۸. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — بند ۴: سقفِ ابزار بی‌صدا می‌بُرید (۲۰۱ شناسه → ۲۰۰). `insListOrReject` با ۴۱۳ رد می‌کند، `core/ins-batches.mjs` سمتِ مرورگر دسته می‌کند، و `slice(0, 180)` رول و اسکنر برداشته شد. دستهٔ ۲۷۷. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۹ — دو بندِ اولِ ممیزیِ «کامل‌بودنِ داده» بسته شد، و هر دو یک ریشه داشتند: **عددی که از دو راه می‌آمد، دو جواب می‌داد.** (۱) *اضافه‌شماری، نه فقط افتادگی.* نوار زندهٔ اهرم در ۲۰۲۶۰۹۲۰ شماره‌های ۲۲۹ تا ۲۳۳ را **دوبار** داشت، هر جفت با ساعت و قیمت و حجم و پرچمِ یکسان؛ نتیجه ۱٬۸۰۰ معامله و حجم ۶٬۶۶۴٬۴۸۶ در برابر ۱٬۷۹۵ و ۶٬۶۶۰٬۴۲۹ روی تابلو، و اختلافِ ۴٬۰۵۷ واحد دقیقاً جمعِ همان پنج تکرار. `normalizeTrades` فقط نگاشت و پالایش و مرتب‌سازی می‌کرد، پس تکرار تا شمع‌ساز و خلاصهٔ نوار می‌رفت و دوباره جمع می‌شد. `dedupeTrades` حالا معیارش برابریِ **همهٔ** میدان‌های نرمال‌شده است، نه فقط `sequence`: شمارهٔ صفر (یعنی «بالادست شماره نداد») هیچ ردیفی را نمی‌خورد، و دو ردیفِ هم‌شماره با محتوای متفاوت — اصلاح یا ابطال — هر دو می‌مانند و در `conflicts` علامت می‌خورند. حذفِ کورکورانه دقیقاً همان تضادی را پنهان می‌کرد که باید دیده شود. سه ادعای دسته‌های ۲۴۹ و ۲۶۵ و ۲۶۷ که متنِ کدِ جابه‌جاشده را می‌گزیدند به مسیرِ تازه بازنشانی شدند؛ هیچ ادعایی ضعیف نشد.
- ۱۴۰۵/۰۶/۲۹ — ماکتِ ششمِ بوم («صفحه، ساخته‌شده از کیت») پیاده شد. چهار تصمیم: (۱) **کاشی برای عدد، نوار برای شمارش** — ردیفِ هفت‌تایی شد چهار کاشیِ پولی/درصدی، و سه شمارشِ وضعیت («۱۲ نماد قیمت‌خورده»، «۱ سررسیدگذشته»، «شرطِ برقرار») به نوارِ خطیِ زیرش رفتند؛ چیزی حذف نشد، فقط وزنش عوض شد و دستهٔ ۲۷۵ همین را قفل می‌کند. شمارِ شرط **موقعیت** است نه شرط، چون یک موقعیت می‌تواند سه شرطِ هم‌زمان داشته باشد. (۲) گردیِ کنترل از قرصِ کامل به ۱۰ برگشت و کارت به ۱۴ — یک زبانِ شکلی، و قرص فقط برای تراشه و نشان. ستونِ ناوبریِ گروه‌بندی‌شدهٔ ماکت اجرا **نشد**: ترتیبِ ریل دستِ کاربر و کشیدنی است و سرگروهِ ثابت با آن می‌جنگد — تصمیمش با صاحب پروژه است.
- ۱۴۰۵/۰۶/۲۸ — پوستهٔ تیرهٔ مدرن جای «اداریِ روشن» را گرفت: سه سطحِ واقعی، پلهٔ برند از ۶۰۰ به ۴۰۰ (۳٫۱:۱ → ۸٫۹۶:۱)، دکمهٔ قرصی و کاشیِ عددِ بازنویسی‌شده. باگِ راه: تماسِ اولِ `applyTheme` در بوت پیش‌فرض را در `localStorage` می‌نوشت، پس تنظیمِ ذخیره‌شده هرگز غالب نمی‌شد. دستهٔ ۲۷۴. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۸ — یک زبانِ نشانه‌ای به‌جای دو تا: ایموجیِ دکمه‌های پیوندِ استراتژی (🔬/🔔/➕) و خانهٔ «شرط» جدول موقعیت‌ها (🔔/⏸) جایشان را به آیکونِ خانوادهٔ `ui/icons.mjs` دادند، با دو مسیرِ تازه `plus` و `pause`. سه هزینهٔ ایموجی که هیچ‌کدام از خواندنِ کد پیدا نمی‌شوند: رنگش ثابت است و در خانهٔ `.loss` قرمزِ هشدار را نمی‌گیرد و در پوستهٔ تیره با متن هم‌رنگ نمی‌شود · عرضش با فونتِ سیستم عوض می‌شود و ستونِ `tabular-nums` را می‌لرزاند · کنارِ آیکونِ برداریِ همان ردیف دو زبانِ نشانه‌ای می‌شود. `.ic-cell` اندازه را از `em` همان خانه می‌گیرد تا ارتفاعِ ردیف بالا نرود. **ایموجیِ عنوانِ دو تب دست نخورد**: خواستهٔ صریح صاحب پروژه است («ایموجی مناسب هم بذار») و دستهٔ ۸۶ قفلش می‌کند — یک بار برداشته شد و همان دسته قرمز شد، و درست هم بود. دستهٔ ۲۷۳ با ۱۰ ادعا؛ بارگذاریِ هر چهار ماژول در مرورگر واقعی بدون خطا.
- ۱۴۰۵/۰۶/۲۸ — لایهٔ مردهٔ «پوستهٔ شیشه‌ای» برداشته شد: توکن‌ها روزی خنثی شده بودند ولی قاعده‌هایشان ماندند و لبهٔ پایه را می‌پوشاندند — کادرِ محاسبه‌شدهٔ هر `.ghost` برابر `rgba(0, 0, 0, 0)` بود. درسِ ماندگار: توکنی که مقدارش `transparent` است، لبهٔ زیرِ خودش را پاک می‌کند. دستهٔ ۲۷۱. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۷ — «کامل خروجی نمی‌سازه»: ۵۸ ابزار/روز اصلاً درخواست نرفته بودند و هر ۵۸تا پوت بودند — `contractLife` بی `listedFrom` به اولین روزِ **دیده‌شدن** برمی‌گشت و پوتِ کم‌معامله دیرتر دیده می‌شود. `dataExportListingFloors` کفِ هر قرارداد را از سری‌اش می‌گیرد. بازتولید: ۱۰۸۵ جفت به ۱۱۴۳ رسید، دقیقاً ۵۸ تا. دستهٔ ۲۶۵. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۷ — ممیزی فایلِ m15 اهرم (۳۷ برگ، ۸۶۵٬۱۰۶ ریزمعامله) چهار ایراد داد که هر چهار در خودِ فایل اثبات‌پذیر بودند: ۷۳ معاملهٔ باطلِ بیرونِ جلسه که بی‌شرح می‌افتادند، سطلِ پانزدهمِ «۱۲:۳۰» از نبودِ چفتِ پایانِ جلسه، ستونِ وعده‌داده‌شدهٔ «بیرون از جلسه» که وجود نداشت، و نامِ غلطِ ستونِ «مسیر». دستهٔ ۲۶۶ با ۱۴ ادعا. (شرحِ کامل: `docs/worklog/1405-06.md`)
- ۱۴۰۵/۰۶/۲۷ — قفل خروجی دیتا از «کل مشخصاتِ ۲۴٬۹۳۴ قرارداد» به «همان قراردادهای انتخابی» محدود شد. پاس مشخصاتِ چند قرارداد بی‌معامله با پاسخ ۵۰۰ می‌توانست تا ابد دکمه را برای یک سررسید کاملاً معلوم نگه دارد و هم‌زمان سهمیهٔ ریزمعامله را مصرف کند. سرور اکنون تاریخ عرضه و پایان و معلوم‌بودن عرضه را برای کال و پوت جدا می‌فرستد؛ ابزارِ انتخابیِ بی‌تاریخ هنوز صادقانه قفل می‌شود، ولی شکست ابزار نامرتبط مانع نیست. بازسازی مرورگر: ۳ ماه اخیر، اهرم، سررسید ۱۴۰۵/۰۷/۲۹، همهٔ ۳۴ قرارداد (۱۷ کال و ۱۷ پوت) با `detailsComplete=false` فوراً دکمهٔ آماده‌سازی را فعال کردند. دسته‌های ۲۶۴ و ۲۶۸ و دروازهٔ کامل سبز شدند.
- ۱۴۰۵/۰۶/۲۷ — دفترِ قدیمی مهاجرت می‌کند: قاعدهٔ تبعی درست شده بود ولی `mergeRoster` ردیفِ قدیمی را برنده می‌کرد (`old: call · fresh: tabaee · merged: call`)، پس فباهنر و فخوز تا ابد «۱ کال و صفر پوت» می‌ماندند. `side` یک **مشتق** است نه مشاهده — از نام و نماد حساب می‌شود و با عوض شدنِ قاعده باید دوباره حساب شود. حالا `mergeRoster` پس از کامل‌شدنِ نام و نماد سمت را بازمی‌سازد، و `repairRosterSides` خواهرِ `repairRosterBaseNames` هنگام خواندنِ دفتر همان کار را می‌کند و شمارش را لاگ می‌کند. بازسازیِ اجباری با بالابردنِ نسخهٔ دفتر عمداً انتخاب نشد: چند هزار درخواستِ بالادست است، و سهمیهٔ بالادست دقیقاً همان چیزی است که کم داریم؛ ترمیمِ هنگام خواندن صفر درخواست می‌برد. ردیفِ بی‌نام و بی‌نماد سمتش را از دست نمی‌دهد — «نمی‌دانیم» دلیلِ پاک‌کردن نیست. دستهٔ ۲۶۹ به ۳۵ ادعا رسید و بی بازمحاسبهٔ ادغام ۲ رد می‌دهد.
- ۱۴۰۵/۰۶/۲۷ — اختیار خرید تبعی دیگر کالِ استاندارد شمرده نمی‌شود: نماد «ظ» کنار «ه» نشست و نامِ «اختیارخ ت» پیش از «اختیارخ» سنجیده می‌شود، وگرنه به شاخهٔ کال می‌افتاد. دامش هم بسته شد — پایه‌هایی که نامشان با «ت» شروع می‌شود (تاصیکو، تپکو، ترانس، تلیسه) نباید تبعی خوانده شوند، پس الگو «ت» را کلمهٔ مستقل می‌خواهد. پیامدش در اجرای واقعی: فباهنر و فخوز که «۱ کال و صفر پوت» نشان می‌دادند دیگر سریِ استاندارد ندارند، و بخشی از «۶۶ سریِ تک‌سمت» اصلاً سریِ ناقص نبود. مهم‌تر از خودِ اصلاح: ۷۱۵۴ آزمونِ سبز این را نگرفته بودند چون هیچ‌کدام **سمتِ** قرارداد را نمی‌سنجیدند — دستهٔ ۲۱۱ حتی `ظباهنر55` را می‌ساخت و فقط می‌پرسید از دفتر نیفتاده. دستهٔ ۲۶۹ با ۲۵ ادعا نوشته شد و روی کدِ پیش از اصلاح ۱۰ رد می‌دهد؛ دستهٔ ۲۱۱ هم حالا سمت را می‌سنجد.
- ۱۴۰۵/۰۶/۲۷ — دو ایراد باقی‌ماندهٔ ممیزی: قفل خروجی دیگر فقط `missingDays` را نمی‌بیند — `exportBlockers` پاسِ کاتالوگ، مشخصات قراردادهای بی‌معامله و نسخهٔ دفتر را هم می‌شمارد، ولی عمداً روی خودِ `complete` بسته نشد چون آن پرچم «۶۶ جفت ناقص کال/پوت» را هم می‌شمارد که واقعیتِ بازار است و هرگز درست نمی‌شود (همان قفلِ دائمی که یک بار برداشته شد). `rosterHealth` سه پرچم خامِ `catalogComplete`/`detailsComplete`/`versionCurrent` را بیرون می‌دهد تا این تفکیک ممکن باشد. و `instrumentsWithPairs` ابزارِ بی‌جفت را از فهرست شیت‌ها حذف می‌کند، پس قراردادِ بیرونِ بازه دیگر برگِ خالی نمی‌سازد — ملاک «جفت داشتن» است نه «معامله داشتن»، تا برگِ خالیِ قراردادِ زندهٔ بی‌معامله سر جایش بماند. کنترل مرورگر واقعی روی همان حالتی که ممیزی گفت (روزها تمام، کاتالوگ در حال اجرا): دکمه قفل ماند. دستهٔ ۲۶۸ به ۳۶ ادعا رسید.
- ۱۴۰۵/۰۶/۲۷ — سه ایرادِ ممیزیِ «خروجی دیتا» رفع شد، و هر سه یک جنس داشتند — «نمی‌دانیم» که به‌جای یک واقعیت نوشته می‌شد. (۱) اجرا تا تکمیل اسکنِ **همین بازه** قفل است (`missingDays`)؛ قفلِ سراسریِ `complete=false` برنگشت. (۲) قراردادِ `activeFrom=0` دیگر «کرانِ پایینی ندارد» معنی نمی‌شود و وارد بازهٔ تاریخی نمی‌شود — در فایل گزارش‌شده ۲۴ قرارداد برای هر ۱۳۱ روز درخواست رفته بودند، از جمله ضهرم4011 که یک ماه پس از پایان بازه عرضه شده؛ کنارگذاشته‌ها شمرده و گفته می‌شوند. (۳) خالیِ بی‌تأییدِ تابلوی روزانه «خالی، تأییدنشده» است نه «بدون معامله»؛ «بدون معامله» فقط با تأیید منبع دوم نوشته می‌شود. خطِ زمانیِ فایل هم علت را نشان داد: ریزمعاملهٔ پایه تا ۲۰۲۴/۱۰/۱۶ پیوسته آمد و از ۲۰۲۴/۱۰/۱۹ به بعد هیچ — ته‌کشیدنِ سهمیهٔ بالادست وسط اجرا، نه پنجرهٔ نگه‌داری. دستهٔ ۲۶۸ با ۲۰ ادعا.
- ۱۴۰۵/۰۶/۲۷ — «فقط روز آخر می‌آید، کل بازه را بده»: روزی که همهٔ ابزارهای انتخابی‌اش — حتی خودِ پایه — صفر ریزمعامله دارد، واقعیتِ بازار نیست و حالا یک بار بی‌کش دوباره پرسیده می‌شود (`suspectEmptyDays`). دور اولِ موجود فقط جفت‌هایی را دوباره می‌پرسید که تابلوی روزانه تکذیبشان کرده بود، و برای قراردادِ منقضی آن تابلو اغلب در دست نیست — یعنی همان جفت‌ها هرگز دور دوم نمی‌دیدند. `getFresh` به نشانیِ بالادست cache-buster می‌چسباند، تنها راهِ رد شدن از پاسخِ خالیِ کهنه‌ای که لبهٔ CDN نگه داشته و ۲۰۰ و JSON معتبر است. هر جفت حداکثر یک دور دوم می‌بیند و روزِ خطادار یا تک‌ابزاره مشکوک شمرده نمی‌شود. کنترل مرورگر واقعی با بازتولید همان امضا: دور اول همه خالی، یک POST بی‌کش، و ۱۵ ابزار/روز در کل بازه نجات یافت. دستهٔ ۲۶۷ به ۳۶ ادعا رسید.
- ۱۴۰۵/۰۶/۲۷ — تاریخ شمسی کنار میلادی در خروجی دیتا: برگ هر ابزار (خام و شمع)، برگ پوشش دریافت و بازهٔ برگ راهنما هر دو تقویم را می‌گویند. میلادی حذف نشد چون کلیدِ تطبیق با هر منبع دیگری همان است؛ رقم لاتین ماند تا خانه در اکسل مرتب و پالایه شود و تاریخِ نادرست خانهٔ خالی می‌گیرد نه «—». دو ادعای شماره‌ای دستهٔ ۲۶۶ و یکی در ۲۶۷ به نامِ سرستون بسته شدند تا ستونِ تازه ادعای بی‌ربط را نشکند؛ کنترل مرورگر واقعی فایل ۶٬۳۴۱ کیلوبایتی خام و ۳۵ کیلوبایتی پنج‌دقیقه‌ای داد و هر دو ستون سر جایشان بودند.
- ۱۴۰۵/۰۶/۲۷ — خالیِ بی‌شرح دیگر ممکن نیست: فایل گزارش‌شدهٔ سه‌ماههٔ «موج» ۱٬۳۱۶ ابزار/روز را از مسیر تاریخی خواست و هر ۱٬۳۱۶تا خالی و بی‌خطا برگشتند، در حالی که تابلوی روزانه برای خودِ پایه ۳۶٬۱۳۴ معامله ثبت کرده بود — و برنامه نمی‌توانست بگوید بالادست «معامله‌ای نبود» گفت یا چیزی داد که فهرست معامله نیست، چون `firstList` هر دو را یک آرایهٔ خالی می‌کرد. `core/upstream-shape.mjs` این دو را جدا می‌کند، `/api/trades/batch` شکلِ هر دو پرچم را همراه خالی برمی‌گرداند، برگ پوشش ستون «پاسخ بالادست» گرفت و راهنما خط «تفکیک مسیر». صفرِ یک مسیرِ کامل حالا خبرِ اولِ جملهٔ وضعیت است. و `/api/dailies` این تب که تنها جای برنامه بود که شمارِ روز می‌فرستاد، به `n=0` برگشت — راست‌آزماییِ خالی‌ها برای ۸۴۷ ابزار/روزِ منقضی کور شده بود. دستهٔ ۲۶۷ با ۲۵ ادعا.
- ۱۴۰۵/۰۶/۲۷ — خروجی دیتا سبک شد: گزینهٔ تایم‌فریم (ریزمعامله، ۱/۵/۱۵/۳۰/۶۰ دقیقه) که شمع را از همان ریزمعاملهٔ گرفته‌شده می‌سازد بی دریافت دوباره، و کلید ستون‌های مشتق که پیش‌فرض خاموش است چون هر سه حاصل‌ضرب ستون‌های موجودند و اندازهٔ قرارداد به برگ پوشش رفت؛ سطل بی‌معامله ردیف نمی‌گیرد و معاملهٔ باطل در قیمت نمی‌نشیند ولی شمرده می‌شود. اندازه‌گیری واقعی یک روزِ ۱۲٬۶۰۰ ریزمعامله‌ای در سه ابزار: ۱٬۱۹۷ کیلوبایت خام، ۳۶ در یک‌دقیقه، ۹ در پانزده‌دقیقه. دکمهٔ «آزمون یک ابزار/روز» به خواست صاحب پروژه برداشته شد و پاسخِ بدشکلِ تابلوی روزانه دیگر بازبینی را نمی‌اندازد؛ دستهٔ ۲۶۶ با ۴۰ ادعا.
- ۱۴۰۵/۰۶/۲۷ — کشف قراردادهای «خروجی دیتا» و همهٔ مصرف‌کننده‌های زنجیره کامل شد: سقف پنهان ۴۰ نتیجه‌ای TSETMC با شاخه‌های رقمی پیمایش می‌شود، تابلوی جاری قرارداد بی‌معامله را بذر می‌دهد، اختیار تبعی وارد زنجیره نمی‌شود و شمارها فقط شناسهٔ واقعی را حساب می‌کنند. ممیزی واقعی دفتر را از ۹٬۸۰۰ به ۲۴٬۹۳۴ قرارداد رساند؛ فخوز از ۱۵۵ به ۴۵۳ رسید و ۱۸ سررسید عادی‌اش کال/پوت برابر شد. ۶۶ سری رسمیِ تک‌سمت (عمدتاً تعدیل‌شده) صریح گزارش می‌شوند و هیچ شناسه‌ای ساخته نمی‌شود؛ ۷٬۰۳۰ آزمون و کنترل مرورگر واقعی سبز است.
- ۱۴۰۵/۰۶/۲۶ — تب «خروجی دیتا» در روز تعطیل/پیش از بازار دوباره کار می‌کند: تاریخ واقعی نوارِ نگه‌داشته‌شده با اثرانگشت تعداد، حجم و OHLC در تابلوی روزانه تشخیص داده می‌شود، قرارداد منقضیِ جلسهٔ آخر حتی اگر از تابلوی امروز حذف شده باشد از نوار می‌آید، و خالی تاریخیِ تکذیب‌شده یک بار بی‌کش تکرار می‌شود. اجرای واقعی بازهٔ ۱۴۰۵/۰۶/۲۴ تا ۲۵ برای اهرم، ضهرم6040 منقضی و ضهرم7050 فعال هر ۶ ابزار/روز را بدون خطا گرفت: ۴۶٬۳۱۵ ریزمعامله در سه شیت و فایل ۱٬۹۶۵ کیلوبایتی.
- ۱۴۰۵/۰۶/۲۶ — تب «خروجی دیتا» از قفل پوشش ناقص آزاد شد: ناقص‌بودن دفتر دیگر آماده‌سازی را غیرفعال نمی‌کند و داخل فایل ثبت می‌شود؛ جریان به دو دکمهٔ روشن «آماده‌سازی ریزمعاملات» و «خروجی Excel» تقسیم شد. اجرای واقعی فخوز فایل ۲۱ کیلوبایتی داد و کنسول بدون خطا بود؛ دستهٔ ۲۶۴ اکنون ۱۸ ادعا دارد.
- ۱۴۰۵/۰۶/۲۶ — شکست CI تب خروجی دیتا بازتولید و رفع شد: استایل عمومی تب که پس از مرز `.pb-skin` آمده بود، پیش از پوستهٔ اختصاصی منتقل شد؛ Node 24 اکنون ۶۹۳۴ آزمون موتور و ۷۱ نگهبان مخزن را بدون رد می‌گذراند.
- ۱۴۰۵/۰۶/۲۶ — تب مستقل «خروجی دیتا» اضافه شد: انتخاب تکی/چندتایی/همهٔ نمادهای پایه، کشف همهٔ کال و پوت‌های فعال بازه، دریافت ریزمعاملات پایه و اختیار از مسیر تاریخی و نوار امروز، و فایل فشردهٔ xlsx با یک شیت مستقل برای هر ابزار (حتی قرارداد بی‌معامله) و شیت پوشش خطاها؛ دستهٔ ۲۶۴ با ۱۶ ادعا و کنترل مرورگر واقعی سبز شد.
- ۱۴۰۵/۰۶/۲۶ — بیست قلم پیشنهادی در هفت تب: بستن موقعیت و سود تحقق‌یافته، نگاه سبد (یونانی، تقویم سررسید، اتاق سربه‌سر، منحنی کل)، شرط روی موقعیت‌های من، اصطکاک اجرای رول و جست‌وجوی همهٔ سررسیدها، مقایسهٔ ساختارها روی یک نماد، «یا» و تاریخچهٔ شلیک در دیده‌بان، اثر نرخ بدون ریسک بر تلاطم، بایگانی اجراهای آزمایشگاه، صدک اسپرد و نرخ معامله‌شدن سررسید، سلامت داده، پشتیبان و بازیابی، و دفترچهٔ معاملات.
- ۱۴۰۵/۰۶/۲۶ — تب موقعیت‌های من از «عکس» به «روند» رسید: سه سرچشمهٔ روند (روزانه، درون‌روزی، جلسه)، ستون «تغییر امروز» با مبنای آخرین روزِ پیش از امروز، جداشدن سررسیدگذشته از باز، ویرایش موقعیت، و مقصد تازهٔ «افزودن به موقعیت‌های من» در نوار پیوند جست‌وجوی استراتژی‌ها.
- ۱۴۰۵/۰۶/۲۵ — نمودارهای «کل بازه» بعد از اولین قیمت واقعی با آخرین ارزش‌گذاریِ برچسب‌خورده به‌عنوان «قیمت کهنه، حمل‌شده» پیوسته شدند؛ حمل از مرز روز عبور نمی‌کند و آزمون واقعیِ پای چهارمعامله‌ای در همهٔ تایم‌فریم‌ها صفر شکاف پس از اولین مشاهده داد.
- ۱۴۰۵/۰۶/۲۵ — «کل بازه»: رسّام دیگر سطل‌های خالیِ دو سر جلسه را حذف نمی‌کند، تک‌مشاهده را در جای زمانی واقعی رسم می‌کند، KPI را فقط از سطل واقعی می‌سازد و جدول قیمت کهنهٔ حمل‌شده را صریح نشان می‌دهد؛ آزمون واقعی ۱۴۰۵/۰۶/۲۳ روی ۵/۱۵/۳۰/۶۰ دقیقه به‌ترتیب ۴۲/۱۴/۷/۴ سطل داد.

## ۱۴۰۵/۰۶/۲۶ (هفتم) — بیست قلمِ پیشنهادی، از فهرست تا کد

صاحب پروژه فهرست پیشنهادها را خواست و بعد گفت «همه را انجام بده». هفت
گروه، نُه ماژول موتور، یازده ماژول نما، یک تب تازه، و ده دستهٔ آزمون.

### آنچه در راه معلوم شد و فهرست را عوض کرد

سه پیشنهاد، وقتی به کد رسیدم، یا از قبل بودند یا ساختنی نبودند:

**گرمانمای تلاطم** از قبل در داشبورد رصد زنده بود (`iv-heatmap`). چیزی
اضافه نشد و همین در پیام کامیتش نوشته شد.

**«اسپرد امروز در برابر عادتِ خودش»** ساختنی نیست: دفترِ سفارشِ روزهای
گذشته ذخیره نمی‌شود و بالادست برای گذشته فقط پایانی و حجم و موقعیت باز
می‌دهد. هر «میانگین تاریخی اسپرد» از چیز دیگری، عددِ ساختگی است. جایش
**صدکِ تابلوی امروز** آمد — «از ۹۰٪ تابلو تنگ‌تر» — و علتِ نبودِ نسخهٔ
تاریخی در خودِ کد نوشته شد تا فردا کسی نسازدش.

**`tradedPct`** از قبل در موتور ساخته می‌شد و هیچ نمایی نداشت. سنجه‌ای که
دیده نمی‌شود سنجه نیست.

### قاعده‌ای که در همهٔ بیست قلم تکرار شد

جمعِ نصفه ساخته نمی‌شود، و نامِ آنچه کم است گفته می‌شود:

```
یونانیِ سبد    یک موقعیتِ بی‌تلاطم → کل جمع NaN + نامِ همان موقعیت
منحنیِ سبد     روزی که یکی قیمت ندارد → نقطه نمی‌شود + نامش در gaps
دفتر بسته‌ها   یک برگهٔ خروجِ ناقص → جمعِ سود تحقق‌یافته نامعلوم
تقویم سررسید  یک وجهِ نامعلوم → وجهِ آن روز نامعلوم، نه جمعِ بقیه
```

### سه خطایی که آزمون گرفت، نه چشم

**نسبتِ دو بار ضرب‌شده.** `row.__legs` نسبتش در تعداد قرارداد ضرب شده ولی
موقعیتِ ثبت‌شده تعداد را جدا نگه می‌دارد و `markToMarket` خودش ضرب می‌کند.
بی تقسیم بر `units`، هر عدد پولیِ موقعیت `units` برابر می‌شد.

**`payoff` در `postMessage`.** «بهترینِ هر ساختار» می‌تواند بیرونِ برشِ
`limit` باشد، یعنی شیئی که در `res.rows` نیست. بی حلقهٔ جداگانه برای
پاک‌کردنِ `payoff` از آن، کلِ پیام با «could not be cloned» می‌افتاد و
کاربر یک جدولِ خالیِ بی‌علت می‌دید.

**شناسهٔ تکراری.** `bt-run-note` از قبل روی یک `<p>` بود؛ ورودیِ تازه با
همان شناسه یعنی `.value` همیشه `undefined`. در مرورگر پیدا شد، نه در دیف.

### ترتیبی که تصادفی نیست

**بسته‌شده مقدم بر سررسیدگذشته.** وارونه‌اش یعنی هر موقعیتِ بسته‌شده با
گذشتِ زمان بی‌صدا به بایگانی می‌پرد و سودِ تحقق‌یافته‌اش از جمع می‌افتد.

**ردیفِ حذف پیش از خودِ حذف.** بعدش عنوان و نماد و تعدادِ موقعیت در دست
نیست و ردیفِ دفترچه به هیچ اشاره می‌کند.

**قیمتِ پیشنهادیِ خروج یخ می‌زند.** اگر هر پانزده ثانیه با مظنهٔ تازه عوض
می‌شد، عددی که کاربر داشت تایپ می‌کرد زیر دستش می‌پرید.

### قلمِ بازِ «نرخ ۳۰٪» — یک قدم جلوتر، بی گرفتنِ تصمیم

دفتر کار اندازه گرفته بود که علتِ غالبِ ستون‌های خالیِ تلاطم یک **تنظیم**
است. حالا در پنل جزئیات هر ترکیب، همان پا با چند نرخ سنجیده می‌شود، با
کفِ نظریِ هر نرخ و فاصلهٔ قیمت تا آن. **هیچ نرخی «درست» اعلام نمی‌شود** —
انتخابش تصمیم صاحب پروژه است و همچنان باز. آنچه اضافه شد یک کرانِ
اندازه‌گیری‌شده است: بالاترین نرخی که با آن تلاطم هنوز حل می‌شود.

### سنجیده‌شده

مرورگر واقعی روی سرور محلی: هر دوازده تب بار شد، **صفر خطای صفحه**. مسیرهای
کامل آزموده شدند — بستن موقعیت با رد شدنِ تاریخِ نامعتبر، انتقال موقعیت به
تب رول در صفحهٔ تازه، شرطِ برقرار روی نوار بالای صفحه، گرفتن و خواندن فایل
پشتیبان، و ثبت خودکار ردیف دفترچه پس از بستن.

### ادعاها

دسته‌های ۲۵۵ تا ۲۶۳، به‌علاوهٔ افزوده‌های ۰۵۲ و ۲۵۴. جمع مجموعه از ۶۶۶۱ به
۶۸۹۰ ادعا رسید.

## ۱۴۰۵/۰۶/۲۶ (ششم) — روند سود و زیان موقعیت باز

سود و زیانِ لحظه‌ایِ موقعیتِ باز، روندش در سه مقیاس، و فرستادنِ ترکیب از
«در جست‌وجوی استراتژی‌ها» به موقعیت‌های من با امکانِ ویرایش در مقصد.

دو درسِ ماندگار: **عدد کافی نبود، مبنا لازم بود** — «تغییر امروز» بی
اعلامِ اینکه مبنایش آخرین روزِ پیش از امروز است، ساعت ۹:۰۵ بی‌معنی است.
و **قیمتِ ثبت، قیمتِ مدل نیست** — دکمه فرم را پیش‌پر می‌کند و کاربر تأیید
می‌کند، چون قیمتی که سفارش با آن پر شده تا سررسید مخرجِ بازده می‌ماند.

یک اشکالِ ریز که می‌توانست همه‌چیز را چند برابر کند: `row.__legs` نسبتش
در تعداد قرارداد ضرب شده بود ولی `markToMarket` خودش ضرب می‌کند. دستهٔ
۰۵۲ قفلش کرد. دسته‌های ۲۵۳ و ۲۵۴ هم.
