# دفتر کار

این فایل حافظه بین اجراهاست. هر اجرای زمان‌بندی‌شده اول این را می‌خواند، یک
قلم از بک‌لاگ برمی‌دارد، انجامش می‌دهد، و همین‌جا ثبت می‌کند.

قاعده هر اجرا:

```text
۱  node tests/run.mjs   باید سبز باشد پیش از هر تغییر
۲  یک قلم از بک‌لاگ، نه بیشتر — کوچک و کامل
۳  آزمون رگرسیون برای هر اصلاح رفتاری
۴  node tests/run.mjs   باید سبز باشد بعد از تغییر
۵  کامیت، پوش، و یک ردیف در «انجام‌شده»
۶  برگرد به قلم بعدی — تمام کردن یک قلم پایان اجرا نیست
```

**قاعده صریح کاربر (۱۴۰۵/۰۵/۲۳): هیچ‌وقت متوقف نشو.** اگر یک قلم بزرگ یا
مبهم به نظر رسید، آن را کوچک‌تر کن و شروع کن — کنارش نگذار. اگر به مشکلی
خوردی (خطا، طراحی اشتباه، داده ناکافی)، برگرد، دوباره فکر کن، برنامه تازه
بریز، و ادامه بده. تنها دلیل قابل قبول برای توقف، رسیدن به سقف واقعی
ظرفیت/usage است — نه خستگی از کار، نه «به‌اندازه کافی امروز کار شد»، نه
تصمیم سلیقه‌ای که «این قلم بزرگ است، جای دیگری بهتر است». همیشه یا داری
کد می‌زنی یا داری برای قلم بعدی برنامه می‌ریزی.

---

## انجام‌شده

### ۱۴۰۵/۰۵/۲۹ — ارتقا و مدرن‌سازی جامع UI و UX سامانه بر مبنای دیزاین‌سیستم فین‌تک

به درخواست کاربر، ظاهر و تجربه کاربری (UI/UX) سامانه رصد آپشن با الهام از داشبورد فین‌تک مدرن و با حفظ کامل ساختار کلی (منوی ثابت سمت راست، نوار بالا و صفحات) به شکل جزءبه‌جزء بازطراحی و ارتقا یافت:

۱. **توکن‌های رنگ و پوسته (`ui/style.css`):**
   - پس‌زمینه پوسته دفتر با خاکستری بسیار ملایم (`#f8fafc`) و پنل‌های سفید خالص برای کاهش خستگی چشم تنظیم شد.
   - لایه‌های سایه چندگانه و نرم (`--shadow-2xs` تا `--shadow-lg`)، گردی گوشه‌ها (`--radius-lg: 16px`, `--radius-md: 10px`) و حاشیه‌های شیشه‌ای ملایم اضافه شد.
   - پوسته تیره «تابلو» با کنتراست شفاف و رنگ‌های نئونی اصلاح شد.

۲. **نوار بالا و شاخص‌های سلامت (`.top`, `.health`, `.pill`):**
   - هدر چسبان شیشه‌ای (`backdrop-filter: blur(16px) saturate(1.8)`) با نشانگر نبض زنده جریان داده (`.pill.link.open::before`).
   - کارت‌های آماری متصل، ساعت دریافت و منوی بازشوی سقف سررسید با چیدمان و دکمه‌های تمیزتر.

۳. **منوی کناری سمت راست (`.rail`):**
   - جعبه جست‌وجوی کپسولی با حاشیه نرم و سایه فوکوس ارتقا یافت.
   - دکمه‌های تب‌ها با نوار نشانگر اختصاصی سمت راست، پس‌زمینه ملایم فعال و بج‌های جهت استراتژی (`.tone`) زیباتر شدند.

۴. **کارت‌های شاخص‌های کلیدی (KPIs) و جداول داده (`.kpi`, `table.data`):**
   - کارت‌های آماری با خط نشانگر رنگی و تایپوگرافی مونو برجسته شدند.
   - جداول با سرستون شیشه‌ای چسبان، نشانگرهای مرتب‌سازی صعودی/نزولی، راه‌راه ملایم و هایلایت استاندارد ردیف‌های ATM بازطراحی شدند.

۵. **نمودارهای بازده (Payoff) و کنترل‌ها:**
   - تولتیپ تعاملی شیشه‌ای شناور، اسلایدرهای روان با رنگ تاکیدی، دکمه‌های گرادیانی با افکت کلیک (`active: scale(0.98)`) و چیپ‌های فیلتر کپسولی.

۶. **انطباق ۱۰۰٪ با نگهبانان مخزن:**
   - تمام اندازه‌های قلم خام به متغیرهای توکن تبدیل شدند و آزمون‌های ۲۲ گانه نگهبان و ۶۹۳ آزمون هسته بدون هیچ خطایی سبز ماندند.

### ۱۴۰۵/۰۵/۲۹ — کشوی «نمای مسیر» برداشته شد (پرسش کاربر)

پرسش: «با توجه به منطق جدید، وجود این دکمه کشویی الزامی ندارد؟» بررسی شد
و پاسخ برای هر سه حالتش جدا درآمد.

`فقط ریزمعامله روز سنجش` تکراری بود — و نسخهٔ بدترِ چیزی که همان صفحه
پایین‌تر دارد. پنل درون‌روزی دقیقاً همان آرایه و همان `netPnl` را رسم
می‌کند، ولی با `timeScale` (محور ساعت ۹:۰۰ تا ۱۲:۳۰) و `step` (مسیر
پله‌ای، که برای «آخرین قیمت مشاهده‌شده» تنها رسم درست است). نمای کلی همان
داده را روی محور اندیسی و با درون‌یابی خطی می‌کشید. هیچ چیزی نداشت که آن
پنل نداشته باشد.

`روزهای قبل + ریزمعامله روز آخر` — حالت پیش‌فرض — دو ایراد بی‌صدا داشت:

  ۱. محور افقی بر پایه اندیس است. چند صد نقطه ریزمعامله کنار چند ده روز،
     بخش روزانه را به نواری باریک تبدیل می‌کرد؛ خطِ مرزی که برای همین
     اضافه شده بود، مشکل را توضیح می‌داد نه حل. شیب دو طرف مرز اصلاً با هم
     قابل مقایسه نبود، چون واحد محور یکی نبود.
  ۲. مسیر روی روزِ بازشده بریده می‌شد. یعنی «بهترین نقطه»، «بدترین نقطه»،
     «کوتاه‌ترین زمان سود» و «سود/زیان نهایی» با کلیک روی هر ردیف جدول
     روزبه‌روز عوض می‌شدند — در بخشی که عنوانش «عملکرد کلی این بازه» است.
     کارت خلاصهٔ یک بازه نباید به اینکه کاربر کدام روز را باز کرده وابسته
     باشد.

پس کشو کامل برداشته شد و نمای کلی همیشه کل بازه را در تفکیک روز می‌سازد،
مستقل از روزِ بازشده. حالا هر بخش یک کار دارد: نمای کلی → بازه در تفکیک
روز؛ پنل درون‌روزی → یک روز در تفکیک ثانیه؛ گام سوم → بازه در تایم‌فریم
انتخابی.

یک تغییر رفتار که باید صریح گفته شود: «سود/زیان نهایی» و «بازده نهایی» و
«تغییر نماد پایه» حالا از روز سنجش با همان مبنای قیمتی‌ای می‌آیند که خود
کاربر روی ریل انتخاب کرده، نه از آخرین تیک مشترک. با مبنای `LAST` این دو
یک چیزند؛ با مبناهای دیگر، عدد نمای کلی حالا با سناریوی انتخابی کاربر
هم‌خوان است. عدد تیک‌دقیق سر جایش است: «آفست پایان مشاهده» در پنل
درون‌روزی.

آنچه با کشو مُرد و برداشته شد: `combinedBacktestPath` در
`core/backtest.mjs`، خط مرز روز آخر در `chart()`، و `.backtest-split` در
شیوه‌نامه. `granularity` می‌ماند، چون راهنمای نمودار و تحلیل تایم‌فریم هنوز
از آن می‌خوانند.

۶ آزمون در گروه ۴۰ جایگزین آزمون‌های تابع حذف‌شده شد، از جمله یکی که صادرات
واقعی موتور را می‌خواند نه متن کد. مجموع ۶۹۲ → ۶۹۳، صفر رد. چیدمان نوار
اجرا در مرورگر بررسی شد: دو ستون، بدون ستون خالی.

---

### ۱۴۰۵/۰۵/۲۹ — نوار «سقف سررسید» باز می‌شد و خالی بود (گزارش کاربر)

گزارش: «دکمه سقف سررسید در بالا سمت چپ را که کلیک می‌کنم چیزی را نشان
نمی‌دهد.» بازتولید شد، و علتش یک اشتباه انتخاب منبع بود، نه خطای رابط.

نوار فهرست سررسیدها را از `/api/watch` می‌گرفت. حلقه دیده‌بان بیرون از
ساعت بازار عمداً پارک می‌شود (`server/server.mjs`: `if (!gate.open) return
true`)، پس آن نقطه پایانی شب‌ها آرایه خالی برمی‌گرداند — با کد ۲۰۰ و بدون
هیچ خطایی. یعنی خرابیِ بی‌صدا: کد سالم بود، داده نبود، و رابط راهی نداشت
این دو را از هم تشخیص دهد. و سقف موقعیت را کارگزار معمولاً پس از بستن
بازار اعلام می‌کند، یعنی دقیقاً همان ساعتی که این نوار باید کار کند.

منبع به `/api/history/universe` عوض شد. آن نقطه پایانی از قبل برای همین
ساخته شده و توضیحش هم همین را می‌گوید: اگر عکس لحظه‌ای خالی باشد خودش از
بالادست می‌گیرد، چون تاریخچه باید شب و روز قابل بررسی باشد.

سه اشکال دیگر که ضمن بازبینی همین مسیر پیدا شد:

**علامت‌خورده‌ها گروگان زنجیره بودند.** `paintPanel` سه خروجی زودهنگام
داشت و هر سه کل پنل را با یک جمله جایگزین می‌کردند. یعنی هر وقت زنجیره
نمی‌آمد، سررسیدهایی که کاربر قبلاً علامت زده بود نه دیده می‌شدند نه
برداشته — در حالی که همچنان در `data/settings.json` نشسته بودند و همچنان
هر اسکن و هر بک‌تست را عوض می‌کردند. تنظیمی که راه خاموش‌کردنش نیست، بدتر
از تنظیمِ نبوده است. حالا بخش «علامت‌خورده‌های بیرون از زنجیره» همیشه رسم
می‌شود — چه زنجیره نیامده باشد، چه آن سررسید گذشته باشد — و «پاک کردن
همه» هم دیگر پشت زنجیره قفل نیست.

**زنجیره خالی کش می‌شد.** شرط `if (chain || loading) return` بود، و یک
`Map` خالی هم راست است. پس یک بارِ ناموفق تا بارگذاری دوباره صفحه ادامه
پیدا می‌کرد و باز کردن دوباره نوار هیچ تلاشی نمی‌کرد. شرط به `chain?.size`
عوض شد و دکمه «تلاش دوباره» اضافه شد.

**خطا فارسی نمی‌شد.** پیام خام بالادست مستقیم نشان داده می‌شد
(«`Error: HTTP 403`»). حالا از `humanizeUpstreamError` رد می‌شود و متن
خام در `title` می‌ماند: پیام فارسی برای تصمیم، متن خام برای عیب‌یابی.

دو نکته پیاده‌سازی که عمدی‌اند: پنل **پیش از** رفتن به شبکه یک بار رسم
می‌شود، چون منتظر ماندن یعنی چند لحظه پنل خالی و پنلِ خالی از نگاه کاربر
یعنی «کار نمی‌کند». و تیک‌زدن پنل را از نو نمی‌سازد، وگرنه تمرکز صفحه‌کلید
هر بار به بالای فهرست می‌پرد و علامت‌زدن چند سررسید پشت سر هم غیرممکن
می‌شود.

`ui/expiries.mjs` به مسیرهای نسبی برگشت تا مثل `ui/table.mjs` هم در
مرورگر بار شود هم در آزمون Node، و `strandedKeys` از دلش بیرون کشیده شد
تا رفتار «علامت‌خورده بیرون از زنجیره» واقعاً آزمون شود نه با جست‌وجوی متن
در کد.

۱۲ آزمون در گروه ۴۷. مجموع ۶۸۰ → ۶۹۲، صفر رد. تأیید در مرورگر واقعی:
پنل بی‌درنگ باز می‌شود، دو سررسید علامت‌خورده دیده و برداشته می‌شوند، و
خطای بالادست با دکمه تلاش دوباره فارسی نشان داده می‌شود.

---

### ۱۴۰۵/۰۵/۲۹ — ستون‌های مشخصات قرارداد، و فرض‌های نمودار

**ستون‌ها.** جدول ستون‌های تصمیم را داشت ولی مشخصات خود قرارداد و وضعیت
بازارش را نداشت. ۲۶ ستون تازه، همه از داده‌ای که قبلاً گرفته شده؛ هیچ
درخواست تازه‌ای به بازار اضافه نشد. ۴۸ → ۷۴ ستون.

قاعده‌ای که شکل این ستون‌ها را تعیین کرد: چیزی جمع می‌شود که جمعش معنی
داشته باشد. نوشنال و ارزش ذاتی و ارزش زمانی و حجم و موقعیت باز جمع‌شدنی‌اند؛
مظنه و تلاطم ضمنی نیستند و به‌ازای هر پا فهرست می‌شوند، چون میانگین دو
مظنه عددی است که در هیچ دفتر سفارشی وجود ندارد. حجم مظنه کمینه پا را
می‌دهد و فاصله بدترین پا را، چون سقف اجرا را همان پای تنگ می‌بندد.

دو تفکیک که در تابلو یکی به نظر می‌رسند: «وجه تضمین» و «تضمین لازم» —
کاوردکال اولی را ندارد و دومی را دارد؛ و «دارایی مسدودی» که از همان تفکیک
پوششِ موتور وجه تضمین می‌آید، نه از تخمین تازه.

حذف و افزودن و جابه‌جایی ستون از قبل در `ui/table.mjs` بود و دست نخورد.

**نمودار.** نوار «فرض‌های منحنی امروز» اضافه شد: روز مانده، نوسان دلخواه،
نرخ بهره. منحنی سررسید فرض‌پذیر نیست و تکان نمی‌خورد؛ آنچه فرض‌پذیر است
ارزش‌گذاری مدل است. اسلایدر روز مانده یک متمم است — افق ارزش‌گذاری برابر
نزدیک‌ترین سررسید منهای روز مانده — و روی صفر، منحنی امروز دقیقاً روی
منحنی سررسید می‌خوابد. همین در آزمون ۴۶ قفل شد، چون اگر جهتش برعکس شود
نمودار بی‌صدا غلط می‌شود و شکلش هنوز باورپذیر است. نوار وقتی فرض‌ها از
واقعیت فاصله می‌گیرند رنگ هشدار می‌گیرد و دکمه بازگشت دارد.

ضمن آزمودن در مرورگر، یک ایراد قدیمی هم پیدا شد: برچسب قیمت پایه با لنگر
«میانه» وقتی نزدیک لبه بازه بود نیمی از متن بیرون قاب می‌افتاد و بریده
دیده می‌شد. لنگر حالا از فاصله تا لبه انتخاب می‌شود. همین برای برچسب
سربه‌سری هم اعمال شد.

### ۱۴۰۵/۰۵/۲۹ — سررسید با سقف پر، و تصمیم منبع اندازه قرارداد

**باگ گزارش‌شده کاربر.** تیک «سقف موقعیت پر» زده می‌شد ولی همان سررسید باز
در فیلترهای تحلیل تاریخی می‌آمد و وارد محاسبه می‌شد. علتش این بود که قید
فقط در `core/scan.mjs` (مسیر زنده) اعمال می‌شد و کل خانواده تحلیل تاریخی —
تحلیل تاریخی، بک‌تست سریع، بک‌تست سبد — آن را **اصلاً نمی‌دید**؛ هر سه
`flattenActiveContracts(ua)` را روی کل زنجیره صدا می‌زدند.

`blockedExpirySet` و `expiryBlocked` از `core/scan.mjs` به `core/chain.mjs`
منتقل شدند (و از scan دوباره صادر می‌شوند تا مصرف‌کننده‌ها نشکنند)، چون
فقط مسیر زنده به آن‌ها نیاز ندارد. `withoutBlockedExpiries` تازه است و
`flattenActiveContracts` حالا قید را می‌گیرد. هر سه تب تاریخی وصل شدند.

**«بقیه قراردادها را نشان نمی‌دهد».** عوض‌شدن تیک سررسید،
`invalidateLoadedHistory()` را صدا می‌زد که کارت قراردادها را می‌بندد —
بی‌هیچ پیامی. کاربر یک تیک می‌زد و همه قراردادها ناپدید می‌شدند، که دقیقاً
شبیه خرابی است. حالا گفته می‌شود چرا و چه باید کرد.

**نمایش.** سررسید پرشده در انتخابگر پنهان نمی‌شود — علامت «سقف موقعیت پر»
می‌گیرد و از انتخاب درمی‌آید. پنهان‌کردنش گیج‌کننده بود («سررسیدم کو؟») و
انتخاب‌پذیر گذاشتنش همان باگ اول بود.

چرا تحلیل تاریخی هم باید این قید را رعایت کند، با اینکه سقف «امروز» پر شده
و در گذشته نبوده: قید، تصمیم کاربر است نه استنتاج از داده. سقف پر یعنی
امروز نمی‌شود موقعیت فزاینده گرفت، پس عددی که از بازپخش گذشته آن سررسید
درمی‌آید تصمیمی را تغذیه می‌کند که اجرایش ممکن نیست.

**تصمیم منبع اندازه قرارداد.** `/api/optionmeta` مشخصات تک‌قراردادی می‌دهد
(اندازه و ضرایب A و B و C همان قرارداد) ولی به هیچ مسیر محاسبه‌ای وصل
نشد. دلیل، همان دو قیدی است که کاربر گذاشت: سرعت و ثابت‌ماندن کیفیت
محاسبات. اندازه از ردیف دیده‌بان می‌آید که همان مشخصات را دسته‌جمعی و بدون
درخواست اضافه می‌رساند، پس عدد بهتری در کار نیست و فقط هزاران درخواست
اضافه می‌ماند. ضرایب وجه تضمین هم اگر از آنجا خوانده شوند خروجی وجه تضمین
عوض می‌شود — تصمیم جداگانه‌ای است که باید آگاهانه گرفته شود، نه به‌عنوان
اثر جانبی. تصمیم و دلیلش در `server/server.mjs` بالای همان endpoint و در
کارت «اندازه قرارداد هر پا» ثبت شد تا کسی بعداً آن را «بهبود» ندهد.

آزمون ۴۴ قید را در هر سه لایه قفل می‌کند و با کد قدیم رد می‌شود (۳ ترکیب
نشتی روی سررسید بسته)، به‌علاوه نگهبانی که هر تب تاریخیِ بدون قید را
می‌گیرد.

### ۱۴۰۵/۰۵/۲۹ — اندازه قرارداد از مشخصات خودِ قرارداد

خواسته کاربر: افزایش سرمایه اندازه قرارداد را عوض می‌کند، پس اندازه باید
از مشخصات هر قرارداد استخراج شود. بررسی نشان داد زنجیره از قبل
`r.contractSize` را per-strike می‌خواند، ولی سه جا آن را دور می‌زد:

**باگ واقعی — مسیر تاریخی.** `generateHistoricalCombos` تعداد سهم پای
پایه را از `contracts[0]` می‌گرفت. آن فهرست به سررسید مرتب است، پس همیشه
از نزدیک‌ترین سررسید می‌آمد — حتی وقتی خودِ ترکیب روی سررسید دور بسته
می‌شد. اگر سری نزدیک تعدیل شده بود و دور نه، کاوردکال با تعداد سهم غلط
ساخته می‌شد و پوشش و وجه تضمین و سرمایه، همه به همان نسبت غلط. آزمون ۴۳
این را با دو سری ۱۱۰۰ و ۱۰۰۰ قفل می‌کند و بدون اصلاح رد می‌شود.

**اندازه فرضی، بی‌نشانه.** پنج جا `|| 1000` داشتند. اندازه در هر عدد پولی
ضرب می‌شود، پس فرض غلط کل ردیف را به همان نسبت غلط می‌کند — و عددی که ده
درصد غلط است دقیقاً شبیه عددی است که درست است. حالا زنجیره طبق قاعده ۲-۴
عدد نمی‌سازد: اگر تابلو اندازه ندهد صفر می‌ماند و `sizeFromSpec` پایین
است. لایه بالاتر پیش‌فرض اعلامی کاربر را می‌گذارد و ردیف برچسب «اندازه
قرارداد فرضی» می‌گیرد. ترکیبی که پاهایش دو اندازه دارند، «اندازه قرارداد
ناهمگون» می‌گیرد.

**منبع غلط ولی نهفته — مسیر زنده.** `core/scan.mjs` اندازه پای سهم را از
`strikeList[0]` می‌گرفت. امروز این عدد درست درمی‌آید، چون استراتژی‌های
دارای پای سهم همه تک‌سررسیدی‌اند و در یک سررسید همه قیمت‌های اعمال یک
اندازه دارند. به منبع درست وصل شد تا اولین استراتژی چندسررسیدیِ دارای سهم
بی‌صدا نشکند.

`contractSize` در تنظیمات از «مبنای محاسبه» به «فقط پیش‌فرض» تغییر نام و
معنی داد: مبنا نیست، جایگزینِ نشان‌دار است. `buildLegs` هم `sizes` کلیدی
گرفت، چون ترنری قبلی‌اش (`underlying ? size : size`) خودش نشانه همان
اشتباه بود: یک اندازه برای همه پاها.

نکته باز: `/api/optionmeta` مشخصات رسمی هر قرارداد را می‌دهد — اندازه و
حتی ضرایب A و B و C همان قرارداد — ولی هیچ مصرف‌کننده‌ای ندارد. اندازه
فعلاً از ردیف دیده‌بان می‌آید که همان مشخصات را دسته‌جمعی می‌رساند.

### ۱۴۰۵/۰۵/۲۹ — سه خواسته کاربر: فونت فارسی، مبنای محاسبه، مرجع فرمول‌ها

**۱. متن فارسی خوانا شد.** سه چیز ناخوانایش کرده بود و هر سه در CSS
بی‌صدا بودند. اول اندازه: ۱۵۲ اندازه قلم خام در فایل بود که ۵۹ تایشان زیر
۱۱ پیکسل بودند — خط فارسی نقطه و سرکش دارد و «ب» و «پ» و «ت» و «ث» فقط با
شمردن نقطه از هم جدا می‌شوند، پس خیلی زودتر از لاتین ناخوانا می‌شود. همه
به یک مقیاس توکنی با کف ۱۱ پیکسل منتقل شدند. دوم پیوند: یازده قاعده روی
متن فارسی `letter-spacing` می‌گذاشتند، دو تایشان با `text-transform:
uppercase` — اصطلاح تایپوگرافی لاتین روی خطی که وصل است. سوم فونت: `.unit`
و دکمه‌های نمودار متن فارسی داشتند ولی با فونت تک‌عرض لاتین رندر می‌شدند،
که حرف فارسی ندارد؛ مرورگر حرف‌به‌حرف به فونت دیگری می‌افتاد و «ثانیه» به
شکل «ثا نیه» درمی‌آمد. همین ایراد بعداً در بلوک رابطه‌های تب فرمول‌ها هم
تکرار شد و همان‌جا هم به فونت فارسی برگشت. پشته فارسی بازچیده شد: تاهوما
پیش از Segoe UI، چون پوشش فارسی Segoe UI ناقص است. `tests/guards.mjs`
بخش ۵ هر پنج قاعده را قفل می‌کند.

**۲. مبنای محاسبه از موتور به تنظیمات آمد.** چهار عدد در دل موتور سخت‌کد
بودند و در تب تنظیمات دیده نمی‌شدند، در حالی که هر چهار تا روی هر ستون
بازده اثر می‌گذارند: اندازه قرارداد (۱۰۰۰ سهم)، روز سال (۳۶۵، مخرج T و
مخرج سالانه‌سازی)، روز ماه (۳۰)، و آستانه سررسید نزدیک (۷ روز). هیچ‌کدام
ابدی نیستند — اندازه قرارداد با افزایش سرمایه تعدیل می‌شود. حالا در گروه
تازه «مبنای محاسبه» ثبت‌اند و `basisOf` آن‌ها را به موتور می‌رساند، به
همان الگوی `feesOf`. آزمون ۴۱ اثرشان را می‌سنجد و یک نگهبان دارد که هر
«تقسیم بر ۳۶۵» تازه در موتور را رد می‌کند.

**۳. مرجع فرمول‌ها در تب تنظیمات.** `core/formulas.mjs` تازه: هفده کارت
برای زنجیره مشترک محاسبه — قیمت اجرا، جریان نقد و کارمزد، وجه تضمین،
سرمایه درگیر، بازده، سربه‌سری، یونانی و احتمال، هزینه اجرا — و یک کارت
برای هر سی‌ویک استراتژی فهرست. هر کارت می‌گوید کدام کلید تنظیمات را
می‌خواند و کلیک روی نام کلید، کنترلش را در همان صفحه پیدا و برجسته
می‌کند. کاوردکال به خواست صریح کاربر گام‌به‌گام باز شده، با هر دو نرخش:
بازده ایستا و بازده اگر اعمال شود. آزمون ۴۲ نمی‌گذارد توضیح از کد عقب
بیفتد — هر استراتژی باید کارت داشته باشد، هر کارت چهار ردیف پایه، و هر
کلید نام‌برده باید در SCHEMA باشد.

اعداد پیش‌فرض با جست‌وجوی منابع بازار ایران بررسی شدند و هیچ‌کدام عوض
نشد: ضرایب وجه تضمین A = ۰٫۲ و B = ۰٫۱ و C = ۱۰٬۰۰۰ ریال، کارمزد اختیار
۰٫۰۰۱۰۳ هر سمت، کارمزد اعمال ۰٫۰۰۰۵، کارمزد سهم ۰٫۰۰۳۷۱۲ خرید و ۰٫۰۰۸۸
فروش (جمع ۰٫۰۱۲۵۱۲)، و اندازه قرارداد ۱۰۰۰ سهم.

### ۱۴۰۵/۰۵/۲۸ — هشت خواسته کاربر: از حذف تب موتور تا تحلیل تایم‌فریم

هشت خواسته در یک دور، هر کدام یک کامیت.

**۱. تب «موتور و نمودار بازده» برداشته شد.** یادگار فاز دو بود: پیش از
وصل‌شدن به بازار ثابت می‌کرد موتور مشترک کار می‌کند. حالا سی‌ویک تب
استراتژی و سه تب بک‌تست همان موتور را با قیمت واقعی صدا می‌زنند.
`ui/chart.mjs` ماند — موقعیت‌های من و نمودار روزانه تحلیل تاریخی همان
`mountPayoff` را می‌کشند. آزمون مسیر امن سرور که به آن فایل اشاره می‌کرد به
`ui/tabs/backtest.mjs` منتقل شد. ۴۰ تب → ۳۹ تب.

**۲. مشخصات موقعیت از جعبه کشیدنی به نوار ثابت.** کشیدن، جای جعبه را از
دست کاربر می‌گرفت (تا «بازنشانی جایگاه» هرجا رها شده بود می‌ماند) و چون
داخل بخش نتایج بود، پیش از اجرای تحلیل اصلاً وجود نداشت. حالا اولین فرزند
تب است و با `sticky; top: 0` بالای ناحیه پیمایش می‌چسبد؛ در مرورگر با
اسکرول ۹۰۰ پیکسلی، `y` نوار روی ۷۵ ثابت ماند. کل کد اشاره‌گر و حالت
`data-detached` رفت و یک دکمه جمع‌کردن با حافظه مرورگر جایش نشست. محتوا از
«نام و بازه» به ده قلم رسید: تاریخ ورود، تاریخ خروج، مدت نگهداری، تعداد
واحد، مبنای ورود/خروج، پایه ورود→خروج، جریان نقدی خالص، سرمایه درگیر، وجه
تضمین و نتیجه پایان — به‌علاوه قیمت خروج در کارت هر پا. کلیک روی هر خانه
ماتریس ورود×خروج هم نوار را با همان ورود و خروج پر می‌کند؛ تا دیروز
مشخصات موقعیتی را نشان می‌داد که چند کلیک قبل اجرا شده بود.

**۳. یک انتخابگر تاریخ برای کل برنامه.** تا امروز چهار شکل جدا بود: ریل
افقی کارت در دو تب بک‌تست، دو `select` بلند در ماتریس، سه نوار لغزنده در
بازه و نمودار روزانه، و یک ورودی متنی آزاد در موقعیت‌های من. حالا
`ui/datewheel.mjs` تنها پیاده‌سازی است: ستون کارت با پیمایش چرخ ماوس.
عمودی، چون کارت دوخطی (نام روز و تاریخ شمسی) در ستون جا می‌گیرد؛ ریل افقی
هر کارت را به ۱۴۸ پیکسل عرض مجبور می‌کرد. چرخ ماوس فقط وقتی رویداد را
می‌گیرد که جایی برای رفتن باشد — در دو سر فهرست رها می‌شود تا نشانگر روی
جعبه صفحه را قفل نکند. در مرورگر: کارت‌ها هم‌راستا در `x` و پشت هم در `y`،
چرخ انتخاب را جابه‌جا کرد و `defaultPrevented` گرفت، و در انتهای فهرست
رهایش کرد. نوارهای لغزنده اندیس روز را نگه می‌داشتند؛ حالا خودِ تاریخ در
`dataset.value` است و یک تبدیل کمتر بین اندیس و تاریخ داریم.

**۴. قیمت دستی هر پا در روز ورود و روز خروج بک‌تست سریع.**
`replayHistory` کلید `manualExit` گرفت و `closeAtDate` آرگومان `manuals`.
قیمت دستی خروج فقط روی روز پایان می‌نشیند، نه روی همه روزهای مسیر — کاربر
یک قیمت برای یک روز وارد کرده و پخش‌کردنش روی کل مسیر یعنی ساختن داده‌ای
که وجود ندارد. `manualPriceCheck` سه حالت را جدا می‌کند: داخل بازه، بیرون
از بازه، و نامعلوم؛ حالت سوم برای وقتی است که خودِ کمترین و بیشترین آن روز
موجود نباشد و «در بازه بود» ادعای بی‌پشتوانه باشد. بیرون بودن جلوی محاسبه
را نمی‌گیرد — سناریوی «اگر با این قیمت می‌بستم» تمام معنایش همین است — ولی
زیر هر ورودی پیام با بازه واقعی همان روز می‌نشیند. در مرورگر با ورودی
۹۹۹۹۹۹ پیام «در بازه روز نیست؛ بازه معامله‌شده ۲٬۶۰۴ تا ۲٬۹۳۶ بود» آمد و
محاسبه ادامه یافت. قیمت دستی با تعویض ترکیب یا تاریخ پاک می‌شود.

**۵. نمای مسیر در سه گام.** اول عملکرد کلی بازه با برچسب بازه و تعداد روز
معتبر؛ بعد جدول روزبه‌روز از ورود تا خروج با قیمت و اثر هر پا، سود ناخالص
و خالص، کارمزد، بازده، افت از قله، وجه تضمین و وضعیت؛ بعد پنل ریزمعامله.
هر ردیف روز با کلیک یا صفحه‌کلید ریزمعاملهٔ همان روز را باز می‌کند و پنل
تاریخ روز باز‌شده را در عنوانش می‌نویسد. `combinedBacktestPath` آرگومان
`intradayDate` گرفت؛ بدون آن، برای یک روز میانی همه روزهای بعدش هم در مسیر
می‌ماندند و نمودار یک پرش دروغین نشان می‌داد. ریزمعاملهٔ هر روز کش می‌شود
چون رفت‌وبرگشت بین روزها نباید همان چند درخواست را دوباره بفرستد.

**۶. تحلیل کل بازه روی تایم‌فریم انتخابی.** چهار تابع تازه در
`core/backtest.mjs`: `bucketIntradayPath`، `intradayHoldingSummary`،
`timeOfDayProfile` و `intradayEntryExitProfile`. سه قاعده در همه‌شان یکی
است — سطل بی‌مشاهده ساخته نمی‌شود، هر عدد تجمیعی تعداد نمونه‌اش را همراه
می‌برد، و هیچ قیمتی درون‌یابی نمی‌شود. مدت سود و زیان با «ثانیهٔ
مشاهده‌شده» شمرده می‌شود نه ثانیهٔ تقویمی، و آفست صفر در هیچ‌کدام از دو
سطل نمی‌افتد. ماتریس «کِی وارد شوی و کِی خارج» عمداً از آفست موقعیتِ روز
ورود جداست: آنجا قیمت ورود ثابت است، اینجا هر دو سر متغیرند؛ رتبه‌بندی با
میانه است و کف سطل پنج دقیقه، چون سطل یک‌دقیقه‌ای بیش از بیست هزار جفت در
هر روز می‌سازد — و همین کف صریح در رابط گفته می‌شود. در رابط: هشت کارت،
چهار نمودار (آفست کل، تفکیک پاها، بازده در برابر پایه، قیمت نماد پایه)، و
چهار جدول. در مرورگر روی نمونه نُه‌روزه: ۱۲۶ سطل پانزده‌دقیقه‌ای که با
تعویض به سی دقیقه بدون هیچ درخواست تازه‌ای به ۶۳ سطل رسید، ۱۴ ردیف رفتار
ساعتی، ۱۹۶ خانه ماتریس، و «بهترین جفت: ورود ۰۹:۰۰ و خروج ۱۰:۰۰ روی ۹ روز».

**۷. سررسیدهای با سقف موقعیت پر.** روی برخی نمادها — امروز اهرم — سقف
موقعیت باز یک سررسید پر می‌شود و فقط آفست موقعیت قبلی ممکن می‌ماند.
برنامه این را نمی‌دانست و همچنان برای آن سررسید استراتژی پیشنهاد می‌داد.
این وضعیت در تابلو نیست و کارگزار اعلامش می‌کند، پس ورودی دستی است و جایش
نوار بالای برنامه است — روی هر سی‌ویک تب استراتژی و برترین موقعیت‌ها اثر
دارد. مقدار در `data/settings.json` می‌نشیند نه حافظه مرورگر، چون وارد
محاسبه می‌شود. `core/scan.mjs` پیش از ساخت هر ترکیب فیلتر می‌کند — یک
نقطه، چون همه پیشنهادها از همان‌جا رد می‌شوند. سررسید بسته اصلاً ترکیب
نمی‌سازد و در هیچ سطل قیف دیده نمی‌شد، پس شمارنده `blockedExpiry` اضافه شد
و نوار تشخیص صریح می‌گوید چند سررسید و چرا کنار رفته. زنجیره فقط با باز
شدن نوار گرفته می‌شود.

**۸. بردن موقعیت از آزمون همه استراتژی‌ها به بک‌تست سریع.** پوسته برنامه
شنونده `hashchange` گرفت؛ راه دیگر این بود که تب مبدأ `open` را از
`ui/app.mjs` وارد کند که یک حلقه وارد‌کردن می‌ساخت. جعبه تحویل
`state.handoff` است نه حافظه مرورگر، و تب مقصد بلافاصله برش می‌دارد و
پاکش می‌کند. فقط انتخاب‌ها منتقل می‌شوند نه نتیجه‌ها — اگر نتیجه کپی
می‌شد، دو تب می‌توانستند دو عدد نشان دهند. هرچه چیده نشد صریح گزارش
می‌شود.

**آزمون‌ها:** ۴۸۰ → ۵۹۳. هر گروه با برگرداندن عمدی اصلاح امتحان شد؛ دو
آزمون پس از همان بررسی سفت‌تر شدند چون با `if (false)` و با تغییر برچسب
همچنان سبز می‌ماندند.

**راستی‌آزمایی مرورگر:** چون TSETMC از محیط اجرا در دسترس نیست، یک سرور
بدلی با داده هم‌شکل بالادست (شامل عبور از `normalizeTrades`، همان کاری که
سرور واقعی می‌کند) ساخته و کل مسیر در کرومیوم ۱۴۱ اجرا شد: صفر خطای
کنسول، صفر پیمایش افقی ناخواسته در صفحه و در `stage`، ماتریس هم‌حرکتی ۳۵۷
پیکسل در قاب ۳۵۷ پیکسلی، و ماتریس ورود×خروج ۱۲۱۰ در قاب ۱۲۱۰. معامله‌های
۰۸:۴۵ و ۱۳:۰۵ نمونه، طبق انتظار بیرون از جلسه کنار گذاشته شدند.

**محدودیت:** داده این راستی‌آزمایی ساختگی است، نه تابلوی واقعی. نام میدان
ابطال در پاسخ واقعی `ClosingPrice/GetTrade` هم هنوز تأیید نشده و
`CANCEL_KEYS` ندانستن را در `canceledKnown` علامت می‌زند.

### ۱۴۰۵/۰۵/۲۷ — بازبینی خط زمانی مشترک: چهار اصلاح

بازبینی کار عامل قبلی روی همین شاخه. سه ایراد واقعی و یک ایراد چیدمانی
بسته شد؛ بقیه کار تأیید شد.

- **حجم تجمعی سطر دوبار شمرده می‌شد.** `cumulativeVolume` هر نقطه از جمعِ
  حجم پاها بسته می‌شد. وقتی دو پا روی یک قرارداد بنشینند (نسبتی، رول)، یک
  معامله فیزیکی به هر دو اندیس می‌نشیند و جمع پاها آن را دو بار می‌شمارد:
  اندازه‌گیری روی دو پای هم‌قرارداد با ۱۵۰ قرارداد واقعی، عدد ۳۰۰ می‌داد.
  حالا جمع رویدادها نگه داشته می‌شود — همان واحدی که `intervals.volume` با
  آن ساخته می‌شود. این میدان هنوز مصرف‌کننده‌ای در رابط ندارد، ولی عدد
  غلطِ خفته هم عدد غلط است.
- **نام قرارداد بالادست در برچسب سری نمودار فرار داده نمی‌شد.** تا پیش از
  این تغییر برچسب‌ها رشته ثابتِ خودِ کد بودند؛ حالا `nameOf(leg)` در آن‌ها
  می‌نشیند و مستقیم داخل `innerHTML` راهنما و Tooltip می‌رفت. با نامی حاوی
  `<img src=x onerror=…>` در مرورگر واقعی، سه عنصر تزریق‌شده ساخته شد و
  کد اجرا شد. فرار حالا داخل خودِ `chart` انجام می‌شود تا فراخوان بعدی
  نتواند دوباره فراموشش کند.
- **ماتریس هم‌حرکتی همیشه افقی بود.** `.history-table` کف `min-width: 920px`
  دارد — درست برای جدول‌های پهن تاریخچه، غلط برای یک مربع ۲×۲ در ستون
  باریک تحلیل: پهنای اندازه‌گیری‌شده ۹۲۰ در قابی ۲۸۸ پیکسلی. با
  `.history-table.backtest-correlation` به ۳۳۱ رسید. دو کلاس لازم بود چون
  قاعده `history-table` پایین‌تر در همان فایل می‌آید.
- **توضیح ماتریس داخل جعبه پیمایش بود** و با جدول افقی کشیده می‌شد؛ همان
  جمله‌ای که «مقدار نامعلوم» را از «همبستگی صفر» جدا می‌کند. حالا بیرون از
  قاب، هم‌عرض ستون.
- `TASK_STATUS.md` «شاخه انتشار» را به نام شاخه کوتاه‌عمر تسک تغییر داده
  بود؛ آن شاخه بعد از ادغام حذف می‌شود و سند روی `main` به چیزی اشاره
  می‌کرد که وجود ندارد. برگشت به `main`.
- پنج آزمون رگرسیون تازه: دو تا برای حجم هم‌قرارداد، یکی برای فرار برچسب،
  دو تا برای جای توضیح و کف پهنای ماتریس. هر پنج‌تا با برگرداندن اصلاح
  عمداً امتحان شدند و رد دادند. مجموع ۴۸۰ آزمون.
- **راستی‌آزمایی مرورگر.** چون TSETMC از این محیط در دسترس نیست، یک سرور
  بدلی با داده ساختگی هم‌شکل بالادست ساخته شد و کل مسیر تب در کرومیوم
  اجرا شد: ۴۵۴ نقطه مشترک از ۰۹:۰۰:۰۰ تا ۱۲:۲۹:۵۳، هر چهار نمودار پله‌ای
  با محور ساعت، ۱۴ ردیف پنجره، ماتریس ۲×۲، ۲۲۸ ردیف نوار، بدون خطای
  کنسول. معامله‌های ۰۸:۴۵ و ۱۳:۰۵ کنار گذاشته شدند.
  محدودیت: این داده ساختگی است، نه تابلوی واقعی؛ ادعای عامل قبلی درباره
  آزمون زنده روی `ضهمن5033 + ضهمن5034` از این‌جا قابل تأیید نبود.

### ۱۴۰۵/۰۵/۲۶ — خط زمانی مشترک ریزمعامله بک‌تست سریع

- معاملات همه پاهای استراتژی و نماد پایه فقط در جلسه ۹:۰۰ تا ۱۲:۳۰ روی
  اتحاد ثانیه‌های مشاهده‌شده هم‌تراز شدند. تا وقتی هر پا دست‌کم یک معامله
  ندارد عدد مالی ساخته نمی‌شود؛ پس قیمت پیش از اولین مشاهده یا قیمت مدل جای
  داده واقعی نمی‌نشیند.
- هر نقطه قیمت آخر، حجم همان ثانیه، حجم تجمعی، تعداد معاملات، سن قیمت و
  وضعیت تازگی هر پا را حمل می‌کند. معامله‌های هم‌ثانیه با آخرین قیمت همان
  ثانیه و حجم جمع‌شده ثبت می‌شوند و پیش‌گشایش/پس از ۱۲:۳۰ کنار می‌رود.
- نمودارهای تعاملی پله‌ای با محور واقعی ساعت برای آفست کل، اثر مستقل هر پا،
  درصد حرکت قیمت و حجم تجمعی اضافه شد. نمودار پله‌ای است چون بین دو معامله
  مشاهده تازه‌ای نداریم و خط مورب ادعای حرکت تدریجی نادرست می‌ساخت.
- کارت‌های بهترین/بدترین لحظه، افت از قله، درصد زمان در سود و درصد زمان
  دارای قیمت تازه، جدول پنجره‌های ۱۵ دقیقه‌ای، ماتریس هم‌حرکتی اثر پاها،
  نوار مشترک قیمت/حجم و CSV همه نقاط اضافه شد.
- دوازده آزمون تازه مرز ساعت بازار، هم‌ترازی نماد پایه، تجمیع معامله‌های
  هم‌ثانیه، حجم، سن، تازگی، خلاصه بازه‌ای، ماتریس و قرارداد رابط را قفل
  کردند. آزمون زنده مرورگر روی اسپرد `ضهمن5033 + ضهمن5034` در
  ۱۴۰۵/۰۵/۲۵، ۳۰ نقطه مشترک از ۰۹:۱۵:۰۱ تا ۱۲:۲۷:۳۰ و همه نمودارها را بدون
  خطای کنسول ساخت. نسخه به `0.11.0` رسید.

### ۱۴۰۵/۰۵/۲۶ — اصلاح endpoint و کارت قیمت بک‌تست سریع

- مسیر نامعتبر `ClosingPrice/GetTrade/.../false` که پاسخ ۴۰۴ می‌داد با
  endpoint تاریخی تک‌معامله `Trade/GetTradeHistory/.../true` جایگزین شد.
  سازنده مسیر پس از اعتبارسنجی کد ابزار و تاریخ در `server/guard.mjs` قرار
  گرفت تا نام و شکل endpoint در آزمون رگرسیون قفل شود.
- دو پنل «قیمت روز ایجاد/خروج» دیگر عکس دارایی پایه را تکرار نمی‌کنند.
  `strategyLegSnapshots` قیمت‌های پنج‌گانه، حجم، تعداد و ارزش را برای هر پای
  انتخاب‌شده جدا می‌سازد؛ دارایی پایه فقط در استراتژی‌هایی نمایش داده می‌شود
  که واقعاً پای underlying دارند.
- انتخابگر تاریخ عمودی به نوار افقی کارت‌محور با اسکرول چسبان، برجستگی تاریخ
  فعال و پشتیبانی کلیدهای چهار جهت تبدیل شد. همین ظاهر مشترک در دو بخش بک‌تست
  یکسان می‌ماند.
- پنج آزمون تازه endpoint صحیح، رد مسیر ناامن و استقلال قیمت پاها از قیمت
  نماد پایه را پوشش دادند. نسخه به `0.10.1` رسید.
- دریافت ریزمعامله هر نماد مستقل شد تا شکست نماد پایه یا یک قرارداد، پاسخ
  موفق بقیه را دور نریزد. آزمون زنده روی `ضهمن5034` نیز پاسخ روزهای
  ۱۴۰۵/۰۵/۲۴ و ۱۴۰۵/۰۵/۲۵ را با وضعیت ابطال معتبر تأیید کرد.
- محاسبه روز نگه‌داری از اختلاف تاریخ تقویمی استفاده می‌کند؛ اجرای تست نزدیک
  مرز UTC دیگر موقعیت ثبت‌شده «امروز» را یک‌روزه گزارش نمی‌کند.

### ۱۴۰۵/۰۵/۲۶ — آزمون هم‌زمان همه استراتژی‌ها

- تب مستقل «آزمون همه استراتژی‌ها» بعد از بک‌تست سریع اضافه شد. کاربر یک
  نماد، روز ایجاد، روز سنجش، مبنای پنج‌حالته ورود/خروج، تعداد واحد و حداقل
  ارزش/حجم پایه و هر قرارداد را انتخاب می‌کند؛ همه الگوهای قابل اجرا در
  Worker ساخته و تا تاریخ یکسان بازپخش می‌شوند.
- فقط ترکیبی وارد گزارش می‌شود که دقیقاً در روز سنجش قیمت و نقدشوندگی معتبر
  همه پاها را داشته باشد. سقف ترکیب هر استراتژی قابل تنظیم و سقف‌خوردن آن در
  گزارش آشکار است؛ هیچ قیمت گمشده‌ای از روز قبل پر نمی‌شود.
- گزارش تعداد و درصد سودده/زیان‌ده/خنثی، دسته استراتژی، برنده و بازنده،
  بهترین و بدترین ترکیب و رتبه‌بندی مقاوم با میانه بازده را نشان می‌دهد.
  هر ردیف با کلیک مسیر روزانه تعاملی، تغییر نماد پایه و اثر مالی هر پا را
  باز می‌کند.
- قیمت دستی برای هر پا مستقل است. تحلیل حساسیت پویا با دامنه و گام دلخواه و
  ماتریس ۲۵حالته مبنای ورود×خروج نیز روی همان ترکیب باز می‌شود؛ این طراحی
  قیمت‌های متفاوت قراردادها را با یک قیمت دستی مشترک مخلوط نمی‌کند.
- `core/portfolio.mjs` تجمیع خالص و آزمون‌پذیر گزارش را انجام می‌دهد. چهار
  آزمون تازه شمارش معتبر، درصد برد، رتبه‌بندی با میانه و گزارش دسته‌ها را
  قفل کردند. آزمون واقعی مرورگر با نماد «بساما» ۲۹ استراتژی و ۴۳۰ ترکیب
  معتبر را بدون خطای کنسول گزارش کرد؛ دسکتاپ و عرض موبایل نیز بررسی شدند.

### ۱۴۰۵/۰۵/۲۶ — بازبینی بک‌تست سریع

بازبینی فنی شاخهٔ `codex/add-quick-backtest`. کار از نظر معماری سالم بود:
موتور مشترک ماند (`grossCash`/`entryFees`، همان فرمول `history.mjs`)، رنگ‌ها
از توکن آمدند، و برچسب‌گذاری صداقت عددی جدی گرفته شده بود. سه اصلاح اعمال شد:

- **ابطال معامله دیگر «نمی‌دانیم» را «باطل نشده» جا نمی‌زند.** نرمال‌ساز
  ریزمعامله از `server/server.mjs` به `core/backtest.mjs` منتقل شد (سرور در
  زمان import پورت می‌گیرد، پس آزمون‌پذیر نبود) و حالا کنار `canceled` یک
  `canceledKnown` هم می‌دهد. نام میدان ابطال در پاسخ واقعی TSETMC تأیید نشده
  است؛ اگر بالادست ساکت باشد، نوار اجرا صریح می‌گوید معاملهٔ باطل‌شده کنار
  گذاشته نشده. شش آزمون این تفکیک را قفل می‌کنند.
- **راهنمای نمودار دیگر دو چیپ هم‌رنگ نشان نمی‌دهد.** `basePct` و
  `baseCumulativePct` یک خط منطقی‌اند که بین دو نام میدان تقسیم شده؛ حالا
  هم‌برچسب‌اند و راهنما بر پایهٔ برچسب یکتا ساخته می‌شود.
- **مرز روز آخر روی نمودار دیده می‌شود.** محور افقی بر پایهٔ اندیس است، پس
  چند صد نقطهٔ ریزمعامله بخش روزانه را باریک می‌کند. یک خط‌چین با برچسب
  «شروع ریزمعامله روز آخر» اضافه شد تا شیب تند انتها با حرکت چندروزه اشتباه
  نشود.

نگرانی اولیه دربارهٔ هزینهٔ مبنای `FIRST` (ماتریس از ۱۶ به ۲۵ خانه) با
اندازه‌گیری رد شد: سناریوی ۹۰ روزه با ۴ پا، میانه ۶۰ms برای ۲۵ خانه و ۲.۴ms
برای هر خانه. اختلاف با حالت ۱۶ خانه حدود ۲۲ میلی‌ثانیه است — نامحسوس. مبنا
ماند.

بررسی: ۴۵۴ آزمون موتور، ۱۷ نگهبان، صفر رد. تب در کرومیوم بدون خطای کنسول بالا
آمد و سبک‌های تازهٔ نمودار جداگانه رندر و دیده شدند. نمودار با دادهٔ واقعی
بازار در این محیط قابل دیدن نبود (بالادست از پشت پروکسی در دسترس نیست).

### ۱۴۰۵/۰۵/۲۵ — بک‌تست سریع بعد از ماتریس‌ها

- تب مستقل «بک‌تست سریع» بعد از تحلیل تاریخی اضافه شد؛ انتخاب نماد،
  استراتژی، تعداد واحد، ترکیب قرارداد و دو تاریخ فقط از روزهای دارای داده
  معتبر انجام می‌شود.
- انتخابگر چرخشی تاریخ و دکمه ریلی پنج‌حالته قیمت شامل اولین، آخرین، پایانی،
  کمترین و بیشترین ساخته شد؛ حجم، تعداد و ارزش معاملات همان روز کنار قیمت‌ها
  دیده می‌شود.
- نقطه پایانی ریزمعامله روزانه به سرور اضافه شد. مسیر چندروزه تا روز قبل
  روزانه است و روز آخر می‌تواند با آخرین معامله مشاهده‌شده هر پا تا هر لحظه
  بازپخش شود؛ این ارزش‌گذاری صریحاً تضمین اجرای هم‌زمان معرفی نمی‌شود.
- سود و زیان مبلغی/درصدی، تغییر نماد پایه، کوتاه‌ترین زمان رسیدن به سود،
  بهترین و بدترین نقطه و اثر خالص هر پا در نمودار و جدول تعاملی گزارش می‌شود.
- پروفایل افق نگهداری ماتریس به نتیجه وصل شد تا افق مقاوم چند ورود با مسیر
  مشخص بک‌تست مقایسه شود و انتخاب پس‌نگر یک خانه با «بهینه» اشتباه نشود.
- آزمون‌های مبنای اولین قیمت، اعتبار تاریخ مسیر سرور، ریزمعامله چندپا، اثر هر
  پا، تغییر پایه و مسیر ترکیبی اضافه شد؛ نسخه به `0.9.0` رسید.

### ۱۴۰۵/۰۵/۲۵ — حذف پنل جزئیات فنی نوار سلامت

به درخواست کاربر، پنل بازشونده‌ای که «درخواست بالادست»، «اصابت کش»، «تأخیر
بالادست»، «سن عکس سرور»، «قطعی اتصال» و «آخرین خطا» را نشان می‌داد حذف شد.
دکمه «جزئیات»، نشانه‌گذاری HTML، سبک‌های اختصاصی و کد به‌روزرسانی عناصر آن
از `ui/index.html`، `ui/style.css` و `ui/app.mjs` کنار رفت. شاخص‌های تصمیم‌ساز
نوار سلامت (بازار، اتصال، آخرین دریافت، تعداد قرارداد و خطای جاری) حفظ شدند.
دو آزمون رگرسیون در `tests/run.mjs` بازگشت پنل یا دسترسی دوباره به شناسه‌های
حذف‌شده را رد می‌کنند.

### ۱۴۰۵/۰۵/۲۵ — مدرن‌سازی رابط کاربری (CSS)

- **سیستم طراحی:** توکن‌های جدید `--radius-sm/md/lg/pill`، `--shadow-sm/md/lg`
  و `--transition-fast/med` اضافه شدند؛ توکن‌های سایه در هر دو پوسته (دفتر و
  تابلو) مقدار جدا گرفتند تا پوسته دوم عقب نماند. `--radius` از ۶px به ۸px
  ارتقا یافت.
- **نوار بالا:** اثر شیشه‌ای (glassmorphic) با `backdrop-filter: blur(14px)`،
  مرز نیمه‌شفاف و سایه ملایم.
- **دکمه‌ها:** `.btn`/`.primary` از gradient به رنگ جامد accent، گوشه `14px`،
  hover با بالاآمدن ۲px و سایه عمیق‌تر، active با `scale(.97)`.
  `.ghost` از gradient به پس‌زمینه ساده `--panel`، `.btn.sec` با hover accent.
  `.step-btn` دایره‌ای (TradingView-style). `.chip` با `font-weight: 500` و
  hover بالاآمدن ملایم.
- **فیلدها:** `border-radius: 8px`، `padding: 8px 12px`، سایه داخلی ملایم،
  حلقه فوکوس درخشان accent با `box-shadow: 0 0 0 3px`.
- **کارت‌ها:** مرز نیمه‌شفاف، `box-shadow-md`، hover با سایه عمیق‌تر.
- **جداول:** سرستون شیشه‌ای با `backdrop-filter`، zebra-striping ملایم،
  مرز جداکننده ضخیم‌تر `2px`. ردیف‌ها با transition نرم‌تر.
- **ناوبری کناری:** جست‌وجو با سایه داخلی و فوکوس درخشان، `.tab-btn` با
  نوار فعال ۴px (از ۳px)، تراز بَج‌ها بزرگ‌تر و خواناتر.
- **اسکرول‌بار:** سفارشی ۶px باریک با رنگ هماهنگ تم (هم webkit هم Firefox).
- **تولتیپ:** شیشه‌ای با `backdrop-filter: blur(14px)` و سایه عمیق.
- **KPI:** نوار کناری ۴px، hover با بالاآمدن و سایه. تگ‌ها با فضای بیشتر.
- **سایر:** `::selection` رنگ accent، `text-rendering: optimizeLegibility`،
  `.scan-progress-fill` با `border-radius` کپسولی.
- رنگ سخت‌کدشده CSS بیرون بلوک توکن از ۱۴ به **۹** رسید، و سقف جغجغه در
  `tests/guards.mjs` هم به ۹ پایین آمد تا این دستاورد قفل شود.
- **بازبینی (Claude):** چهار اصلاح روی همین شاخه — راه‌راه ردیف‌ها با
  `:not(.atm):not(.picked)` صریح شد تا با ردیف نزدیک‌به‌پول و نامزد رول
  هم‌وزن نماند و به ترتیب نوشتن قواعد وابسته نباشد؛ یک بلوک عمومی
  `prefers-reduced-motion` برای پوشش ترنزیشن‌ها و جابه‌جایی‌های تازه؛ حذف
  توکن بی‌استفاده `--ease`؛ و پایین‌آوردن سقف نگهبان.
- **اصلاح نگهبان (Antigravity):** `rel()` در `tests/guards.mjs` جداکننده
  مسیر ویندوز را عادی می‌کند؛ بدون آن، نگهبان رنگِ جاوااسکریپت روی ویندوز
  بی‌صدا صفر می‌شمرد و الکی سبز می‌ماند.
- **معیار:** ۴۳۸ آزمون موتور و ۱۷ نگهبان، صفر رد.

### ۱۴۰۵/۰۵/۲۴ — قرارداد کار عامل‌ها، CI و نگهبان‌های قواعد

- مخزن از این پس با کار **ترتیبی** چند عامل (Claude، Codex، Antigravity)
  پیش می‌رود: در هر لحظه یک عامل، روی شاخه کوتاه‌عمر مخصوص همان تسک، با
  پیشوند نام عامل (`ag/`، `codex/`، `claude/`). پوش مستقیم روی `main`
  ممنوع؛ ادغام فقط پس از بازبینی و هیچ عاملی PR خودش را ادغام نمی‌کند.
- `AGENTS.md` قرارداد کامل شد — قواعد فنی (صفر وابستگی npm، ESM خالص،
  رابط و اعداد فارسی، صداقت عددی، موتور مشترک، رنگ از توکن) و گردش کار.
  `CLAUDE.md` و `GEMINI.md` اشاره‌گر به همان‌اند تا هر ابزاری با هر
  قرارداد نام‌گذاری، همان یک متن را بخواند.
- `tests/guards.mjs` اضافه شد: ۱۷ نگهبان که قواعد را ماشینی می‌سنجند،
  چون متن قاعده خواهش است و آزمونش قانون. دو نگهبان رنگ به‌صورت جغجغه
  کار می‌کنند — سقف امروز (۱۸ در `ui/*.mjs`، ۱۴ در `ui/style.css` بیرون
  بلوک توکن) را نگه می‌دارند و با هر توکن‌سازی باید پایین بیایند.
- `.github/workflows/ci.yml` روی هر PR بررسی نحوی همه ماژول‌ها، آزمون
  موتور و نگهبان‌ها را اجرا می‌کند. بدون `npm install`، چون نباید باشد.
- شاخه `main` با ruleset محافظت شد: PR اجباری، CI سبز اجباری، بدون
  force-push و بدون حذف، و فهرست Bypass خالی — چون عامل‌ها با اعتبارنامه
  همان مالک وصل می‌شوند و هر استثنایی برای مالک، استثنا برای عامل‌هاست.
- `AUTOMATION.md` به‌عنوان فایل تاریخی علامت خورد؛ اجرای خودکار ساعتی
  متوقف است و در تعارض، `AGENTS.md` مقدم است.
- معیار انتشار: **۴۳۸ آزمون موتور و ۱۷ نگهبان، صفر رد**.

### ۱۴۰۵/۰۵/۲۳ — نسخه ۰٫۸٫۰: کارت شناور، ماتریس واحد و خروجی جامع

- کارت مشخصات موقعیت با دستگیره Pointer در صفحه جابه‌جا می‌شود، قابل تغییر
  اندازه است، دکمه بازنشانی دارد و متن مشخصات و پاهای آن درشت‌تر شده است.
- کدهای خام دارایی پایه و قراردادها از نام جایگزین، جست‌وجو، کارت، جدول و
  فایل خروجی حذف شدند؛ شناسه داخلی فقط برای واکشی داده و محاسبه باقی می‌ماند.
- ماتریس تکراری حذف شد و یک ماتریس با همه تنظیمات مستقل و دو حالت «انباشته
  از ورود» و «تغییر همان روز» باقی ماند. فونت پیش‌فرض آن بزرگ‌تر است و زوم
  ۷۵ تا ۲۵۰ درصد دارد.
- CSV جامع و فایل چندبرگی Excel همه خانه‌های معتبر، توضیحات استراتژی، مبانی
  قیمت، فیلترها، جریان نقدی، سرمایه، وجه تضمین، نقدشوندگی، کارمزد، افت،
  جزئیات هر پا و پروفایل افق نگهداری را صادر می‌کنند.
- نمودار اثر تجمعی پاها سری پررنگ «برآیند کل پاها» گرفت و ظاهر دکمه‌های اصلی،
  ثانویه و ریلی با حالت‌های hover/active/disabled یکپارچه شد.
- چهار آزمون رگرسیون برای داده جامع ماتریس، نام‌گذاری امن و نشت‌نکردن شناسه
  خام اضافه شد؛ بررسی نحوی، `diff --check` و **۴۳۸ آزمون با صفر رد** معیار
  انتشار است.

### ۱۴۰۵/۰۵/۲۳ — نسخه ۰٫۷٫۰: کارت ثابت قرارداد و کنترل مستقل ماتریس

- با انتخاب هر ترکیب جدول، کارت چسبان مشخصات کامل همان موقعیت ساخته می‌شود:
  پایه و کد آن، تاریخ‌ها، مبنای ورود/خروج و برای هر پا نماد، کد، خرید/فروش،
  نوع، اعمال، سررسید، اندازه، نسبت کل و قیمت ورود. کارت هنگام اسکرول نتایج
  ثابت می‌ماند.
- محتوای اصلی و پنل سمت راست به دو ناحیه اسکرول مستقل تبدیل شدند و دستگیره
  هر دو در لبه راست قاب خودشان قرار گرفت.
- ماتریس ورودهای پیاپی اکنون فهرست مستقل ترکیب قراردادها، مبنای جداگانه
  ورود و تسویه، بازه شروع/پایان، تعداد واحد و چهار حد نقدشوندگی دارد. جزئیات
  Heatmap نیز دقیقاً با همین آرگومان مستقل بازسازی می‌شود.
- Tooltip نمودار سود و زیان، فاصله نقطه زیر موس از قیمت پایه همان روز را هم
  به ریال و هم درصد نشان می‌دهد.
- بررسی نحوی، `diff --check` و **۴۳۴ آزمون با صفر رد** عبور کردند. آزمون
  مرورگر حضور ده کنترل ماتریس، حالت sticky کارت و اسکرول مستقل دو قاب را
  تأیید کرد؛ API تاریخی بالادست در نوبت کنترل زنده با timeout پاسخ نداد.

### ۱۴۰۵/۰۵/۲۳ — نسخه ۰٫۶٫۰: ماتریس ورود×خروج و نمودارهای تصمیم روزانه

- زیر ماتریس ورودهای پیاپی، Heatmap تعاملی تازه‌ای اضافه شد که ردیف را تاریخ
  ورود و ستون را تاریخ خروج می‌گیرد و بین بازده انباشته و تغییر بازده همان روز
  جابه‌جا می‌شود.
- کلیک هر خانه مسیر کامل معامله را بازسازی می‌کند: بهترین/بدترین نقطه، اولین
  روز سود، درصد سود حفظ‌شده تا خروج، بازده پایه، کارمزد، افت و اثر هر پا.
- پروفایل افق نگهداری، نتایج همه ورودها را برای هر تعداد روز معاملاتی تجمیع
  می‌کند و با «میانه منهای یک‌چهارم انحراف معیار» افق مقاوم‌تر را برجسته می‌کند.
- نمودارهای زمانی Tooltip نشانگر، پنج گام محور، عنوان محور و کلید مستقل هر سری
  گرفتند؛ نمودار پراکندگی و توزیع نیز اطلاعات هاور/فوکوس روشن دارند.
- نمودار روزانه سود/زیان با قیمت‌های ورود ثابت روز اول اضافه شد. اسلایدر روز
  فقط نشانگر قیمت پایانی پایه را جابه‌جا می‌کند و سربه‌سری، فاصله، حداکثر
  سود/زیان و درصد، سود سناریویی سررسید و آفست واقعی روز را هم‌زمان می‌گوید.
  ناحیه سود/زیان، اعمال، سربه‌سری، پایه و شاخص‌های تصمیم مستقل خاموش می‌شوند.
- شش آزمون تازه برای خانه‌های تجمعی/روزانه، روز ورود، پروفایل افق و بازسازی
  مسیر اضافه شد؛ مجموع **۴۳۴ قبول و صفر رد**.

### ۱۴۰۵/۰۵/۲۴ — تحلیل تاریخی واقعی استراتژی‌ها

یک تب پایه تازه برای بازپخش قراردادهای فعال از اولین روز معامله تا آخرین
روز ساخته شد. ورود و آفست چهار مبنای مستقل آخرین/پایانی/کمترین/بیشترین
دارند؛ حالت دستی قیمت ورود هر پا را، حتی بیرون دامنه روز، می‌پذیرد. حالت
خودکار فقط استراتژی انتخاب‌شده را روی نماد پایه انتخابی ترکیب‌سازی می‌کند.

هسته تازه `core/history.mjs` محاسبه روزبه‌روز، توقف در اولین سررسید، سود
ناخالص/خالص، کارمزد، مخرج سرمایه، تغییر روزانه/تجمعی پایه، افت از قله،
مقایسه ۱۶ مبنا، حساسیت ورود و ماتریس ورودهای پیاپی را جدا از DOM نگه می‌دارد.
`worker/history-worker.mjs` اسکن ترکیب‌ها و ماتریس سنگین را بیرون نخ رابط
انجام می‌دهد. سرور دو نقطه پایانی تاریخچه دسته‌ای و فهرست قرارداد فعال گرفت
که بیرون ساعات بازار هم قابل فراخوانی‌اند.

رابط شامل کنترل ریلی تاریخ، انتخاب دستی/خودکار، ورودی واحد استراتژی، قیمت
دستی هر پا، KPI، سه نمودار زمانی، نمودار ریسک/بازده، جدول ترکیب‌ها، جدول
روزانه، تجزیه سود هر پا، ماتریس مبنا، حساسیت، ماتریس ورودهای پیاپی و CSV است.

۱۹ آزمون تازه برای ورود/آفست مستقل، داده گمشده، قیمت دستی، تاریخ و روز،
تغییرات پایه، کارمزد/سرمایه، مقیاس واحد، توقف سررسید و ترکیب‌سازی اضافه شد.
سه آزمون قدیمی مسیر ثابت هم برای ویندوز/یونیکس قابل‌حمل شدند. مجموع آزمون:
۴۱۶ قبول و صفر رد.

### ۱۴۰۵/۰۵/۲۴ — متن وضعیت اسکن دو تب aria-live نداشت (قلم پ-۶ بک‌لاگ، دور پنجاه‌ویکم)

دور ۱۳ همین رفتار را برای `#msg` تنظیمات/موقعیت‌های من داد؛ دو `span`
مشابه در `ui/tabs/strategy.mjs:90` و `ui/tabs/top.mjs:55` جا افتاده
بودند. این دو، پیام‌های زنده اسکن را می‌گیرند («مرحله یک در … میلی‌ثانیه…»،
«خطا: …»، «مرحله دو کامل…») ولی بدون `role="status"`/`aria-live`،
کاربر صفحه‌خوان هیچ‌وقت از پیشرفت یا نتیجه یک اسکن باخبر نمی‌شد —
باید خودش می‌رفت متن را دستی پیدا می‌کرد.

رفع دوخطی: `role="status" aria-live="polite"` به هر دو `span#status`
اضافه شد.

۰ آزمون تازه — یک ویژگی HTML است. تأیید با Playwright روی هر دو تب
واقعی (بدون نیاز به داده بازار، چون فقط ویژگی استاتیک عنصر بررسی شد):
تب برترین موقعیت‌ها و تب استراتژی (کاوردکال) هر دو `role="status"`/
`aria-live="polite"` روی `#status` داشتند. مجموع آزمون همچنان ۳۹۷.

### ۱۴۰۵/۰۵/۲۴ — سرستون جدول مرتب‌شدنی aria-sort داشت ولی صفحه‌کلید-پذیر نبود (قلم پ-۶ بک‌لاگ، دور پنجاهم)

دور ۳۳ `aria-sort` استاندارد را به `<th>` سرستون جدول اصلی
(`ui/table.mjs`) اضافه کرد — همان جدولی که تب‌های استراتژی و برترین
موقعیت‌ها استفاده می‌کنند. ولی خودِ `<th>` نه `tabindex` داشت نه
`keydown`، فقط یک `click` listener. یعنی ARIA به صفحه‌خوان می‌گفت «این
ستون قابل‌مرتب‌سازی است و الان این‌طرف مرتب شده» ولی کاربر صفحه‌کلیدی
هیچ‌وقت نمی‌توانست به خودِ سرستون برسد تا آن مرتب‌سازی را عوض کند —
دقیقاً همان ناهماهنگی نقش/رفتار که دور ۲۸ برای نمودار (`role="img"`
روی چیزی که کیبورد داشت) رفع کرد، این‌بار برعکس: نقش درست بود، کیبورد
نبود.

رفع: `th.tabIndex = 0` و `th.setAttribute('role', 'button')` کنار
`draggable` موجودش نشستند؛ منطق مرتب‌سازی از داخل `click` به یک تابع
مشترک `sortByThis()` بیرون کشیده شد تا `keydown` (Enter/Space) هم
همان را صدا بزند — بدون تکرار منطق.

۰ آزمون تازه — رفتار DOM/کیبورد محض است. تأیید با Playwright:
`makeTable` مستقیم با داده ساختگی — سرستون `tabindex="0"`/`role="button"`
داشت، فوکوس گرفت، Enter مرتب‌سازی را روی همان ستون برد (`aria-sort`
از `none` به `descending`، متن «مرتب بر نام ↓» هم به‌روز شد)، فاصله
بلافاصله بعدش جهت را برگرداند (`ascending`) — دقیقاً همان رفتاری که
کلیک ماوس می‌داد. مجموع آزمون همچنان ۳۹۷.

### ۱۴۰۵/۰۵/۲۴ — خطای بی‌نگهبان ریسه اسکن، دکمه اسکن را برای همیشه «در حال اسکن…» قفل می‌کرد (قلم پ-۶ بک‌لاگ، دور چهل‌ونهم)

پنجمین بازبینی هدفمند کد، بالاترین شدت تا امروز — یک باگ سرتاسری، نه
مخصوص یک تب. `ui/scanner.mjs:ask()` برای هر پیام به ریسه یک `Promise`
می‌سازد که فقط `resolve` دارد، هرگز `reject`. `worker/scan-worker.mjs`
هم هیچ `try/catch` دور بدنه `self.onmessage` نداشت. یعنی اگر هر بار
اجرای `buildChain`/`scan`/`scanAll` (یا هرجای دیگر مسیر پیام) خطا
می‌داد — یک لبه ناشناخته در داده زنجیره، هر باگ کوچک دیگری — آن پیام
هرگز جواب نمی‌گرفت: `Promise`ی متناظرش تا ابد معلق می‌ماند. هر تبی که
اسکن می‌زند (`strategy.mjs`، `top.mjs`) این `await` را داخل
`try { } finally { busy=false; runBtn.disabled=false; }` دارد — ولی
`finally` وقتی اجرا می‌شود که `await` تمام شود؛ اگر هرگز تمام نشود،
دکمه اسکن، نوار پیشرفت، همه برای همیشه در حالت «در حال اسکن…» قفل
می‌مانند، بدون هیچ پیام خطا، بدون هیچ راه بازیابی جز رفرش کامل صفحه.

دو لایه رفع:
۱. `worker/scan-worker.mjs` — بدنه `self.onmessage` به یک تابع
   `handleMessage(m)` منتقل شد و کل فراخوانی‌اش در `try/catch` نشست؛
   خطا با `self.postMessage({ type: 'error', id: m.id, error: ... })`
   برمی‌گردد به‌جای این‌که بی‌صدا گم شود.
۲. `ui/scanner.mjs:worker.onerror` — نگهبان آخر: اگر خطا اصلاً به یک
   `id` مشخص نرسد (مثلاً خرابی سطح ماژول ریسه، پیش از رسیدن به
   `try/catch` داخلی)، همه `Promise`های معلق در `waiting` یک‌جا با
   `{error}` آزاد می‌شوند. `runScan()` هم یک بررسی جا افتاده گرفت:
   مرحله دو `two.error` را حالا صریح می‌بیند (مرحله یک از قبل داشت)،
   نه این‌که شیء ناقص را بی‌قید به `onStage` بدهد.

۰ آزمون تازه — رفتار async/worker محض است، در Node بدون jsdom قابل
واحدآزمایی نیست. تأیید با Playwright، سه سناریوی مستقل: (۱) ریسه جعلی
که هر پیام را با `type:'error'` جواب می‌دهد — `chainDetail()` واقعی در
۱۳ میلی‌ثانیه با `{error: '...'}` resolve شد، نه معلق ماند؛ (۲) ریسه‌ای
که در همان سطح ماژول `throw` می‌کند (خرابی که هرگز به یک پیام نمی‌رسد) —
نگهبان `onerror` در ۱۱ میلی‌ثانیه همان `Promise` را با `{error}` آزاد
کرد؛ (۳) ریسه واقعی (بدون دستکاری) هنوز برای یک اسکن معتبر عادی درست
کار می‌کند — بدون نماد انتخاب‌شده، صفر ردیف در ۱ میلی‌ثانیه، بدون خطا.
مجموع آزمون همچنان ۳۹۷.

### ۱۴۰۵/۰۵/۲۴ — flashTimer در dispose تنظیمات/موقعیت‌های من پاک نمی‌شد (قلم پ-۶ بک‌لاگ، دور چهل‌وهشتم)

آخرین قلم چهارمین بازبینی هدفمند کد. `strategy.mjs` و `top.mjs` هر دو
`flashTimer` خودشان را در تابع dispose برگشتی پاک می‌کنند؛
`settings.mjs` و `positions.mjs` — با همان الگوی `flash()`/`flashTimer`
دور ۲۷ — این خط را نداشتند. بستن هرکدام از این دو تب بلافاصله بعد از
یک پیام flash (مثلاً «ذخیره تنظیمات» یا کلیک نامعتبر «افزودن») یک
`setTimeout` معلق باقی می‌گذاشت که بعداً روی یک گره DOM جداشده
(`#msg`ی تبی که دیگر باز نیست) اجرا می‌شد — بی‌خطر ولی نشتی، درست
همان چیزی که تابع dispose برای جلوگیری از آن هست.

رفع تک‌خطی هر دو جا: `clearTimeout(flashTimer)` به تابع dispose برگشتی
اضافه شد، کنار پاک‌سازی‌های موجود (`spy.disconnect()`، `clearInterval`،
`chart?.destroy()`).

۰ آزمون تازه — رفتار پاک‌سازی چرخه عمر DOM محض است. تأیید با
Playwright، این‌بار نه فقط با نبود خطا بلکه با شمارش واقعی: سراسری
`window.clearTimeout` جاسوسی شد. در تب تنظیمات، کلیک «بازگشت به
پیش‌فرض» یک بار `clearTimeout` زد (خودِ `flash()`)؛ تعویض فوری تب یک
بار دیگر زد — دقیقاً `dispose` تازه. همان الگو در تب موقعیت‌های من با
کلیک نامعتبر «افزودن» هم تکرار و تأیید شد. مجموع آزمون همچنان ۳۹۷.

با این دور، هر شش قلم چهارمین بازبینی هدفمند کد (دور ۴۳ تا ۴۸) برداشته
شد.

### ۱۴۰۵/۰۵/۲۴ — localStorage بدون نگهبان می‌توانست انتخابگر نماد یا کل بوت برنامه را بترکاند (قلم پ-۶ بک‌لاگ، دور چهل‌وهفتم)

`table.mjs` و تاشدگی فهرست کناری (`app.mjs`) از قبل هر دسترسی به
`localStorage` را `try/catch` می‌گیرند («حافظه پر یا قفل») — یک الگوی
عمدی که در حافظه خصوصی/محدودشده مرورگر (که در آن هر متد `Storage`
می‌تواند پرتاب کند، نه فقط وقتی ظرفیت تمام شود) برنامه را سرپا نگه
می‌دارد. سه جای دیگر همین قاعده را نداشتند:

- `ui/picker.mjs` — بارگذاری/ذخیره انتخاب نماد (سه تب از این ماژول
  استفاده می‌کنند). بی‌نگهبان یعنی خودِ انتخابگر نماد در محیط محدود از
  کار می‌افتاد.
- `ui/app.mjs` — دو خط `applyTheme(localStorage.getItem('theme') || ...)`
  در بخش «شروع»، **پیش از** `buildRail()`. این جدی‌ترین جا بود: پرتاب
  بی‌نگهبان همان‌جا یعنی کل برنامه قبل از رسیدن به فهرست کناری می‌ترکید،
  نه فقط پوسته اشتباه بماند.
- `ui/app.mjs:applyTheme()` — نوشتن `localStorage.setItem('theme', ...)`
  بعد از تغییر بصری پوسته (`document.body.dataset.theme`) ولی پیش از
  به‌روزرسانی برچسب دکمه؛ پرتاب اینجا پوسته را بصری عوض می‌کرد ولی برچسب
  دکمه پوسته را همان قبلی جا می‌گذاشت.

رفع هر سه با همان الگوی موجود: `try/catch` دور هر دسترسی، با مقدار
پیش‌فرض معقول در `catch` (مجموعه خالی برای انتخاب، `null` برای پوسته
ذخیره‌شده تا `|| 'ledger'` جایگزینش کند).

۰ آزمون تازه — رفتار محیط مرورگر محض است، نه منطق خالص. تأیید با
Playwright: `Storage.prototype.getItem`/`setItem`/`removeItem` همه با
`page.addInitScript` پیش از بارگذاری صفحه پرتاب‌گر شدند (شبیه‌سازی
حافظه محدودشده واقعی، نه فقط localStorage خالی). صفحه کامل بوت شد
(۳۷ تب در فهرست کناری)، تب برترین موقعیت‌ها با انتخابگر نماد واقعی باز
شد و کلیک روی پیش‌تنظیم «همه» بدون خطا اجرا شد، دکمه پوسته هم بدون خطا
تعویض کرد — صفر خطای صفحه در هر سه سناریو. مجموع آزمون همچنان ۳۹۷.

### ۱۴۰۵/۰۵/۲۴ — رنگ سلول زیان در ردیف کهنه، با style این‌لاین قایم می‌شد (قلم پ-۶ بک‌لاگ، دور چهل‌وششم)

دقیقاً همان مشکل style این‌لاین دورهای ۲۴/۳۰ (ردیف نزدیک‌به‌پول
دیده‌بان زنجیره، نامزد انتخاب‌شده جدول رول) — این‌بار رنگ متن به‌جای
پس‌زمینه، و در جدول اصلی (`table.mjs`) که بیشترین تب از آن استفاده
می‌کند. `style.css:567` قاعده `.tbl-body tr.stale td { color: var(--muted) }`
را دارد تا کاربر به مظنه کهنه یک ردیف اعتماد نکند، ولی `draw()` بی‌قید
`td.style.color = 'var(--loss)'` را برای هر سلول عددی منفی این‌لاین
می‌گذاشت — و style این‌لاین همیشه بر هر انتخاب‌گر CSS برنده می‌شود. یعنی
دقیقاً همان ردیفی که باید بی‌اعتمادترین باشد (مظنه کهنه)، سلول‌های
زیانش رنگ قرمز عادی می‌ماندند، به‌جای خاکستری کم‌رنگ — نشان بی‌اعتمادی
یک ستون را می‌پوشاند.

رفع: رنگ منفی به کلاس `td.neg` منتقل شد. اولویت CSS این‌بار به‌جای
اولویت style این‌لاین تصمیم می‌گیرد — `.tbl-body tr.stale td`
(دو کلاس + دو عنصر) از `.tbl-body td.neg` (دو کلاس + یک عنصر) خودبه‌خود
اولویت بیشتری دارد، پس بدون نیاز به `!important` رنگ خاکستری برنده
می‌شود.

۰ آزمون تازه — رفتار CSS محض است. تأیید با Playwright روی `makeTable`
با داده ساختگی: دو ردیف با همان مقدار منفی، یکی با `warn: 'مظنه کهنه
است'` یکی بدون — رنگ رندرشده سلول ردیف کهنه `rgb(95,107,120)`
(خاکستری `--muted`) شد، ردیف تازه `rgb(168,31,50)` (قرمز `--loss`)
ماند؛ پیش از رفع هر دو همان قرمز بودند. مجموع آزمون همچنان ۳۹۷.

### ۱۴۰۵/۰۵/۲۴ — axisNum محور نمودار هم مرز گرد شدن داشت (قلم پ-۶ بک‌لاگ، دور چهل‌وپنجم)

همان دسته باگ دور ۳۹ (`fmt.num`)، این‌بار در `axisNum` — عددی که
برچسب کوتاه محور عمودی نمودار بازده را می‌سازد. واحد (هزار/میلیون/
میلیارد) روی رقم خامِ ورودی تصمیم گرفته می‌شد، بعد گرد می‌کرد.
`axisNum(999960)` چون خودش زیر یک میلیون بود شاخه «هزار» را می‌رفت،
ولی گرد کردن به ۱۰۰۰ هزار می‌رسید — چاپ می‌شد «۱۰۰۰ هزار» به‌جای
«۱٫۰ م». همین برای مرز میلیون→میلیارد هم بود: `axisNum(999996000)`
«۱۰۰۰ م» می‌داد، نه «۱٫۰ میلیارد». مستقیم روی محور عمودی نمودار بازده
دیده می‌شد، هر جایی که یک نقطه شکست نزدیک یکی از این دو مرز بیفتد.

رفع با همان تکنیک دور ۳۹: `axisTier(x)` یک‌بار با رقم خام صدا زده
می‌شود، مقدار گردشده‌اش نگه داشته می‌شود، و `axisTier` دوباره روی همان
مقدار گردشده صدا زده می‌شود — اگر گرد کردن از مرزی رد کرده باشد، واحد
درست بار دوم به دست می‌آید. سه شاخه (هزار/میلیون/میلیارد) در یک تابع
خالص جمع شدند تا این منطق دوباره‌تصمیم‌گیری یک‌جا باشد، نه سه‌بار
تکرار.

سه آزمون تازه: مرز هزار→میلیون، مرز میلیون→میلیارد، و همان مرز اول
برای مقدار منفی. تابع خالص است، مثل رفع دور ۳۹/۳۱ فقط با آزمون واحد
پوشش کامل دارد؛ برای اطمینان بیشتر (چون مستقیم روی محور SVG می‌نشیند)
`mountPayoff` هم مستقیم در Playwright صدا زده شد و برچسب‌های محور واقعی
رندرشده («۲٫۰ م»، «۱۰ هزار»، …) بدون خطای صفحه بررسی شدند. مجموع
آزمون ۳۹۴ → ۳۹۷.

### ۱۴۰۵/۰۵/۲۴ — کلیک سریع دو تب پشت‌سرهم، محتوای تب کندتر را روی تب تازه‌تر می‌نشاند (قلم پ-۶ بک‌لاگ، دور چهل‌وچهارم)

باگ مسابقه واقعی در `ui/app.mjs:open()`. تب هر تب یک `import()` و یک
`mount()` دارد که هر دو `await` می‌خورند. اگر کاربر روی تب دوم کلیک
می‌کرد پیش از تمام شدن `import`/`mount` تب اول، بخش همگام تابع
(روشن کردن تب درست در فهرست کناری، عوض کردن `location.hash` و
`document.title`) بی‌درنگ درست اجرا می‌شد — چون جاوااسکریپت تک‌رشته‌ای
است و این بخش قبل از هر `await` کامل می‌دود. ولی اگر `import`/`mount`
تب اول (کندتر، شاید به‌خاطر شبکه) دیرتر از کلیک دوم برمی‌گشت، همان
تلاش کهنه بی‌قید `stage.innerHTML` را با محتوای خودش پر می‌کرد و
`disposer` را هم می‌نوشت — کاربر فهرست کناری تب دوم را روشن می‌دید ولی
محتوای واقعی تب اول را.

رفع: یک شمارنده نسل (`openGen`) — هر فراخوانی `open()` نسل خودش
(`gen = ++openGen`) را می‌گیرد. بعد از هر `await` (هم `import`، هم
`mount`)، اگر `gen !== openGen` یعنی تلاش تازه‌تری از راه رسیده و این
یکی کهنه است؛ بی‌صدا کنار می‌کشد — اگر خودش موفق به `mount()` شده بود،
همان‌جا `disposer` خودش را صدا می‌زند تا چیزی نشتی نماند، ولی دیگر
`stage`/`disposer` سراسری را دست نمی‌زند. بلوک `catch` هم همین گارد را
دارد — خطای یک تلاش کهنه دیگر ربطی به تب باز فعلی ندارد.

۰ آزمون تازه — رفتار کنترل جریان async محض است. تأیید با Playwright:
تب موقعیت‌های من عمداً ۸۰۰ میلی‌ثانیه کند شد (با `page.route`)، رویش
کلیک شد، ۱۰۰ میلی‌ثانیه بعد (وسط `import` تب اول) روی تب دیده‌بان
زنجیره (سریع) کلیک شد. بعد از صبر کافی برای هر دو: عنوان صفحه، hash،
و تب روشن فهرست کناری هر سه درست «دیده‌بان زنجیره» بودند؛ عنصر جدول
موقعیت‌های من (تب کندتر) اصلاً در DOM ساخته نشده بود — قبل از رفع،
همان لحظه با محتوای دیرآمده جای دیده‌بان زنجیره را می‌گرفت. مجموع
آزمون همچنان ۳۹۴.

### ۱۴۰۵/۰۵/۲۴ — برچسب «بهترین تفاضل» رول همیشه سبز بود، حتی وقتی بهترین گزینه هم زیان‌ده بود (قلم پ-۶ بک‌لاگ، دور چهل‌وسوم)

چهارمین بازبینی هدفمند کد، دقیقاً همان دسته باگی که دورهای ۱۶/۱۷/۳۷/۳۸
برای کارت‌های KPI رفع کرده بودند — این‌بار در برچسب جدول مقایسه
نامزدهای رول (`ui/tabs/roll.mjs`). `bestIdx` نامزدی را پیدا می‌کند که
بیشترین `atSpot` (تفاضل سود و زیان در قیمت فعلی نسبت به نگه داشتن) را
دارد، ولی برچسبش همیشه `class="tag gain"` (سبز) بود — بدون هیچ شرطی
روی خودِ علامت آن عدد. اگر همه نامزدهای رول از نگه داشتن موقعیت فعلی
بدتر بودند (هر سه atSpot منفی)، «بهترین» بینشان هنوز واقعاً بدتر از
نگه داشتن است، ولی رنگ سبز پیام «خبر خوب» می‌داد — دقیقاً برخلاف قاعده
پروژه که «پول‌رسانی باید صادق بماند».

رفع: `bestTone = signTone(best.r.atSpot)` جای `'gain'` ثابت نشست —
همان تابع دور ۱۷ که کارت‌های KPI تب‌های استراتژی/برترین موقعیت‌ها از
قبل دارند، این‌بار روی یک برچسب جدول به‌جای کارت KPI.

۰ آزمون تازه — `signTone` خودش از دور ۱۷ کامل آزمون دارد؛ این فقط
سیم‌کشی جای دیگری به همان تابع است. تأیید: چون تب تحلیل رول به داده
دفتر سفارش زنده نیاز دارد و بازار وصل نیست، منطق واقعی (`reduce` برای
بهترین نامزد، بعد `signTone` رویش) با `import()` مستقیم در مرورگر روی
دو سناریوی ساختگی امتحان شد — همه نامزدها منفی: بهترین همچنان `loss`
(قرمز)؛ حداقل یک نامزد مثبت: بهترین درست `gain` (سبز) با اندیس درست.
مجموع آزمون همچنان ۳۹۴.

### ۱۴۰۵/۰۵/۲۴ — جست‌وجوی انتخابگر نماد، حرف عربی (ي/ك) را نمی‌شناخت (قلم پ-۶ بک‌لاگ، دور چهل‌ودوم)

باگ واقعی، نه فقط ظاهری، و نمونه‌اش از قبل در همین کدبیس حل شده بود —
فقط سیم‌کشی نشده بود. فهرست کناری تب‌ها (`ui/app.mjs`) از دور خودش یک
تابع `norm()` دارد که حروف عربی رایج در داده رسمی بورس (ي، ك) را به
معادل فارسی‌شان (ی، ک) برمی‌گرداند، چون کاربر ممکن است هرکدام را
تایپ کند و نماد باید پیدا شود. جعبه جست‌وجوی انتخابگر نماد
(`ui/picker.mjs`) — دقیقاً همان دسته کنترل، روی همان داده — این تابع
را نداشت؛ `list.filter((u) => u.name.includes(filter) || ...)` یک
`String.includes` خام بود. یعنی کاربری که نام نمادی را با شکل «غلط»
حرف عربی/فارسی تایپ می‌کرد، هیچ نتیجه‌ای نمی‌گرفت، حتی اگر نماد دقیقاً
همان‌جا در فهرست بود — جست‌وجوی تب‌ها همین مشکل را نداشت، انتخابگر نماد
داشت.

اصلاح: `norm()` از `app.mjs` به `ui/fmt.mjs` منتقل شد (به اسم `normFa`،
چون فایل مشترک فرمت/متن است و از قبل `toEnDigits` مشابهش را دارد)،
`app.mjs` هم به‌جای نسخه محلی‌اش همین را وارد می‌کند — یک منبع، نه دو
پیاده‌سازی یکسان. `picker.mjs` هم روی فیلتر و روی نام/نماد هر سطر همین
تابع را می‌زند.

پنج آزمون تازه روی خودِ `normFa` (تابع خالص): ي/ك عربی، نیم‌فاصله،
فاصله اضافه، ورودی خالی/نامعتبر. تأیید رفتار واقعی با Playwright:
چون بازار بسته بود و دیده‌بان زنجیره داده‌ای نداشت، `makePicker` مستقیم
با سه ردیف ساختگی صدا زده شد (دو تا با نام عربی — «علي رضا»،
«كامل صنعت» — یکی معمولی). جست‌وجوی فارسی «علی» ردیف عربی «علي رضا» را
پیدا کرد؛ جست‌وجوی فارسی «کامل» ردیف عربی «كامل صنعت» را هم پیدا کرد —
هر دو پیش از اصلاح نتیجه خالی می‌دادند. مجموع آزمون ۳۸۹ → ۳۹۴.

### ۱۴۰۵/۰۵/۲۴ — کنترل اسکن تب استراتژی مستقیم روی تنظیمات مشترک می‌نوشت (قلم پ-۶ بک‌لاگ، دور چهل‌ویکم)

باگ واقعی، نه فقط ظاهری. `ui/tabs/strategy.mjs` کنترل‌های «کنترل اسکن»
(مبنای قیمت، حالت اجرا، مبنای رتبه‌بندی، بازه روز، پنجره قیمت اعمال، …)
روی تغییر مستقیم می‌نوشتند: `state.settings = { ...state.settings, [key]: v }`.
`state.settings` یک شیء مشترک کل برنامه است — همان چیزی که `top.mjs`،
`positions.mjs`، `roll.mjs`، `chain.mjs` همه با `s = () => state.settings`
زنده می‌خوانند، و همان چیزی که `settings.mjs` مقدار اولیه فرمش را از
آن می‌سازد. یعنی تغییر «مبنای قیمت» یا «مبنای رتبه‌بندی» در یک تب
استراتژی، بی‌صدا و بدون هیچ پیام تأییدی، محاسبه هر تب دیگر باز یا
بعداً بازشده را هم عوض می‌کرد؛ و اگر کاربر بعدتر تب تنظیمات را باز
می‌کرد و «ذخیره تنظیمات» می‌زد، همین مقدار اکتشافیِ موقت برای همیشه
روی سرور می‌نشست.

اصلاح: هر نمونه تب حالا یک `overrides` محلی در closure خودش دارد؛
`s()` ترکیب `{ ...state.settings, ...overrides }` را برمی‌گرداند، و
دستکاری کنترل به‌جای شیء مشترک روی همین `overrides` می‌نویسد. اسکن و
محاسبه‌های همان تب هنوز مقدار محلی را می‌بینند (چون از `s()` عبور
می‌کنند)، ولی هیچ تب دیگری و هیچ ذخیره تنظیماتی از آن خبردار نمی‌شود.

۰ آزمون تازه — رفتار DOM/وضعیت محض است. تأیید با Playwright: تب
پوشش‌دار خرید باز شد، `rankBy` از طریق کنترل از «بازده ماهانه» به
«بیشینه بازده» تغییر کرد؛ `state.settings.rankBy` همان مقدار قبلی
ماند (پیش و پس از تغییر یکسان — پیش از اصلاح این آزمون رد می‌شد).
بعد تب تنظیمات باز شد و فیلد `rankBy` فرمش هم همان مقدار اصلی را نشان
داد، نه مقدار محلیِ override‌شده. مجموع آزمون همچنان ۳۸۹.

### ۱۴۰۵/۰۵/۲۴ — دکمه‌های پیش‌تنظیم انتخابگر نماد، aria-pressed نداشتند (قلم پ-۶ بک‌لاگ، دور چهلم)

آخرین قلم سومین بازبینی هدفمند کد. چهار دکمه بالای انتخابگر نماد
(«همه»/«پرمعامله»/«دارای مظنه»/«هیچ» — سه تب از آن استفاده می‌کنند)
هیچ نشان فعالی نداشتند، برخلاف هر گروه دکمه تعاملی دیگر برنامه: چیپ
سررسید زنجیره، چیپ نمای استراتژی، چیپ جهت تب موتور، همه از قبل
`aria-pressed` دارند و از همان قاعده CSS مشترک (`.ghost[aria-pressed="true"]`)
رنگ می‌گیرند. کاربر بعد از کلیک روی «پرمعامله» هیچ راهی نداشت بفهمد
همان پیش‌تنظیم هنوز فعال است یا خودش دستی چیزی را عوض کرده.

`ui/picker.mjs` حالا `activePreset` را در closure نگه می‌دارد — با هر
کلیک روی یک پیش‌تنظیم برابرش می‌شود و `syncPresetButtons()` صدا زده
می‌شود (`aria-pressed` روی دکمه‌ی متناظر می‌نشیند، بقیه `false`).
CSS تازه لازم نبود — `.ghost[aria-pressed="true"]` از قبل در style.css
بود. با اولین ویرایش دستی (تیک زدن یک ردیف)، `activePreset` به `null`
برمی‌گردد و هر چهار دکمه بی‌فشار می‌شوند — چون انتخاب دیگر دقیقاً
همان پیش‌تنظیم نیست.

۰ آزمون تازه — رفتار DOM محض است. تأیید با Playwright: `makePicker`
مستقیم با داده ساختگی — اول همه دکمه‌ها `false`؛ کلیک «دارای مظنه» فقط
همان یکی `true` کرد؛ کلیک «همه» جا عوض کرد (`quoted`→false،
`all`→true)؛ تیک زدن دستی یک ردیف هر چهار دکمه را به `false` برگرداند.
مجموع آزمون همچنان ۳۸۹.

با این دور، هر پنج قلم سومین بازبینی هدفمند کد (دور ۳۶ تا ۴۰) برداشته
شد.

### ۱۴۰۵/۰۵/۲۴ — fmt.num مرز گرد شدن به هزار، جداکننده هزارگان را گم می‌کرد (قلم پ-۶ بک‌لاگ، دور سی‌ونهم)

همان دسته باگ دور ۳۱ («−۰»)، این‌بار در `fmt.num`. شاخه‌بندی سه‌گانه‌اش
(گروه‌بندی‌شده با جداکننده هزارگان برای `|v|≥۱۰۰۰`، چهار رقم اعشار
برای `|v|<۱`، دو رقم اعشار بین این دو) روی مقدار خامِ ورودی تصمیم
می‌گرفت و بعد `toFixed` گردش می‌کرد. عددی مثل `۹۹۹٫۹۹۶` چون خودش زیر
۱۰۰۰ بود شاخه بدون گروه‌بندی را می‌رفت، ولی `toFixed(2)` آن را به
`"1000.00"` گرد می‌کرد — چاپ می‌شد `«۱۰۰۰٫۰۰»` بدون `٬`، برخلاف هر عدد
دیگری که همان ۱۰۰۰ را نشان می‌دهد. همین کاوش یک مرز دوم مشابه هم پیدا
کرد: عددی مثل `۰٫۹۹۹۹۶` که زیر ۱ بود و به ۱ گرد می‌شد، همچنان با چهار
رقم اعشار (`«۱٫۰۰۰۰»`) چاپ می‌شد، نه دو رقم معمول اعداد ≥۱.

رفع: بعد از گرد کردن اول با شاخه اولیه، شاخه دوباره از روی مقدار
گردشده تعیین می‌شود — اگر عبور از یک آستانه (۱ یا ۱۰۰۰) رخ داده باشد،
قالب درست همان‌جا انتخاب می‌شود، نه رقم خام پیش از گرد کردن.

چهار آزمون تازه: مرز ۱۰۰۰ (مثبت و منفی)، مرز ۱ (چهار رقم → دو رقم)،
و دو مقدار دور از هر مرزی که باید دست‌نخورده بمانند. تابع خالص است،
مثل رفع دور ۳۱ فقط با آزمون واحد پوشش کامل دارد، بدون نیاز به
Playwright. مجموع آزمون ۳۸۵ → ۳۸۹.

### ۱۴۰۵/۰۵/۲۴ — کارت «بازده دوره» تب موتور، NaN را قرمز نشان می‌داد (قلم پ-۶ بک‌لاگ، دور سی‌وهشتم)

همان دسته باگ دور ۳۷، این‌بار در `ui/tabs/engine.mjs`. `strategy.mjs`
و `top.mjs` از دور ۱۷ تابع `signTone(value)` را دارند — دقیقاً برای
همین ساخته شده که مقدار نامتناهی (NaN) را بی‌رنگ نگه دارد، نه بر اساس
falsy بودنش قرمز کند. `engine.mjs` این تابع را وارد نکرده بود و به‌جایش
شرط دستی `row.retMaxPct > 0 ? 'gain' : 'loss'` داشت که `NaN > 0`
(نادرست) را همیشه `'loss'` می‌گرفت.

`core/evaluate.mjs` هر وقت `cap<=0` یا بیشترین سود نامتناهی باشد
(`ok(bestPnl)` نادرست) `retMax` را NaN می‌کند. دومی در همین تب موتور
به‌سادگی با یک کلیک قابل‌رخ‌دادن است: هر استراتژی با سقف سود نامحدود
(مثلاً «استرادل خرید») ذاتاً بیشترین سودش بی‌نهایت است، پس «بازده دوره»
(بازده نسبت به سقف سود) تعریف‌نشده می‌ماند — ولی کارتش تا امروز مرز
قرمز زیان می‌گرفت، درست کنار کارت «بیشترین سود» که همان لحظه «∞» با
مرز سبز نشان می‌دهد. دو کارت هم‌جوار پیام متناقض می‌دادند.

رفع تک‌خطی: `signTone` از `ui/fmt.mjs` وارد شد، جای شرط دستی نشست.

۰ آزمون تازه — خودِ `signTone` از دور ۱۷ کامل آزمون دارد؛ این فقط
سیم‌کشی یک تب دیگر به همان تابع است. تأیید با Playwright روی تب واقعی
(بدون نیاز به بازار، این تب همیشه با داده دستی کار می‌کند): «استرادل
خرید» انتخاب شد، قیمت هر دو پا صفر شد — «بیشترین سود» درست «∞»/سبز
ماند، «بازده دوره» به «—٪» با `class="v "` (بی‌رنگ) رفت، نه دیگر
`class="v loss"` قبلی. مجموع آزمون همچنان ۳۸۵.

### ۱۴۰۵/۰۵/۲۴ — کارت‌های KPI موقعیت‌های من با صفر موقعیت هم سبز بودند (قلم پ-۶ بک‌لاگ، دور سی‌وهفتم)

باگ واقعی، دقیقاً همان مکانیزمی که دورهای ۱۶/۱۷/۳۱ برای جاهای دیگر
رفع کرده بودند، این‌بار در خودِ منبع رنگ کارت‌های KPI. `positions.mjs`
یک `isGain` مشترک (`tot >= 0`) به هر پنج کارت می‌داد؛ `kpiTone` هم
یک `isGain` نادرست (falsy) را بی‌قید و شرط `'loss'` می‌گرفت. حالت
پیش‌فرض تب — همان چیزی که هر کاربری بار اول باز کردن می‌بیند، صفر
موقعیت — یعنی `tot=۰` (۰ ≥ ۰ درست) و `cap=۰`. نتیجه: کارت «سود و زیان
جاری» با مقدار ۰ برچسب سبز «در سود» می‌گرفت، و کارت «بازده روی سرمایه»
با مقدار `fmt.pct(NaN)` → «—٪» هم مرز سبز داشت — یک عدد نامعلوم با
رنگ قطعی.

دو رفع هم‌زمان و هم‌جهت: `kpiTone(label, isGain)` در `ui/fmt.mjs`
حالا اگر `isGain` برابر `null`/`undefined` باشد بی‌رنگ (`''`) برمی‌گرداند
— قبلاً `null` هم falsy حساب می‌شد و رنگ قرمز می‌گرفت. `positions.mjs`
دیگر یک `isGain` مشترک برای همه کارت‌ها نمی‌سازد؛ «سود و زیان جاری»
از `positions.length ? tot >= 0 : null` می‌آید (بدون موقعیت، چیزی برای
«در سود بودن» نیست) و «بازده روی سرمایه» از `cap > 0 ? tot >= 0 : null`
(همان شرط تقسیم بر صفری که قبلاً فقط روی خودِ عدد اعمال می‌شد، حالا روی
رنگش هم).

دو آزمون تازه روی `kpiTone(label, null/undefined)`. تأیید با
Playwright روی تب واقعی: با صفر موقعیت هر پنج کارت `class="v "` (بی‌رنگ)
داشتند، حتی «سود و زیان جاری» و «بازده روی سرمایه»؛ با یک موقعیت
آزمایشی واقعاً زیان‌ده (از راه `PUT /api/positions`)، هر دو کارت درست
`class="v loss"` گرفتند — رنگ‌دهی معمولی دست‌نخورده ماند. مجموع آزمون
۳۸۳ → ۳۸۵.

### ۱۴۰۵/۰۵/۲۴ — تبی که یک‌بار در باز شدن خطا می‌داد، برای همیشه غیرقابل‌بازکردن می‌ماند (قلم پ-۶ بک‌لاگ، دور سی‌وششم)

سومین بازبینی هدفمند کد، این‌بار بالاترین شدت را روی یک باگ واقعی
خودِ پوسته برنامه پیدا کرد، نه یک تب خاص. `ui/app.mjs:open()` متغیر
`current = id` را پیش از `try { await import/mount }` می‌نویسد. اگر
`mod.mount()` خطا می‌داد (هر دلیلی — باگ در تب، داده خراب، یا صرفاً
شکست شبکه هنگام import ماژول)، کارت «تب باز نشد» نشان داده می‌شد، ولی
`current` هرگز به حالت قبل برنمی‌گشت. نگهبان بالای همان تابع
(`if (!t || current === id) return;`) کلیک بعدی روی همان تب را بی‌صدا
هیچ‌کاره می‌کرد — تنها راه نجات، باز کردن یک تب دیگر بود؛ خودِ همان تب
تا آخر همان نشست مرورگر قفل می‌ماند.

رفع تک‌خطی: بلوک `catch` حالا `current = null;` هم می‌زند — همان
جایی که کارت خطا رندر می‌شود. کلیک بعدی روی همان تب دیگر بی‌اثر نیست،
دوباره `import`/`mount` را امتحان می‌کند.

۰ آزمون تازه — رفتار کنترل جریان DOM محض است. تأیید با Playwright: یک
ماژول جعلی جای `positions.mjs` سرو شد که هر بار `mount()`اش با پیام
شمارنده‌دار خطا می‌دهد («تلاش شماره ۱»، «تلاش شماره ۲»، …) — کلیک اول
پیام «تلاش شماره ۱» را نشان داد، کلیک دوم روی همان تب (بدون رفتن به
تب دیگر) پیام «تلاش شماره ۲» را نشان داد؛ یعنی `mount()` واقعاً بار
دوم صدا زده شد، نه این‌که کلیک دوم بی‌اثر مانده باشد. مجموع آزمون
همچنان ۳۸۳.

### ۱۴۰۵/۰۵/۲۴ — کنتراست --muted در پوسته دفتر زیر آستانه WCAG AA بود (قلم پ-۶ بک‌لاگ، دور سی‌وپنجم)

بازبینی قبل این را به‌عنوان یافته گزارش کرده بود بدون اندازه‌گیری
تأییدشده؛ این دور اول با محاسبه واقعی نسبت کنتراست (فرمول لومینانس
نسبی WCAG، نه فقط چشم) درستی‌اش را سنجید. `--muted: #7f8c9b` در پوسته
دفتر (روشن، `:root`) روی `--panel` (سفید) دقیقاً ۳٫۴۳:۱ و روی
`--panel-2` ۳٫۲۲:۱ می‌داد — هر دو زیر آستانه ۴٫۵:۱ متن معمولی AA.
`--muted` رنگ برچسب کارت KPI، عدد محور نمودار، و بیشتر متن‌های ریز
(۱۰ تا ۱۲٫۵ پیکسل) کل رابط است، یعنی همه این‌ها زیر آستانه بودند —
واقعی، نه گمانه. پوسته تابلو (تیره) از قبل ۵٫۰۹ تا ۵٫۴۷:۱ داشت،
دست‌نخورده ماند.

رفع: فقط مقدار `--muted` در `:root` به `#5f6b78` عوض شد — همان جهت
رنگ (خاکستری مایل به آبی)، فقط تیره‌تر. کنتراست تازه ۵٫۴۴:۱ روی
`--panel` و ۵٫۱۱:۱ روی `--panel-2`، هر دو با فاصله راحت از آستانه.

۰ آزمون تازه — یک مقدار CSS است. تأیید با Playwright روی صفحه واقعی،
نه فقط محاسبه دستی: یک کارت `.kpi` واقعی رندر شد، رنگ رندرشده
`getComputedStyle` گرفته شد (`rgb(95,107,120)`، دقیقاً `#5f6b78`)، و
همان فرمول WCAG رویش اجرا شد — نسبت رندرشده ۵٫۴۴:۱. مجموع آزمون
همچنان ۳۸۳.

### ۱۴۰۵/۰۵/۲۴ — نقش نمودار تعامل‌پذیر هنوز role="img" بود (قلم پ-۶ بک‌لاگ، دور سی‌وچهارم)

دور ۲۸ زوم/پیمایش واقعی صفحه‌کلید را به `.chart-canvas` اضافه کرد
(فلش/±/Home)، ولی خودِ `role` روی همان عنصر همچنان `"img"` مانده بود —
یعنی «تصویر ایستا» در واژگان ARIA. اکثر صفحه‌خوان‌ها نقش `img` را
غیرقابل‌تعامل می‌گیرند و وارد حالت مرور معمولی (نه حالت فرم/تعامل)
می‌شوند؛ همان کیبورد تازه دور ۲۸ برای این کاربر عملاً نامرئی می‌ماند.

رفع: `role="img"` روی `.chart-canvas` به `role="group"` عوض شد —
نقشی برای یک ناحیه تعامل‌پذیر، بدون قبضه کردن کل ناوبری صفحه مثل
`role="application"` (که برای یک ابزار زوم/پیمایش ساده زیاده‌روی بود).
`aria-label` راهنمای کیبورد همان‌جا می‌ماند. لایه‌بندی با SVG درونی
دست‌نخورده است — `<svg role="img" aria-label="نمودار بازده در سررسید">`
داخل همان `div` هنوز جای خودش را دارد، چون آن یکی واقعاً تصویر ایستای
لحظه رسم است؛ گروه تعامل‌پذیر بیرونی و تصویر ایستای درونی، دو لایه
معنایی جدا و هر دو درست‌اند.

۰ آزمون تازه — یک ویژگی HTML است. تأیید با Playwright: `mountPayoff`
مستقیم با داده ساختگی — `.chart-canvas` بیرونی `role="group"` و
`tabindex="0"` و `aria-label` راهنما داشت؛ `<svg>` درونی همچنان
`role="img"`ی خودش را با برچسب «نمودار بازده در سررسید» نگه داشته
بود، بدون تداخل. مجموع آزمون همچنان ۳۸۳.

با این دور، هر چهار قلم بازبینی دوم (دور ۳۱ تا ۳۴) برداشته شد.

### ۱۴۰۵/۰۵/۲۴ — سرستون جدول مرتب‌شدنی، aria-sort ندارد (قلم پ-۶ بک‌لاگ، دور سی‌وسوم)

`ui/table.mjs` کلیک روی سرستون را می‌گیرد و مرتب می‌کند، و بصری هم
درست نشان می‌دهد — فلش `↓`/`↑` و رنگ تاکید (دور ۱۸ فهرست، از قبل بود).
ولی هیچ‌کدام از این‌ها برای صفحه‌خوان قابل‌فهم نبود: خودِ `<th>` هیچ‌وقت
ویژگی استاندارد `aria-sort` نمی‌گرفت، و متن راهنمای «مرتب بر …»
(`.tbl-sort`) هم `role`/`aria-live` نداشت — برخلاف پیام‌های ذخیره/خطای
تنظیمات و موقعیت‌های دستی که دور ۱۳ همین رفتار را گرفتند.

رفع دوخطی: `apply()` کنار `th.dataset.sorted` موجود (که همان بصری را
می‌سازد)، حالا `aria-sort` استاندارد را هم می‌نویسد — `ascending`/
`descending`/`none`، دقیقاً همان سه حالتی که از قبل محاسبه می‌شد، فقط
با اسم استاندارد. `.tbl-sort` هم `role="status" aria-live="polite"`
گرفت، همان قرارداد دور ۱۳.

۰ آزمون تازه — رفتار ARIA/DOM محض است. تأیید با Playwright: `makeTable`
مستقیم با داده ساختگی — سرستون `مقدار` (مبنای مرتب‌سازی اولیه) با
`aria-sort="descending"` شروع شد و `نام` با `none`؛ کلیک روی `نام`
جا عوض کرد (`نام` → `descending`، `مقدار` → `none`)؛ `.tbl-sort` هم
`role="status"`/`aria-live="polite"` داشت و متنش «مرتب بر نام ↓» درست
به‌روز شد. مجموع آزمون همچنان ۳۸۳.

### ۱۴۰۵/۰۵/۲۴ — حذف موقعیت دستی بدون تأیید، یک کلیک داده بازگشت‌ناپذیر را نابود می‌کرد (قلم پ-۶ بک‌لاگ، دور سی‌ودوم)

بالاترین شدت در فهرست بازبینی دوم: دکمه «حذف» هر ردیف تب موقعیت‌های من
بی‌درنگ `positions.splice(...)` می‌زد و ذخیره می‌کرد — بدون `confirm`،
بدون Undo. برخلاف «بازگشت به پیش‌فرض» تنظیمات (که هنوز نیاز به «ذخیره»
جدا دارد) یا خالی‌کردن کش سرور (کامل بازیافتنی از بازار)، این داده
دستی‌ای را نابود می‌کند که کاربر نمی‌تواند از بازار بازتولید کند —
قیمت ورود واقعی، تاریخ ورود. یک کلیک اشتباه یا لمس تصادفی موبایل یعنی
از دست رفتن کامل آن رکورد.

رفع: `confirm()` بومی پیش از `splice`، با عنوان موقعیت در متن پیام —
پروژه صفر وابستگی دارد، `confirm()` همان سطح محافظت رایج یک عملیات
مخرب تک‌کلیکی را بدون هیچ کد اضافه می‌دهد. لغو، ردیف را دست‌نخورده
می‌گذارد؛ پذیرش، همان مسیر قبلی (splice → save → render) را ادامه
می‌دهد.

۰ آزمون تازه — رفتار تعامل مرورگر محض است (`window.confirm`), مطابق
قرارداد دورهای مشابه. تأیید با Playwright روی تب واقعی (با یک موقعیت
آزمایشی از راه `PUT /api/positions`، چون داده بازار زنده لازم نبود):
لغو تأیید ردیف را دست‌نخورده گذاشت، پذیرش تأیید همان ردیف را حذف کرد؛
متن پیام هم عنوان موقعیت واقعی را داشت. مجموع آزمون همچنان ۳۸۳.

### ۱۴۰۵/۰۵/۲۴ — منفی خیلی کوچک «−۰» چاپ می‌شد، انگار هنوز کمی زیان مانده (قلم پ-۶ بک‌لاگ، دور سی‌ویکم)

باگ واقعی محاسباتی، نه فقط ظاهری — از یک بازبینی هدفمند تازه پیدا شد.
`Math.round(-0.4)` در جاوااسکریپت `-0` می‌دهد، و `(-0).toLocaleString('en-US')`
رشته `"-0"` برمی‌گرداند؛ همین‌طور `(-0.001).toFixed(2)` رشته `"-0.00"`
می‌دهد. `ui/fmt.mjs` این رشته خام را مستقیم به فارسی می‌برد، پس
`fmt.money(-0.4)` تا امروز `"−۰"` چاپ می‌کرد و `fmt.pct(-0.001)`
`"−۰٫۰۰"` — یک نشانه منفی جلوی عددی که ریاضی دقیقاً صفر است. چون
`grouped`/`pct`/`num` مشترک‌ترین توابع قالب‌بندی کل رابط‌اند، این هرجا
یک سود و زیان نزدیک صفر یا یک یونانی عمیق OTM (دلتای ۰٫۰۰۰۴-) نمایش
داده می‌شد اثر می‌گذاشت — یک موقعیت دقیقاً سربه‌سر می‌توانست به چشم
«کمی در زیان» بیاید.

رفع: `stripNegZero(s)` تازه در `ui/fmt.mjs` — رشته خروجی `Math.round`/
`toFixed` را قبل از فارسی‌نویسی می‌گیرد و اگر دقیقاً `"-0"` یا
`"-0.00"`… بود، نشانه منفی را می‌اندازد. فقط رشته‌ای که واقعاً صفر است
دست می‌خورد؛ منفی معمولی (`-500`) یا منفی کوچکی که با همان دقت هنوز
صفر نمی‌شود (`-0.0001` در `num` با چهار رقم اعشار) دست‌نخورده می‌ماند.

پنج آزمون تازه: `money(-0.4)`، `pct(-0.001)`، `int(-0.2)` هر سه به صفر
ساده، `num(-0.0001)` که واقعاً صفر نمی‌شود همچنان منفی، و منفی معمولی
دست‌نخورده. مجموع آزمون ۳۷۸ → ۳۸۳.

### ۱۴۰۵/۰۵/۲۴ — ردیف‌های دو جدول دستی باقی‌مانده هم با صفحه‌کلید باز می‌شوند (قلم پ-۶ بک‌لاگ، دور سی‌ام)

دور ۲۶ فقط `table.mjs` (جدول مجازی‌سازی‌شده تب‌های استراتژی و برترین
موقعیت‌ها) را با صفحه‌کلید قابل‌دسترس کرد. دو جدول دستی دیگر مانده
بودند — کوچک‌تر و مجازی‌سازی‌نشده، پس همان `tabindex`/`role`/`keydown`
مستقیم روی هر `<tr>` کافی بود، نه الگوی `activeIdx` در گردش:

- `ui/tabs/positions.mjs` — ردیف فهرست موقعیت‌های من. یک نکته اضافه
  داشت: هر ردیف یک دکمه «حذف» تودرتو هم دارد. اگر `keydown` بی‌قید روی
  `tr` می‌نشست، Enter زدن روی خودِ دکمه حذف (که رویداد کلیک/کیبورد خودش
  را دارد) از راه حباب‌زنی تا ردیف هم بالا می‌رفت و جزئیات موقعیتِ همین
  الان حذف‌شده را هم باز می‌کرد. نگهبان `if (e.target !== tr) return;`
  همین را می‌گیرد — فقط وقتی خودِ ردیف تمرکز دارد، نه فرزندش.

- `ui/tabs/roll.mjs` — ردیف جدول مقایسه نامزدهای رول. حین همین تغییر،
  یک باگ واقعی هم پیدا شد که دقیقاً همان الگوی دور ۲۴ (هاور ردیف
  نزدیک‌به‌پول) بود: نامزد انتخاب‌شده با `style="background:var(--accent-soft)"`
  این‌لاین رنگ می‌گرفت که همیشه بر `:hover` برنده می‌شد. رفع پیشگیرانه با
  کلاس تازه `.picked` (همان نسخه CSS دور ۲۴، برای این جدول جداگانه).

۰ آزمون تازه — رفتار DOM/تعامل محض است، مطابق همان قرارداد دورهای
مشابه (۱۹-۲۱، ۲۴، ۲۶). تأیید با Playwright: تب
موقعیت‌های من با یک موقعیت آزمایشی (از راه `PUT /api/positions`،
چون داده بازار زنده لازم نبود) — ردیف `tabindex="0"`/`role="button"`
داشت، فوکوس گرفت، Enter دقیقاً همان جزئیات کلیک ماوس را باز کرد؛ فوکوس
روی دکمه «حذف» و Enter رویش ردیف را حذف کرد بدون باز کردن جزئیات موقعیت
حذف‌شده (نگهبان `e.target` درست کار کرد). تب تحلیل رول به داده دفتر
سفارش زنده نیاز دارد و بازار وصل نبود؛ همان قطعه HTML/رفتاری که
`roll.mjs` برای `#cand` می‌سازد مستقیم تزریق شد — نامزد انتخاب‌شده حین
هاور از `accent-soft` به `panel-2` عوض شد (رفع باگ)، و Enter روی ردیف
دوم بدون خطا اجرا شد. مجموع آزمون ۳۷۸ (بدون تغییر).

### ۱۴۰۵/۰۵/۲۴ — نشان «تغییر کرد» اسکن پیوسته، اسکلت نیمه‌کاره تکمیل شد (قلم پ-۶ بک‌لاگ، دور بیست‌ونهم)

`ui/table.mjs` از قبل خودش می‌گفت «رنگ سه لایه دارد... نشانگر جهت
تغییر نسبت به عکس لحظه‌ای قبلی» و `rowClass()`اش `r.__flash` را
می‌خواند و کلاس `flash` برمی‌گرداند — ولی هیچ‌جای کد `__flash` نوشته
نمی‌شد و کلاس `flash` در `style.css` هم تعریف نداشت. یعنی در اسکن
پیوسته (چک‌باکس «اسکن پیوسته» تب‌های استراتژی و برترین موقعیت‌ها)، وقتی
رتبه یک ردیف با تیک تازه واقعاً عوض می‌شد، هیچ نشانه‌ای رویش نبود —
کاربر باید خودش رتبه‌ها را زیر و رو می‌دید.

نویسنده تازه: `changedIds(prevRows, nextRows, key)` در `ui/table.mjs` —
تابع خالص که شناسه ردیف‌هایی را می‌دهد که مقدار ستون `key` نسبت به
اسکن قبلی بیش از یک کف شناوری کوچک تغییر کرده؛ بدون `prevRows` (اولین
اسکن هر نشست) مجموعه خالی می‌دهد. هر دو تب `ui/tabs/strategy.mjs` (بعد
از مرحله دو) و `ui/tabs/top.mjs` آن را با `s().rankBy` — همان معیاری که
جدول رویش مرتب و رتبه‌بندی می‌شود — روی آخرین اسکن تمام‌شده (`lastFullRows`)
صدا می‌زنند، `r.__flash` را روی ردیف‌های تغییرکرده می‌گذارند، و ۱۷۰۰
میلی‌ثانیه بعد خودشان پاکش می‌کنند و `table.redraw()` می‌زنند — وگرنه
نشان برای همیشه (تا تیک بعدی) روشن می‌ماند و با هر اسکرول دوباره
پررنگ می‌شد. `style.css` یک `@keyframes row-flash` تازه دارد (نه
`pulse` قدیمی که برای اسکلت بارگذاری بی‌نهایت تکرار می‌شود — این یک‌بار
پخش می‌شود و محو می‌رود)، با حالت `prefers-reduced-motion`.

شش آزمون تازه روی `changedIds`: بدون `prevRows` خالی، فقط ردیف واقعاً
تغییرکرده در مجموعه، ردیف تازه (بی‌سابقه) فلش نمی‌گیرد، کف شناوری
ناچیز نادیده گرفته می‌شود، کلید نامعتبر/نبود خالی می‌دهد، مقدار
غیرعددی نادیده گرفته می‌شود. تأیید یکپارچگی با Playwright: `makeTable`+
`changedIds` مستقیم در مرورگر — تیک اول صفر فلش، تیک دوم فقط ردیفی که
واقعاً عوض شده کلاس `flash`/انیمیشن `row-flash` گرفت و ردیف بی‌تغییر
`animationName: none` ماند؛ جدا، شبیه‌سازی تایمر ۱۷۰۰ میلی‌ثانیه‌ای
نشان داد کلاس درست پاک می‌شود. مجموع آزمون ۳۶۹ → ۳۷۸.

### ۱۴۰۵/۰۵/۲۴ — زوم/پیمایش نمودار بازده حالا با صفحه‌کلید هم کار می‌کند (قلم پ-۶ بک‌لاگ، دور بیست‌وهشتم)

`ui/chart.mjs`ی `mountInteractive` (نمودار بازده و نمودار تفاضل رول، هر
دو تعامل‌پذیر) زوم و پیمایش را فقط با `wheel`/`pointerdown` می‌گرفت.
`.chart-canvas` نه `tabindex` داشت نه `keydown` — فقط سه دکمه نوار
ابزار (`+`، `−`، «نمای اول») با صفحه‌کلید قابل‌دسترس بودند؛ خودِ نمودار،
جایی که بیشترین تعامل واقعی رخ می‌دهد، برای کاربر صفحه‌کلیدی مرده بود.

راه‌حل سیم‌کشی محض بود — ریاضی زوم/پیمایش (`zoomAt`، `clampRange`) از
قبل آماده بود، چون همان توابع را دکمه‌های نوار ابزار صدا می‌زدند:
`.chart-canvas` حالا `tabindex="0"` و `aria-label` دارد، و `keydown`
تازه فلش چپ/راست را به همان `clampRange` پیمایش ماوس، `+`/`−` را به
همان `zoomAt` دکمه‌ها، و `Home` را به `reset()` دوبار-کلیک وصل می‌کند.
متن راهنمای بالای نمودار (`PAYOFF_HINT`) هم «فلش/±/Home با صفحه‌کلید»
گرفت تا این قابلیت برای کاربر ماوسی هم قابل کشف باشد، نه فقط صفحه‌خوان.

۰ آزمون تازه — رفتار تعامل DOM محض است. تأیید با Playwright: چون
نمودار واقعی به داده بازار نیاز دارد، `mountPayoff` مستقیم با `import()`
در مرورگر با یک پای کال ساختگی صدا زده شد. نمای اول `[۶۵۰۰,۱۳۵۰۰]`
(پهنای ۷۰۰۰)؛ `+` پهنا را به ~۵۳۸۵ باریک کرد؛ دو بار `−` به ۹۱۰۰ باز
کرد؛ فلش راست همان پهنا را به `[۶۳۶۰,۱۵۴۶۰]` جابه‌جا کرد (پیمایش به
سمت قیمت بالاتر)؛ `Home` دقیقاً به نمای اول برگشت. کانواس تمرکز
(`focus()`) هم گرفت و `tabindex`/`aria-label` هر دو حاضر بودند. مجموع
آزمون ۳۷۲.

### ۱۴۰۵/۰۵/۲۴ — پیام flash با تایمر کهنه زودتر از موعد پاک می‌شد (قلم پ-۶ بک‌لاگ، دور بیست‌وهفتم)

باگ واقعی، نه فقط ظاهری: `ui/tabs/settings.mjs` و `ui/tabs/positions.mjs`
هرکدام یک `flash(text)` دارند که پیام را می‌نویسند و با `setTimeout`
بعد از چند ثانیه پاکش می‌کنند. هر فراخوانی تایمر مستقل خودش را می‌ساخت.
اگر دو پیام نزدیک هم می‌آمدند — مثلاً در تنظیمات «بازگشت به پیش‌فرض»
سریع پشت‌سرِ «ذخیره تنظیمات»، یا در موقعیت‌های من افزودن دو موقعیت پشت
هم — تایمر پیام اول (که زودتر شروع شده بود) پیام دوم را، که تازه‌تر و
هنوز معتبر بود، زودتر از موعدش پاک می‌کرد.

رفع تک‌خطی هر دو جا: id تایمر قبلی در closure نگه داشته می‌شود و
`clearTimeout` می‌خورد پیش از این‌که تایمر تازه ساخته شود — همان انضباط
نگهبان کلیک دوباره دورهای ۱۹ تا ۲۱، این‌بار برای تایمر به‌جای کلیک.

۰ آزمون تازه — رفتار زمان‌بندی DOM محض است. تأیید با Playwright روی تب
تنظیمات: کلیک «بازگشت به پیش‌فرض» (پیام اول، ددلاین t+۲۶۰۰)، ۵۰
میلی‌ثانیه بعد کلیک «ذخیره تنظیمات» (پیام دوم واقعی، ددلاین قدیمی هنوز
t+۲۶۰۰ بدون رفع). درست حوالی همان ددلاین قدیمی، پیام دوم هنوز روی صفحه
بود؛ فقط ۹۰۰ میلی‌ثانیه دیرتر (ددلاین واقعی‌اش) پاک شد. مجموع آزمون
همچنان ۳۷۲.

### ۱۴۰۵/۰۵/۲۴ — ردیف‌های جدول اصلی حالا با صفحه‌کلید هم باز می‌شوند (قلم پ-۶ بک‌لاگ، دور بیست‌وششم)

`ui/table.mjs` (جدولی که تب‌های استراتژی و برترین موقعیت‌ها استفاده
می‌کنند) ردیف‌ها را فقط با کلیک ماوس باز می‌کرد. قاب پیمایش
(`.tbl-body`) از قبل `tabindex="0"` داشت — یعنی با Tab قابل‌رسیدن بود —
ولی هیچ `keydown` رویش نبود، پس رسیدن به آن با صفحه‌کلید نقطه کور بود:
جزئیات هیچ ردیفی باز نمی‌شد.

چون این جدول مجازی‌سازی‌شده است (فقط ردیف‌های داخل دید واقعاً در DOM
هستند، بقیه با یک `<tr>` فاصله‌گذار جا خالی می‌کنند)، `tabindex` تک‌تک
روی هر ردیف کار درستی نبود — با اسکرول، ردیف‌های DOM عوض می‌شوند و
ترتیب Tab بی‌معنی می‌شد. راه‌حل، الگوی «tabindex در گردش» است: خودِ
`activeIdx` عددی (نه شناسه سطر — چون با هر مرتب‌سازی/اسکن تازه `view`
از نو می‌چیند) در closure جدول نگه داشته می‌شود؛ کلید پایین/بالا آن را
جابه‌جا می‌کند و در صورت نیاز `body.scrollTop` را هم دستی تنظیم می‌کند
تا ردیف تازه همیشه داخل دید بماند، Enter/Space همان `opts.onPick`ی را
صدا می‌زند که کلیک ماوس صدا می‌زد. کلیک ماوس هم `activeIdx` را همگام
می‌کند تا فلش بعد از کلیک از همان ردیف ادامه دهد، نه از صفر.

نشانه بصری همان الگوی برجستگی صفحه‌کلید فهرست کناری (`data-kbd-active`
در `app.mjs`) — یک `box-shadow` داخلی رنگ تاکید، در `style.css`.

۰ آزمون تازه — رفتار DOM/تعامل محض است. تأیید با Playwright: چون
جدول واقعی به یک اسکن زنده نیاز دارد و بازار وصل نیست، `makeTable` را
مستقیم با `import()` در مرورگر با داده ساختگی صدا زدم. سه فلش پایین
پشت‌سرهم `activeIdx` را ۰→۱→۲ برد (با `data-kbd-active="1"` قابل
ردیابی)، Enter دقیقاً همان ردیف `view[2]` را به `onPick` داد، فلش بالا
برگشت به ۱. جدا، کلیک روی ردیف دوم `activeIdx` را به همان ردیف همگام
کرد و فلش پایین بعدی از همان‌جا ادامه داد، نه از ابتدای فهرست. مجموع
آزمون همچنان ۳۷۲.

### ۱۴۰۵/۰۵/۲۴ — دکمه پوسته حالا می‌گوید در کدام پوسته‌ای و کلیک بعدی کجا می‌برد (قلم پ-۶ بک‌لاگ، دور بیست‌وپنجم)

دکمه بالای صفحه همیشه فقط «پوسته» می‌گفت، چه در پوسته دفتر (روشن) چه
تابلو (تیره). کاربر بدون کلیک کردن هیچ راهی نداشت بفهمد الان کدام
پوسته فعال است یا کلیک بعدی کدام‌یک را باز می‌کند — دقیقاً همان مشکلی
که سوییچ بی‌برچسب هر رابطی دارد.

`THEME_NAME`/`THEME_NEXT` در `ui/app.mjs` نگاشت کوتاهی هستند که همان
برچسب‌های «دفتر»/«تابلو» را از `core/settings.mjs` (گزینه `theme`، قبلاً
فقط در پنل تنظیمات دیده می‌شد) می‌آورند. `applyTheme()` حالا کنار
`data-theme` و `localStorage`، متن دکمه را به `پوسته: دفتر`/`پوسته: تابلو`
و `title` را به «تعویض به پوسته …» به‌روز می‌کند — همان تابعی که هم موقع
بار اول صفحه و هم موقع کلیک صدا زده می‌شود، پس هر دو مسیر هم‌زمان درست
می‌مانند.

۰ آزمون تازه — منطق تصمیم به کوتاهی یک نگاشت دو-کلیدی است، بدون
شاخه‌ای که ارزش آزمون خالص داشته باشد؛ رفتار واقعی DOM است. تأیید با
Playwright: برچسب اولیه «پوسته: دفتر» / title «تعویض به پوسته تابلو»؛
بعد از یک کلیک برچسب «پوسته: تابلو» / title «تعویض به پوسته دفتر» و
`data-theme=board`؛ کلیک دوم برگشت به «پوسته: دفتر». مجموع آزمون ۳۷۲.

### ۱۴۰۵/۰۵/۲۴ — هاور روی ردیف نزدیک‌به‌پول دیده‌بان زنجیره بی‌اثر بود (قلم پ-۶ بک‌لاگ، دور بیست‌وچهارم)

`ui/tabs/chain.mjs` ردیف نزدیک‌به‌پول (فاصله اعمال تا آخرین قیمت زیر
۲٪) را با `style="background:var(--accent-soft)"` این‌لاین رنگ می‌کرد.
این همیشه بر قاعده عمومی `table.data tbody tr:hover` در `style.css`
برنده می‌شد (اولویت style این‌لاین از هر انتخاب‌گر CSS بالاتر است)،
پس هاور روی دقیقاً همان ردیفی که کاربر بیشتر از همه رویش دقیق می‌شود —
نزدیک‌ترین قیمت اعمال به پول — هیچ بازخوردی نداشت، برخلاف بقیه ردیف‌های
همان جدول.

رفع: `style` این‌لاین با کلاس `atm` جایگزین شد؛ در `style.css` دو قاعده
اضافه شد — `tr.atm` همان رنگ قبلی را با اولویت کلاس (نه این‌لاین) می‌دهد،
و `tr.atm:hover` صریحاً به رنگ هاور معمولی برمی‌گردد تا کلاس خودش هم
هاور را نبلعد.

۰ آزمون تازه — رفتار CSS محض است. تأیید با Playwright: چون دیده‌بان
زنجیره به داده بازار زنده نیاز دارد و بازار همین حالا وصل نیست، یک
جدول کوچک با همان کلاس‌های `table.data`/`tr.atm` مستقیم داخل صفحه
تزریق شد — پیش از هاور رنگ `rgb(216,235,234)` (accent-soft) بود، حین
هاور به `rgb(246,248,250)` (panel-2) عوض شد، دقیقاً همان رنگی که ردیف
معمولی حین هاور می‌گیرد. مجموع آزمون همچنان ۳۷۲.

### ۱۴۰۵/۰۵/۲۴ — باز کردن تب در موبایل، صفحه را تا محتوای تازه پایین می‌برد (قلم پ-۶ بک‌لاگ، دور بیست‌وسوم)

زیر ۸۲۰ پیکسل (مرز `@media` موجود در `style.css`)، فهرست کناری ۳۴+ تب
به‌جای ستون کنار، بالای محتوا می‌نشیند (`grid-template-columns: 1fr`).
تا این دور، کلیک روی تبی که پایین یک فهرست بلند بود کاربر را همان‌جا
پایین رها می‌کرد — محتوای تازه بالای صفحه رندر می‌شد ولی از دید بیرون
بود، و کاربر باید خودش دستی اسکرول می‌کرد بالا تا آن را ببیند.

`ui/app.mjs:open()` حالا بعد از مانت شدن محتوای واقعی تب (نه روی اسکلت
خالی «در حال باز کردن…» که هنوز صفحه را کوتاه نگه می‌دارد) با
`matchMedia('(max-width: 820px)')` بررسی می‌کند و اگر موبایل بود
`stage.scrollIntoView({behavior:'smooth', block:'start'})` می‌زند.
اولین تلاش زودتر از موعد بود — قبل از رسیدن محتوا، صفحه هنوز آن‌قدر
بلند نشده بود که stage بتواند واقعاً بالای دید بنشیند (Playwright نشان
داد فاصله ۴۲۸ پیکسل باقی می‌ماند)؛ جابه‌جایی صدا به بعد از mount این را
حل کرد (فاصله نهایی ~۰).

۰ آزمون تازه — رفتار کاملاً DOM/چیدمان است، نه منطق خالص، و مطابق
قرارداد دورهای قبل (مثلاً دور بیست‌ویکم) فقط با Playwright واقعی تأیید
می‌شود: در ویوپورت موبایل (۳۹۰×۷۰۰)، اسکرول ۴۰۰ دستی، کلیک روی تب
موقعیت‌های من، فاصله بالای stage از بالای صفحه نزدیک صفر شد. در ویوپورت
دسکتاپ (۱۴۰۰×۹۰۰) همان کلیک هیچ اسکرولی ایجاد نکرد (۱۲۰ → ۱۲۰) — رفتار
موبایل به دسکتاپ درز نکرده. مجموع آزمون همچنان ۳۷۲.

### ۱۴۰۵/۰۵/۲۴ — عنوان تب مرورگر حالا با تب باز عوض می‌شود (قلم پ-۶ بک‌لاگ، دور بیست‌ودوم)

عنوان تب مرورگر همیشه ثابت «رصد استراتژی آپشن» بود، صرف‌نظر از این‌که
کاربر کدام یک از ۳۴+ تب را باز کرده. کسی که چند تب مرورگر کنار هم باز
دارد (مثلاً یکی زنجیره اختیار، یکی موقعیت‌های من، یکی یک استراتژی) از
روی نوار تب مرورگر نمی‌توانست بفهمد کدام‌یک کدام صفحه است.

راه‌حل یک تابع خالص در `ui/fmt.mjs` — `pageTitle(tabTitle)` که
`«نام تب — رصد استراتژی آپشن»` برمی‌گرداند، یا فقط برند تنها اگر تبی باز
نباشد — و یک خط در `ui/app.mjs:open()` که `document.title` را با هر باز
شدن تب همراه می‌کند (همان جایی که `location.hash` هم به‌روز می‌شود).
سه آزمون رگرسیون روی خودِ تابع خالص. چون `ui/app.mjs` در سطح ماژول به
`document`/`window` دست می‌زند (هیچ jsdom در تست‌ها نیست)، منطق تصمیم را
از سیم‌کشی DOM جدا کردم — همان الگویی که `kpiTone`/`signTone`/
`coverageInfo` قبلاً برای همین دلیل در `fmt.mjs` نشسته‌اند.

تأیید بصری با Playwright واقعی: سرور محلی بالا آمد، صفحه باز شد
(عنوان اولیه «رصد استراتژی آپشن»)، تب «دیده‌بان زنجیره اختیار» از فهرست
کناری کلیک شد، عنوان به «دیده‌بان زنجیره اختیار — رصد استراتژی آپشن»
عوض شد. سرور بعد از تأیید بسته شد.

### ۱۴۰۵/۰۵/۲۳ — دکمه‌های «ذخیره تنظیمات»/«خالی کردن کش سرور» هم همان نگهبان کلیک دوباره را گرفتند (قلم پ-۶ بک‌لاگ، دور بیست‌ویکم)

تکمیل الگوی دو دور قبل (ماشین زمان، افزودن/حذف موقعیت): دو دکمه باقی‌مانده
در تب تنظیمات که `await fetch(...)` می‌زدند («ذخیره تنظیمات»،
«خالی کردن کش سرور») هنوز بدون نگهبان بودند. برخلاف افزودن موقعیت این‌ها
مقداری idempotent هستند (دو کلیک دوباره داده را دوتایی نمی‌کند)، ولی
برای یکدستی رفتار در کل برنامه — هر دکمه‌ای که با یک fetch در حال پرواز
باز هم کلیک‌پذیر بماند خلاف قرارداد ثابت‌شده دور دوم است — همان
نگهبان/`try-finally` روی هر دو نشست.

هر دو دکمه از کلاس `.btn`/`.btn.sec` هستند که `.btn:disabled` را از قبل
داشت (بر خلاف `.ghost` در دور نوزدهم)، پس نیازی به CSS تازه نبود.

تأیید در مرورگر واقعی با Playwright — این‌بار مسیر کامل قابل‌اجرا بود
چون هیچ‌کدام به داده بازار نیاز ندارند (فقط سرور محلی): تب تنظیمات باز
شد، کلیک روی «ذخیره تنظیمات» بلافاصله `disabled=true` نشان داد و بعد
از برگشتن پاسخ به `false` برگشت؛ همان برای «خالی کردن کش سرور». صفر
خطای کنسول تازه.

۰ آزمون تازه. مجموع ۳۶۹ → ۳۶۹.

### ۱۴۰۵/۰۵/۲۳ — کلیک دوباره روی «افزودن»/«حذف» تب موقعیت‌های من می‌توانست دوبار اثر کند (قلم پ-۶ بک‌لاگ، دور بیستم)

باگ واقعی‌تر از دو دور قبل: دکمه «افزودن» فرم دستی تب موقعیت‌های من
`positions.push(...)` را همگام و بی‌قید انجام می‌داد، بعد `await
save()` (fetch به `/api/positions`) می‌زد. چون هیچ نگهبانی نبود، یک
دوبار-کلیک سریع (یا دوبار لمس روی موبایل) واقعاً دو موقعیت یکسان اضافه
می‌کرد — نه فقط یک درخواست شبکه اضافه بی‌اثر مثل دور نوزدهم، بلکه داده
واقعی کاربر (موقعیت دستی ثبت‌شده) را دوتایی می‌کرد. دکمه‌های «حذف» هر
ردیف هم همین مشکل را داشتند: کلیک دوم قبل از برگشتن `render()`، روی
اندیس آرایه‌ای که همین الان یک‌بار `splice` شده بود عمل می‌کرد و می‌توانست
ردیف اشتباه را حذف کند.

اصلاح: `#add` همان الگوی دور نوزدهم — نگهبان `if (addBtn.disabled)
return`، `disabled=true` قبل از کار، `try/finally` دور بدنه اصلی. دکمه‌های
`[data-del]` نیازی به بازگرداندن `disabled` در `finally` ندارند چون
`render()` کل جدول (و دکمه‌هایش) را از نو می‌سازد — فقط نگهبان و
`disabled=true` قبل از `splice` کافی بود تا کلیک دوم روی همان دکمه، تا
قبل از رندر تازه، بی‌اثر بماند.

تأیید در مرورگر واقعی با Playwright: تب «موقعیت‌های من» با کلیک از
بخش کناری باز شد، بدون خطای کنسول تازه (تنها خطا همان محدودیت CDN
وزیرمتن، ب-۵)؛ `#add` پس از باز شدن تب موجود و فعال است. مسیر کامل
دوبار-کلیک با فرم پر از داده واقعی قابل‌اجرا نبود چون انتخاب قرارداد
اختیار به `chainDetail` زنده نیاز دارد که در این محیط در دسترس نیست
(بازار بسته، بالادست وصل نمی‌شود، همان محدودیت ب-۵)؛ به‌جایش منطق نگهبان
دقیقاً همان الگوی از قبل روی `runBtn` و دکمه ماشین زمان تست‌شده است.
چیزی به `tests/run.mjs` اضافه نشد — همان دلیل دو دور قبل (تابع export
نمی‌شود، کاملاً DOM/fetch است).

۰ آزمون تازه. مجموع ۳۶۹ → ۳۶۹.

### ۱۴۰۵/۰۵/۲۳ — دکمه ماشین زمان حین دریافت تاریخچه هنوز کلیک‌پذیر بود (قلم پ-۶ بک‌لاگ، دور نوزدهم)

همان باگ‌الگوی دور دوم (دکمه اسکن)، این‌بار در دکمه «ماشین زمان — اگر
همین ترکیب را چند روز پیش می‌گرفتم» در پانل جزئیات تب استراتژی:
`runTimeMachine` یک fetch به `/api/daily` می‌زند که چند صد میلی‌ثانیه تا
چند ثانیه طول می‌کشد، ولی خودِ دکمه هیچ‌وقت غیرفعال نمی‌شد. کلیک دوباره
حین بارگذاری یک fetch دوم و مستقل راه می‌انداخت که به‌جای صف‌شدن با
اولی مسابقه می‌داد — نتیجه دیداری خرابی نداشت (آخرین fetch که برمی‌گشت
برنده بود) ولی هم درخواست شبکه اضافی بی‌فایده می‌ساخت و هم با قرارداد
تثبیت‌شده دور دوم («دکمه حین کار async غیرفعال می‌شود») ناهم‌خوان بود.

اصلاح: `runTimeMachine(r, out, btn)` حالا دکمه را می‌گیرد، یک نگهبان
`if (btn.disabled) return` اضافه دارد، و بدنه اصلی (بعد از دو مسیر
کوتاه همگام که اصلاً fetch نمی‌زنند) در `try/finally` رفت — همان الگوی
دقیق `runBtn` در همین فایل. `ui/style.css` تازه `.ghost:disabled` هم
گرفت (کدر و نشانگر ممنوع، `.ghost:disabled:hover` هم به رنگ عادی
برمی‌گردد تا هاور روی دکمه غیرفعال گمراه‌کننده نباشد) — قبلاً فقط
`.btn:disabled` این استایل را داشت، دکمه‌های `.ghost` غیرفعال هیچ نشانه
دیداری نداشتند.

تأیید در مرورگر واقعی با Playwright: هر ۳۷ تب استراتژی بدون خطای
کنسول تازه باز شدند (تنها خطا همان محدودیت شناخته‌شده CDN وزیرمتن،
ب-۵)؛ استایل `.ghost:disabled` مستقیم روی یک دکمه ساختگی تأیید شد —
`opacity: 0.6` و `cursor: not-allowed` واقعاً اعمال می‌شوند. مسیر کامل
fetch با داده زنده قابل‌اجرا نبود (بازار بسته، محیط به بالادست وصل
نیست، همان محدودیت ب-۵)؛ `try/finally` همان الگوی از قبل تست‌شده
`runBtn` است، پس منطق re-entrancy با اطمینان بالا درست است. چیزی به
`tests/run.mjs` اضافه نشد چون این تابع export نمی‌شود و کاملاً DOM/fetch
است (مثل دور دوم که همین الگو برای `runBtn` هم بدون آزمون خودکار ماند).

۰ آزمون تازه (منطق DOM/async، نه محاسباتی). مجموع ۳۶۹ → ۳۶۹.

### ۱۴۰۵/۰۵/۲۳ — برچسب بریده‌شده در legend نمودار مقایسه‌ای حالا tooltip کامل دارد (قلم پ-۶ بک‌لاگ، دور هجدهم)

`compareLabel` (قلم الف-۱، دور ۱۵ فهرست دورها) نام استراتژی + پاها را
برای legend نمودار به ۲۲ نویسه می‌برد و بقیه‌اش را با «…» می‌بلعد —
برای برچسب‌های بلند (مثلاً «اسپرد گاوی کال — خرید کال ۱۰۰۰۰ و فروش کال
۱۲۰۰۰») متن واقعی هیچ‌جا قابل‌دیدن نبود، نه در legend و نه با هاور.

اصلاح: `compareFullLabel(r)` تازه در `ui/compare.mjs` (کنار
`compareLabel`، هر دو از یک هسته مشترک `fullCompareText` می‌آیند) متن
کامل بریده‌نشده را برمی‌گرداند. دو نقطه فراخوانی (`ui/tabs/top.mjs`،
`ui/tabs/strategy.mjs`) هر دو فیلد `full` را کنار `label` به آرایه
`compare` اضافه می‌کنند. `ui/chart.mjs` این `full` را تا `legendItems`
حمل می‌کند و فقط وقتی با `label` فرق دارد (یعنی واقعاً بریده شده) یک
`<title>` داخل `<text>` legend می‌نشاند — SVG `<title>` روی هاور مرورگر
tooltip بومی نشان می‌دهد، بدون کتابخانه تازه. برچسب‌های کوتاه («امروز»،
«سررسید»، یا خودِ برچسب کوتاه‌نشده) دست‌نخورده می‌مانند، چون `full ===
label` است.

۳ آزمون تازه در گروه ۳۱ روی `compareFullLabel` (بریده نمی‌شود، با شروع
برچسب کوتاه یکی است، برای متن کوتاه با `compareLabel` برابر است) —
خودِ رسم SVG در `chart.mjs` مثل همیشه قابل import در Node نیست (مسیر
مطلق `/ui/...`)، پس با Playwright روی صفحه واقعی تأیید شد: `mountPayoff`
مستقیم با پایه‌های ساختگی (یک برچسب کوتاه، یک برچسب بلند مصنوعی) صدا
زده شد؛ خروجی SVG دقیقاً یک `<title>` داشت — روی برچسب بلند بریده‌شده،
نه روی برچسب کوتاه.

۳ آزمون در گروه. مجموع ۳۶۶ → ۳۶۹، صفر رد.

### ۱۴۰۵/۰۵/۲۲ — «بهترین/میانه بازده ماهانه» در تب استراتژی و برترین موقعیت‌ها هم رنگ سود/زیان گرفت (قلم پ-۶ بک‌لاگ، دور هفدهم)

ادامه همان شکاف دور شانزدهم، این‌بار در `ui/tabs/strategy.mjs` و
`ui/tabs/top.mjs`: کارت‌های KPI این دو تب هیچ‌وقت رنگ نمی‌گرفتند — نه
فقط «بازده روی سرمایه»، بلکه هیچ‌کدام. برای «بهترین بازده ماهانه» و
«میانه بازده ماهانه» این یعنی حتی اگر بهترین ردیف موجود هم زیان‌ده
بود، کارت همان مرز خاکستری خنثی را داشت — دقیقاً چیزی که دور دهم قرار
بود از اسکن سریع حذف کند («چشم باید هر عدد را تک‌تک بخواند»).

اصلاح: تابع خالص تازه `signTone(value)` در `ui/fmt.mjs` (کنار
`kpiTone` — همان توجیه، ولی این یکی مستقیم از علامت خودِ عدد کار
می‌کند، نه از تطبیق برچسب، چون این دو تب برچسب‌هایی مثل «میانه احتمال
سود» هم دارند که علی‌رغم داشتن کلمه «سود» یک احتمال است نه یک مقدار
علامت‌دار، پس نباید قرمز/سبز شود). دو تب هر کدام آرایه `items` را به
تاپل چهارتایی (مثل `engine.mjs`/`roll.mjs`) تغییر دادند و فقط
«بهترین/میانه بازده ماهانه» را رنگ می‌کنند؛ بقیه کارت‌ها (شمارنده‌های
خنثی مثل «ردیف قابل اجرا»، «عمق کامل») دست‌نخورده ماندند.

با Playwright تأیید شد که تب و اسکن بدون خطای کنسول بالا می‌آیند؛ چون
بازار جمعه بسته است و این محیط به داده زنده وصل نیست (همان محدودیت
ب-۵)، رنگ واقعی کارت روی داده اسکن‌شده در مرورگر قابل‌تأیید نبود — به‌جایش
منطق علامت‌دهی با ۴ آزمون واحد روی `signTone` قفل شد.

۴ آزمون در گروه. مجموع ۳۶۲ → ۳۶۶، صفر رد.

### ۱۴۰۵/۰۵/۲۲ — کارت «بازده روی سرمایه» در تب موقعیت‌های من رنگ سود/زیان نمی‌گرفت (قلم پ-۶ بک‌لاگ، دور شانزدهم)

باگ ریز پیدا شد در `ui/tabs/positions.mjs`: منطق رنگ کارت‌های KPI فقط
برچسبی را که شامل کلمه «سود» بود رنگ می‌کرد (`k.includes('سود')`) — یعنی
«سود و زیان جاری» رنگ می‌گرفت ولی «بازده روی سرمایه»، که دقیقاً همان
علامت را دارد (هر دو از `tot` می‌آیند)، همیشه بی‌رنگ می‌ماند. این برخلاف
قرارداد دور دهم بود («مرز رنگی سود/زیان روی کارت‌های KPI، برای اسکن
سریع») و برخلاف `ui/tabs/engine.mjs` که «بازده دوره» را هم صریح رنگ
می‌کند.

اصلاح: تابع خالص `kpiTone(label, isGain)` به `ui/fmt.mjs` اضافه شد (کنار
`coverageInfo`، با همان توجیه — تابع خالص، آزمون‌پذیر، ممکن است بیش از
یک تب لازمش داشته باشد) و `positions.mjs` به‌جایش از این تابع استفاده
می‌کند. تشخیص با regex `/سود|بازده/` روی برچسب، هر دو کارت را می‌گیرد و
بقیه (سرمایه درگیر، موقعیت باز، قیمت‌گیری) بی‌رنگ می‌مانند.

۵ آزمون تازه در گروه کارت‌های KPI/coverageInfo. با Playwright روی تب
موقعیت‌های من تأیید شد: `class="v gain"` روی هر دو کارت ظاهر می‌شود.

۵ آزمون در گروه. مجموع ۳۵۷ → ۳۶۲، صفر رد.

### ۱۴۰۵/۰۵/۲۴ — جعبه جست‌وجوی انتخابگر نماد، استایل این‌لاین به‌جای کلاس مشترک (قلم پ-۶ بک‌لاگ، دور پانزدهم)

`ui/picker.mjs` (انتخابگر نماد پایه، در سه تب: برترین موقعیت‌ها، دیده‌بان
زنجیره، تب استراتژی) جعبه جست‌وجویش را با یک بلوک `style="..."` این‌لاین
می‌ساخت، جدا از الگوی بقیه پروژه که همه ورودی‌ها کلاس مشترک دارند
(`.field input`، `.rail-search input`). نتیجه‌اش این بود که این یکی ورودی
در کل رابط کاربری هیچ حالت focus قابل‌دیدنی نداشت — کلیک روش هیچ مرز رنگی
نمی‌گرفت، برخلاف هر ورودی دیگر که مرزش به `var(--accent)` تغییر می‌کند.

اصلاح: بلوک این‌لاین حذف شد، به‌جایش `.picker-tools input[type="search"]`
در `style.css` همان مقادیر را می‌دهد به‌اضافه `transition` و `:focus`.
تأیید با Playwright: تمرکز روی جعبه حالا مرز سبز-آبی می‌گیرد، تایپ و
فیلتر کردن فهرست دست‌نخورده کار می‌کند. چون فقط یک ورودی بصری اضافه شد و
منطق فیلتر عوض نشد، آزمون رگرسیون لازم نبود؛ ۳۵۷ آزمون موجود سبزند.

### ۱۴۰۵/۰۵/۲۴ — سرستون جدول‌های کوچک روی ردیف اول می‌افتاد (قلم پ-۶ بک‌لاگ، دور چهاردهم)

قاعده عمومی `table.data thead th { position: sticky; top: 46px }` برای جدولی
نوشته شده بود که با کل صفحه اسکرول می‌شود و باید زیر نوار بالای ۴۶ پیکسلی
بچسبد. اما همه شش جای فعلی که از `table.data` استفاده می‌کنند، یا داخل
`.tbl-body` هستند (جدول بزرگ مجازی‌سازی‌شده، که قبلاً خودش override با
`top:0` گرفته بود) یا داخل `.scroll` — یک قاب پیمایش خودمختار کوچک، نه کل
صفحه. برای این چهارتای دوم (دفتر بازه‌ها در تب موتور، جدول نامزدهای رول،
دیده‌بان زنجیره اختیار، فهرست موقعیت‌های من) همان top:46 اعمال می‌شد: چون
فاصله واقعی سرستون تا بالای قاب خودش کمتر از ۴۶ بود، مرورگر سرستون را ۴۶
پیکسل داخل قاب پایین می‌انداخت — نتیجه‌اش این بود که ردیف اول جدول بالای
سرستون می‌نشست و سرستون بین ردیف اول و دوم گیر می‌کرد (با Playwright روی
دفتر بازه‌ها تأیید شد: ترتیب دیداری ردیف۱→سرستون→ردیف۲ به‌جای سرستون→ردیف۱→
ردیف۲).

اصلاح: `.scroll table.data thead th { top: 0; }` — همان ترفند override با
مشخصیت بالاتر که برای tbl-body قبلاً جواب داده بود.

دیده‌بان زنجیره یک لایه مشکل اضافه داشت: سرستونش دو ردیف است (گروه
خرید/فروش، بعد برچسب هر ستون). اگر هر th مستقل بچسبد، هر دو ردیف سعی
می‌کنند در همان top:0 بنشینند — با یک صفحه آزمایشی جدا (همان مارک‌آپ #chain
با ۲۰ ردیف ساختگی، روی style.css واقعی پروژه) تأیید شد که موقع اسکرول ردیف
دوم روی اول می‌نشیند و ردیف اول کاملاً از دید بیرون می‌رود. اصلاح جداگانه:
به‌جای چسباندن هر th، خودِ `#chain thead` می‌چسبد (`position:sticky` روی
group ردیف هم در کرومیوم کار می‌کند) و th‌ها به `position:static` برمی‌گردند
— هر دو ردیف با هم یک واحد می‌مانند.

هر دو حالت (استراحت و بعد از اسکرول) با آزمایش جدا تأیید شد چون بازار جمعه
بسته بود و دیده‌بان زنجیره واقعی داده نداشت؛ صفحه آزمایشی همان style.css
واقعی را لود می‌کرد، فقط مارک‌آپ و داده‌اش ساختگی بود. جدول دفتر بازه‌ها هم
با داده واقعی (فرم دستی تب موتور) قبل/بعد تأیید شد. رفتار محاسباتی چیزی
عوض نشده، پس آزمون رگرسیون لازم نبود؛ ۳۵۷ آزمون موجود سبزند.

### ۱۴۰۵/۰۵/۲۴ — پیام ذخیره/خطا برای صفحه‌خوان هم اعلام می‌شود (قلم پ-۶ بک‌لاگ، دور سیزدهم)

`#msg` در تب تنظیمات («ذخیره شد»، «ذخیره نشد: ...») و تب موقعیت‌های دستی
(«موقعیت افزوده شد»، «ذخیره نشد») فقط با رنگ (`.saved` سبز/قرمز) خبر
می‌دادند. کاربر صفحه‌خوان که تمرکزش هنوز روی دکمه است، هیچ‌وقت متن تازه‌ای
را که بعد از کلیک داخل آن span نوشته می‌شود نمی‌شنود — چون هیچ منطقه‌ای
به‌عنوان زنده (`aria-live`) معرفی نشده بود.

اصلاح: هر دو span، `role="status" aria-live="polite"` گرفتند. تغییر فقط
یک صفت HTML است، چیزی بصری عوض نمی‌شود؛ با Playwright تأیید شد که خودِ
صفت‌ها روی عنصر نشسته‌اند و رفتار فلش پیام (نمایش و محو خودکار بعد از
دو و نیم ثانیه) دست‌نخورده مانده. چون رفتار قابل‌مشاهده تغییر نکرده،
آزمون رگرسیون لازم نبود؛ ۳۵۷ آزمون موجود سبزند.

### ۱۴۰۵/۰۵/۲۴ — پنل انتخاب ستون با کلیک بیرون یا Escape بسته می‌شود (قلم پ-۶ بک‌لاگ، دور دوازدهم)

پنل انتخاب ستون جدول (`ui/table.mjs`، `buildPanel`) فقط با کلیک روی دکمه
«بستن» خودش یا دوباره کلیک روی دکمه «ستون‌ها» بسته می‌شد — رفتار استاندارد
هر پنل شناور در وب (کلیک بیرون، یا Escape) نداشت. روی جدول‌های بلند این
یعنی برای بستن پنل باید دوباره تا بالای جدول اسکرول می‌کردی تا دکمه را
پیدا کنی.

اصلاح: `togglePanel` حالا وقتی باز می‌شود دو شنونده سطح‌سند اضافه می‌کند —
`mousedown` بیرون از پنل و دکمه‌اش، و `keydown` برای Escape — و هر دو را
وقتی بسته می‌شود برمی‌دارد، تا شنونده زنده نماند وقتی پنل بسته است. کلیک
داخل خودِ پنل (چک‌باکس‌ها، دکمه‌های «همه»/«نمای آماده») دست‌نخورده ماند
چون `closeOnOutside` هر رویدادی که هدفش داخل پنل باشد را نادیده می‌گیرد.

تأیید با Playwright روی تب «برترین موقعیت‌ها»: باز کردن پنل، کلیک روی
چک‌باکس داخلش (باز می‌ماند)، کلیک بیرون از پنل (بسته می‌شود)، باز کردن
دوباره و Escape (بسته می‌شود). چون رفتار جدید کاملاً در لایه رابط کاربری
است و منطق موتور دست نخورده، آزمون رگرسیون لازم نبود؛ ۳۵۷ آزمون موجود
سبزند.

### ۱۴۰۵/۰۵/۲۴ — نوار پرش تنظیمات، الان چیپ کارت جاری را نشان می‌دهد (قلم پ-۶ بک‌لاگ، دور یازدهم)

نوار پرش تب تنظیمات (دور پنجم) فقط برای رفتن به یک کارت بود، نه برای نشان
دادن کجای صفحه‌ای الان — با فهرست ده‌کارتیِ بلند، بعد از چند اسکرول معلوم
نبود کدام بخش زیر دیدت است.

اصلاح: `IntersectionObserver` روی هر کارت، با `rootMargin` که ناحیه دیدش
را به یک نوار باریک زیر نوار پرش (که خودش `sticky` است) محدود می‌کند؛ هر
کارتی که وارد آن نوار شود، چیپ خودش را `aria-pressed="true"` می‌کند —
همان استایلی که قبلاً برای چیپ‌های فعال دیگر جای پروژه بود، پس CSS تازه
لازم نشد.

دو نکته لبه‌ای که فقط با آزمون واقعی در مرورگر معلوم شدند:

```text
بار اول   قبل از هر اسکرول هیچ کارتی هنوز وارد نوار مشاهده نشده، پس هیچ
          چیپی روشن نبود — چیپ اول را پیش‌فرض روشن گذاشتم.
ته صفحه   آخرین کارت («نمایش») کوتاه‌تر از نوار مشاهده است؛ حتی در ته
          صفحه هم داخل آن نوار نمی‌افتد. رویداد scroll جدا رسیدن به ته
          صفحه واقعی را می‌بیند و چیپ آخر را زور می‌کند. مسابقه ترتیب
          اجرا بین آن رویداد و IntersectionObserver هم بود — هرکدام
          دیرتر برسد باید همین بررسی «ته صفحه» را داشته باشد، وگرنه
          IntersectionObserver با آخرین کارتی که واقعاً از نوار گذشته
          (نه کارت واقعاً آخر) رویش را می‌نوشت.
```

تأیید با Playwright: بار اول چیپ «منبع داده و زمان دریافت» روشن، کلیک
روی چیپ چهارم دقیقاً همان چیپ را روشن نگه می‌دارد، اسکرول تا ته صفحه چیپ
«نمایش» را روشن می‌کند — در هر دو پوسته دفتر و تابلو. چون فقط رفتار
بصری تازه است و منطق موتور دست نخورده، آزمون رگرسیون لازم نبود؛ ۳۵۷
آزمون موجود سبزند.

### ۱۴۰۵/۰۵/۲۴ — مرز رنگی سود/زیان روی کارت‌های KPI (قلم پ-۶ بک‌لاگ، دور دهم)

کارت‌های `.kpi` (بازده دوره، بیشترین سود/زیان، نقد خالص، و بقیه) همه یک‌شکل
بودند — فقط عدد داخلشان رنگ سود/زیان داشت، خودِ کارت خنثی بود. برای اسکن
سریع یک ردیف پنج‌شش‌تایی، چشم باید هر عدد را تک‌تک بخواند تا بفهمد کدام
منفی است.

اصلاح: مرز سمت شروع (`border-inline-start`) کارت، رنگ سود/زیان همان مقدار
داخلش را می‌گیرد — با `:kpi:has(.v.gain)` / `:has(.v.loss)`، بی‌آنکه چیزی
در HTML تغییر کند (پروژه از قبل جای دیگری، `.cmp-row:has(...)`، از `:has`
استفاده می‌کرد، پس همان پشتیبانی مرورگر کافی است). کارت‌های خنثی (مثل
«سرمایه درگیر») همان مرز خاکستری قبلی را نگه می‌دارند. هاور هم یک سایه
ظریف اضافه کرد تا کارت‌ها کمی زنده‌تر باشند.

تأیید با Playwright روی تب «موتور و نمودار بازده» در هر دو پوسته (دفتر و
تابلو) — مرز قرمز روی «بازده دوره»، «بیشترین زیان»، «نقد خالص» و مرز سبز
روی «بیشترین سود» درست دیده می‌شود؛ کارت‌های بی‌رنگ («سرمایه درگیر»، «وجه
تضمین»، «هزینه اجرا») دست‌نخورده ماندند. چون تغییر فقط CSS است و رفتاری
عوض نشده، آزمون رگرسیون لازم نبود؛ ۳۵۷ آزمون موجود همچنان سبزند.

### ۱۴۰۵/۰۵/۲۳ — دو باگ ریز: دکمه خالی‌کردن کش ساکت شکست می‌خورد، برچسب «اسکن نشده» جایی که اسکنی نیست (قلم پ-۶ بک‌لاگ، دور نهم)

دو باگ کوچک ولی واقعی، هر دو از جنس همان چیزی که دور ۶ تا ۸ دنبالش
بودند — پیام یا رفتار رابط کاربری که با واقعیت آن تب جور نیست:

**۱. `ui/tabs/settings.mjs` — دکمه «خالی کردن کش سرور» بدون try/catch
بود.** دکمه «ذخیره تنظیمات» کنارش هم موفقیت پیام می‌دهد هم شکست
(`flash('ذخیره نشد: ...', true)`)؛ دکمه کش هیچ‌کدام را نداشت — اگر
درخواست شکست می‌خورد (خطای شبکه، ۵۰۰ از سرور)، کلیک بی‌صدا هیچ اتفاقی
نمی‌افتاد، نه پیام موفقیت نه خطا. اصلاح: همان الگوی دکمه ذخیره،
try/catch به‌علاوه بررسی `r.ok`.

**۲. `ui/tabs/positions.mjs` — برچسب «بی‌قیمت — اسکن نشده».** این تب
اصلاً دکمه اسکن یا مفهوم اسکن ندارد؛ قیمت‌گیری خودکار روی مونت و هر
۱۵ ثانیه اتفاق می‌افتد. واژه «اسکن نشده» از تب‌های استراتژی/برترین
موقعیت‌ها قرض گرفته شده بود و به کاربر می‌گفت دکمه‌ای هست که نزده،
در حالی که نیست. به «بی‌قیمت — قیمت‌گیری نشد» تغییر کرد — صادق به هر دو
حالت (هنوز نرسیده یا واقعاً شکست خورده)، بدون ارجاع به یک اسکن نامعلوم.

هر دو با Playwright تأیید شد: شکست شبیه‌سازی‌شده کش سرور حالا پیام قرمز
«خالی نشد: خالی نشد» می‌دهد (همان الگوی تکراری «ذخیره نشد: ذخیره نشد»ی
که دکمه ذخیره از قبل دارد — سازگار با قرارداد موجود، نه ناهماهنگی
تازه)؛ برچسب تب موقعیت‌ها بدون قیمت، درست «بی‌قیمت — قیمت‌گیری نشد» نشان
می‌دهد. آزمون خودکار Node ندارد (رفتار DOM/fetch، مثل دورهای قبلی همین
خانواده). ۳۵۷ آزمون بدون تغییر سبز ماند.

### ۱۴۰۵/۰۵/۲۳ — رول به سررسید دیگر: تفاضل روی افق مشترک «امروز» (قلم الف-۵، ایده باز مانده)

آخرین ایده باز الف-۵: «مقایسه بین چند سررسید مختلف هم‌زمان». مشکل واقعی
این بود که `rollAnalysis` (`core/positions.mjs`) همیشه از `analyzePayoff`
می‌گذشت — تابعی که هر پا را «در سررسیدش» ارزش‌گذاری می‌کند بدون توجه به
اینکه واقعاً کِی سررسید می‌شود. وقتی پای تازه از سررسید دیگری بود (نه
فقط قیمت اعمال دیگر)، `diff(S) = nextAn.at(S) − curAn.at(S)` عملاً دو
منحنی را که هرکدام روی یک تاریخ تقویمی متفاوت رسم شده بودند، روی هم
می‌گذاشت — مقایسه دو چیز که اصلاً هم‌زمان نیستند.

**تصمیم طراحی:** وقتی موقعیت پس از رول دیگر تک‌سررسیدی نیست، هر دو طرف
(فعلی و پس از رول) با `analyzeMixed` روی یک افق مشترک — پیش‌فرض «امروز»
(`horizonDays: 0`) — سنجیده می‌شوند، نه سررسید هیچ‌کدام. این تنها گزینه
بدون ابهام است: افق دیگری (مثلاً «سررسید نزدیک‌تر») یک انتخاب سلیقه‌ای
اضافه می‌کرد بی‌آنکه سؤال «همین الان چه کنم» را بهتر جواب بدهد.

```text
core/positions.mjs   rollAnalysis: تشخیص می‌دهد cur و nextLegs هم‌سررسیدند
                      یا نه. اگر بله، همان جبر تکه‌ای-خطی دقیق قبلی
                      دست‌نخورده می‌ماند (اکثریت رول‌ها، صفر ریسک رگرسیون).
                      اگر نه، analyzeMixed با opt.sigma/rFree/divYield/
                      horizonDays، به‌علاوه ریشه‌یابی تفاضل با نمونه‌برداری
                      چگال (۶۰۰ نقطه) + تنصیف چون دیگر تکه‌ای-خطی نیست.
                      خروجی approx:true و note توضیح می‌دهد.
ui/tabs/roll.mjs     فیلد تازه «هم‌زمان مقایسه با سررسید دیگر — اختیاری».
                      نامزدهای سررسید دوم به همان فهرست تخت اضافه می‌شوند
                      (هرکدام days خودش را حمل می‌کند)؛ جدول و legend نمودار
                      وقتی واقعاً چند سررسید در فهرست است ستون/برچسب سررسید
                      هم نشان می‌دهند. تلاطم مبنا از قیمت زنده پای بسته‌شونده
                      با impliedVol استخراج می‌شود (یا s().volManual اگر
                      volSource دستی باشد) — نه عدد سرانگشتی.
```

بازبینی صداقت: چون این مسیر تقریبی (بلک-شولز) است، نه جبر دقیق سررسید،
`note` صریح می‌گوید تقریبی است — مطابق قاعده «ادعای اجرا باید صادق
بماند»، آمار رول چند-سررسیدی خودش را دقیق جا نمی‌زند.

آزمون: سناریوی رول موجود («کال ۱۱۰/۳۰روزه ببند، ۱۲۰/۹۰روزه بفروش») از
اول هم چند-سررسیدی بود ولی موتور قدیم این را نادیده می‌گرفت؛ عدد ادعاشده
تست قدیم (`diff(130000) > 0`) در واقع محصول همان نادیده‌گرفتن بود، نه
رفتار درست — با اصلاح، مرز تصمیم واقعی بالاتر رفت (۱۵۲٬۷۷۳، تقریباً
دست‌نخورده چون در آن قیمت ارزش زمانی ناچیز است) و ادعای تست به نقطه‌ای
واقعاً فراتر از مرز اصلاح شد. سه آزمون تازه: `approx=true` روی رول
چند-سررسیدی، هویت جبری `diff` دقیقاً برابر تفاضل دو `analyzeMixed`
مستقل، و رول هم‌سررسید معمولی هنوز `approx` نمی‌گیرد (رگرسیون مسیر دقیق).
۳۵۳ → ۳۵۷، صفر رد.

بازبینی UI با Playwright: چون این محیط به بازار وصل نیست (محدودیت ب-۵)،
تب رول اصلاً موقعیتی برای نمایش ندارد (فهرست موقعیت‌ها همیشه خالی است) و
زنجیره اختیار هم از یک Web Worker با حالت داخلی می‌آید، نه یک درخواست
شبکه‌ای قابل mock ساده — پس تأیید کامل انتها-به-انتها با داده واقعی ممکن
نبود. آنچه تأیید شد: تب بدون خطای جاوااسکریپت بالا می‌آید و فیلد تازه با
برچسب و یادداشت درست رندر می‌شود. صحت محاسباتی از آزمون‌های جبری بالا
می‌آید، نه از این بازبینی بصری.

الف-۵ حالا کامل است — هر دو ایده باز (جدول/نمودار چندنامزدی هم‌سررسید، و
مقایسه چندسررسیدی) انجام شد.

### ۱۴۰۵/۰۵/۲۳ — پیام خالی تب دیده‌بان زنجیره، ارجاع به نوار تشخیصی نداشت (قلم پ-۶ بک‌لاگ، دور هشتم)

باگ کوچک ولی واقعی، از همان خانواده دور ۶/۷: پیام پیش‌فرض جدول خالی
(`ui/table.mjs:383`) می‌گوید «نوار تشخیص بالا می‌گوید ترکیب‌ها کجا
افتادند» — درست برای تب‌های استراتژی و برترین موقعیت‌ها که واقعاً نوار
تشخیص دارند، ولی تب «دیده‌بان زنجیره» (`ui/tabs/chain.mjs`) اصلاً نوار
تشخیص ندارد؛ فقط یک عکس لحظه‌ای زنجیره است که با اشتراک `onChain` پر
می‌شود، نه با دکمه اسکن. قبل از رسیدن اولین عکس لحظه‌ای (یا وقتی محیط به
بازار وصل نیست)، کاربر پیامی می‌دید که به چیزی ارجاع می‌داد که آنجا
وجود ندارد.

اصلاح: دو پیام مخصوص همین تب — «در حال دریافت داده زنجیره اختیار…» تا
اولین عکس لحظه‌ای برسد، «این عکس لحظه‌ای هیچ نمادی ندارد.» بعد از آن اگر
واقعاً خالی بود. با Playwright تأیید شد.

### ۱۴۰۵/۰۵/۲۳ — تب «برترین موقعیت‌ها» هم پیام «هنوز اسکن نزدی» جدا شد (قلم پ-۶ بک‌لاگ، دور هفتم)

باگ از همان خانواده دور ششم: تب «برترین موقعیت‌ها» (`ui/tabs/top.mjs`)
هیچ‌وقت `table.setEmptyMessage(...)` صدا نمی‌زد، پس پیش از اولین اسکن
جدول پیام پیش‌فرض «ردیفی نمانده. نوار تشخیص بالا می‌گوید ترکیب‌ها کجا
افتادند.» را نشان می‌داد — دقیقاً همان دروغ («یک اسکن انجام شد و هیچ
نیفتاد») که دور ششم برای تب استراتژی اصلاح کرده بود، اینجا جا مانده
بود چون هر تب کدش را جدا دارد.

اصلاح، همان الگوی دور ششم: `hasScanned`/`NOT_SCANNED_MSG` اضافه شد،
پیام «هنوز اسکن نزدی» بلافاصله بعد از ساخت جدول ست می‌شود، و اولین
اجرای موفق اسکن با `table.setEmptyMessage(null)` آن را برمی‌دارد تا
پیام پیش‌فرض جدول برای «ردیفی واقعاً نماند» جای خودش را باز کند.

با Playwright روی صفحه واقعی تأیید شد: قبل از اسکن، متن جدول دقیقاً
«هنوز اسکن نزدی — نماد را انتخاب کن و دکمه اسکن را بزن.» است، نه پیام
«ردیفی نمانده».

آزمون خودکار Node ندارد — این کلاس رفتار (پیام خالی جدول) در دور ششم هم
فقط با DOM/Playwright تأیید شده بود، نه در `tests/run.mjs`؛ همان قاعده
اینجا هم رعایت شد. ۳۵۳ آزمون بدون تغییر سبز ماند.

### ۱۴۰۵/۰۵/۲۳ — حالت مقایسه‌ای: موقعیت‌های دیگر هم‌نماد روی نمودار بازده (قلم الف-۱، سؤال باقی‌مانده)

آخرین سؤال مانده از «صفحه جزئیات هر موقعیت» (خواسته ۶، الف-۱): «حالت
مقایسه‌ای با استراتژی‌های مشابه همان نماد، روی یک نمودار». دو تصمیم
طراحی قبل از کد لازم بود و اینجا گرفته شد:

**تعریف «مشابه» عمداً ساده ماند: فقط هم‌نماد بودن.** جهت، سررسید، و
استراتژی فیلتر نمی‌شوند — کاربر خودش با تیک زدن دقیق‌تر می‌کند. تعریف
سخت‌گیرانه‌تر (مثلاً «فقط همان جهت») فقط مقایسه‌های واقعاً جالب (یک
اسپرد در برابر یک کاورد روی همان نماد) را از دید پنهان می‌کرد.

**واحد مقایسه، سود و زیان خام است، نه بازده نرمال‌شده.** چون خود سود و
زیان خام همان چیزی است که نمودار اصلی هم نشان می‌دهد — نرمال‌سازی به
درصد بازده یک محور دوم می‌طلبید (دو مقیاس روی یک نمودار، گمراه‌کننده)
یا مقیاس اصلی را کج می‌کرد.

پیاده‌سازی، الگوی موجود نمودار تفاضل رول (`diffFrame`/`opt.extra`) را
برای نمودار بازده هم تکرار کرد، نه اختراع دوباره:

```text
ui/compare.mjs (تازه)   sameUnderlyingCandidates — هم‌نماد، به‌جز خود ردیف، سقف ۱۰
                        compareLabel — برچسب کوتاه برای legend
                        MAX_COMPARE = 4 — سقف منحنی هم‌زمان
ui/chart.mjs            payoffAt (تازه) — فقط ارزیاب at()، بدون نقاط رسم
                        EXTRA_STYLE مشترک بین frame() و diffFrame() شد
                        (قبلاً محلی diffFrame، فقط ۲ رنگ؛ حالا ۴)
                        frame() یک opt.compare می‌پذیرد: نمونه‌برداری
                        یکنواخت روی بازه فعلی، مقیاس Y از منحنی اصلی
                        legend عمومی شد: فهرست پویا به‌جای دو خط ثابت
ui/style.css            --cmp1..4 (هر پوسته)، curve-extra3/4 تازه،
                        .cmp-list/.cmp-row برای فهرست تیک
ui/tabs/strategy.mjs    فهرست تیک زیر legend، مقایسه با redraw سبک
ui/tabs/top.mjs         همان الگو
```

انتخاب تیک‌ها با کلید ردیف (`compareIds`) نگه داشته می‌شود و با تعویض
ردیف پاک می‌شود؛ رفرش پیوسته همان ردیف (اسکن پیوسته) انتخاب را نگه
می‌دارد چون `sameRow` تشخیص داده می‌شود، عین منطق زوم/پن قبلی.

بازبینی دستی با Playwright روی یک صفحه موقت (نه بخشی از برنامه، پاک شد)
چون این محیط به بازار وصل نیست و اسکن واقعی صفر ردیف می‌دهد (همان
محدودیت ب-۵/ب-۸) — تأیید شد که هر ۴ منحنی مقایسه‌ای هم‌زمان با رنگ و
الگوی خط‌چین جدا رسم می‌شوند، legend اسم هرکدام را می‌گوید، سقف ۴تایی
واقعاً رعایت می‌شود، و پوسته تیره هم رنگ‌های جدا را درست می‌خواند.
نمودار تفاضل رول (`diffSvg`/`mountDiff`) هم با `EXTRA_STYLE` مشترک
دوباره تأیید شد که نشکسته.

۶ آزمون تازه در گروه ۳۱ (منطق خالص `ui/compare.mjs`؛ رسم SVG در
`ui/chart.mjs` وارد کردن مطلق `/core/...` دارد و در Node قابل import
نیست، پس فقط با Playwright تأیید شد، نه در تست خودکار). مجموع ۳۴۷ →
۳۵۳، صفر رد.

الف-۱ حالا کامل است — هر پنج سؤال «صفحه جزئیات هر موقعیت» انجام شد.

### ۱۴۰۵/۰۵/۲۳ — نمودار تفاضل رول: چند نامزد هم‌زمان، نه یکی‌یکی (قلم الف-۵)

الف-۵ یک ایده باز داشت: «نمودار تفاضل روی چند نامزد هم‌زمان به‌جای
یکی». جدول مقایسه نامزدها (دور قبل همین بک‌لاگ) عدد هر نامزد را در یک
نقطه (قیمت پایه فعلی) می‌دهد؛ چیزی که آنجا نبود، شکل کلی تفاضل هر نامزد
روی کل بازه قیمت پایه بود — همان چیزی که نمودار تفاضل تک‌نامزدی فعلی
می‌دهد، ولی برای یک نامزد در یک لحظه.

```text
ui/chart.mjs
  diffFrame() یک آرایه opt.extra می‌پذیرد: [{fn, label}, ...] تا دو تا.
  هر تابع روی همان شبکه نمونه‌برداری اصلی رسم می‌شود، با مقیاس Y همان
  منحنی انتخاب‌شده (بدون محاسبه دوباره — این‌ها فقط برای شکل کلی‌اند، نه
  خواندن دقیق عدد)، هر کدام با الگوی خط‌چین جدا (extra1 خط‌چین، extra2
  نقطه‌چین) تا از هم و از منحنی اصلی متمایز بمانند. راهنمای رنگی داخل
  خود SVG سه برچسب می‌گذارد: «انتخاب‌شده» + برچسب هر نامزد اضافه. بدون
  opt.extra، رفتار قبلی دقیقاً همان می‌ماند.
ui/tabs/roll.mjs
  دو نامزد بعدیِ برتر جدول مقایسه (`candRows`، به‌جز انتخاب‌شده) به‌عنوان
  extra به mountDiff داده می‌شوند — از `rollAnalysis` که برای جدول مقایسه
  از قبل حساب شده بود، دوباره نه؛ فقط `.diff` هر کدام است که استفاده
  می‌شود.
ui/style.css   .curve-extra1/.curve-extra2 — کم‌رنگ‌تر و نازک‌تر از
               منحنی اصلی، چون نقششان زمینه است نه کانون توجه
```

تأیید: چون بازار بسته است، مسیر واقعی رول (که به دیده‌بان زنجیره زنده
نیاز دارد) امتحان نشد؛ به‌جایش خود `mountDiff` مستقیم با یک تابع اصلی و
دو تابع extra ساختگی صدا زده شد — هر دو مسیر اضافه در DOM ظاهر شدند، هر
سه برچسب راهنما درست بود، و اسکرین‌شات نشان داد خط اصلی توپر و دو خط
اضافه با الگوی متفاوت (خط‌چین و نقطه‌چین) هم‌زمان روی یک محور دیده
می‌شوند. حالت بدون extra هم جدا آزمایش شد — نه مسیر اضافه‌ای ساخته شد و
نه راهنمای رنگی، یعنی هیچ رفتار قبلی نشکسته. هیچ خطای کنسولی نبود. چیزی
به آزمون‌های Node اضافه نشد چون کاملاً DOM/SVG است (همان محدودیتی که
منحنی «امروز» هم داشت). ۳۴۷ → ۳۴۷.

### ۱۴۰۵/۰۵/۲۳ — منحنی «امروز» کنار «سررسید» روی نمودار بازده (قلم الف-۱، «نمودار دقیق و کامل»)

الف-۱ دو زیرخواسته مانده داشت که با هم گره خورده بودند: «نمودار دقیق و
کامل با همه سناریوها» و «مقایسه با استراتژی‌های مشابه». دور قبل تصمیم
گرفت اینجا بایستد چون طراحی لازم داشت. این دور، زیرخواسته اول را به یک
تصمیم مشخص و قابل‌اجرا تبدیل کرد: نمودار فعلی فقط یک سناریو نشان
می‌داد — بازده در سررسید. سناریوی واقعاً غایب، «همین امروز چقدر
می‌ارزد» بود، چون هنوز هیچ پایی سررسید نشده و ارزش زمانی دارد. این را
اضافه کردن، بدون دست زدن به معماری موجود ممکن بود:

```text
core/mixed.mjs
  analyzeMixed() یک افق ارزش‌گذاری ثابت داشت: نزدیک‌ترین سررسید. حالا
  opt.horizonDays آن را قابل بازنویسی می‌کند — horizonDays:0 یعنی «امروز»،
  هیچ پایی سررسید نشده، همه با بلک-شولز قیمت می‌خورند. بدون این پارامتر،
  رفتار قبلی دقیقاً همان می‌ماند (تست پایین‌تر این را تضمین می‌کند).
ui/chart.mjs
  todaySeries() جدید، همان analyzeMixed را با horizonDays:0 صدا می‌زند —
  برای هر ترکیبی کار می‌کند، تک‌سررسید یا چندسررسید، چون فرمول فقط به
  «چند روز مانده» هر پا وابسته است. frame() یک منحنی دوم خط‌چین می‌کشد
  (رنگ accent-2، جدا از رنگ خط اصلی) روی همان محور، به‌علاوه یک راهنمای
  کوچک داخل خود SVG («امروز» / «سررسید»). بدون تلاطم معتبر (opt.sigma)،
  چیزی رسم نمی‌شود — نه خط غلط با تلاطم حدسی.
ui/style.css   .curve-today و .curve2-legend — خط‌چین با رنگ var(--accent-2)
```

این تغییر روی همه تب‌هایی که `mountPayoff` را با `sigma` صدا می‌زنند
خودکار ظاهر می‌شود (تب استراتژی، تب برترین موقعیت‌ها، تب موتور) — دقیقاً
همان فلسفه «یک موتور مشترک» پروژه؛ تب‌هایی که sigma نمی‌دهند (پوزیشن‌ها،
رول) بی‌تغییر می‌مانند، چون `todaySeries` بدون تلاطم معتبر چیزی
برنمی‌گرداند، نه خطا می‌دهد.

زیرخواسته دوم («مقایسه با استراتژی‌های مشابه، روی یک نمودار») هنوز باز
است — آن یک تصمیم طراحی جدا لازم دارد (نمودار چند-موقعیتی کاملاً متفاوت
از دو منحنی هم‌موقعیت).

تأیید: سه آزمون تازه در `tests/run.mjs` (گروه ۳۰) — بدون `horizonDays`
رفتار قبلی دقیقاً یکی می‌ماند (رگرسیون)؛ با `horizonDays:0` روی یک کال
تک‌پا، سود و زیان دقیقاً برابر فرمول بلک-شولز است (هویت جبری، نه فقط
علامت)؛ در نقطه اعمال، امروز و سررسید عمداً فرق دارند (ارزش زمانی هنوز
هست). ۳۴۴ → ۳۴۷. در مرورگر واقعی: تب موتور با کاوردکال باز شد (تلاطم
۰٫۶ همیشه ثابت آنجا، پس این تب دقیقاً همان محیطی است که این قلم نیاز
دارد، بدون وابستگی به بازار زنده) — منحنی خط‌چین «امروز» دقیقاً همان‌جا
که خط پیوسته «سررسید» شکستگی تیز دارد، نرم و گرد دیده شد؛ راهنمای رنگی
داخل نمودار هم درست ظاهر شد. هیچ خطای کنسولی نبود.

### ۱۴۰۵/۰۵/۲۳ — پیام «هنوز اسکن نزدی» جدا از «ردیفی نمانده» (قلم پ-۶ بک‌لاگ، دور ششم)

در تب استراتژی، همان لحظه صفحه باز می‌شد، `buildTable()` با `rows=[]`
`table.set([])` را صدا می‌زد و جدول از همان اول پیام «ردیفی نمانده. نوار
تشخیص بالا می‌گوید ترکیب‌ها کجا افتادند» را نشان می‌داد — جمله‌ای که
معنایش «اسکن انجام شد و هیچ ترکیبی نماند» است، درحالی‌که هنوز هیچ اسکنی
اصلاً زده نشده بود. همین دور قبل (اسکلت بارگذاری) این نکته را ثبت کرده
بود ولی حلش نکرده بود چون موضوع جدا بود.

```text
ui/table.mjs
  متد تازه setEmptyMessage(text) به بیرون صادر شد؛ وقتی view خالی است و
  loading هم فعال نیست، این پیام سفارشی به‌جای متن ثابت «ردیفی نمانده»
  نشان داده می‌شود (نال یعنی برگرد به همان متن پیش‌فرض).
ui/tabs/strategy.mjs
  پرچم hasScanned اضافه شد. buildTable() تا وقتی هیچ اسکنی زده نشده،
  emptyMessage را به «هنوز اسکن نزدی — نماد را انتخاب کن و دکمه اسکن را
  بزن» می‌گذارد. run() همان اول (وقتی نماد انتخاب شده و busy می‌شود)
  hasScanned را true و پیام را null می‌کند — چون از این لحظه به بعد،
  خالی بودن جدول دیگر «هنوز شروع نشده» نیست، واقعاً نتیجه اسکن است.
```

تب برترین موقعیت‌ها (`top.mjs`) دست نخورد چون از همان اول این مشکل را
نداشت — آنجا `table.set()` فقط بعد از یک اسکن واقعی صدا زده می‌شود، نه
در لحظه ساخت جدول، پس جدولش پیش از اولین اسکن فقط خالی می‌ماند بدون
هیچ پیام گمراه‌کننده‌ای.

تأیید در مرورگر واقعی: بلافاصله بعد از باز شدن تب استراتژی، متن جدول
دقیقاً پیام تازه بود، نه «ردیفی نمانده»؛ کلیک روی چیپ نمای دیگر («سرمایه»)
که `buildTable()` را دوباره صدا می‌زند، همان پیام «هنوز اسکن نزدی» را
نگه داشت (چون هنوز هیچ اسکنی زده نشده). هیچ خطای کنسولی نبود. چیزی به
آزمون‌ها اضافه نشد چون کاملاً DOM است. ۳۴۴ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — نوار پرش و سلسله‌مراتب تایپوگرافی در تب تنظیمات (قلم پ-۶ بک‌لاگ، دور پنجم)

تب تنظیمات ده کارت پشت‌سرهم دارد (منبع داده، سهمیه، ساعات بازار، کارمزدها،
وجه تضمین، مبنای اجرا، غربال، ترکیب‌سازی، تلاطم، نمایش) — همه هم‌وزن،
هم‌رنگ، هم‌اندازه، بدون هیچ راه میان‌بری به‌جز اسکرول دستی. یک اسکرین‌شات
تمام‌صفحه گرفته شد تا مشکل واقعی دیده شود، نه حدس زده شود: صفحه یک دیوار
یکنواخت از جعبه‌های سفید است.

```text
ui/tabs/settings.mjs
  هر کارت گروه یک id گرفت (grp-<key>)؛ بالای صفحه یک <nav class="settings-nav">
  اضافه شد که برای هر گروه یک چیپ می‌سازد (از همان کلاس .chip موجود، چیز
  تازه‌ای اختراع نشد) و کلیکش با scrollIntoView کارت مربوطه را نرم به بالای
  دید می‌آورد.
ui/style.css
  .settings-nav — چسبان زیر نوار بالای صفحه (top:46px)، پس‌زمینه نیم‌شفاف
  با blur تا روی محتوای رد شده هم خوانا بماند.
  .settings-card { scroll-margin-top: 108px } — تا کلیک چیپ کارت را زیر
  دو نوار چسبان (بالای صفحه + نوار پرش) پنهان نکند.
  .settings-card > h3 — فقط برای همین صفحه (کلاس settings-card اضافه شد
  کنار card، نه به‌جایش، تا h3 مشترک بقیه تب‌ها دست‌نخورده بماند) یک نوار
  رنگی var(--accent) در شروع خط گرفت و کمی بزرگ‌تر شد — یک لنگر بصری واضح
  که چشم اول به آن می‌افتد، نه به فیلدها.
```

تأیید در مرورگر واقعی: اسکرین‌شات پیش/پس گرفته شد — نوار پرش با ۱۰ چیپ
درست زیر توضیح صفحه ظاهر می‌شود؛ کلیک روی چیپ ششم («کارمزدها») کارت
درست را به فاصله ۱۱۰px از بالای صفحه رساند (زیر هر دو نوار چسبان، نه
زیرشان پنهان). نوار رنگی کنار عنوان هر کارت هم در تصویر واضح دیده شد.
هیچ خطای کنسولی نبود. چیزی به آزمون‌ها اضافه نشد چون کاملاً DOM/CSS است.
۳۴۴ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — اسکلت بارگذاری به‌جای جدول خالی تا داده اول برسد (قلم پ-۶ بک‌لاگ، دور چهارم)

جدول مشترک (`ui/table.mjs`) وقتی هیچ ردیفی ندارد، همیشه یک پیام ثابت
نشان می‌داد: «ردیفی نمانده. نوار تشخیص بالا می‌گوید ترکیب‌ها کجا افتادند.»
این پیام برای دو حالت کاملاً متفاوت یکسان بود — «اسکن هنوز در حال اجراست،
داده اول نرسیده» و «اسکن تمام شد و واقعاً هیچ ترکیبی نماند» — و در حالت
اول، صرفاً یک صفحه خالی و مبهم بود تا چند ثانیه بعد ردیف‌ها یک‌جا ظاهر شوند.

```text
ui/table.mjs
  makeTable() یک پرچم داخلی loading گرفت. draw() حالا سه راه دارد:
  ردیف واقعی هست → همان قبل؛ ردیفی نیست و loading=true → شش ردیف اسکلت
  با نوار خاکستری تپنده به‌جای هر سلول؛ ردیفی نیست و loading=false → همان
  پیام قبلی «ردیفی نمانده». set(rows) همیشه loading را false می‌کند —
  چون رسیدن نتیجه، حتی نتیجه خالی، یعنی بارگذاری تمام شده. متد تازه
  setLoading(bool) به بیرون صادر شد.
ui/tabs/strategy.mjs   ui/tabs/top.mjs
  run() همان لحظه که busy=true می‌شود، table.setLoading(true) هم صدا
  می‌زند؛ مسیر خطا هم صریحاً setLoading(false) می‌کند تا اسکلت برای همیشه
  گیر نکند اگر بالادست خطا بدهد.
ui/style.css   .skel-row/.skel-bar — نوار خاکستری با همان انیمیشن pulse
               که پیشتر برای نشانگر اتصال زنده به کار رفته بود (چیز تازه‌ای
               اضافه نشد)، با حالت کاهش‌حرکت هم هماهنگ.
```

اسکلت فقط وقتی نشان داده می‌شود که view فعلاً خالی است — اگر اسکن دومی
روی نتایج قبلی زده شود، ردیف‌های قدیمی تا رسیدن ردیف‌های تازه سرجایشان
می‌مانند (بدون فلاش اسکلت روی داده موجود)، چون این قلم دقیقاً هدفش
«جدول خالیِ اولین بار» بود، نه هر اسکن.

تأیید در مرورگر واقعی: چون بازار بسته است، مسیر واقعی از طریق دکمه اسکن
با شبکه امتحان نشد؛ به‌جایش `makeTable` مستقیم با یک جدول آزمایشی صدا
زده شد — `setLoading(true)` روی جدول خالی دقیقاً ۶ ردیف اسکلت و ۱۲ نوار
ساخت؛ `set(rows واقعی)` اسکلت را حذف و ردیف واقعی گذاشت؛ `set([])`
(نتیجه واقعاً خالی) پیام قدیمی را نشان داد نه اسکلت؛ `setLoading(true)`
دوباره روی نتیجه خالی، اسکلت را برگرداند. یک اسکرین‌شات از حالت اسکلت
روی تب استراتژی هم گرفته شد — شش نوار خاکستری زیر سرستون‌های خلاصه، به‌جای
صفحه سفید. هیچ خطای کنسولی دیده نشد. چیزی به آزمون‌ها اضافه نشد چون
کاملاً DOM است (همان الگوی `moveColumn`/`insertColumn` که تابع خالص
جداست و آزمون دارد، اینجا صدق نمی‌کند چون تابع تازه به DOM وابسته است).
۳۴۴ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — نوار پیشرفت واقعی حین اسکن دومرحله‌ای (قلم پ-۶ بک‌لاگ، دور سوم)

`runScan` در `ui/scanner.mjs` از قبل دقیقاً دو رویداد `onStage('one', …)` و
`onStage('two', …)` می‌داد، ولی تب استراتژی فقط متن نوار وضعیت را عوض
می‌کرد — هیچ نشانه بصری از پیشرفت واقعی نبود، فقط جمله‌ای که ممکن است
دیده نشود.

```text
ui/tabs/strategy.mjs
  <div class="scan-progress" id="progress"><div id="progress-fill"></div></div>
  زیر نوار کنترل اسکن اضافه شد، پیش‌فرض مخفی. setProgress(pct) در شروع
  اجرا ۵٪، بعد از پایان مرحله یک ۵۰٪، بعد از پایان مرحله دو ۱۰۰٪ می‌گذارد؛
  روی خطا فوراً مخفی می‌شود؛ در finally اگر هنوز دیده است به ۱۰۰٪ می‌رسد
  و بعد از ۴۰۰ میلی‌ثانیه محو می‌شود. عددها واقعی‌اند نه انیمیشن جعلی —
  چون فقط دو رویداد گسسته از worker می‌آید (نه پیشرفت پیوسته)، نوار هم
  فقط دو پله دارد، نه شبیه‌سازی نرم بین آن‌ها.
ui/style.css   .scan-progress/.scan-progress-fill — خط باریک با ترنزیشن
               عرض، رنگ از var(--accent)، همرنگ هر دو تم روشن/تیره
```

این قلم فقط تب استراتژی است (تنها مسیر دومرحله‌ای)؛ تب برترین موقعیت‌ها
(`top.mjs`) تک‌مرحله‌ای است (`runScanAll`، بدون `onStage`)، پس نوار
پیشرفت برایش معنا ندارد و دست نخورد.

تأیید در مرورگر واقعی: نوار در حالت اولیه مخفی است (`display:none`)؛
کلیک روی دکمه بدون نماد انتخابی (مسیر کوتاه که زودتر از `setProgress`
برمی‌گردد) نوار را مخفی نگه می‌دارد — رفتار درست. چون بازار بسته است و
این محیط به بالادست وصل نمی‌شود، مسیر کامل «مرحله یک → مرحله دو» با
داده زنده امتحان نشد؛ به‌جایش با دستکاری مستقیم استایل تأیید شد که
CSS واقعاً یک خط ۴px با پرشدگی رنگ accent و ترنزیشن نرم می‌سازد. هیچ
خطای کنسولی از تغییرات این قلم دیده نشد (تنها خطای کنسول همان محدودیت
شناخته‌شده اتصال بالادست بود). چیزی به آزمون‌ها اضافه نشد چون کاملاً DOM
و بدون منطق محاسباتی جدید است. ۳۴۴ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — دکمه اسکن، حالت در حال اجرا نشان می‌دهد (قلم پ-۶ بک‌لاگ، دور دوم)

دور اول پ-۶ ترنزیشن و حالت active اضافه کرد؛ این دور یک باگ کوچک UX
واقعی را گرفت: دکمه «اسکن» در تب‌های استراتژی و برترین موقعیت‌ها حین
اجرای واقعی (که چند ثانیه طول می‌کشد) کاملاً عادی و کلیک‌پذیر می‌ماند —
هیچ نشانه‌ای که «همین الان دارد کار می‌کند» نبود، فقط متن نوار وضعیت
پایین صفحه که ممکن است دیده نشود.

```text
ui/tabs/strategy.mjs   ui/tabs/top.mjs
  دکمه اسکن حین اجرا disabled می‌شود و متنش «در حال اسکن…» می‌شود؛
  await runScan/runScanAll داخل try/finally رفت تا اگر خطا هم بخورد
  دکمه برای همیشه غیرفعال نماند — قبلاً هم پرچم busy در مسیر خطای
  top.mjs یک return زودهنگام داشت که این را تضمین نمی‌کرد
ui/style.css   .btn:disabled — کدر و نشانگر «ممنوع»، تا حالت غیرفعال
               از حالت عادی واقعاً قابل تشخیص باشد
```

همچنین ترنزیشن مرده `.card { transition: border-color }` که دور قبل
یادداشت شده بود حذف شد — هیچ `.card:hover`ای هیچ‌وقت رنگ مرز را عوض
نمی‌کرد، پس افکتی نداشت که کامل شود.

تأیید در مرورگر واقعی: هر دو تب بدون خطای کنسول باز شدند؛ کلیک روی
دکمه بدون انتخاب نماد (مسیر کوتاه که قبل از تنظیم busy برمی‌گردد) دکمه
را دست‌نخورده نگه داشت — دقیقاً رفتار موردانتظار. چون این محیط داده
بازار ندارد، مسیر واقعی «حین اسکن» (که واقعاً چند ثانیه طول می‌کشد) با
داده زنده امتحان نشد؛ منطق try/finally با بازبینی کد و همان الگوی
busy موجود تأیید شد. چیزی به آزمون‌ها اضافه نشد چون کاملاً DOM است.
۳۴۴ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — جدول مقایسه همه نامزدهای رول، نه فقط یکی‌یکی (قلم الف-۵ بک‌لاگ)

بک‌لاگ می‌گفت «سناریوهای مختلف، جدول‌ها و نمودارهای خلاقانه‌تر» بدون
جزئیات بیشتر — کوچکش کردم به یک قلم مشخص. قبل از این، کاربر باید یکی‌یکی
هر قیمت اعمال را در کشویی «قرارداد تازه» انتخاب می‌کرد و نتیجه را می‌دید؛
مقایسه پنج قیمت اعمال یعنی پنج بار انتخاب و نگاه کردن.

```text
ui/tabs/roll.mjs   جدول تازه «مقایسه نامزدهای رول» بالای نمودار تفاضل.
                    برای هر قیمت اعمال موجود در همان سررسید،
                    rollAnalysis همان‌جا صدا زده می‌شود (تابع خالص از
                    قبل موجود، فقط روی کل candidates حلقه می‌زند) و
                    تفاضل در قیمت فعلی، خالص نقدی رول، سقف سود و
                    سربه‌سری پس از رول کنار هم می‌آید. بهترین ردیف
                    (بیشترین تفاضل) برچسب سبز می‌گیرد. کلیک روی هر
                    ردیف همان کشویی «قرارداد تازه» را عوض می‌کند و
                    draw() دوباره اجرا می‌شود — یک منبع حقیقت، نه دو
                    مسیر انتخاب موازی
```

تأیید در مرورگر واقعی: تب بدون خطای کنسول باز شد؛ `rollAnalysis` مستقیم
با سه نامزد فرضی (سه قیمت اعمال مختلف) امتحان شد و برای هر کدام عدد
منطقی و متفاوت داد — هر چه قیمت اعمال دورتر، پریمیوم پای تازه کمتر و
خالص نقدی رول بدتر، دقیقاً رفتار موردانتظار یک فروش کال که به قیمت
اعمال بالاتر رول می‌شود. چون این تب به موقعیت واقعی ثبت‌شده و زنجیره
زنده نیاز دارد که در این محیط نیست، کلیک واقعی روی یک ردیف با داده
واقعی امتحان نشد — همان محدودیت مستندشده در ب-۷/ب-۶/الف-۱. چیزی به
`tests/run.mjs` اضافه نشد چون این تغییر کاملاً DOM است و `rollAnalysis`
خودش از قبل در گروه ۱۳ آزموده شده. ۳۴۴ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — تصویر آینده: ریسک و ریوارد بر اساس صدک‌های محتمل قیمت پایه (قلم الف-۱، سؤال ۳)

سومین و آخرین سؤال مانده از الف-۱. موتور از قبل `probOfProfit` را از
توزیع لگاریتم-نرمال با روند صفر می‌سازد (`core/bs.mjs`، `probAbove`/
`probBelow`)، ولی هیچ‌جا آن توزیع را به کاربر «نشان» نمی‌داد — فقط یک
عدد خلاصه (احتمال سود) بیرون می‌داد. کم داشته دقیقاً همان معکوس تابع
بود.

```text
core/bs.mjs   ninv(p) — معکوس نرمال استاندارد، تقریب اکلام. priceQuantile
              (S, p, T, sigma) — معکوس probBelow: صدک قیمت پایه در
              سررسید. probBelow(S, priceQuantile(S,p,T,sig), T, sig) === p
ui/tabs/strategy.mjs   جدول تازه «تصویر آینده» زیر جدول سناریوی ثابت:
              صدک‌های ۵/۲۵/۵۰/۷۵/۹۵٪ با قیمت پایه متناظر و سود/زیان همان
              نقطه از an.at(level) — همان تابع بازدهی که نمودار و
              سناریوی ثابت هم از آن می‌آیند. هشدار مدل (همان که در
              docstring خودِ probAbove بود) رونویسی شد، نه دوباره نوشته
```

میانه توزیع همیشه زیر قیمت پایه است — نه باگ، خاصیت خودِ «روند صفر»
است (میانگین نگه داشته می‌شود، نه میانه)، و آزمون همین را قفل کرده تا
با یک اصلاح آینده اشتباهی جابه‌جا نشود.

۱۵ آزمون در گروه ۱: `ninv` رفت‌وبرگشت با `ncdf` روی شش مقدار، و صدک هر
پنج درصد وقتی از `probBelow` عبور کند دقیقاً همان درصد را برمی‌گرداند
(نه فقط علامتش). تأیید در مرورگر واقعی: چهار تب بدون خطای کنسول باز
شدند؛ `priceQuantile` روی یک فروش کال لخت مستقیم امتحان شد — زیر قیمت
اعمال دقیقاً بیشترین سود ثابت می‌ماند، بالای آن دقیقاً به اندازه فاصله
از اعمال کم می‌شود، همان شکل بازده که نمودار SVG هم می‌کشد. الف-۱ حالا
هر پنج زیرسؤالش یا انجام شده یا (برای نمودار کامل و مقایسه چند
استراتژی) طراحی جداگانه نیاز دارد. ۳۲۹ → ۳۴۴.

### ۱۴۰۵/۰۵/۲۳ — بازخورد لمسی روی دکمه‌ها، چیپ‌ها، و ردیف جدول (قلم پ-۶ بک‌لاگ، دور اول)

پ-۶ باز است («الهام از سبک UI جذاب وب... فقط CSS/چیدمان»)، پس این دور
یک قلم مشخص برداشت: تغییر حالت هاور/فوکوس اکثر جاها فوری بود، بدون
گذار نرم؛ چند جا هم اصلاً بازخورد نداشت.

```text
.btn ، .ghost ، .chip ، .step-btn   ترنزیشن background/border/color
                                    اضافه شد؛ هر چهارتا حالا :active هم
                                    دارند (کمی تیره‌تر یا رنگ فشرده)،
                                    وگرنه کلیک هیچ بازخورد لمسی نداشت
.chip                               اصلاً هاور نداشت (فقط حالت
                                    aria-pressed رنگ عوض می‌کرد)؛ حالا
                                    مثل بقیه دکمه‌ها هاور دارد
table.data tbody tr                هاور ردیف فوری بود؛ حالا نرم
.field input/select                فوکوس border-color فوری بود؛ حالا نرم
```

`.card` یک ترنزیشن `border-color` داشت که به هیچ قاعده‌ای وصل نبود
(هیچ‌جا `.card:hover` رنگ مرز را عوض نمی‌کرد) — کد مرده از یک تصمیم
نیمه‌کاره قبلی. این دور دست‌نخورده ماند چون معلوم نبود منظور اصلی چه
افکتی بود؛ برای دور بعد یادداشت شد.

تأیید در مرورگر واقعی با Playwright: هر دو پوسته (دفتر روشن، تابلو
تیره) اسکرین‌شات گرفته شد، بدون خطای کنسول، بدون شکستگی چیدمان. آزمون‌ها
دست‌نخورده ماندند چون این تغییر کاملاً CSS است. ۳۲۹ → ۳۲۹.

### ۱۴۰۵/۰۵/۲۳ — ماشین زمان: شبیه‌سازی بلک-شولز روی تاریخچه قیمت پایه (قلم پ-۴ بک‌لاگ)

بک‌لاگ خودش طراحی را از پیش نوشته بود: قیمت واقعی گذشته اختیار نداریم
(دیده‌بان تاریخچه ذخیره نمی‌کند)، پس با قیمت پایانی تاریخی پایه از
`/api/daily` (همان که برای تلاطم تاریخی گرفته می‌شود) و تلاطم امروز،
بلک-شولز هر روز را دوباره قیمت می‌زند. صریحاً شبیه‌سازی است نه بازسازی —
برچسبش هم همین را می‌گوید.

```text
core/timemachine.mjs   timeMachine(legs, closes, {daysToday, sigma, rFree,
                        divYield}) — تابع خالص. ارزش کل ترکیب را از دید
                        دارنده حساب می‌کند (خرید مثبت، فروش منفی)؛ همین
                        یک فرمول برای هر دو جهت درست است چون
                        سود=ارزش(t)−ارزش(ورود) با علامت signedQty خودش
                        جهت را می‌گیرد، نیازی به شاخه زدن نیست
ui/tabs/strategy.mjs   دکمه «ماشین زمان» زیر پانل جزئیات هر ردیف. کلیک،
                        n=volDays روز تاریخچه پایه را می‌گیرد و جدول
                        تاریخ/قیمت/روز-تا-سررسید/سود‌وزیان می‌سازد، با
                        هشدار زرد بالای جدول: «شبیه‌سازی... نه قیمت
                        واقعی». خطای بالادست (که همیشه ۲۰۰ با
                        {error:...} برمی‌گردد، نه کد HTTP بد) جدا از
                        «تاریخچه کافی نیست» تشخیص داده می‌شود
```

۹ آزمون در گروه ۲۹: روز ورود سود صفر است، روز تا سررسید یکنوا کاهشی
است، و مهم‌تر — سود دقیقاً برابر تفاضل جبری `bsPrice` دو روز است (نه
فقط علامتش)، فروش دقیقاً علامت برعکس خرید از همان یک فرمول می‌گیرد.
تأیید در مرورگر واقعی: هر چهار تب امتحان‌شده بدون خطای کنسول بار شدند،
`timeMachine` مستقیم در مرورگر با سه روز تاریخچه دقیقاً همان جوابی داد
که Node می‌دهد، و مسیر خطای بالادست (که این محیط واقعاً با آن روبه‌رو
می‌شود چون به بازار وصل نیست) پیام درست «دریافت تاریخچه ناموفق» را
نشان داد، نه پیام گمراه‌کننده «تاریخچه کافی نیست». چون دکمه به یک ردیف
واقعی از اسکن نیاز دارد و این محیط داده بازار ندارد، کلیک واقعی روی
دکمه در یک ردیف زنده امتحان نشد — همان محدودیت مستندشده در قلم‌های
ب-۷/۱-ب-۶. ۳۲۰ → ۳۲۹.

### ۱۴۰۵/۰۵/۲۳ — «اگر همین حالا ببندی» و «اگر با آخرین/پایانی تسویه کنی» (قلم الف-۱، سؤال‌های ۴ و ۵)

بزرگ‌ترین قلم بک‌لاگ (الف-۱، صفحه جزئیات هر موقعیت) پنج زیرخواسته داشت؛
سه‌تای آخرش عملاً «ارزش‌گذاری با مبناهای مختلف» بود و یادداشت خودِ بک‌لاگ
می‌گفت موتور همه‌اش را دارد، فقط نمایش کنار هم مانده. این دو زیرخواسته
(بستن فوری، تسویه با آخرین/پایانی) دقیقاً همان الگوی `closePrice`/
`markToMarket` بود که برای موقعیت‌های واقعی در `core/positions.mjs` از
قبل نوشته شده بود — فقط برای یک ردیف غربال (نه یک موقعیت ثبت‌شده) هرگز
صدا زده نمی‌شد.

```text
core/positions.mjs   closeValuation(legs, quotes, basis, fees) — تابع خالص
                      تازه، منطق «بستن یعنی معامله در جهت مخالف» را از
                      داخل markToMarket بیرون کشید تا هم آنجا و هم
                      evaluate از یک‌جا بیایند
core/evaluate.mjs     سه فیلد تازه در خروجی هر ردیف: instantClosePnl
                      (مبنای دفتر سفارش — اگر همین حالا بگیری و آفست
                      کنی)، settleLastPnl و settleClosePnl (مبنای آخرین
                      معامله و قیمت پایانی — فقط مرجع، ادعای اجرا ندارند).
                      از همان quotes ای که برای قیمت ورود استفاده شد،
                      پس دوباره‌کاری دفتر سفارش لازم نبود
ui/tabs/engine.mjs    ui/tabs/strategy.mjs   ui/tabs/top.mjs
                      سه ردیف تازه در پانل جریان نقد؛ فقط دفتر سفارش
                      ادعای اجرا دارد، دو مبنای دیگر با برچسب «مرجع»
```

بستن فوری همیشه از تسویه با آخرین/پایانی بدتر است چون اسپرد کامل را دو
بار می‌پردازی (یک‌بار ورود، یک‌بار خروج)، آن دو فقط یک‌بار. این رابطه در
آزمون قفل شد، نه فقط توضیح داده شد.

۴ آزمون در گروه ۹: بدون کارمزد، سه عدد دقیقاً برابر تفاضل bid با
ask/last/close هستند (فرمول‌شان قابل پیش‌بینی است، نه فقط علامت‌شان)؛ با
کارمزد واقعی، بستن فوری همیشه از هر دو مرجع بدتر است. تأیید در مرورگر
واقعی با Playwright روی تب موتور: قیمت دستی وارد شد، هر سه عدد با رقم
فارسی رندر شدند، بستن فوری منفی‌تر از دو مرجع دیگر بود — دقیقاً همان
رابطه‌ای که آزمون قفل کرده. تب‌های برترین موقعیت‌ها، استراتژی، و فروش
پوت بدون خطای کنسول باز شدند. ۳۱۶ → ۳۲۰.

### ۱۴۰۵/۰۵/۲۳ — کنترل‌های تنظیمات: اسلایدر و دکمه گام به‌جای فیلد خام (قلم پ-۵ بک‌لاگ)

`core/settings.mjs` برای هر عدد از قبل `min`/`max`/`step` داشت، ولی تب
تنظیمات فقط یک `<input type="number">` خام می‌ساخت. کاربر خواست از
همان داده برای اسلایدر و دکمه گام استفاده شود.

```text
ui/tabs/settings.mjs   هر فیلد num/pct حالا سه کنترل هم‌زمان دارد: دکمه
                        −/+ (یک گام f.step)، فیلد عددی (برای مقدار دقیق —
                        کارمزد با شش رقم اعشار را اسلایدر به‌تنهایی
                        نمی‌شود دقیق تنظیم کرد)، و input[type=range] با
                        همان min/max/step خام SCHEMA. سه‌تا با رویداد
                        input دوطرفه هم‌زمان می‌مانند؛ write() (بازگشت به
                        پیش‌فرض، و پاسخ ذخیره از سرور) اسلایدر را هم
                        به‌روز می‌کند
ui/style.css            .num-ctl ، .step-btn ، .num-range — هم‌راستا با
                        پوسته روشن/تیره موجود (accent-color از متغیر
                        تم)
```

بازه‌ها دقیقاً همان چیزی ماندند که در SCHEMA بود — برای فیلدهایی مثل
سرمایه در دسترس (۰ تا ۱۰¹³) اسلایدر خام درشت است، ولی این عمداً است:
خواسته کاربر «از همان داده آماده» بود، نه بازطراحی مقیاس هر فیلد.

تأیید در مرورگر واقعی با Playwright: حرکت اسلایدر فیلد عددی را عوض
می‌کند، دکمه + یک گام دقیق اضافه می‌کند و اسلایدر را هم‌زمان می‌کند،
تایپ دستی در فیلد عددی اسلایدر را هم‌زمان می‌کند، و «بازگشت به
پیش‌فرض» هر دو کنترل را با هم به مقدار پیش‌فرض برمی‌گرداند. اسکرین‌شات
کامل صفحه تنظیمات گرفته شد — هر ۹ گروه، حدود چهل فیلد عددی، همه با
اسلایدر و دکمه رندر شدند، بدون خطای کنسول. آزمون‌ها دست‌نخورده ماندند
چون این تغییر کاملاً DOM است، منطق خالص جدیدی ندارد. ۳۱۶ → ۳۱۶.

### ۱۴۰۵/۰۵/۲۳ — رقم لاتین باقی‌مانده در چند تب، و یک باگ واقعی همراهش (قلم پ-۲ بک‌لاگ)

بازبینی کامل هر ماژول `ui/` نشان داد بیشتر رابط از قلم فارسی قبلی
(۱۴۰۵/۰۵/۲۲) عبور کرده، ولی چند جا از قلم در رفته بودند — همه از الگوی
یکسان: شمارنده یا برچسب کوچک مستقیم در قالب رشته درج شده، نه از
`fmt.int`/`faDigits` عبور کرده.

```text
ui/tabs/chain.mjs      drawFlow() از faDigits استفاده می‌کرد بدون
                        import کردنش — ReferenceError خاموش در همان
                        try/catch بلعیده می‌شد و «وضعیت جریان داده» در
                        این تب همیشه خالی می‌ماند. این باگ واقعی رفتار
                        بود، نه فقط رقم لاتین — الان import شد
ui/picker.mjs           تعداد قرارداد/مظنه هر نماد، و خلاصه انتخاب
ui/tabs/positions.mjs   تعداد قرارداد، روز نگه‌داری، تاریخ ورود، تعداد
                        نماد قیمت‌گیری‌شده (در جدول و پانل جزئیات)
ui/tabs/roll.mjs        روز تا سررسید در کشویی انتخاب، تعداد قرارداد
ui/tabs/strategy.mjs    شمار نماد/ردیف در نوار وضعیت، «از N» در KPI، شمار
                        پا، زمان اسکن دو مرحله‌ای (میلی‌ثانیه/ردیف/عمق)
ui/tabs/top.mjs         همان الگوی زمان اسکن
ui/tabs/soon.mjs        شماره فاز، شمار قیمت اعمال/سررسید، رشته پاها
                        (این تب دیگر واقعاً در مسیر تب‌ها نیست — هر
                        استراتژی کاتالوگ mod خودش را دارد — ولی درست‌کردنش
                        صفر ریسک بود و اگر تبی بدون mod اضافه شود درست
                        رفتار می‌کند)
```

واحد «ms» انگلیسی در `chain.mjs` هم به «میلی‌ثانیه» رفت، هم‌راستا با
اصلاح مشابه در پنل جزئیات (قلم پ-۷، همین اجرا).

تأیید در مرورگر واقعی با Playwright: تب دیده‌بان زنجیره باز شد و پانل
«وضعیت جریان داده» — که پیش از این اصلاح همیشه خالی می‌ماند — حالا نُه
ردیف با رقم فارسی نشان می‌دهد؛ هیچ خطای کنسول در تب‌های زنجیره، برترین
موقعیت‌ها، موقعیت‌های من، و تحلیل رول دیده نشد. آزمون‌ها دست‌نخورده
ماندند چون خودِ الگو (`fmt.int`/`faDigits`) قبلاً در گروه ۲۱ آزموده شده؛
این قلم فقط مصرف‌کننده‌های ازقلم‌افتاده را وصل کرد. ۳۱۶ → ۳۱۶ (بدون
آزمون تازه، چون تغییر خالص DOM/رشته است، نه منطق خالص).

### ۱۴۰۵/۰۵/۲۳ — پنل «جزئیات» خواناتر و صادق‌تر شد (قلم پ-۷ بک‌لاگ)

بک‌لاگ می‌گفت «آخرین خطا» متن خام جاوااسکریپت نشان می‌دهد و «سن عکس سرور»
رقم خام ثانیه است. بازبینی `ui/app.mjs` تأیید کرد:

```text
d-err   قبلاً h.lastError مستقیم چاپ می‌شد — چیزی مثل «TypeError: fetch
        failed» یا «AbortError: The operation was aborted» که از
        server/server.mjs:171 (`${e.name}: ${e.message}`) می‌آید. کاربر
        فارسی‌زبان از این متن چیزی نمی‌فهمد.
d-age   قبلاً `${faDigits(h.watchAgeSec)} ثانیه` بود — رقم خام، نه فاصله
        خوانا (برخلاف «آخرین دریافت» که از قبل faAgo داشت).
d-ms    واحد «ms» انگلیسی بود، برخلاف قاعده زبان فارسی رابط.
```

اصلاح:

```text
ui/fmt.mjs   humanizeUpstreamError(raw) — تابع خالص، چند الگوی شناخته‌شده
             خطای بالادست را به فارسی خوانا می‌برد (بی‌پاسخی ، اتصال
             برقرار نشد ، HTTP با کد فارسی ، جیسون نامعتبر)؛ ناشناس هم
             پیام عمومی می‌دهد، سقوط نمی‌کند
ui/app.mjs   d-err متن خوانا می‌گیرد؛ خام در title همان عنصر می‌ماند برای
             کسی که واقعاً می‌خواهد جزئیات فنی را ببیند — چیزی حذف نشد،
             فقط لایه اول خواناتر شد. d-age به faAgo رفت (همان تابعی که
             «آخرین دریافت» از قبل استفاده می‌کرد). d-ms واحدش فارسی شد.
```

۶ آزمون در گروه ۲۱. تأیید در مرورگر واقعی با Playwright: چهار الگوی خطا
هر کدام فارسی و بدون حرف لاتین درمی‌آیند، کد HTTP با رقم فارسی چاپ
می‌شود، و پنل جزئیات باز شده روی سرور محلی (بدون خطای واقعی) «—» و «هیچ»
درست نشان می‌دهد. ۳۱۰ → ۳۱۶.

### ۱۴۰۵/۰۵/۲۳ — نشان شکل بازده از فهرست کناری حذف شد (قلم پ-۳ بک‌لاگ)

هر ردیف فهرست کناری یک SVG کوچک داشت که شکل بازده استراتژی را نشان
می‌داد، کنار برچسب جهت (صعودی/نزولی) و شمار پا. کاربر خواست حذف شود —
با برچسب متنی و رنگی که مانده، فهرست شلوغ‌تر از لازم بود.

`ui/glyph.mjs` بعد از حذف این یک مصرف‌کننده‌اش کاملاً بلااستفاده ماند،
پس کل فایل حذف شد، نه فقط فراخوانی‌اش؛ همراه با CSS مرده‌اش
(`.glyph`، `.g-line`، `.g-zero`). برچسب جهت و شمار پا دست‌نخورده ماندند.

تأیید در مرورگر واقعی: هیچ `svg.glyph` در فهرست نیست، برچسب جهت هنوز هست.
آزمون‌ها ۳۱۰/۳۱۰ سبز ماندند (این تغییر منطق را لمس نکرد، فقط رندر).

### ۱۴۰۵/۰۵/۲۳ — تب «برترین موقعیت‌ها»، غربال کل کاتالوگ یک‌جا (قلم الف-۳ بک‌لاگ)

هر تب استراتژی فقط خودش را می‌دید. این قلم یک تب تازه می‌خواست که روی همه
سی‌ویک استراتژی اسکن کند و بهترین‌ها را کنار هم بگذارد.

```text
core/scan.mjs      scanAll({defs, chain, uaKeys, settings, ...}) — روی هر
                    def صدا می‌زند، نتیجه‌ها را ادغام می‌کند، و با همان
                    s.rankBy که هر تب تنها روی خودش اعمال می‌کند، یک‌جا
                    رتبه‌بندی می‌کند. تابع خالص، در Node آزمون‌پذیر بدون
                    ریسه یا مرورگر
worker/scan-worker.mjs  پیام تازه scan-all، همان الگوی scan موجود
ui/scanner.mjs      runScanAll — فقط استراتژی‌های feasible را می‌فرستد؛
                    غیرشدنی‌ها در تابلو اصلاً اجرا ندارند
ui/tabs/top.mjs      تب تازه: جدول با ستون «استراتژی» (که از قبل در
                    COLUMNS پین بود، انگار برای همین روز آماده مانده بود)،
                    و پانل جزئیات با همان الگوی نمودار تعامل‌پذیر و
                    برچسب پوشش/ریسک که در دو قلم قبلی ساخته شد
```

عمداً فقط مرحله یک است، بدون عمق دفتر سفارش: عمق برای سی‌ویک استراتژی
یک‌جا یعنی سی‌ویک درخواست دفتر سفارش پیاپی، هزینه‌ای که یک نمای کلی اولیه
توجیه نمی‌کند. پانل جزئیات صریح می‌گوید: «این ردیف فقط مرحله یک است؛ برای
عدد اجرایی، تب همان استراتژی را باز کن و دوباره اسکن بزن» — پول‌رسانی
صادق می‌ماند، ادعای اجرا نمی‌شود.

۷ آزمون در گروه ۲۸، روی زنجیره مصنوعی با پنج قیمت اعمال و دو سررسید:
نتیجه کل ردیف‌های تک‌استراتژی را هم شامل می‌شود، بیش از یک استراتژی
واقعاً حاضر است، سقف `limit` رعایت می‌شود، و رتبه‌بندی نزولی روی ادغام
چند استراتژی حفظ می‌شود. تأیید کامل در مرورگر واقعی: تب باز می‌شود، ۴۲
ردیف از ۲۳ استراتژی مختلف نمایش داده شد، کلیک روی یک ردیف نمودار
تعامل‌پذیر و برچسب‌های پوشش/ریسک را درست نشان داد. ۳۰۳ → ۳۱۰.

### ۱۴۰۵/۰۵/۲۳ — حالت پوشش دیگر خام انگلیسی نبود؛ ریسک‌دار و کم‌ریسک صریح جدا شدند (قلم الف-۶ بک‌لاگ)

در صفحه جزئیات هر ترکیب، ردیف «پوشش» عین همان مقدار خام موتور را چاپ
می‌کرد: `full` ، `naked` ، `partial` ، `none` — نه فارسی (برخلاف قاعده
پروژه)، نه رنگی، هیچ نشانه‌ای که کدام حالت خطرناک است. زیان نامحدود هم
جای دیگری نبود؛ فقط در شمارنده فهرست کلی («۲ ردیف زیان نامحدود») دیده
می‌شد، نه روی خودِ آن ردیف وقتی بازش می‌کنی.

```text
ui/fmt.mjs        coverageInfo(state) → {label, tone} — تابع خالص، پس
                  اینجاست نه در یک تب خاص. چهار حالت moduleمرز.mjs را به
                  برچسب فارسی و یکی از چهار تُن (gain/warn/loss/flat)
                  می‌برد؛ full=gain ، partial=warn ، naked=loss ، none=flat
ui/tabs/strategy.mjs  ردیف «پوشش» حالا span.tag رنگی است. ردیف تازه
                  «سقف زیان» هم اضافه شد: نامحدود=loss ، محدود=gain.
                  «بیشترین زیان» در legend نمودار هم وقتی بی‌نهایت است
                  قرمز می‌شود، نه فقط علامت ∞ بی‌رنگ
```

۵ آزمون در گروه ۲۱ (چون `coverageInfo` حالا در `ui/fmt.mjs` است، همان‌جا
که «یک منبع برای کل رابط» است). تأیید در مرورگر واقعی با داده بازار
مصنوعی: فروش کال بدون پوشش برچسب قرمز «لخت — بدون پوشش» و «نامحدود —
ریسک‌دار» می‌گیرد؛ کاوردکال برچسب سبز «کامل» و «محدود» می‌گیرد. ۲۹۸ → ۳۰۳.

### ۱۴۰۵/۰۵/۲۳ — وضعیت کلی بازار در دیده‌بان زنجیره: تلاطم ضمنی، نسبت پوت به کال، نزدیک‌ترین سررسید (قلم الف-۴ بک‌لاگ)

بک‌لاگ یک تب تازه «داشبورد بازار» می‌خواست. قبل از ساختنش تب «دیده‌بان
زنجیره اختیار» را دوباره خواندم — همان چیزی که خواسته ۱۱ می‌گفت (وضعیت
کلی نمادها، سررسید، حجم، موقعیت باز) اکثرش از قبل آنجا بود، فقط دو سنجه
کم داشت. پس به‌جای یک تب موازی، همان تب کامل شد:

```text
core/chain.mjs   underlyingList اکنون سه فیلد تازه می‌دهد:
                 nearestDays  روز تا نزدیک‌ترین سررسید همان پایه
                 pcRatio      نسبت موقعیت باز پوت به کال، کل زنجیره
                 atmIv        تلاطم ضمنی نزدیک‌ترین قیمت اعمال به پایه،
                              نزدیک‌ترین سررسید — از impliedVol همان موتور
                              مشترک، نه محاسبه‌گر جدا
ui/tabs/chain.mjs  سه ستون تازه در جدول نمادها؛ atmIv به درصد نمایش
                 (فراکشن خام در موتور می‌ماند، مثل سایر سیگماها)
```

«بیشترین موقعیت باز» نیاز به کار تازه نداشت — ستون موقعیت باز از قبل بود
و جدول با کلیک سرستون مرتب می‌شود؛ کاربر با یک کلیک بیشترین را بالا
می‌آورد.

تلاطم ضمنی از قیمت پایانی می‌آید نه تقاضا/عرضه، چون بیرون از ساعت بازار
هم موجود است — نمایشی است برای مقایسه نمادها، نه ورودی اجرایی؛ ادعای
اجرا هنوز فقط از دفتر سفارش می‌آید.

۵ آزمون در گروه ۲۷: نزدیک‌ترین سررسید درست انتخاب می‌شود، نسبت پوت به کال
از موقعیت باز واقعی حساب می‌شود، و تلاطم ضمنی بازیابی‌شده با سیگمای واقعیِ
مولد قیمت آزمایشی کمتر از ۰٫۱٪ اختلاف دارد. تأیید در مرورگر واقعی: هر سه
ستون در جدول ظاهر می‌شوند و محاسبه مستقیم روی همان ماژول سرویس‌شده همان
جواب Node را می‌دهد. ۲۹۳ → ۲۹۸.

### ۱۴۰۵/۰۵/۲۳ — فیلتر موقعیت باز هرگز اجرا نمی‌شد، به‌علاوه سه فیلتر نقدشوندگی تازه (قلم الف-۲ بک‌لاگ)

در بازبینی کد برای افزودن فیلترهای تازه، یک باگ واقعی در `generateCombos`
پیدا شد: تنظیم «حداقل موقعیت باز» (`minOpenInt`) از قبل در `settings.mjs`
و در فرم تنظیمات بود، ولی هیچ‌وقت واقعاً اعمال نمی‌شد.

```text
if (t.side === 'sell' && (q.bidQty < s.minBidQty || q.oi < s.minOpenInt)) missing = missing || false;
```

سطر آخر بی‌اثر است — همیشه همان مقدار قبلی `missing` را برمی‌گرداند،
چه شرط برقرار باشد چه نه. کاربر عددی تنظیم می‌کرد، شاهد هیچ تغییری در
نتیجه نبود. اصلاح شد به `missing = true`.

همین بازبینی، دقیقاً همان الگوی بک‌لاگ الف-۲ بود: «باید در
`core/settings.mjs` و `passesFilters` هم‌زمان اضافه شوند تا فیلتر واقعاً
اعمال شود نه فقط دیده.» سه گزینه تازه از فهرست خواسته کاربر اضافه شد:

```text
minLegVol        حداقل حجم معاملات امروز هر پا — q.vol، تا حالا هیچ‌جا
                  استفاده نمی‌شد با اینکه از دیده‌بان می‌آید
minLegValue      حداقل ارزش معاملات امروز هر پا (ریال) — q.value، همان‌جا
minUaLiquidity   حداقل نقدشوندگی کل زنجیره پایه (ریال) — نه یک پا، مجموع
                  ارزش معاملات کل زنجیره؛ در generateCombos قبل از حلقه
                  سررسیدها سنجیده می‌شود، پس اگر پایه رد شود کل نماد یک‌جا
                  حذف می‌شود، بدون ساخت حتی یک ترکیب
```

نقدشوندگی پایه از حجم/ارزش خودِ سهم پایه نیامد — دیده‌بان اختیار دفتر
سفارش یا حجم سهم پایه را نمی‌دهد (این محدودیت در کد مستند بود:
`underlyingQuote` عمداً `oi: 0, vol: 0` می‌گذارد). به‌جایش از ارزش
معاملات کل زنجیره اختیار همان پایه استفاده شد — عددی که بدون درخواست
اضافه، از همان یک عکس لحظه‌ای دیده‌بان در دسترس است.

۱۰ آزمون در گروه ۲۶، با رکورد دیده‌بان مصنوعی که موقعیت باز و حجم و ارزش
هر پا را جدا کنترل می‌کند. آزمون باگ قبلی صریح است: سقف بالاتر از موقعیت
باز واقعی باید ردیف را بیندازد، و پیش از اصلاح نمی‌انداخت. تأیید در
مرورگر واقعی: هر سه برچسب تازه در تب تنظیمات، کنار «حداقل موقعیت باز»
موجود، درست ظاهر می‌شوند. ۲۸۳ → ۲۹۳.

### ۱۴۰۵/۰۵/۲۳ — بقیه نمودارها هم تعامل‌پذیر شدند (قلم ب-۴ بک‌لاگ)

`positions.mjs` و `roll.mjs` هنوز از `payoffSvg` ایستا استفاده می‌کردند —
همان محدودیتی که تب استراتژی قبلاً داشت. `roll.mjs` هم علاوه بر دو نمودار
بازده، نمودار تفاضل (`diffSvg`) داشت که اصلاً زوم نداشت، درست همان‌جایی که
مرز تصمیم رول را باید از نزدیک دید.

```text
ui/chart.mjs           زیرساخت زوم/کشیدن/خط‌راهنما از mountPayoff به یک
                        تابع مشترک mountInteractive منتقل شد. frame()
                        برای بازده و diffFrame() تازه برای تفاضل، هر دو
                        همان زیرساخت را تغذیه می‌کنند — به‌جای کپی صد و
                        پنجاه خط رویداد pointer/wheel برای هر کدام.
                        mountDiff صادر شد؛ diffSvg ایستا هم ماند (چاپ).
ui/tabs/positions.mjs  پانل جزئیات یک موقعیت به mountPayoff رفت. قیمت‌گیری
                        هر پانزده ثانیه دوباره drawDetail را صدا می‌زند؛
                        اگر همان موقعیت باز است زوم می‌ماند (الگوی چارت
                        ب-۷)، اگر موقعیت دیگری انتخاب شد نمای اول تازه است.
ui/tabs/roll.mjs       هر سه نمودار (تفاضل ، موقعیت فعلی ، موقعیت پس از
                        رول) به mountDiff/mountPayoff رفتند. کلید سناریو
                        `موقعیت:پای‌بسته‌شونده:نامزدتازه` تعیین می‌کند رفرش
                        قیمت همان سه‌تایی است یا انتخاب واقعاً عوض شده.
```

تأیید: چون این محیط به داده بازار وصل نیست، اسکن کامل رابط ممکن نبود.
`mountPayoff`/`mountDiff`/`payoffSvg`/`diffSvg` مستقیم از ماژول واقعی با
پایه‌های ساختگی در Playwright امتحان شدند — هر چهار امضا هنوز درست کار
می‌کنند، `initRange` هم برای نمودار بازده هم برای تفاضل زوم را دقیقاً
برمی‌گرداند، و `mountDiff` مرز تصمیم را درست پیدا می‌کند. هر سه تب
(استراتژی، موقعیت‌های من، تحلیل رول) بدون خطای صفحه باز می‌شوند. با یک
موقعیت ساختگی از راه API، پانل جزئیات `positions.mjs` واقعاً باز شد، نمودار
تعامل‌پذیر رسم شد، و زوم با غلتک روی مرورگر واقعی کار کرد. تب رول به همان
شکل بسته ماند چون قرارداد تازه به `chainDetail` زنده نیاز دارد که در این
محیط در دسترس نیست؛ منطق زوم آن دقیقاً همان الگوی امتحان‌شده در دو تب
دیگر است. چیزی به `tests/run.mjs` اضافه نشد چون این‌ها همه DOM‌اند.

### ۱۴۰۵/۰۵/۲۳ — زوم نمودار در اسکن پیوسته دور ریخته می‌شد (قلم ب-۷ بک‌لاگ)

`showDetail` هر بار `chart?.destroy()` می‌زد و `mountPayoff` را از نو
می‌ساخت — هم با کلیک کاربر روی یک ردیف، هم با هر تیک «اسکن پیوسته».
نتیجه: کاربری که روی نمودار زوم کرده بود، با هر رفرش (هر `watchIntervalSec
× 3` ثانیه) به نمای اول پرت می‌شد؛ همان چیزی که بک‌لاگ «پیمایش کاربر از
دست می‌رود» می‌نامید.

```text
ui/chart.mjs      mountPayoff حالا view() برمی‌گرداند (بازه [lo,hi] فعلی)
                  و opt.initRange می‌گیرد تا نمای شروع را به‌جای نمای اول
                  بنشاند
ui/tabs/strategy.mjs  showDetail قبل از destroy بازه چارت را می‌گیرد. اگر
                  ردیف تازه همان id ردیف قبلی است (رفرش، نه انتخاب دستی)،
                  همان بازه به mountPayoff تازه داده می‌شود؛ اگر ردیف واقعاً
                  عوض شده، از نمای اول شروع می‌کند — که رفتار درست همان‌جاست
```

تأیید: چون این محیط به داده بازار واقعی وصل نیست (پروکسی، و بازار هم بسته
است)، اسکن کامل رابط کاربری قابل اجرا نبود. به‌جایش `mountPayoff` مستقیم
از طریق مرورگر واقعی (Playwright) با پایه‌های ساختگی امتحان شد: زوم دستی
بازه را باریک می‌کند، رفرش با `initRange` دقیقاً همان بازه را برمی‌گرداند،
و مونت یک موقعیت دیگر بدون `initRange` به نمای اول برمی‌گردد. هر سه سنجه
سبز. چیزی به `tests/run.mjs` اضافه نشد چون `mountPayoff` کاملاً DOM است
(SVG، رویداد pointer/wheel) و پروژه هیچ کتابخانه DOM برای Node ندارد.

### ۱۴۰۵/۰۵/۲۳ — میان‌بر صفحه‌کلید در فهرست کناری (قلم ب-۶ بک‌لاگ)

سی‌وچهار تب فیلترشدنی بود ولی رسیدن به جست‌وجو و باز کردن یکی بدون موس
همیشه ممکن نبود.

```text
/ یا Ctrl+K       نشانگر را به جست‌وجوی فهرست می‌برد و متن قبلی را انتخاب
                  می‌کند؛ اگر همان لحظه داخل ورودی دیگری تایپ می‌کنی، «/»
                  دست‌نخورده در همان ورودی می‌ماند و فوکوس دزدیده نمی‌شود
بالا و پایین      بین تب‌های فیلترشده حرکت می‌کنند، با شناسه نه اندیس نگه
                  داشته می‌شوند چون فهرست با هر تایپ از نو ساخته می‌شود
اینتر             همان تب برجسته را باز می‌کند؛ رفتار قبلی «همیشه اولی»
                  حالا فقط پیش‌فرض برجستگی است، نه رفتار قفل‌شده اینتر
```

برجستگی صفحه‌کلید (`data-kbd-active`) از انتخاب واقعی تب باز
(`aria-current`) جدا نگه داشته شد — یکی می‌گوید «الان اینجایی»، دیگری
می‌گوید «اینتر همین را باز می‌کند».

تأیید در مرورگر واقعی با Playwright: نه سنجه، از فوکوس گرفتن با `/` و
Ctrl+K تا کف نشکستن ArrowUp پیاپی و باز نشدن دزدانه فوکوس وقتی جای دیگری
تایپ می‌شود. چیزی در `tests/run.mjs` اضافه نشد چون این منطق کاملاً به DOM
و رویداد صفحه‌کلید بند است، نه تابع خالص.

### ۱۴۰۵/۰۵/۲۳ — تلاطم ضمنی، نیوتن روی وگا (قلم ب-۳ بک‌لاگ)

`impliedVol` فقط تنصیف بود: تا ۱۲۰ بار `bsPrice` صدا می‌زد، و در اسکن کامل
برای هر پای هر ترکیب یک‌بار اجرا می‌شود — یعنی همین حلقه، صداشده‌ترین بخش
کل موتور است.

```text
core/bs.mjs   نیوتن روی وگا، با تنصیف به‌عنوان تور ایمنی (رویه rtsafe):
              هر گام داخل کران [lo,hi] نگه داشته می‌شود؛ اگر گام از کران
              بیرون بزند یا وگا نزدیک صفر باشد، همان لحظه به تنصیف صرف
              سوییچ می‌کند، با کرانی که نیوتن تا همان‌جا تنگ کرده
opt.newtonIters  گزینه تازه، پیش‌فرض ۱۵؛ صفرش می‌کند به رفتار قدیم برگردد
```

روی شبکه‌ای از ۸۴۰ ترکیب (سه قیمت پایه، هفت نسبت اعمال، چهار سررسید، پنج
تلاطم)، میانگین فراخوانی `bsPrice` هر بار از ۲۲٫۳ به ۱۰٫۱ افت کرد — حدود
نصف.

ناحیه در پول عمیق و نزدیک سررسید ذاتاً بدشرط است: وگا نزدیک صفر یعنی قیمت
روی بازه‌ای وسیع از تلاطم تقریباً ثابت می‌ماند، پس بازیابی خودِ تلاطم
دقیق نیست — نه با تنصیف صرف، نه با نیوتن. این رفتار قبلی هم بود؛ آزمون
تازه آن را جدا می‌سنجد تا با ناحیه خوش‌رفتار قاطی نشود.

۶ آزمون در گروه ۲۵. ۲۷۷ → ۲۸۳.

### ۱۴۰۵/۰۵/۲۳ — عقب‌نشینی حلقه دیده‌بان (قلم ب-۱ بک‌لاگ)

اگر بالادست قطع بود، `watchLoop` هر `watchIntervalSec` (کف ۲ ثانیه) دوباره
می‌کوبید — هر بار هم `get()` به تنهایی `retries` تلاش با عقب‌نشینی داخلی
می‌کرد، ولی خودِ حلقه هیچ حافظه‌ای از شکست‌های پیاپی نداشت.

```text
server/backoff.mjs   watchBackoffSec(interval, fails, cap=300) — خالص.
                      بدون شکست همان فاصله عادی؛ هر شکست پیاپی فاصله را
                      دو برابر می‌کند تا سقف ۵ دقیقه
watchTick             حالا true/false برمی‌گرداند به‌جای بلعیدن خطا
watchLoop             شمار شکست پیاپی را نگه می‌دارد، با هر موفقیت صفر
                      می‌شود؛ stat.watchConsecutiveFails از /api/health
                      دیده می‌شود
```

بازار بسته «موفق» حساب می‌شود — آن حالت نیاز به عقب‌نشینی ندارد، فقط
دیده‌بان خاموش است.

۸ آزمون در گروه ۲۴. ۲۶۹ → ۲۷۷.

### ۱۴۰۵/۰۵/۲۳ — سقف کش سرور (قلم ب-۲ بک‌لاگ)

`cache` یک `Map` بود که فقط با `DELETE /api/cache` دستی خالی می‌شد. یک
نشست طولانی روی بازار پر از نماد یعنی هزاران کلید URL که هرگز آزاد
نمی‌شوند.

```text
server/cache.mjs   evictOldest(map, max) — خالص، قدیمی‌ترین ورودی‌ها را
                   به ترتیب درج حذف می‌کند تا اندازه از سقف پایین بیاید
core/settings.mjs  کلید تازه maxCacheEntries، گروه سهمیه، سرور، پیش‌فرض
                   ۲۰۰۰
server.mjs         بعد از هر cache.set، evictOldest صدا زده می‌شود؛ سقف
                   از S خوانده می‌شود پس تغییر زنده تنظیمات همان لحظه
                   اثر می‌کند
```

تأیید در مرورگر واقعی: فیلد تازه با برچسب و راهنما زیر «سهمیه درخواست»
در تب تنظیمات ظاهر می‌شود، و `/api/health` مقدار `cacheSize` را همچنان
درست گزارش می‌کند.

۷ آزمون در گروه ۲۳. ۲۶۲ → ۲۶۹.

### ۱۴۰۵/۰۵/۲۲ — جابه‌جایی ستون با کشیدن (خواسته ۷)

سرستون‌ها `draggable` شدند. انداختن روی یک سرستون، ستون کشیده‌شده را
دقیقاً جای آن می‌نشاند. حالت «قبل یا بعد از مقصد» عمداً نیامد: در صفحه
راست‌به‌چپ «قبل» یعنی سمت راست، و همین یک کلمه تصمیم را مبهم می‌کند.

دو چیز که در مسیر شکست و اصلاح شد:

```text
تیک ستون تازه   قبلاً کل فهرست را به ترتیب قرارداد از نو می‌ساخت. تا وقتی
                چیدمان ثابت بود بهترین کار بود، ولی حالا یعنی یک تیک، تمام
                کشیدن‌های کاربر را دور می‌ریزد. insertColumn اول می‌سنجد
                چیدمان هنوز قراردادی هست یا نه: اگر هست، ستون سر جای
                قراردادی‌اش می‌نشیند؛ اگر کاربر چیزی جابه‌جا کرده، چیدمان
                مال اوست و ستون تازه ته صف می‌رود.
پرچم کشیدن      نسخه اول پرچم را تا مصرف شدن نگه می‌داشت. نتیجه: اولین کلیک
                واقعی بعد از هر کشیدن — روی هر ستونی — بلعیده می‌شد و
                مرتب‌سازی کار نمی‌کرد. در مرورگر گرفته شد، نه در بازبینی
                کد. حالا پرچم تا پایان همان نوبت حلقه رویداد زنده است.
```

ترتیب، کنار انتخاب ستون، در همان کلید حافظه مرورگر می‌ماند.

۱۳ آزمون در گروه ۲۲ روی دو تابع خالص، به‌علاوه هشت سنجه در مرورگر واقعی:
ترتیب عوض شد، طول و مجموعه کلیدها ثابت ماند، مرتب‌سازی ناخواسته نشد، کلیک
هنوز مرتب می‌کند، و چیدمان بعد از بارگذاری مجدد ماند. ۲۴۹ → ۲۶۲.

### ۱۴۰۵/۰۵/۲۲ — عدد فارسی در کل رابط، نوار وضعیت، و نشان استراتژی

سه خواسته کاربر از فهرست تازه.

**۱ همه عددها فارسی.** قالب‌بندی پراکنده بود: ۲۷ فراخوانی
`toLocaleString('en-US')` در هشت فایل، به‌علاوه `toFixed` و درج مستقیم
`${عدد}`. یک ماژول `ui/fmt.mjs` منبع واحد شد و بقیه به آن وصل شدند.

قالب‌بندی با دست انجام می‌شود نه با `toLocaleString('fa-IR')`، به دو دلیل:
آن تابع پیش از عدد منفی یک نویسه نامرئی (U+200E) می‌گذارد که داخل SVG
دردسر می‌سازد، و خروجی‌اش به نسخه ICU مرورگر بند است پس آزمون واحد چیزی
را قفل نمی‌کند. حالا در انگلیسی قالب داده می‌شود و نویسه‌ها برمی‌گردند:
رقم فارسی، «٬» هزارگان، «٫» اعشار، «−» منفی.

`toEnDigits` هم هست، برای وقتی که کاربر عدد را فارسی تایپ کند.

بررسی با مرورگر روی هر ۳۶ تب: هیچ رقم لاتینی در متن دیده‌شدنی نماند —
جز داخل `input type=number` که باید لاتین بماند.

قاعده `.payoff text` از `unicode-bidi: bidi-override` به `isolate` رفت.
override نشانه منفی را درست می‌کرد ولی واژه فارسی محور («۲٫۵ میلیارد») را
حرف‌به‌حرف وارونه می‌کرد.

**۲ نوار وضعیت بازبینی شد.** معیار: چیزی بماند که کاربر بر اساسش تصمیم
می‌گیرد.

```text
رفت    درخواست بالادست ، اصابت کش ، میلی‌ثانیه ، سن عکس  →  پشت دکمه «جزئیات»
ماند   بازار باز/بسته ، خطا (فقط وقتی وجود دارد)
آمد    حباب اتصال جریان — متصل ، در حال اتصال ، قطع ، با نقطه نبض‌دار
آمد    ساعت آخرین دریافت داده، که بعد از دو دقیقه سکوت زرد می‌شود
```

قبلاً هیچ نشانه‌ای از قطع شدن جریان نبود: «سن عکس» بی‌حرکت می‌ماند و
شبیه عدد درست به نظر می‌رسید. شمارنده‌های فنی حذف نشدند، فقط پیش‌فرض
بسته‌اند.

نام روز هفته هم فارسی شد. حباب بازار «Thu روز معاملاتی نیست» می‌گفت —
کلید انگلیسی برای تنظیم `tradeDays` لازم است، ولی متن کاربر نه.

**۳ نشان شکل بازده در فهرست.** سی‌ویک استراتژی با اسم‌های شبیه هم؛ «کندور
آهنی» و «پروانه آهنی» را از متن باید خواند، از شکل در یک نگاه می‌شود
تفکیک کرد.

نشان از موتور واقعی می‌آید نه از تصویر آماده: پاها با قیمت اعمال نرمال
حول صد ساخته می‌شوند، با بلک-شولز قیمت می‌خورند، و از همان تابع
تکه‌ای-خطی عبور می‌کنند که ستون‌های جدول از آن بیرون می‌آید. پس اگر موتور
عوض شود، نشان هم عوض می‌شود و دروغ نمی‌گوید.

جعبه‌اسپرد و تبدیل و برگردان بازده صاف دارند و اول بی‌نشان ماندند. صاف
بودن خاصیت تعریفی‌شان است نه نبود داده، پس خط صاف کشیده می‌شود. هر ۳۱
استراتژی نشان دارد.

فهرست هم ۲۶۴ به ۳۰۰ پیکسل رفت و قلم تب از ۱۳ به ۱۴٫۵.

۱۸ آزمون در گروه ۲۱. ۲۳۱ → ۲۴۹.

### ۱۴۰۵/۰۵/۲۲ — بازه سود از لبه پنجره رسم بریده می‌شد (قلم ۱ بک‌لاگ)

`analyzeMixed` روی `[۰٫۳۵ ، ۲٫۲] × قیمت پایه` نمونه می‌گرفت و `regions` را
مستقیم از همان شبکه می‌خواند. هر ترکیبی که در سقوط شدید سود می‌داد، بازه
سودش «از ۳۵٪ قیمت پایه» گزارش می‌شد، و سربه‌سری بیرون پنجره اصلاً پیدا
نمی‌شد.

اندازه خطا روی یک مورب پوت‌دار: ۷۵٫۹۷٪ در برابر ۳۶٫۸۰٪ — ۳۹ واحد درصد از
احتمال سود دور ریخته می‌شد.

```text
شبکه جست‌وجو   پنجره رسم به‌علاوه دو دنباله هندسی تا میدان دور
پنجره رسم      حالا قیمت اعمال را هم می‌گیرد، نه فقط ضریبی از قیمت پایه
مرز بازه       خودِ سربه‌سری است نه نزدیک‌ترین نقطه شبکه، پس بازه‌ای که به
               صفر یا بی‌نهایت می‌رسد همان‌جا بسته می‌شود
```

دنباله‌ها هندسی‌اند نه خطی، چون در محور قیمت فاصله معنی‌دار نسبی است.

۸ آزمون در گروه ۲۰. ۲۲۳ → ۲۳۱.

### ۱۴۰۵/۰۵/۲۱ — نوار تشخیص دروغ می‌گفت (گزارش کاربر)

کاربر گزارش داد تب خالی است. نوار تشخیص می‌گفت از ۲۸۰ ترکیب، ۲۵۱ تا برای
«عمق ناکافی» افتاده‌اند و ۲۹ تا برای فیلترها. صفر ردیف مانده.

بازتولید نشان داد دو علت کاملاً متفاوت هر دو در همان سطل می‌افتند، و
هیچ‌کدام ربطی به عمق ندارد:

```text
مبنای قیمت مرجع    پایانی و آخرین و کمترین و بیشترین طبق طراحی ادعای اجرا
                   ندارند، پس quality پای‌ها 'reference' می‌شود و کل ردیف
                   غیرقابل‌اجرا. این خطا نیست، تنظیم کاربر است — ولی نوار
                   تشخیص می‌گفت «عمق ناکافی» و کاربر راهی نداشت بفهمد.
حجم مظنه صفر       قیمت هست ولی حجمی پشتش نیست. generateCombos فقط px > 0
                   را می‌سنجید نه حجم را، پس ترکیب ساخته می‌شد و بعد
                   walkBook با filled = 0 برمی‌گشت. این بی‌مظنه بودن است.
```

سطل «عمق ناکافی» عملاً هیچ‌وقت به‌درستی پر نمی‌شد: پرشدن ناقص فقط پرچم
می‌گیرد و ردیف را نمی‌اندازد.

اصلاح:

```text
legPrices[].quality    کیفیت ماشین‌خوان روی هر پا ثبت می‌شود، جدا از متن برچسب
unexecutableReason()   علت واقعی را از همان کیفیت درمی‌آورد
سطل refBasis           سطل تازه برای مبنای قیمت مرجع
نوار تشخیص             سطل صفر پنهان می‌شود، و راهنمای اقدام‌پذیر چاپ می‌شود
```

راهنما دیگر فقط نمی‌شمارد، می‌گوید چه چیزی را عوض کن: «مبنا را دفتر سفارش
کن، یا نمایش غیرقابل اجرا را روشن کن.»

۱۶ آزمون در گروه ۱۹، به‌علاوه تأیید در مرورگر واقعی. ۲۰۷ → ۲۲۳.

### ۱۴۰۵/۰۵/۲۱ — مرز ورودی سرور (قلم‌های ۱ تا ۳ بک‌لاگ)

سه قلم که یک چیز بودند: سرور به هر چیزی که از بیرون می‌آمد اعتماد می‌کرد.
منطق در `server/guard.mjs` جدا شد تا بی‌نیاز از سرور آزمون شود.

```text
عبور از ریشه   startsWith(ROOT) مقایسه رشته‌ای است نه مقایسه مسیر، پس پوشه
               هم‌نام‌شروع کنار ریشه از آن رد می‌شد. path.relative جایش را
               گرفت. رمزگشایی درصدی هم اضافه شد، وگرنه %2e%2e%2f از فیلتر
               می‌گذشت. بایت صفر هم رد می‌شود.
کد ابزار       ins مستقیم داخل مسیر بالادست می‌نشست. حالا باید ^\d{1,32}$
               باشد وگرنه ۴۰۰ می‌گیرد. فهرست دسته‌ای هم پاک و یکتا می‌شود.
سقف بدنه       یک مگابایت، سنجیده حین دریافت نه بعدش. گذشتن از آن ۴۱۳ می‌دهد
               و جیسون خراب ۴۰۰، نه ۵۰۲ که خطای بالادست را تداعی می‌کرد.
```

۲۸ آزمون در گروه ۱۸. ۱۷۹ → ۲۰۷.

### ۱۴۰۵/۰۵/۲۱ — چهار خواسته رابط کاربری

**۱ فهرست استراتژی‌ها به سمت راست.** در راست‌به‌چپ، ستون اول شبکه همان سمت
راست است، پس ترتیب در `index.html` جا‌به‌جا شد و `grid-template-columns` هم.
مرز جداکننده از `border-inline-start` به `border-inline-end` رفت.

سی‌وچهار تب در یک ستون بدون کمک یعنی پیمایش. سه چیز اضافه شد: جست‌وجوی زنده
(با یکسان‌سازی ی و ک عربی و فارسی)، بخش‌های تاشو با حالت ماندگار، و برچسب
جهت هر استراتژی — صعودی ، نزولی ، خنثی ، تلاطم.

**۲ سرستون حالا واقعاً بالای جدول می‌چسبد.** باگ یک تداخل مشخصیت بود:

```text
table.data thead th    مشخصیت (۰،۱،۳)   top: 46px
.tbl-body thead th     مشخصیت (۰،۱،۲)   top: 0
```

قاعده عمومی برنده می‌شد، پس سرستون داخل قاب پیمایشِ جدول ۴۶ پیکسل — همان
قد نوار بالای صفحه — پایین‌تر می‌افتاد و دو ردیف اول زیرش پنهان می‌شد.
انتخابگر به `.tbl-body table.data thead th` بالا برده شد. با مرورگر تأیید شد:
فاصله سرستون تا لبه قاب، چه قبل و چه بعد از پیمایش، صفر است.

**۳ نمودار تعامل‌پذیر شد.** `mountPayoff` جای رشته ایستا را گرفت:

```text
غلتک          زوم حول همان نقطه‌ای که نشانگر رویش است
کشیدن         پیمایش افقی
حرکت نشانگر   خط راهنما و خواندن سود و زیان سر همان قیمت پایه
دوبار کلیک    برگشت به نمای اول
```

زوم فقط روی محور قیمت پایه است و محور عمودی هر بار از داده همان بازه دوباره
حساب می‌شود، پس بزرگ‌نمایی یک ناحیه باریک خودش قد نمودار را پر می‌کند.
تب موتور هم به همین مؤلفه وصل شد، پس نسخه دوم منطق رسم حذف شد.

محورها هم بازنویسی شدند: گام گرد ۱-۲-۵، عدد کوتاه (k و M)، مقیاس قیمت پایه
زیر محور و قیمت اعمال بالای آن، و حذف گامی که روی یک قیمت اعمال می‌افتد.
برچسب عددی محور `direction: ltr` گرفت، وگرنه در صفحه راست‌به‌چپ «−۴۰M» به
شکل «۴۰M−» دیده می‌شد.

**۴ ستون‌های جدول انتخابی شدند.** نمای آماده نقطه شروع است، نه قفس: دکمه
«ستون‌ها» پانلی گروه‌بندی‌شده باز می‌کند و هر ستونی از قرارداد ستونی مشترک
اضافه یا کم می‌شود. انتخاب هر جدول جدا در حافظه مرورگر می‌ماند، با کلید
`استراتژی:نما`. ترتیب ستون از قرارداد می‌آید نه از ترتیب کلیک، و آخرین ستون
حذف نمی‌شود.

هشت ستون تازه حول سربه‌سری، چون «سربه‌سری ۹۶٬۲۲۳» تا وقتی ندانی پایه کجاست
چیزی نمی‌گوید:

```text
قیمت پایه            نزدیک‌ترین سربه‌سری
فاصله تا سربه‌سری ٪  علامت‌دار — مثبت یعنی پایه باید بالا برود
حاشیه امن ٪          همان بدون علامت، برای مرتب‌سازی
سربه‌سری پایین و بالا
پهنای سربه‌سری ٪     برای استرادل و کندور
```

۱۳ آزمون در گروه ۱۷. آیکون درون‌خطی هم اضافه شد تا ۴۰۴ favicon تمام شود.

### ۱۴۰۵/۰۵/۲۱ — کرانداری موتور چند-سررسیدی

باگ: `core/mixed.mjs` شیب انتهایی را از لبه پنجره رسم می‌خواند
(`anchor * 2.2`). آن‌جا پای زنده هنوز ارزش زمانی دارد، پس شیب ظاهری صفر
نبود — برای تقویمی کال استاندارد ‎−۰٫۱۸‎ درآمد. آستانه هم `EPS = 1e-9`
مطلق بود، که از باقیمانده عددی ارزش زمانی خیلی کوچک‌تر است.

نتیجه: هر ترکیب تقویمی و مورب «زیان نامحدود» می‌گرفت. چون مخرج بازده
بیشترین زیان است، بازده همه این ردیف‌ها صفر می‌شد و ۳ تب تقویمی عملاً از
رتبه‌بندی می‌افتادند — همان تب‌هایی که `mixed.mjs` برایشان نوشته شده.

اصلاح:

```text
شیب مجانبی  در ده انحراف معیار لگاریتمی دورتر سنجیده می‌شود، نه لبه پنجره رسم
آستانه      با اندازه موقعیت مقیاس می‌خورد (1e-6 × بزرگ‌ترین پا)، نه عدد مطلق
اکسترمم     دو انتهای واقعی هم نامزد بیشترین سود و زیان‌اند، نه فقط نمونه‌های پنجره
atZero      حالا واقعاً نزدیک صفر است، نه S=1
```

تأیید عددی: تقویمی خرید ۳۰۰۰/۵۰۰۰ → بیشترین زیان ۲٬۰۰۰٬۰۰۰ که دقیقاً
بدهکار خالص است. نسبت تقویمی (فروش ۲، خرید ۱) هنوز نامحدود، با شیب
مجانبی ‎−۱۰۰۰‎ که دقیقاً اندازه پای لخت است.

۹ آزمون در گروه ۱۶. مجموع ۱۵۷ → ۱۶۶، صفر رد.

---

## بک‌لاگ

به ترتیب اثر. هر قلم مستقل است.

### پ — خواسته‌های کاربر، پیام زنده ۱۴۰۵/۰۵/۲۳

هفت خواسته، مستقیم از کاربر وسط یک اجرا. اولویت بالاتر از الف/ب دارند
چون تازه‌ترین‌اند. پنج‌تا انجام شد (پ-۳ نشان بازده، پ-۷ پنل جزئیات، پ-۲
رقم لاتین، پ-۵ اسلایدر تنظیمات، پ-۴ ماشین زمان)؛ پ-۱ بازبینی شد ولی
جایی برای اصلاح پیدا نشد (توضیح زیرش). مانده: پ-۶.

**پ-۱ — عدد منفی داخل پرانتز، قرمز.** جاهایی که عدد منفی با پرانتز نشان
داده می‌شود (نه با علامت −)، رنگ قرمز (`var(--loss)`) بگیرد.

> بازبینی ۱۴۰۵/۰۵/۲۳: کل `ui/` و `core/` جست‌وجو شد — هیچ جای فعلی کد عدد
> منفی را با پرانتز نشان نمی‌دهد؛ همه جا از نشانه `−` (از `ui/fmt.mjs`)
> استفاده می‌شود، و `ui/table.mjs:354` هم از قبل هر سلول عددی منفی جدول
> اصلی را قرمز می‌کند. اگر منظور کاربر جای دیگری بود که این بازبینی ندید،
> باید با مثال دقیق (کدام تب، کدام ستون) دوباره مطرح شود؛ در غیر این
> صورت این قلم را می‌شود از بک‌لاگ برداشت.

**پ-۶ — الهام از سبک UI جذاب وب و بهینه‌سازی همین چارچوب.** بدون اضافه
کردن کتابخانه (پروژه صفر وابستگی دارد)، فقط CSS/چیدمان. باز است، هر
اجرا یک قلم مشخص بردارد.

```text
دور ۱ (۱۴۰۵/۰۵/۲۳)   ترنزیشن و حالت active روی دکمه/چیپ/ردیف جدول
دور ۲ (۱۴۰۵/۰۵/۲۳)   دکمه اسکن حین اجرا disabled می‌شود؛ ترنزیشن مرده
                     .card حذف شد
دور ۳ (۱۴۰۵/۰۵/۲۳)   نوار پیشرفت واقعی حین اسکن دومرحله‌ای (فقط تب
                     استراتژی؛ توضیح در «انجام‌شده» بالا)
دور ۴ (۱۴۰۵/۰۵/۲۳)   اسکلت بارگذاری به‌جای جدول خالی تا داده اول برسد
                     (تب استراتژی و برترین موقعیت‌ها)
دور ۵ (۱۴۰۵/۰۵/۲۳)   نوار پرش و لنگر رنگی روی عنوان کارت‌های تنظیمات
دور ۶ (۱۴۰۵/۰۵/۲۳)   پیام «هنوز اسکن نزدی» جدا از «ردیفی نمانده» در
                     تب استراتژی
دور ۷ (۱۴۰۵/۰۵/۲۳)   همان پیام برای تب برترین موقعیت‌ها هم — دور ۶ فقط
                     تب استراتژی را گرفته بود
دور ۸ (۱۴۰۵/۰۵/۲۳)   پیام خالی تب دیده‌بان زنجیره، ارجاع غلط به نوار
                     تشخیص نامعلوم را حذف کرد
دور ۹ (۱۴۰۵/۰۵/۲۳)   دکمه خالی‌کردن کش ساکت شکست می‌خورد؛ برچسب
                     «اسکن نشده» در تبی که اسکنی ندارد
دور ۱۰ (۱۴۰۵/۰۵/۲۴)  مرز رنگی سود/زیان روی کارت‌های KPI، برای اسکن سریع
دور ۱۱ (۱۴۰۵/۰۵/۲۴)  نوار پرش تنظیمات به scrollspy تبدیل شد — چیپ کارت
                     جاری خودش روشن می‌ماند
دور ۱۲ (۱۴۰۵/۰۵/۲۴)  پنل انتخاب ستون جدول با کلیک بیرون یا Escape بسته
                     می‌شود
دور ۱۳ (۱۴۰۵/۰۵/۲۴)  پیام ذخیره/خطای تنظیمات و موقعیت‌های دستی
                     aria-live گرفت، برای صفحه‌خوان
دور ۱۴ (۱۴۰۵/۰۵/۲۴)  سرستون جدول‌های کوچک (دفتر بازه‌ها، رول، زنجیره،
                     موقعیت‌های من) روی ردیف اول می‌افتاد یا گم می‌شد
دور ۱۵ (۱۴۰۵/۰۵/۲۴)  جعبه جست‌وجوی انتخابگر نماد، از استایل این‌لاین به
                     کلاس مشترک — حالا focus قابل‌دیدن دارد
دور ۱۶ (۱۴۰۵/۰۵/۲۲)  کارت «بازده روی سرمایه» در تب موقعیت‌های من هم رنگ
                     سود/زیان گرفت، هم‌قرارداد با «سود و زیان جاری»
دور ۱۷ (۱۴۰۵/۰۵/۲۲)  «بهترین/میانه بازده ماهانه» در تب استراتژی و
                     برترین موقعیت‌ها هم رنگ سود/زیان گرفت
دور ۱۸ (۱۴۰۵/۰۵/۲۳)  برچسب بریده‌شده legend نمودار مقایسه‌ای حالا
                     tooltip کامل دارد
دور ۱۹ (۱۴۰۵/۰۵/۲۳)  دکمه ماشین زمان حین دریافت تاریخچه غیرفعال
                     می‌شود؛ .ghost:disabled به CSS اضافه شد
دور ۲۰ (۱۴۰۵/۰۵/۲۳)  دکمه‌های افزودن/حذف موقعیت دستی از دوبار-کلیک
                     محافظت شدند — قبلاً واقعاً دوتایی ثبت می‌کرد
دور ۲۱ (۱۴۰۵/۰۵/۲۳)  دکمه‌های ذخیره تنظیمات/خالی‌کردن کش هم همان
                     نگهبان کلیک دوباره گرفتند، برای یکدستی
دور ۲۲ (۱۴۰۵/۰۵/۲۴)  عنوان تب مرورگر با تب باز عوض می‌شود
دور ۲۳ (۱۴۰۵/۰۵/۲۴)  باز کردن تب در موبایل، خودکار تا محتوای تازه
                     اسکرول می‌کند — قبلاً کاربر پایین فهرست رها می‌شد
دور ۲۴ (۱۴۰۵/۰۵/۲۴)  هاور روی ردیف نزدیک‌به‌پول دیده‌بان زنجیره بی‌اثر
                     بود — style این‌لاین به کلاس تبدیل شد
دور ۲۵ (۱۴۰۵/۰۵/۲۴)  دکمه پوسته حالا وضعیت جاری و مقصد کلیک بعدی را
                     می‌گوید، نه فقط برچسب ثابت «پوسته»
دور ۲۶ (۱۴۰۵/۰۵/۲۴)  ردیف‌های جدول اصلی (table.mjs) با فلش بالا/پایین
                     و Enter هم باز می‌شوند — قبلاً فقط کلیک ماوس
دور ۲۷ (۱۴۰۵/۰۵/۲۴)  پیام flash تنظیمات/موقعیت‌های من با تایمر کهنه
                     زودتر از موعد پاک می‌شد — باگ واقعی، نه فقط ظاهری
دور ۲۸ (۱۴۰۵/۰۵/۲۴)  زوم/پیمایش نمودار بازده با فلش/±/Home هم کار
                     می‌کند — قبلاً فقط سه دکمه نوار ابزار کیبورد داشتند
دور ۲۹ (۱۴۰۵/۰۵/۲۴)  نشان «تغییر کرد» اسکن پیوسته — اسکلت نیمه‌کاره
                     table.mjs (r.__flash خوانده می‌شد، نوشته نمی‌شد) کامل شد
دور ۳۰ (۱۴۰۵/۰۵/۲۴)  ردیف‌های positions.mjs/roll.mjs هم با صفحه‌کلید باز
                     می‌شوند؛ باگ هاور مشابه دور ۲۴ در roll.mjs هم رفع شد
دور ۳۱ (۱۴۰۵/۰۵/۲۴)  منفی خیلی کوچک «−۰» چاپ می‌شد؛ باگ محاسباتی واقعی
                     در fmt.mjs، رفع در stripNegZero
دور ۳۲ (۱۴۰۵/۰۵/۲۴)  حذف موقعیت دستی حالا confirm می‌خواهد — قبلاً یک
                     کلیک داده بازگشت‌ناپذیر را بی‌درنگ نابود می‌کرد
دور ۳۳ (۱۴۰۵/۰۵/۲۴)  سرستون جدول aria-sort گرفت، نوار «مرتب بر …» هم
                     role="status" aria-live، برای صفحه‌خوان
دور ۳۴ (۱۴۰۵/۰۵/۲۴)  role نمودار تعامل‌پذیر از img (ایستا) به group
                     عوض شد — کیبورد دور ۲۸ حالا برای صفحه‌خوان هم بروز است
دور ۳۵ (۱۴۰۵/۰۵/۲۴)  کنتراست --muted پوسته دفتر با محاسبه واقعی WCAG
                     تأیید و رفع شد (۳٫۲۲:۱ → ۵٫۱۱:۱ روی panel-2)
دور ۳۶ (۱۴۰۵/۰۵/۲۴)  تبی که یک‌بار خطا می‌داد برای همیشه قفل می‌ماند —
                     current در catch باز نمی‌گشت، کلیک بعدی بی‌اثر بود
دور ۳۷ (۱۴۰۵/۰۵/۲۴)  کارت‌های KPI موقعیت‌های من با صفر موقعیت هم سبز
                     بودند — kpiTone حالا null را بی‌رنگ می‌گیرد
دور ۳۸ (۱۴۰۵/۰۵/۲۴)  کارت «بازده دوره» تب موتور NaN را قرمز می‌گرفت —
                     signTone جای شرط دستی نشست، همان الگوی دور ۱۷
دور ۳۹ (۱۴۰۵/۰۵/۲۴)  fmt.num مرز گرد شدن به ۱۰۰۰ و مرز گرد شدن به ۱
                     را هر دو گم می‌کرد؛ شاخه‌بندی بعد از گرد کردن رفع شد
دور ۴۰ (۱۴۰۵/۰۵/۲۴)  دکمه‌های پیش‌تنظیم انتخابگر نماد aria-pressed
                     گرفتند — با ویرایش دستی انتخاب پاک می‌شود
دور ۴۱ (۱۴۰۵/۰۵/۲۴)  کنترل اسکن تب استراتژی مستقیم روی state.settings
                     مشترک می‌نوشت — باگ واقعی، نه ظاهری؛ رفع با
                     overrides محلی به‌جای شیء مشترک
دور ۴۲ (۱۴۰۵/۰۵/۲۴)  جست‌وجوی انتخابگر نماد حرف عربی (ي/ك) نمی‌شناخت؛
                     norm() فهرست تب‌ها به fmt.mjs منتقل و اینجا هم وصل شد
دور ۴۳ (۱۴۰۵/۰۵/۲۴)  برچسب «بهترین تفاضل» رول همیشه سبز بود، حتی اگر
                     بهترین گزینه هم زیان‌ده بود — signTone رفعش کرد
دور ۴۴ (۱۴۰۵/۰۵/۲۴)  کلیک سریع دو تب پشت‌سرهم محتوای تب کندتر را روی
                     تب تازه‌تر می‌نشاند — شمارنده نسل رفعش کرد
دور ۴۵ (۱۴۰۵/۰۵/۲۴)  axisNum محور نمودار هم مرز گرد شدن داشت — همان
                     تکنیک دور ۳۹، این‌بار برای هزار/میلیون/میلیارد
دور ۴۶ (۱۴۰۵/۰۵/۲۴)  رنگ سلول زیان در ردیف کهنه با style این‌لاین قایم
                     می‌شد — همان مشکل دورهای ۲۴/۳۰، این‌بار رنگ متن
دور ۴۷ (۱۴۰۵/۰۵/۲۴)  localStorage بدون نگهبان در picker.mjs و بوت
                     app.mjs می‌توانست انتخابگر نماد یا کل بوت را بترکاند
دور ۴۸ (۱۴۰۵/۰۵/۲۴)  flashTimer در dispose تنظیمات/موقعیت‌های من پاک
                     نمی‌شد — همان الگوی strategy.mjs/top.mjs وصل شد
دور ۴۹ (۱۴۰۵/۰۵/۲۴)  خطای بی‌نگهبان ریسه اسکن دکمه اسکن را برای همیشه
                     قفل می‌کرد — بالاترین شدت تا امروز، رفع دولایه
دور ۵۰ (۱۴۰۵/۰۵/۲۴)  سرستون جدول مرتب‌شدنی aria-sort داشت ولی
                     صفحه‌کلید-پذیر نبود — tabindex/role/keydown اضافه شد
دور ۵۱ (۱۴۰۵/۰۵/۲۴)  متن وضعیت اسکن دو تب aria-live نداشت — همان
                     رفتار دور ۱۳ برای #msg، اینجا هم وصل شد
```

با دور ۴۰، سومین بازبینی هدفمند کد (هر پنج قلمش) کامل برداشته شد.
دور ۴۱ و ۴۲ بازبینی مستقل تازه‌ای بودند (نه از فهرست پایین) که هر
کدام یک باگ واقعی دیگر پیدا کردند. دور ۴۳ تا ۴۸ — چهارمین بازبینی
هدفمند کد — کامل برداشته شد. دور ۴۹ تا ۵۱ از فهرست پنجمین بازبینی
هدفمند کد بودند و برداشته شدند؛ سه‌تای دیگر مانده، به ترتیب ارزش:

```text
label بدون for در سه تب       positions.mjs خط ۶۸، roll.mjs خط
                                ۳۸-۴۷ (پنج select)، engine.mjs خط
                                ۱۰۱ (بدون id اصلاً) — label و
                                کنترلش خواهروبرادر ساخته می‌شوند بدون
                                for/id. settings.mjs و strategy.mjs از
                                قبل label for="..." درست دارند. کلیک
                                روی برچسب کنترل را فوکوس نمی‌کند.

دکمه پنل ستون‌ها aria-pressed دارد، باید aria-expanded باشد
                                table.mjs خط ۳۱۲: دکمه «ستون‌ها»
                                پنل را باز/بسته می‌کند — طبق WAI-ARIA
                                دکمه بازکننده محتوا باید aria-expanded
                                داشته باشد نه aria-pressed (که برای
                                سوییچ روشن/خاموش ثابت است)؛
                                detail-btn و رول تاشدگی فهرست کناری
                                app.mjs از قبل aria-expanded درست
                                دارند.

سلول شوک سناریو تب استراتژی از signTone استفاده نمی‌کند (کم‌اولویت)
                                strategy.mjs خط ۲۷۱: g.pnl>=0?'gain':'loss'
                                دستی؛ با شوک سناریو در بیشینه (۶۰٪) و
                                یک موقعیت چندسررسیدی خاص، analyzeMixed
                                می‌تواند NaN بدهد که همچنان قرمز
                                می‌ماند. بازتولید محدود، آخر صف.
```

### الف — خواسته‌های کاربر، فهرست ۱۴۰۵/۰۵/۲۲

سه قلم از این فهرست انجام شد (عدد فارسی، نوار وضعیت، فهرست ویژوال).
مانده‌ها، به ترتیبی که ارزش دارد:

**الف-۱ — صفحه جزئیات هر موقعیت (خواسته ۶). ✓ کامل شد ۱۴۰۵/۰۵/۲۳.**
بزرگ‌ترین قلم و بیشترین ارزش. کلیک روی هر ردیف:

```text
نمودار دقیق و کامل با همه سناریوها                                   ✓ ۱۴۰۵/۰۵/۲۳
حالت مقایسه‌ای با استراتژی‌های مشابه همان نماد، روی یک نمودار         ✓ ۱۴۰۵/۰۵/۲۳
تصویر آینده: ریسک و ریوارد بر اساس مسیرهای محتمل قیمت پایه           ✓ ۱۴۰۵/۰۵/۲۳
«اگر همین حالا بگیرم و آفستش کنم چه می‌شود؟» — سود و زیان بستن فوری     ✓ ۱۴۰۵/۰۵/۲۳
«اگر به قیمت آخرین یا پایانی تسویه کنم چه می‌شود؟»                      ✓ ۱۴۰۵/۰۵/۲۳
```

هر پنج سؤال انجام شد. «نمودار دقیق و کامل با همه سناریوها» به منحنی
«امروز» کنار منحنی «سررسید» تعبیر شد. «حالت مقایسه‌ای» با تعریف ساده
«مشابه یعنی فقط هم‌نماد» و تا ۴ منحنی هم‌زمان پیاده شد (توضیح هر دو در
«انجام‌شده» بالا).

**الف-۵ — تحلیل رول پیشرفته‌تر (خواسته ۲). ✓ کامل شد ۱۴۰۵/۰۵/۲۳.**
سناریوهای مختلف، جدول‌ها و نمودارهای خلاقانه‌تر. سه قلم برداشته شد —
جدول مقایسه همه نامزدهای رول در همان سررسید، نمودار تفاضل چندنامزدی
هم‌زمان، و مقایسه بین چند سررسید مختلف هم‌زمان با افق مشترک «امروز»
(توضیح هر سه در «انجام‌شده» بالا).

### ب — قلم‌های فنی

**ب-۵ — قلم از شبکه بیرونی می‌آید.** `index.html` وزیرمتن را از
`cdn.jsdelivr.net` می‌گیرد. ابزار محلی نباید برای قلمش به اینترنت وصل
شود. (تلاش شد فایل قلم کنار پروژه آورده شود ولی پروکسی محیط اجازه نداد؛
باید از ماشین خود کاربر برداشته شود.)

**ب-۸ — اتحاد وجه تضمین در ضهرم۵۰۳۳ نمی‌خواند.** محاسبه ۷٬۱۰۱٬۰۰۰ در
برابر تابلو ۷٬۰۶۱٬۰۰۰ — اختلاف ۴۰٬۰۰۰. پنج مشاهده دیگر می‌خوانند.

> بازبینی ۱۴۰۵/۰۵/۲۳: `core/margin.mjs` را کامل خواندم — فرمول همان
> شش‌مشاهده‌ای است که در بالای فایل مستند شده، و `size` از
> `d.contractSize` واقعی API می‌آید (core/chain.mjs:120)، نه هاردکد ۱۰۰۰.
> هیچ باگ کد آشکاری در بازبینی پیدا نشد. اختلاف ۴۰٬۰۰۰ دقیقاً برابر
> ۴×C (C=۱۰٬۰۰۰) است — نشانه یک اختلاف در ورودی S یا K یا size نزدیک مرز
> گردکردن، نه در خودِ فرمول. بدون داده خام لحظه مشاهده (S پایه، K، قیمت
> پایانی اختیار، contractSize واقعی برگشتی از API برای همین سری) قابل
> بازتولید نیست — بازار الان بسته و این محیط به بازار وصل نیست (همان
> محدودیت ب-۵). برای ادامه، این چهار عدد خام از تابلوی همان لحظه لازم
> است؛ تا وقتی نرسد این قلم پیش نمی‌رود.
## نسخه ۰٫۴٫۰ — تحلیل آماری، نقدشوندگی و خروج بهینه (۱۴۰۵/۰۵/۲۳)

- فیلتر مستقل هر سرستون به جدول‌های تحلیل تاریخی اضافه شد؛ متن و مقایسه عددی را می‌پذیرد.
- حجم، تعداد معامله و ارزش روزانه پایه و هر پا وارد بازپخش شد؛ ارزش برآوردی از رسمی جداست.
- چهار آستانه زمان اجرا برای حداقل ارزش/حجم پایه و هر قرارداد اضافه شد و روی ورود و آفست اعمال می‌شود.
- کارت‌های خلاصه با تعداد و درصد مثبت، منفی و سربه‌سر، میانگین، میانه و ضریب سود بازطراحی شد.
- آمار توزیع، صدک‌ها، انحراف معیار، رشته‌های سود/زیان، همبستگی پایه و تفکیک روز هفته اضافه شد.
- وجه تضمین ورود، وجه تضمین روزانه، وجه تضمین شرطی و تفکیک هر پای فروش نمایش داده می‌شود.
- اثر تجمعی و تغییر روزانه سود هر پا در هر روز، نمودار و CSV گزارش می‌شود.
- بهترین خروج مشاهده‌شده با قیمت هر پا و بهینه‌ساز هدف سود/حداکثر روز معاملاتی اضافه شد.
- ۹ آزمون تازه برای ارزش تاریخی، نقدشوندگی، آمار، وجه تضمین، اثر هر پا و بهینه‌ساز اضافه شد؛ مجموع ۴۲۵ قبول و صفر رد.
## نسخه ۰٫۵٫۰ — مرتب‌سازی، انتخاب سررسید و جریان نقدی (۱۴۰۵/۰۵/۲۳)

- فیلترهای زیر سرستون حذف و مرتب‌سازی صعودی/نزولی قابل‌دسترسی جایگزین شد.
- انتخاب‌گر یک، چند یا همه سررسیدهای شناسایی‌شده اضافه شد؛ بارگیری و ترکیب‌سازی فقط روی همان انتخاب انجام می‌شود.
- بازده تجمعی سهم پایه به جدول اصلی ترکیب‌ها اضافه شد.
- سرمایه درگیر، پرداختی ورود، دریافتی ورود و خالص جریان نقدی پس از کارمزد در جدول و کارت‌ها اضافه شد.
- سه آزمون تازه برای جریان نقدی، سرمایه و بازده پایه اضافه شد؛ مجموع ۴۲۸ قبول و صفر رد.
