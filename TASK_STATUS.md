# وضعیت تسک‌های پروژه

آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۳۱
نسخه برنامه: `0.24.0`

آخرین نسخه ثبت‌شده در گیت پیش از این تغییر: `9aa52d5`

این فایل مرجع خلاصه وضعیت خواسته‌های کاربر است. جزئیات فنی هر تغییر در
`WORKLOG.md` نگهداری می‌شود. بعد از هر تغییر مهم، وضعیت این فایل نیز باید
به‌روزرسانی شود.

> **بایگانی.** خواسته‌های تمام‌شده به `docs/status/completed.md` رفتند.
> اینجا فقط کارِ در جریان می‌ماند تا خواندنش ارزان بماند.

## بازهٔ تحلیل: قرارداد منقضی در هر محاسبه (۱۴۰۵/۰۶/۱۰ — کامل)

بازخورد کاربر دو چیز را رد کرد: تب جدا، و ساختِ دستی دفتر. هر دو اصلاح
شدند. حالا هر جا بازهٔ تاریخی گرفته می‌شود — آزمون همه استراتژی‌ها،
آزمایشگاه آپشن، تحلیل تاریخی، نگاه باز، رصد یونانی — فهرست قراردادها از
همان بازه می‌آید و قرارداد سررسیدشده هم داخلش است. اگر سرور روزی را
نداشته باشد، در پس‌زمینه می‌گیردش و پیشرفتش را همان‌جا می‌نویسد؛ کار
متوقف نمی‌شود. در بازهٔ دوساله ۵۸ نماد پایه و ۷۹۳۳ قرارداد وارد آزمون
می‌شوند که ۷۱۷۴ تای آن‌ها داخل همان بازه سررسید شده‌اند.

سه نقص را فقط اجرای واقعی و مرورگر گرفتند: بازنویسی پوشش با اسکنِ ناموفق،
قفل‌شدن رابط پشت حلقهٔ انتظار، و سه کمکیِ جاماندهٔ رابط پس از جداکردن
ریاضی. **محدودیت:** اسکن زندهٔ بالادست روی ماشین عامل ممکن نیست.

## دفتر قراردادهای تاریخی (۱۴۰۵/۰۶/۱۰ — کامل)

خواستهٔ کاربر: در هر بازه‌ای که انتخاب می‌کند همهٔ قراردادهای آن بازه در
محاسبه بیایند، حتی آن‌هایی که حالا منقضی شده‌اند؛ وضعیت هر قرارداد به او
گفته شود؛ و چیزی از گذشته جا نماند.

هر پنج بند انجام شد. `core/option-roster.mjs` هویت و عمر هر قرارداد را
نگه می‌دارد و `contractStatus` تاریخ را اجباری می‌گیرد — سررسید یک پرچم
ثابت نیست، مرزی است که روی زمان حرکت می‌کند. `/api/history/universe?date=`
برای هر تاریخِ داخل پوشش دفتر، فهرست را از دفتر می‌دهد نه از تابلوی
امروز، پس همهٔ تب‌های تاریخی خودبه‌خود قرارداد سررسیدشده را می‌بینند. تب
تازهٔ «دفتر قراردادهای تاریخی» وضعیت هر قرارداد در تاریخ یا بازهٔ انتخابی
را با برچسب نشان می‌دهد و می‌شمارد چند تا **داخل همان بازه** سررسید
شده‌اند.

دو ابزار دفتر را می‌سازند: `tools/roster-scan.mjs` از بالادست (با
checkpoint) و `tools/roster-import.mjs` از فایل آمادهٔ کاربر. روی فایل
واقعی کاربر: ۷۹۳۳ از ۷۹۷۴ ردیف پذیرفته، ۳۲ صندوق و اوراق کنار گذاشته، ۹
نامِ بریده که هیچ عددی برایشان حدس زده نشد و شمارشان در رابط دیده می‌شود.
پذیرش مرورگر واقعی انجام شد. **محدودیت:** اسکن زندهٔ بالادست روی ماشین
عامل ممکن نیست و نخستین اجرایش با کاربر است.

## رتبه‌بندی بازه‌ای همه استراتژی‌ها (۱۴۰۵/۰۶/۰۶ — کامل)

هدف کاربر تشخیص صریح بهترین و بدترین استراتژی در بازه انتخابی است. رتبه
روز پایان با میانه بازده خالص حفظ شده و پایداری بازه جداگانه از میانه رتبه
روزانه گزارش می‌شود. نمای دوطرفه بهترین تا بدترین، Heatmap روز × استراتژی
و نمودار تغییر جایگاه به انتخاب مشترک استراتژی و جدول ترکیب‌ها وصل‌اند.
هسته، رابط و دروازه کامل سبزند. پذیرش مرورگر با داده واقعی انجام شد:
۳۷۶ ترکیب، ۳۰ استراتژی دارای نتیجه، ۱۸۰ خانه Heatmap و ۳۰ خط رتبه بدون
خطای کنسول نمایش داده شدند؛ انتخاب مشترک و پیمایش داخلی موبایل نیز تأیید
شد. هدف لمس خط رتبه برای استفاده واقعی از ۱٫۴ به ۱۴ پیکسل افزایش یافت.

## استودیوی سفر زمانی سبد (۱۴۰۵/۰۶/۰۳ — کامل)

کامل و بایگانی‌شده در `docs/status/completed.md`. نقشهٔ فنی و وضعیت
وزن‌دار هر فاز در `PORTFOLIO_TIME_TRAVEL.md` است.

## موارد فنی باقی‌مانده خارج از خواسته‌های اصلی

این موارد مانع استفاده از قابلیت تحلیل تاریخی نیستند، ولی برای پاک‌سازی
فنی آینده در `WORKLOG.md` باز مانده‌اند:

1. اتصال صحیح چند `label` به کنترل متناظر در سه تب برای دسترس‌پذیری.
2. جایگزینی `aria-pressed` با `aria-expanded` در دکمه پنل ستون‌ها.
3. نمایش خنثی مقدار نامعتبر سناریوی شوک به‌جای رنگ زیان در یک حالت مرزی.
4. محلی‌کردن قلم رابط برای حذف وابستگی اختیاری قلم به CDN.
5. تطبیق اختلاف ۴۰٬۰۰۰ ریالی یک نمونه وجه تضمین (`ضهرم5033`)؛ نیازمند
   برداشت تازهٔ زمان‌دار از تابلو است. اختلاف و حسابش کنار همان جدول در
   `tests/run.mjs` مستند شد؛ برداشت بعدی باید `at` و `src` هم داشته باشد.
6. ACL خرابِ `.git/FETCH_HEAD` در کلون دائمی ماشین کاربر — بیرون از این
   مخزن؛ با بازنشانی دسترسی پوشهٔ `.git` و سپس `git fsck` حل می‌شود.
7. ~~سوگیری بقا در بک‌تست~~ — **بسته شد (۱۴۰۵/۰۶/۱۰).** فرضِ اولیه این
   بود که تنها راه، آرشیو روزانه از امروز به بعد است. مسیر
   `GetInstrmentsHistoryInDay` تاریخ می‌گیرد، پس گذشته یک بار برای همیشه
   ساختنی بود. برای هر تاریخِ داخل پوشش دفتر، فهرست از دفتر می‌آید و
   قرارداد سررسیدشده هم داخلش است. بیرون پوشش، هشدار صریح می‌ماند.
8. تسویهٔ نقدی و فیزیکی مسیر مقرراتی مستقل ندارد: نیاز نقدی روز تسویه،
   مالیات، تخصیص و نکول بازنمایی نمی‌شوند. نیازمند متن رسمی فرایند تسویه
   است؛ بدون آن هر مدلی حدس است و در ستون پولی می‌نشیند (ب-۱۰).
9. تست مرورگری سبک در CI: هر ابزار مرورگرگردانی یک وابستگی npm است و
   قاعدهٔ «صفر وابستگی» را می‌شکند. جایگزین ممکن، سنجش مسیر HTTP و
   یکپارچگی ماژول‌ها با `fetch` بدون DOM است (ب-۱۱). تصمیمش با کاربر.

## معیار کنترل فعلی

- آزمون‌های ثبت‌شده: **۴۶۱۴ قبول، صفر رد** (`run.mjs`) و **۶۶ قبول، صفر رد**
  (`guards.mjs`).
  روی LF و CRLF یکسان.
- شاخه انتشار: `main`.
- فایل جزئیات تغییرات: `WORKLOG.md`.
- راهنمای توسعه ساعتی: `AUTOMATION.md`.
