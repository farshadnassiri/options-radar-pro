# وضعیت تسک‌های پروژه

آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۱۹
نسخه برنامه: `0.13.0`

آخرین نسخه ثبت‌شده در گیت پیش از این تغییر: `48c00c8`

این فایل مرجع خلاصه وضعیت خواسته‌های کاربر است. جزئیات فنی هر تغییر در
`WORKLOG.md` نگهداری می‌شود. بعد از هر تغییر مهم، وضعیت این فایل نیز باید
به‌روزرسانی شود.

## خلاصه پیشرفت

| بخش | انجام‌شده | نیمه‌کاره | باقی‌مانده | متوقف/لغوشده به درخواست کاربر |
|---|---:|---:|---:|---:|
| قابلیت‌های مستقیم برنامه | ۴۵ از ۴۵ (۱۰۰٪) | ۰ | ۰ | ۰ |
| اجرای محلی و مخزن GitHub | ۲ از ۲ (۱۰۰٪) | ۰ | ۰ | ۰ |
| توسعه خودکار ساعتی محلی | ۰ | ۰ | ۰ | ۱ (متوقف موقت) |
| اجرای خودکار هنگام خاموش‌بودن لپ‌تاپ | ۰ | ۰ | ۰ | ۱ |
| بهبودهای فنی اضافه، خارج از خواسته‌های اصلی | ۵۱ انجام‌شده | ۰ | ۵ | ۰ |

در نتیجه، از **۴۷ خواسته فعال و مستقیم، هر ۴۷ مورد پیاده‌سازی شده‌اند**.
توسعه ساعتی در ۲۰۲۶/۰۸/۱۴ با درخواست کاربر موقتاً متوقف شد. یک خواسته
جداگانه درباره اجرای ابری هنگام خاموش‌بودن لپ‌تاپ نیز با پیام «فعلاً
بی‌خیال» لغو شد. هیچ‌کدام کار فعال باقی‌مانده محسوب نمی‌شوند.

> «انجام‌شده» یعنی کد، رابط و آزمون آن در پروژه موجود است؛ به معنی تضمین
> سودآوری مالی یا تأیید همه اعداد با داده زنده همه کارگزاری‌ها نیست.

## سه خواسته تازه (۱۴۰۵/۰۵/۲۹)

| خواسته | وضعیت | محل پیاده‌سازی |
|---|---|---|
| فونت فارسی خوانا و زیبا شود | انجام‌شده | `ui/style.css`، `tests/guards.mjs` بخش ۵ |
| فرمول‌های محاسباتی از تنظیمات خوانده و ویرایش شوند | انجام‌شده | `core/settings.mjs` گروه «مبنای محاسبه»، `core/evaluate.mjs`، آزمون ۴۱ |
| نحوه محاسبه هر استراتژی توضیح داده شود | انجام‌شده | `core/formulas.mjs`، `ui/tabs/settings.mjs`، آزمون ۴۲ |

## خواسته‌های مستقیم برنامه

| ردیف | خواسته | وضعیت | محل اصلی پیاده‌سازی یا شاهد |
|---:|---|---|---|
| ۱ | بازپخش استراتژی از تاریخ شروع تا پایان، یک ردیف برای هر روز معاملاتی همراه تاریخ و نام روز | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۲ | انتخاب مستقل مبنای قیمت ورود و آفست از آخرین، پایانی، کمترین و بیشترین | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۳ | ورود دستی قیمت برای هر پای استراتژی، حتی بیرون بازه قیمت همان روز | انجام‌شده | `core/history.mjs`، آزمون‌های بخش ۳۲ |
| ۴ | حفظ وضعیت «فاقد داده» بدون جایگزینی قیمت روز قبل یا قیمت مدل | انجام‌شده | `core/history.mjs` |
| ۵ | توقف بررسی در اولین سررسید، چون هدف نوسان‌گیری است | انجام‌شده | `core/history.mjs`، آزمون توقف سررسید |
| ۶ | بررسی قراردادهای فعال از اولین روز معامله موجود تا آخرین روز | انجام‌شده | `server/server.mjs`، `worker/history-worker.mjs` |
| ۷ | انتخاب نماد پایه، تاریخ شروع و تاریخ پایان با کنترل‌های ریلی | انجام‌شده | `ui/tabs/history.mjs`، `ui/style.css` |
| ۸ | دو حالت انتخاب دستی قراردادها و ساخت همه ترکیب‌های ممکن فقط برای استراتژی انتخابی | انجام‌شده | `worker/history-worker.mjs`، `strategies/catalog.mjs` |
| ۹ | شناسایی سررسیدها و انتخاب یک، چند یا همه سررسیدها | انجام‌شده | `ui/tabs/history.mjs`، نسخه `0.5.0` |
| ۱۰ | نمایش نماد پایه و درصد تغییر/بازده آن در ردیف‌های جدول اصلی | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۱۱ | حذف فیلتر زیر سرستون و مرتب‌سازی صعودی/نزولی با کلیک یا صفحه‌کلید | انجام‌شده | `ui/table.mjs` |
| ۱۲ | دریافت حجم و ارزش پایه و قراردادها و فیلتر نقدشوندگی زمان اجرا | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۱۳ | تعداد و درصد معاملات سودده، زیان‌ده و سربه‌سر و توزیع نتایج | انجام‌شده | `core/history.mjs`، کارت‌ها و نمودار توزیع |
| ۱۴ | آمار کامل هر استراتژی به‌صورت روزانه، کلی و بر اساس روز هفته | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs` |
| ۱۵ | نمایش وجه تضمین کل و تفکیک روزانه هر پای فروش در صورت وجود | انجام‌شده | `core/margin.mjs`، `core/history.mjs` |
| ۱۶ | نمایش اثر تجمعی و تغییر روزانه سود و زیان هر پا در هر مرحله | انجام‌شده | `core/history.mjs`، جدول و CSV تاریخی |
| ۱۷ | تعیین بهترین خروج مشاهده‌شده، قیمت آفست هر پا و قاعده هدف سود/حداکثر روز | انجام‌شده | بهینه‌ساز `core/history.mjs` |
| ۱۸ | نمودارهای زمانی، توزیع، پراکندگی، حساسیت قیمت و ماتریس ۲۵ مبنای ورود/خروج | انجام‌شده | `ui/tabs/history.mjs`، `ui/chart.mjs` |
| ۱۹ | نمایش سرمایه درگیر، مبلغ پرداختی، مبلغ دریافتی و خالص جریان نقدی | انجام‌شده | `core/history.mjs`، نسخه `0.5.0` |
| ۲۰ | ماتریس بازده ورود×خروج با حالت انباشته/تغییر روز، جزئیات کامل با کلیک و تحلیل افق نگهداری | انجام‌شده | `core/history.mjs`، `ui/tabs/history.mjs`، نسخه `0.6.0` |
| ۲۱ | تعاملی‌شدن نمودارهای تاریخی با Tooltip، محور خوانا و خاموش/روشن‌کردن سری‌ها | انجام‌شده | `ui/tabs/history.mjs`، `ui/style.css` |
| ۲۲ | نمودار روزانه افقی قیمت پایه/عمودی سود و زیان با ورودی ثابت روز اول و لایه‌های تصمیم قابل انتخاب | انجام‌شده | `ui/chart.mjs`، `ui/tabs/history.mjs` |
| ۲۳ | ثابت‌ماندن مشخصات کامل استراتژی و قراردادهای انتخاب‌شده هنگام اسکرول | انجام‌شده | کارت چسبان `h-frozen-strategy` در `ui/tabs/history.mjs` و `ui/style.css` |
| ۲۴ | قرارگرفتن دستگیره اسکرول محتوای اصلی و پنل راست در لبه راست قاب خودشان | انجام‌شده | اسکرول مستقل `.stage` و `.rail` در `ui/style.css` |
| ۲۵ | انتخاب مستقل ترکیب، قیمت ورود و تسویه، بازه، تعداد و فیلترهای نقدشوندگی در ماتریس ورود×خروج | انجام‌شده | کنترل‌های `h-rolling-*` و آرگومان مستقل ماتریس در `ui/tabs/history.mjs` |
| ۲۶ | نمایش فاصله ریالی و درصدی نشانگر نمودار از قیمت دارایی پایه روز در Tooltip | انجام‌شده | `mountInteractive` در `ui/chart.mjs` |
| ۲۷ | قابل‌جابه‌جایی‌شدن کارت ثابت، درشت‌شدن متن آن و حذف نمایش کدهای خام دارایی/قرارداد | انجام‌شده | `renderFrozenStrategy` و کنترل Pointer در `ui/tabs/history.mjs`؛ fallbackهای `ui/picker.mjs`، `ui/tabs/positions.mjs` و `ui/tabs/roll.mjs` |
| ۲۸ | افزودن برآیند کل پاها به نمودار اثر تجمعی در کنار اثر مستقل هر پا | انجام‌شده | `paintLegEvolution` در `ui/tabs/history.mjs` |
| ۲۹ | حذف ماتریس تکراری و نگه‌داشتن یک ماتریس ورود×خروج با دو حالت انباشته/تغییر همان روز | انجام‌شده | کارت واحد `matrix-card` در `ui/tabs/history.mjs` |
| ۳۰ | درشت‌شدن فونت ماتریس و کنترل زوم ۷۵ تا ۲۵۰ درصد | انجام‌شده | `applyMatrixZoom` و متغیرهای ماتریس در `ui/style.css` |
| ۳۱ | بازطراحی یکپارچه دکمه‌های اصلی، ثانویه، ریلی و حالت‌های تعاملی | انجام‌شده | `ui/style.css` |
| ۳۲ | خروجی جامع CSV و Excel از ماتریس با همه خانه‌ها، سرمایه، وجه تضمین، نقدشوندگی، کارمزد و جزئیات هر پا | انجام‌شده | `buildMatrixExport` و خروجی‌های چندبرگی در `ui/tabs/history.mjs` |
| ۳۳ | بک‌تست سریع بعد از ماتریس با تاریخ چرخشی قابل اجرا، پنج قیمت روز، ریزمعامله روز آخر، حجم، اثر هر پا، نمودار مبلغ/درصد و پیشنهاد افق ماتریس | انجام‌شده | `core/backtest.mjs`، `ui/tabs/backtest.mjs`، مسیر `/api/trades`، نسخه `0.9.0` |

| ۳۴ | حذف پنل جزئیات فنی شامل درخواست/تأخیر بالادست، اصابت کش، سن عکس، قطعی اتصال و آخرین خطا | انجام‌شده | `ui/index.html`، `ui/app.mjs`، `ui/style.css` و آزمون رگرسیون پوسته |
| ۳۵ | ساخت و سنجش هم‌زمان همه استراتژی‌ها برای یک نماد و دو تاریخ، فیلتر ارزش/حجم، رتبه‌بندی برنده و بازنده، گزارش دسته‌ها، مسیر روزانه قابل کلیک، قیمت دستی هر پا و حساسیت پویای قیمت/مبنای ورود و خروج | انجام‌شده | `core/portfolio.mjs`، `worker/history-worker.mjs`، `ui/tabs/portfolio-backtest.mjs`، نسخه `0.10.0` |
| ۳۶ | اصلاح بک‌تست سریع: endpoint تاریخی تک‌معامله، نمایش قیمت واقعی هر پای استراتژی و انتخابگر تاریخ افقی کارت‌محور | انجام‌شده | `server/server.mjs`، `core/history.mjs`، `ui/tabs/backtest.mjs`، نسخه `0.10.1` |
| ۳۷ | بازپخش ریزمعامله همه پاها در خط زمانی مشترک ۹:۰۰ تا ۱۲:۳۰ همراه قیمت/حجم/سن هر پا، آفست لحظه‌ای، نمودار تفکیکی، جدول ۱۵ دقیقه‌ای، ماتریس هم‌حرکتی و CSV کامل | انجام‌شده | `core/backtest.mjs`، `ui/tabs/backtest.mjs`، نسخه `0.11.0` |
| ۳۸ | حذف کامل تب موتور و نمودار بازده | انجام‌شده | حذف `ui/tabs/engine.mjs` و ردیفش در `ui/app.mjs`؛ ۴۰ تب → ۳۹ تب |
| ۳۹ | حذف حالت کشیدنی مشخصات موقعیت و ثابت‌کردن آن در بالای صفحه، با درج مشخصات کامل (نام استراتژی، تاریخ ورود و خروج و بقیه) هنگام کلیک روی هر موقعیت | انجام‌شده | `renderFrozenStrategy` و `.history-frozen` چسبان در `ui/tabs/history.mjs` و `ui/style.css`؛ آزمون‌های گروه ۳۵ |
| ۴۰ | تبدیل همه انتخابگرهای تاریخ برنامه به کارت عمودی با پیمایش چرخ ماوس | انجام‌شده | `ui/datewheel.mjs`؛ به‌کاررفته در `backtest`، `portfolio-backtest`، `history` و `positions`؛ آزمون‌های گروه ۳۴ |
| ۴۱ | امکان درج قیمت دستی هر پا در روز ایجاد و روز خروج بک‌تست سریع، با هشدار «بیرون از بازه روز» بدون مسدودکردن ادامه | انجام‌شده | `manualExit` و `manualPriceCheck` در `core/history.mjs`، `ui/tabs/backtest.mjs`؛ آزمون‌های گروه ۳۶ |
| ۴۲ | بازطراحی نمای مسیر: اول عملکرد کلی بازه، بعد جدول روزبه‌روز از ورود تا خروج، و ورود به ریزمعاملات هر روز با کلیک روی ردیف | انجام‌شده | `paintOverview`، `paintDayTable` و `openDayIntraday` در `ui/tabs/backtest.mjs`؛ نمای کلی همیشه کل بازه در تفکیک روز؛ آزمون‌های گروه ۴۰ |
| ۴۳ | بررسی عملکرد کلی و به تفکیک پاها از روز اجرا تا روز پایان روی تایم‌فریم انتخابی کاربر، همراه نماد پایه در نمودار و جدول، مدت سوددهی و زیان‌دهی، رفتار مشابه در بازه‌های روز، و بهترین بازه ورود و خروج | انجام‌شده | `bucketIntradayPath`، `intradayHoldingSummary`، `timeOfDayProfile` و `intradayEntryExitProfile` در `core/backtest.mjs`؛ بخش تایم‌فریم در `ui/tabs/backtest.mjs`؛ آزمون‌های گروه ۳۹ و ۴۰ |
| ۴۴ | انتخاب سررسیدهای با سقف موقعیت پر در بالای برنامه و نبود پیشنهاد استراتژی برای آن‌ها | انجام‌شده | `blockedExpiries` در `core/settings.mjs`، فیلتر در `core/scan.mjs`، نوار `ui/expiries.mjs`؛ آزمون‌های گروه ۳۸ |
| ۴۵ | دکمه رصد موقعیت در بک‌تست سریع از داخل آزمون همه استراتژی‌ها و هدایت کاربر به آن تب | انجام‌شده | `state.handoff` و شنونده `hashchange` در `ui/app.mjs`، `watchInBacktest` و `applyHandoff`؛ آزمون‌های گروه ۳۷ |

## اجرا، ذخیره و انتشار کد

| خواسته | وضعیت | توضیح |
|---|---|---|
| اجرای برنامه به‌صورت محلی در صفحه وب | انجام‌شده | اجرا با `node server/server.mjs` و نشانی `http://127.0.0.1:8787` |
| ذخیره محلی، ثبت در Git و Push به مخزن پروژه | انجام‌شده | فایل‌ها ابتدا محلی ثبت و سپس طبق `AGENTS.md` روی شاخه کوتاه‌عمر تسک پوش می‌شوند؛ پوش مستقیم `main` ممنوع است |
| اجرای خودکار توسعه هر یک ساعت و Push بعد از تغییر کامل | متوقف موقت به درخواست کاربر | تنظیمات آن حفظ شده ولی وضعیت تسک `PAUSED` است و تا فعال‌سازی دوباره اجرا نمی‌شود |
| ادامه کار هنگام خاموش‌بودن لپ‌تاپ | لغوشده به درخواست کاربر | راه ابری/GitHub Actions پیاده نشد؛ کاربر گفت «فعلاً بی‌خیال» |

مخزن مقصد:
<https://github.com/farshadnassiri/options-radar-pro>

طبق [مستندات رسمی OpenAI درباره Scheduled tasks](https://learn.chatgpt.com/docs/automations)،
تسکی که به فایل‌های پروژه محلی نیاز دارد فقط با روشن‌بودن رایانه و در حال
اجرا بودن برنامه دسکتاپ می‌تواند به همان پوشه دسترسی داشته باشد.

## موارد فنی باقی‌مانده خارج از خواسته‌های اصلی

این موارد مانع استفاده از قابلیت تحلیل تاریخی نیستند، ولی برای پاک‌سازی
فنی آینده در `WORKLOG.md` باز مانده‌اند:

1. اتصال صحیح چند `label` به کنترل متناظر در سه تب برای دسترس‌پذیری.
2. جایگزینی `aria-pressed` با `aria-expanded` در دکمه پنل ستون‌ها.
3. نمایش خنثی مقدار نامعتبر سناریوی شوک به‌جای رنگ زیان در یک حالت مرزی.
4. محلی‌کردن قلم رابط برای حذف وابستگی اختیاری قلم به CDN.
5. تطبیق اختلاف ۴۰٬۰۰۰ ریالی یک نمونه وجه تضمین (`ضهرم5033`)؛ نیازمند
   داده خام همان لحظه از تابلو است.

## معیار کنترل فعلی

- آزمون‌های ثبت‌شده: **۸۴۶ قبول، صفر رد**.
- شاخه انتشار: `main`.
- فایل جزئیات تغییرات: `WORKLOG.md`.
- راهنمای توسعه ساعتی: `AUTOMATION.md`.
