# راهنمای توسعه خودکار

> **این فایل تاریخی است.** اجرای خودکار ساعتی متوقف شده و مخزن حالا با کار
> ترتیبی چند عامل پیش می‌رود. قرارداد معتبر [`AGENTS.md`](AGENTS.md) است؛
> هرجا این دو با هم نخوانند، `AGENTS.md` مقدم است.

اجرای ساعتی باید هر بار فقط یک بهبود منسجم و قابل بازبینی انجام دهد:

1. ابتدا `git status`، آخرین `WORKLOG.md` و نتیجه آزمون‌ها را بررسی کند.
2. از خواسته‌های ثبت‌شده کاربر یا یک نقص قابل بازتولید، باارزش‌ترین مورد کوچک را انتخاب کند.
3. تغییر را محدود، سازگار با معماری موجود و همراه آزمون پیاده کند.
4. `node tests/run.mjs` و بررسی نحوی همه فایل‌های `.mjs` باید موفق باشند.
5. مستندات مرتبط را به‌روز کند، یک commit روشن بسازد و به شاخه اصلی push کند.

محدودیت‌ها:

- بدون داده معتبر، عدد مالی اختراع نشود.
- قیمت گمشده با روز قبل یا مدل جایگزین نشود.
- force-push، حذف تاریخچه، انتشار عمومی، استقرار و تغییر دسترسی ریپو ممنوع است.
- اگر تغییر امن و مفیدی پیدا نشد، هیچ commit ساختگی ساخته نشود.
- در صورت محدودیت استفاده یا نبود دسترسی لازم، همان اجرا متوقف و علت گزارش شود.
