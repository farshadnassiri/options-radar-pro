# پروتکل کم‌مصرف

> قواعد ثابت کار. **یک بار بخوانید.** کارِ همین حالا در `NEXT.md` است.
> این فایل عمداً کم تغییر می‌کند تا عاملی که قلمی را تمام می‌کند فقط
> `NEXT.md` را بازنویسی کند، نه این را.

---



کار **ترتیبی** است: در هر لحظه یک عامل، یک قلم. این تغییری نکرده. آنچه
تغییر کرده هزینهٔ هر جلسه است — پیش از این هر عامل ۵۷۴ هزار نویسه می‌خواند
تا بفهمد کجاست، و بیشترِ ظرفیتش پیش از نوشتن اولین خط کد تمام می‌شد.

**۱. فقط این دو را بخوانید:** همین فایل، و خروجی `node tools/next.mjs`.
دو تا با هم زیر ۱۰ کیلوبایت‌اند.

**۲. این‌ها را باز نکنید مگر جایی صریح ارجاعتان بدهد:**

`docs/worklog/` و `docs/status/` (بایگانی) · `README.md` (راهنمای کاربر،
نه عامل) · `PORTFOLIO_TIME_TRAVEL.md` (بندهای لازمش در «کار» زیر آمده) ·
`tests/run.mjs` (بارگذار است، ادعا ندارد) · `AGENTS.md` (§۲ را یک‌بار
بخوانید؛ خلاصه‌اش بند ۷ زیر است).

**۳. گشتن ممنوع، پرسیدن آزاد.** به‌جای `grep` و باز کردن فایل:

```bash
node tools/next.mjs map margin      # کدام ماژول این را دارد + صادراتش
node tools/next.mjs suites وجه      # آزمونم را کجا بنویسم
```

**۴. آزمون‌ها شکسته شده‌اند.** `tests/run.mjs` یک بارگذار ۱.۴ کیلوبایتی
است و ۱۲۲ دستهٔ مستقل در `tests/suites/` دارد (میانگین ۶ کیلوبایت). آزمون
تازه را در دستهٔ مربوط بنویسید یا دستهٔ تازه بسازید — بارگذار خودش برش
می‌دارد. **هرگز به `tests/run.mjs` ادعا اضافه نکنید**؛ نگهبان ۹ قرمز
می‌شود.

**۵. همیشه با `--quiet` اجرا کنید.** گزارش کامل ۲۶۶۰ خط است که تقریباً
همه‌اش «✔» است و مستقیم در بافتار شما می‌ریزد.

```bash
node tools/check.mjs             # نحو + هر سه نگهبان، خلاصه. دروازهٔ پیش از پوش
node tests/run.mjs 042 --quiet   # فقط یک دسته، حین کار
```

**۶. هیچ‌وقت بیکار نمانید.** قاعدهٔ صریح صاحب پروژه: تمام‌کردن یک قلم
پایان نوبت نیست — قلم بعدی را بردارید و ادامه دهید. قلم بزرگ یا مبهم را
**کوچک کنید و شروع کنید**، کنارش نگذارید. به مشکل خوردید، برگردید،
دوباره فکر کنید و ادامه دهید. تنها دلیل قابل قبول برای توقف، رسیدن به سقف
واقعی ظرفیت است — نه خستگی، نه «امروز به‌اندازهٔ کافی کار شد». بهینه مصرف
کردن یعنی کارِ بیشتر در همان ظرفیت، نه کارِ کمتر.

**۷. قواعد فنی که بیشترین PR را رد کرده‌اند** (کامل در `AGENTS.md` §۲):
صفر وابستگی npm · فقط `.mjs` و ESM، بدون مرحلهٔ ساخت · هر عدد نمایشی از
`ui/fmt.mjs` با رقم فارسی · ریال داخل، تومان در نمایش · دادهٔ نداشته
«نداشته» بماند، هیچ عددی ساخته نشود · رنگ فقط از توکن CSS · **هر اصلاح
رفتاری یک آزمون رگرسیون دارد**.

**۸. تمام‌کردن یعنی:** `node tools/check.mjs` سبز · کامیت با بدنهٔ واقعی ·
یک ردیف در `WORKLOG.md` · **بازنویسی `NEXT.md` برای عامل بعدی** · پوش به
شاخهٔ خودتان. PR باز نکنید مگر صاحب پروژه بخواهد.

**۹. بعد از هر پوش، درصد پیشرفت را گزارش کنید.** دروازه خودش آن را چاپ
می‌کند؛ همان خط را در گزارشتان بیاورید:

```bash
node tools/progress.mjs          # جدول فازها و جمع کل
node tools/progress.mjs --line   # یک خط، برای گزارش
```

عدد از جدول وزن‌دار `PORTFOLIO_TIME_TRAVEL.md` محاسبه می‌شود، نه از حافظهٔ
کسی. اگر قلمی بستید، فقط «چند از چند» آن فاز را عوض کنید — درصد فاز و جمع
کل خودشان درمی‌آیند، و اگر عددِ نوشته‌شده با محاسبه نخواند دروازه قرمز
می‌شود. درصد پیشرفت هم یک ادعای عددی است و از قاعدهٔ صداقت معاف نیست.

**۱۰. اول گیت‌هاب، بعد کد.** پیش از ادامهٔ هر کارِ نیمه، آخرین وضع
`main` را ببینید — ممکن است همان قلم را کس دیگری تمام کرده باشد.

```bash
git fetch origin && git log --oneline HEAD..origin/main
```

اگر قلمِ روی میز آنجا هست، **ادامه‌اش ندهید**؛ کارِ نیمهٔ خودتان را دور
بریزید و قلم بعدی را بردارید. یک بار واقعاً رخ داد:
`core/portfolio-eligible.mjs` هم‌زمان دو بار نوشته شد، با همان نام فایل و
همان شمارهٔ دسته، و یکی‌شان کامل دور ریخته شد.

**۱۱ و ۱۲ استثنای صریح صاحب پروژه‌اند و فقط تا وقتی معتبرند که Claude
تنها نویسندهٔ فعال باشد.** عامل‌های دیگر (Codex، Antigravity) مثل قبل
رفتار می‌کنند: یک شاخه به‌ازای هر قلم، و PR جدا.

**۱۱. شاخهٔ ماندگار.** همه‌چیز روی `claude/portfolio-dev`: هر قلم یک
کامیت، هر قلم یک پوش، و پیش از شروع هر قلم `git merge origin/main`.

یک PR باز روی همان شاخه می‌ماند — **برای CI، نه برای مرج.** workflow فقط
روی `pull_request` و push به `main` اجرا می‌شود، پس شاخهٔ بدون PR هیچ‌وقت
job ویندوز را نمی‌بیند؛ و تنها چیزی که
`ERR_UNSUPPORTED_ESM_URL_SCHEME` را گرفت همان PR بود، نه دروازهٔ محلی که
لینوکسی است. پس از هر پوش، وضعیت را از خود گیت‌هاب بپرسید — ابزار گیت‌هاب
لازم نیست:

```bash
node tools/ci.mjs            # وضعیت CI روی همین کامیت
node tools/ci.mjs --wait     # منتظر بماند تا تمام شود
```

خروج ۰ سبز · ۱ قرمز · ۲ هنوز تمام نشده · ۳ نشد پرسید. «هنوز شروع نشده»
یعنی نمی‌دانیم، نه سبز. قرمزِ CI کارِ همان نوبت است، نه بعد.

**۱۲. بی‌وقفه.** کدزنی متوقف نمی‌شود: بعد از هر پوش بلافاصله قلم بعدی.
نوبت‌های ساعتی در **جلسه‌های تازه و بی‌بافتار** اجرا می‌شوند، پس `NEXT.md`
را طوری بنویسید که یک عامل سرد بتواند از رویش کار کند.
