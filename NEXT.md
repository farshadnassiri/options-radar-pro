# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعدی بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### ظرفیت و قیمت اجرایی ورود ترکیب — برش چهارم فاز ۳

**چرا.** ترکیب خام اکنون فقط از پاهای دارای حکم پذیرفته ساخته می‌شود، اما
هنوز معلوم نیست چند واحد از کل ساختار را می‌شود هم‌زمان اجرا کرد و قیمت
واقعی ورود آن چیست. VWAP هر پا باید برای حجم مشترک ترکیب دوباره از دفتر
همان snapshot محاسبه شود؛ استفاده از قیمت آخر، پایانی یا VWAP ظرفیت کامل
تک‌پا، عدد ورود دروغ می‌سازد.

**شاخه.** `codex/portfolio-entry-pricing` از آخرین `main`، فقط پس از ادغام
قلم قبل.

**فایل‌ها:**

```text
core/portfolio-entry.mjs            ظرفیت مشترک و طرح اجرای ورود
core/portfolio-candidates.mjs       فقط اگر ارجاع ممیزی عمومی کم دارد
core/exec.mjs                       فقط اگر primitive عمومی دفتر کم دارد
tests/suites/126-portfolio-entry.mjs
```

با `node tools/next.mjs map priceLegs` و
`node tools/next.mjs map walkBook` مسیر مشترک اجرا و با
`node tools/next.mjs suites exec` آزمون‌های نزدیک را ببینید.

**دستهٔ آزمون.** `tests/suites/126-portfolio-entry.mjs` (تازه).

**پذیرش:**

1. فقط خروجی معتبر `portfolioCandidates`، جلسه فعال و snapshot/حکم
   هم‌لحظه پذیرفته شود؛ ترکیب دست‌ساز یا متعلق به لحظه دیگر قیمت نگیرد.
2. ظرفیت کل ساختار برابر کمینه ظرفیت واقعی پاها پس از تقسیم بر `ratio`
   باشد. پای فاقد ظرفیت، ظرفیت صفر فرض نشود؛ کل طرح با علت «فاقد داده» رد
   شود.
3. دفتر هر پای اختیار دوباره برای همان حجم مشترک و سقف مصرف عمق مأموریت
   پیموده شود: خرید از ask و فروش از bid. قیمت آخر، پایانی، میانه یا مدل
   حق جایگزینی دفتر را ندارد.
4. شناسه، سمت، نسبت، اندازه قرارداد و ارجاع حکم از ترکیب خام حفظ شوند؛
   دفتر و مشخصات قرارداد فقط از همان snapshot بازیابی شوند و mismatch
   صریح طرح را رد کند.
5. خروجی هر پا filled، VWAP، top، تعداد سطح و کیفیت را نگه دارد و جمع نقد
   ورود فقط با موتور مشترک payoff/exec و واحد ریال محاسبه شود. نبود یک
   عدد، کل جمع مالی را نامعلوم نگه دارد.
6. سقف بودجه خانواده در این برش فقط گزارش و کنترل شود؛ حجم به‌طور پنهان
   کوچک یا بزرگ نشود. اگر ورود ظرفیت کامل از بودجه بیشتر است، علت و حداکثر
   حجم مجاز بودجه جدا گزارش شود.

**مرز قلم.** امتیازدهی، رتبه‌بندی، انتخاب بهترین‌ها، پیشنهاد UI و ثبت
موقعیت هنوز ممنوع‌اند. این برش فقط طرح اجرای ممیزی‌پذیر می‌سازد.

---

## محدودیت باز از قلم قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز را
روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
