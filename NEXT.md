# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۶/۲۷)

**ممیزی فایلِ m15 اهرم** (۲۰۲۶/۰۶/۲۰ تا ۲۰۲۶/۰۹/۱۸، ۳۷ برگ). چهار ایراد از
خودِ فایل درآمد و هر چهار در `core/data-export.mjs` و `ui/data-export-workbook.mjs`
بسته شد: باطلِ بی‌حجم که میان برگ پوشش و برگ ابزار گم می‌شد · سطلِ اضافهٔ «۱۲:۳۰»
که فقط حراج پایانی داشت · ستونِ وعده‌دادهٔ «بیرون از جلسه» که نبود · ستونِ «مسیر»
که محتوایش پرچمِ endpoint بود. شرح در `WORKLOG.md`، ادعاها در دستهٔ ۲۶۶.

**درسِ قابل‌تعمیم:** عددی که در دو برگِ یک فایل به دو راه حساب می‌شود، آزمونی
می‌خواهد که همان دو راه را کنار هم بگذارد. سه تا از چهار ایراد را ۷٬۲۰۰ آزمونِ
سبز نگرفته بودند چون هیچ‌کدام برگ پوشش را با برگ ابزار تطبیق نمی‌داد.

**قفلِ کند و دائمیِ خروجی دیتا.** قفل فقط قراردادِ **انتخابی** با تاریخ عرضهٔ
نامعلوم را می‌بیند، نه کلِ دفتر؛ شرح در `WORKLOG.md`.

سه رشتهٔ موازی روی **«خروجی دیتا»** بسته شد (شرحشان در `WORKLOG.md`): تب
انتخاب بازه/پایه/قرارداد دارد، هر ابزار یک شیت، و برگ «پوشش دریافت» روز
خالی را از خطا و از درخواست‌نرفته جدا می‌کند.

۱. **کشفِ کاملِ دفتر** — سقفِ پنهانِ ۴۰ نتیجه‌ای TSETMC با شاخه‌های رقمی
پیمایش می‌شود؛ دفتر از ۹٬۸۰۰ به ۲۴٬۹۳۴ قرارداد، بی حدسِ هیچ شناسه‌ای.
۲. **روزِ نوارِ زنده** — `inferLiveSessionDate` روزِ نوارِ نگه‌داشته‌شده در
تعطیلی را با تطبیقِ تعداد، حجم و OHLC روزانه **تأیید** می‌کند، نه حدسِ
تقویمی؛ قراردادِ منقضیِ جلسهٔ آخر هم از نوار پرسیده می‌شود.
۳. **حجم فایل** — ریشه ردیف بود نه ستون: **تایم‌فریم**
(`dataExportCandles`؛ ریزمعامله و ۱/۵/۱۵/۳۰/۶۰ دقیقه) که شیت‌ها را موقع
«خروجی Excel» می‌سازد، پس عوض‌کردنش دریافتِ دوباره نمی‌خواهد؛ سطلِ
بی‌معامله ردیف نمی‌گیرد. و **ستون‌های مشتقِ خاموش**. یک روزِ سه‌ابزاره:
۱٬۱۹۷ کیلوبایت خام · ۳۶ در یک‌دقیقه · ۹ در پانزده‌دقیقه.

دسته‌های ۰۱۳، ۰۸۸، ۲۰۷، ۲۱۰، ۲۱۱، ۲۶۴–۲۶۹. دو دام: زمانِ بالادست `HHMMSS`
است نه ثانیه، و پاسخِ بدشکلِ روزانه کلِ اجرا را نمی‌اندازد. تاریخ شمسی
کنار میلادی در هر برگ می‌آید.

### چیزهایی که جا ماند

ردیفِ دفترچه فقط از تب موقعیت‌ها · دفترچه در پشتیبان نیست · «حرکتِ پس از
هشدار» فقط با جلسهٔ باز · ماژول‌های یتیم (`greeks-watch`، `spread-radar`،
`bereket`، `portfolio-time` با حدود سی `ui/portfolio-*.mjs`) روی دیسک
مانده‌اند؛ حذفشان **تصمیم صاحب پروژه** است.

## کار

### پذیرش قلم‌های تازه در جلسهٔ بازار باز

موتورها در `tests/suites/253-position-track.mjs` و ۲۵۵ تا ۲۶۳ قفل‌اند.
آنچه فقط با بازارِ باز سنجیدنی است: «تغییر امروز» (عدد با تاریخِ مبنا برای
موقعیتِ دیروزدار، «—» با علت برای امروزباز) · روند روزانه روی روزِ
بی‌معاملهٔ یک پا **قطع** شود · درون‌روزی فقط با کلیک · سررسیدگذشته و
بسته‌شده هیچ `books/infos` نگیرند · شرطِ برقرار هم نوار بالا را بزند هم
سلولِ «شرط» را · اصطکاکِ پای یک‌طرفه «؟» بگیرد · ساختارِ بی‌ردیف ته جدول
با علتش بنشیند · بستنِ موقعیت ردیفِ `#journal` بسازد.

### تقویم، رصد زنده و گام سوم بک‌تست

«تا همین لحظه» آخرین روز **امروز**، «تا آخرین روز بسته‌شده» دیروز · شمار
`/api/live-trades` = `ceil(n/24)` و قراردادِ بی‌معامله در `ins=` نیاید ·
پس از ۲۰:۳۰ گرینویچ سقف بازه روزِ تهران باشد · `source=watch-archive`
هیچ ردیف امروزی نسازد و علت بگوید · رصد زنده هیچ `history/universe` یا
`dailies` نبرد و «ارزش خود پایه» صفر نباشد · گام سوم یک POST به‌ازای هر
۱۲۰۰ جفت به‌علاوهٔ یک GET نوار — نه بیشتر؛ جدول پوشش به تعداد روزهای
معاملاتی ردیف داشته باشد و خط روی روزهای بی‌داده **قطع** شود.

## قلم بعدی — نرخ بدون ریسک سراسری ۳۰٪ (تصمیم صاحب پروژه)

```
نرخ ۳۰٪ → کف نظری ۶٬۲۲۸ → قیمت ۴٬۶۳۱ زیر کف  →  belowFloor
نرخ  ۰٪ → کف نظری ۴٬۵۷۲ → قیمت ۴٬۶۳۱ بالای کف →  IV ۱۳٫۸٪
```

علتِ غالبِ ستون‌های خالیِ تلاطم همین است و در پنل جزئیات هر ترکیب دیدنی
شده (`core/iv-rate-scan.mjs`)، ولی تصمیمش گرفته نشده: کفِ بدون‌آربیتراژ
وقتی معنی دارد که پایه را بشود فروش استقراضی کرد، و در این بازار نمی‌شود.
سه راه: نرخِ جدا برای آن کف · بازده نقدیِ هر نماد از منبعی که برنامه
ندارد · مدل سود نقدی گسسته. عددِ فرضی همان است که قاعدهٔ ۲-۴ منع می‌کند.

## قلم‌های باز

- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸. کدام
  endpoint مرجع است؟
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.

**دروازه:** `node tools/check.mjs`
