# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### حفظ lineage سرمایه تا پرونده جلسه دوم — برش پنجم فاز ۷

**چرا.** میان‌بر پرونده اول، قرارداد `capitalContinuity` را به draft تازه
می‌چسباند و گذارهای فرم آن را حمل می‌کنند؛ اما بستن جلسه دوم اکنون
`portfolioCapitalContinuity` را بدون `previous` صدا می‌زند. در نتیجه زنجیره
در کارت و پرونده دوم دوباره از یک ردیف شروع می‌شود و کاربر نمی‌تواند رشد
سرمایه چند سفر را حسابرسی کند.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/portfolio-time-travel-continuation` ادامه می‌دهد. پیش از قلم تازه
`origin/main` را بررسی کنید، اما تا وقتی تعارضی نیست شاخه را عوض نکنید.

**فایل‌های محتمل:**

```text
core/portfolio-capital-continuity.mjs
ui/portfolio-capital-continuity-view.mjs
ui/portfolio-mission-form.mjs
server/portfolio-mission-store.mjs
ui/tabs/portfolio-time.mjs
tests/suites/167-portfolio-capital-lineage-lifecycle.mjs
```

قرارداد پیشین را منبع حقیقت نگه دارید؛ شکل دوم و ضعیف‌تری از lineage نسازید.

**پذیرش:**

1. قرارداد انتقالی معتبر از مرحله `setup` تا `outlook`، `risk`،
   `allocation`، `mission` و `active` بدون تغییر JSON باقی بماند.
2. ذخیره و بازیابی سرور آن را صریح اعتبارسنجی کند؛ نسخه ناشناخته، ردیف
   ناقص، شناسه تکراری، سرمایه نامنطبق یا منشأ دست‌کاری‌شده رد شود.
3. جلسه فعال فقط continuity هم‌سرمایه و دارای شناسه‌های منشأ متفاوت را حمل
   کند؛ ورودی فرم حق ساخت یا اصلاح عدد مالی lineage را ندارد.
4. بستن جلسه دوم، continuity پرونده دوم را با `previous` معتبر بسازد؛ ترتیب
   قدیم به جدید، هویت هر جلسه و تطابق دقیق `finalCapitalRial` قبلی با
   `initialCapitalRial` بعدی حفظ شود.
5. پرونده دوم ذخیره و بازیابی شود و کارت آن هر دو سفر را گزارش کند؛ refresh
   نباید زنجیره را به یک ردیف برگرداند.
6. نبود continuity یعنی جلسه مستقل و معتبر، نه خطا؛ اما continuity موجود و
   خراب باید fail-closed باشد و بی‌صدا حذف نشود.
7. آزمون رفتاری دو جلسه بسته پیاپی و JSON/server round-trip را پوشش دهد؛
   هیچ سرمایه یا بازده تازه‌ای در UI محاسبه نشود.

**مرز قلم.** نمودار رشد چندجلسه‌ای، تغییر سقف مأموریت active، فشرده‌سازی
snapshot و رتبه‌بندی بین سفرها بیرون این برش‌اند.

## محدودیت‌های باز

- ثبت مرحله `active` مأموریت واقعی با snapshot بزرگ هنوز از سقف عمومی یک
  مگابایت رد می‌شود؛ افزایش سراسری مجاز نیست و قرارداد جدا لازم دارد.
- نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه واقعی در چند
  نماد هنوز بازند.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم پنجمین وظیفه
از هفت وظیفه فاز ۷ است؛ پس از پذیرش سهم فاز ۷ باید از ۴٪ به ۵٪ و پیشرفت کل
از ۹۲٪ به ۹۳٪ برسد. دروازه کامل، کامیت دارای بدنه و پوش روی همین شاخه
لازم‌اند؛ سپس بدون انتظار برای مرج قلم بعد را آغاز کنید.
