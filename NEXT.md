# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعدی بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### ساخت ترکیب فقط از حکم‌های پذیرفته — برش سوم فاز ۳

**چرا.** جلسه فعال اکنون حکم هر قرارداد و سمت را از عکس قفل‌شده نشان
می‌دهد. مولد مشترک `core/bereket-candidates.mjs` ترکیب‌های ساختاری را بلد
است، اما خودش درباره کیفیت یا اجراپذیری قرارداد چیزی نمی‌داند. پیش از
قیمت‌گذاری و رتبه‌بندی باید یک مرز خالص بسازیم که فقط خانواده‌های دارای
بودجه و فقط پاهای پذیرفته همان لحظه را به این مولد راه دهد.

**شاخه.** `codex/portfolio-candidates` از آخرین `main`، فقط پس از ادغام
قلم قبل.

**فایل‌ها.** منطق ترکیب را دوباره ننویسید؛ مولد موجود را محدود و ممیزی کنید:

```text
core/portfolio-candidates.mjs       دروازه مأموریت/تخصیص/حکم روی مولد مشترک
core/bereket-candidates.mjs         فقط اگر قرارداد ورودی کمبود عمومی داشت
tests/suites/125-portfolio-candidates.mjs
```

با `node tools/next.mjs map candidate` مولد و با
`node tools/next.mjs suites candidate` آزمون‌های نزدیک را ببینید.

**دستهٔ آزمون.** `tests/suites/125-portfolio-candidates.mjs` (تازه).

**پذیرش:**

1. ورودی فقط جلسه `active` و مدرک اجراپذیری هم‌لحظه آن باشد؛ draft یا
   حکم متعلق به snapshot دیگر هیچ ترکیبی نسازد.
2. فقط تعریف‌های کاتالوگ که `group` آن‌ها در `lockedAllocations` بودجه
   مثبت دارند وارد مولد شوند؛ خانواده تخصیص‌نیافته حذف شود، نه اینکه بودجه
   پیش‌فرض بگیرد.
3. هر پای اختیار ترکیب باید برای همان `ins` و همان سمت خرید/فروش حکم
   پذیرفته داشته باشد. یک پای ردشده کل ترکیب را کنار بگذارد و هیچ getter
   مالی آن ردیف خوانده نشود.
4. شناسه قرارداد، نوع call/put، قیمت اعمال، سررسید و اندازه فقط از snapshot
   منتقل شوند؛ نبود هرکدام باعث حذف نامزد ناقص شود، نه مقدار فرضی.
5. سقف‌های `MAX_PER_DEF` و `MAX_TOTAL` و شناسه پایدار از مولد مشترک حفظ
   شوند؛ پیاده‌سازی موازی `combosFor` ممنوع است.
6. خروجی برای ممیزی، شناسه تعریف، خانواده، پاها و ارجاع حکم هر پا را نگه
   دارد و علت «چرا ترکیبی ساخته نشد» را برای خانواده خالی/پاهای ردشده
   خلاصه کند.

**مرز قلم.** قیمت‌گذاری، توزیع دید کاربر، امتیاز، رتبه‌بندی، پیشنهاد UI و
تشکیل سبد هنوز ممنوع‌اند. این برش فقط فهرست ترکیب خام و قابل اجرا می‌سازد.

---

## محدودیت باز از قلم قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز را
روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
