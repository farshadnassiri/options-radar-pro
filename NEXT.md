# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### نمایش صادقانه شوک سناریو با سود و زیان ناموجود

**چرا.** سلول سود و زیان شبکه شوک در تب استراتژی رنگ را با مقایسه دستی
`g.pnl >= 0` می‌سازد. اگر موتور برای سناریوی چندسررسیدی مقدار `NaN` بدهد،
مقایسه false می‌شود و داده ناموجود مثل زیان قطعی قرمز نمایش داده می‌شود.
قالب و رنگ باید هر دو از قرارداد مشترک `fmt` و `signTone` پیروی کنند.

**شاخه.** با اجازه صریح صاحب پروژه، پس از کامیت قلم جاری همین شاخه
`codex/form-label-accessibility` ادامه داده شود؛ تاریخچه بازنویسی و
force-push نشود.

**فایل‌های محتمل:**

```text
ui/tabs/strategy.mjs
tests/suites/181-strategy-scenario-missing-pnl.mjs
BACKLOG.md
```

**پذیرش:**

1. سود و زیان متناهی مثبت/منفی/صفر همان رنگ مشترک `signTone` را بگیرد.
2. مقدار `NaN`، بی‌نهایت یا ناموجود با «—» و بدون کلاس/رنگ سود یا زیان
   نمایش داده شود؛ صفر مالی ساخته نشود.
3. شبکه سناریو و نمودار از همان مقدار موتور بخوانند و هیچ fallback قیمت یا
   سود و زیان تازه‌ای وارد نشود.
4. آزمون رگرسیون حالت متناهی و ناموجود را قفل کند.

**مرز قلم.** فقط صداقت نمایش سلول شوک؛ تغییر تحلیل چندسررسیدی یا فرمول
سناریو خارج از دامنه است.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md` ثبت و `NEXT.md` برای قلم بعدی بازنویسی شود. دروازه
کامل، کامیت دارای بدنه و پوش روی شاخه کوتاه‌عمر لازم‌اند.
