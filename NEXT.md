# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### امتیازدهی نامزدها بر پایهٔ مأموریت — برش هفتم فاز ۳

**چرا.** حالا برای هر طرح سرمایه‌دار می‌دانیم چقدر سرمایه قفل می‌کند
(`portfolioCapitalRequirement`) و در نقاط قیمتیِ خودِ مأموریت چه سود و
زیانی می‌سازد (`portfolioPlanEvaluation`، و از ۱۴۰۵/۰۶/۰۵ اعدادش دیگر
با تناقض اعمالِ موتور آلوده نیستند). آنچه نداریم یک عدد مقایسه‌پذیر است:
دو طرح با دو سرمایه و دو پروفایل ریسک را نمی‌شود کنار هم گذاشت.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-score.mjs                امتیاز یک طرح ارزیابی‌شده
tests/suites/129-portfolio-score.mjs
```

`node tools/next.mjs map portfolioPlanEvaluation` و
`node tools/next.mjs map validateMissionRisk`.

**پذیرش:**

1. ورودی فقط خروجی معتبر `portfolioPlanEvaluation` برای همان جلسه، همان
   snapshot و همان طرح باشد؛ خروجی دست‌ساز یا ناهم‌لحظه امتیاز نگیرد —
   همان الگوی بازسازی و مقایسهٔ ماژول‌های قبلی.
2. امتیاز از **اجزای نام‌بُرده** ساخته شود و هر جزء با وزن و مقدار خامش
   در خروجی بماند. امتیازی که نشود گفت از کجا آمده، امتیاز نیست.
3. هیچ جزئی از دادهٔ نبوده ساخته نشود: هر جزء که ورودی‌اش `null` است،
   خودش `null` بماند و امتیاز کل هم `null` شود، نه اینکه آن جزء صفر
   حساب شود. صفرِ ساختگی طرحِ بی‌داده را «بد» نشان می‌دهد نه «نامعلوم».
4. طرحی که سقف زیان مأموریت را می‌شکند یا `unlimitedRiskBreach` دارد،
   امتیاز عددی نگیرد و صریح `ineligible` شود با همان علت نام‌بُرده.
5. هم‌ترازی با دید بازار سنجیده شود: طرحی که در نقاط قیمتیِ مأموریت زیان
   می‌دهد نباید امتیاز مثبتِ هم‌ترازی بگیرد. برای دید پرنوسانِ بدون نقطهٔ
   صریح، این جزء `null` بماند نه صفر.
6. هیچ رتبه‌بندی، مرتب‌سازی یا انتخابِ «بهترین» در این قلم نباشد — فقط
   امتیاز یک طرح. مرتب‌سازی قلم بعدی است.

**مرز قلم.** توزیع احتمال، پیشنهاد UI و ثبت موقعیت همچنان ممنوع‌اند.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
