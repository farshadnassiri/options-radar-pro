# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده — یکپارچه‌سازی صفحهٔ رصد زندهٔ بازار

گزارش هشت‌قلمی صاحب پروژه روی همین صفحه. تغییرها:

- **سربه‌سر در زنجیره.** `breakevenGap`/`breakevenGapPct` در
  `core/decision-dashboard.mjs`، با سه ستون تازه در کاتالوگ قرارداد. در هر
  دو سمت «مثبت یعنی هنوز نرسیده‌ایم» — تفاضل خام پوت را وارونه نشان می‌داد.
- **رنگ دو طرفه.** پرچم `sign` روی ستون، در `ui/table.mjs`. فقط ستونی که
  علامتش خبر است؛ نه هر ستون عددی.
- **یک منبع انتخاب.** چهار کشوی نماد/سررسید/قرارداد حذف شدند؛ نقشه تنها
  منبع است و صفحه فقط «سطح دامنه» را می‌پرسد. قاعده در
  `ui/live-dashboard-scope.mjs` (`resolveScope`).
- **تب‌بندی.** `<details>` و ریل عمودی رفتند؛ هفت تب هم‌ردیف. هیچ نمایی
  حذف نشد.
- **حذف نمودارهای نبض بازار** به خواست صاحب پروژه، با ماژول‌ها و دستهٔ
  آزمونش و درخواست دوره‌ای `/api/books`.
- **دریافت فقط وقتی لازم است.** `needsTape` و `shouldFetchRange`. اندازه‌گیری
  مرورگر با بازهٔ ۵ ثانیه: روی نمای ریزمعامله ۵ درخواست در ۲۲ ثانیه، روی هر
  نمای دیگر صفر.
- **کندل افقی** بازطراحی شد و خط مرجع «پایانی دیروز» گرفت.

دستهٔ ۲۴۶ (۲۶ ادعا) این‌ها را قفل می‌کند. دستهٔ ۲۴۷ اندازهٔ قاب را از
`CANDLE_VIEW` می‌خواند، نه از عدد ثابت.

## کار

### پذیرش صفحهٔ رصد زنده در جلسهٔ بازار باز

پذیرش با دادهٔ ساختگی انجام شد، نه با تابلوی زنده. در نخستین جلسهٔ بازار
باز این هفت مورد را روی یک پایهٔ پرمعامله ببین:

۱. **ستون سربه‌سر و فاصله‌اش** را برای یک کال و یک پوت با محاسبهٔ دستی
تطبیق بده. برای پوتی که قیمت جاری زیر سربه‌سرش است، عدد باید **منفی** باشد.

۲. **نوار سطح دامنه**: از نقشه یک قرارداد انتخاب کن و به تب‌های تحلیلی برو؛
«دامنه تحلیل» باید همان نماد و سررسید را بگوید و هیچ‌جا دوباره نپرسد. سطحی
که انتخاب ندارد باید خاموش باشد.

۳. **کندل روزانه**: خط چین «پایانی دیروز» باید سر جای درست باشد. نمادی که
با شکاف باز شده (پایانی دیروز بیرون بازهٔ امروز) را جدا ببین؛ خط نباید از
قاب بیفتد.

۴. **دریافت‌ها**: تب شبکهٔ مرورگر را باز بگذار و ۲ دقیقه روی تب «نبض» بمان؛
نباید هیچ `live-trades` یا `infos` تازه‌ای برود. بعد به نمای «ریزمعامله
قرارداد» برو؛ باید هر تیک یکی برود.

۵. **تب‌ها** را در عرض موبایل ببین؛ نوار باید دو ستونه شود و متن نبرد.

۶. رنگ مثبت و منفی را در ستون‌های تغییر، تغییر موقعیت باز و فاصله تا
سربه‌سر کنترل کن.

۷. دو تب ادغام‌شده («دیده‌بان زنجیره» و «برترین موقعیت‌ها») باید تنبل بار
شوند و پس از ترک صفحه تایمرشان بخوابد.

## قلم بعدی — اختلاف شمارش ریزمعامله

برای طهرم۷۰۶۱ عدد رسمی تابلو ۶٬۷۹۶ معامله بود و نوار ریزمعامله پس از حذف
معاملات باطل ۶٬۷۹۸ ردیف فعال داد. اختلاف از ناسازگاری دو endpoint بالادست
می‌آید. اول باید سنجید کدام مرجع است، بعد تصمیم گرفت کدام عدد نمایش داده
شود — نه اینکه یکی بی‌صدا با دیگری جایگزین شود.

## قلم‌های باز

- **بارِ بالادست**: تصمیم دربارهٔ `byEndpoint` در `/api/health` منتظر
  اندازه‌گیری صاحب پروژه است.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: اگر مقایسهٔ بازه‌ای فراتر از عمر یک نسل قرارداد لازم
  شد، باید مدل صریح شود؛ قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است.
- روز جاری فقط در فاز `open`، `after` یا `ungated` به تاریخ امروز منتسب شود.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل معامله
  یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.
- همه نماهای قدیمی صفحهٔ رصد زنده حفظ شوند — تب‌بندی جای `<details>` را
  گرفت، چیزی حذف نشد.

**دروازه:** `node tools/check.mjs`
