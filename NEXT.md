# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### اتصال سرمایه قطعی به فرم جلسه بعد — برش چهارم فاز ۷

**چرا.** قرارداد خالص تداوم اکنون سرمایه قطعی و منشأ پرونده را حمل می‌کند،
اما کاربر هنوز باید عدد را دستی در مرحله نخست وارد کند و رابطه دو جلسه در
رابط گم می‌شود. میان‌بر باید از خود پرونده بسته آغاز شود و بدون پنهان‌کردن
منشأ، فرم جلسه تازه را آماده کند.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/portfolio-time-travel-continuation` ادامه می‌دهد. پیش از قلم تازه
`origin/main` را بررسی کنید، اما تا وقتی تعارضی نیست شاخه را عوض نکنید.

**فایل‌ها:**

```text
ui/portfolio-capital-continuity-view.mjs
ui/tabs/portfolio-time.mjs
ui/style.css
tests/suites/165-portfolio-capital-continuity-ui.mjs
```

از `portfolioCapitalContinuity` و مسیر موجود نمایش پرونده استفاده کنید.
منطق سرمایه در UI تکرار نشود و این قلم ذخیره سرور یا نمودار رشد را تغییر
نمی‌دهد.

**پذیرش:**

1. پرونده کامل و `ready` یک اقدام روشن «جلسه بعد با این سرمایه» نشان دهد؛
   سرمایه، نماد، جلسه منشأ و لحظه بستن کنار اقدام و با رقم فارسی دیده شوند.
2. اقدام، wizard را به مرحله نخست برگرداند و فقط سرمایه اولیه را از قرارداد
   قطعی پر کند؛ نماد و تاریخ‌های جلسه تازه همچنان انتخاب صریح کاربر باشند.
3. جلسه و portfolio تازه شناسه مستقل بگیرند و قرارداد منشأ به‌عنوان داده
   lineage در draft بماند؛ هیچ سند بسته قبلی mutate نشود.
4. پرونده ناقص، خطای قرارداد، سرمایه نامعلوم یا حالت `exhausted` میان‌بر
   قابل کلیک نسازد و علت فارسی نشان داده شود؛ صفر هرگز مثبت نمایش داده نشود.
5. بازیابی پرونده پس از refresh همان رفتار را داشته باشد و اقدام تکراری
   رویداد یا draft دوم نسازد.
6. همه پول‌ها با `fmt` به تومان و همه رنگ‌ها از توکن باشند؛ چیدمان دسکتاپ
   و موبایل در مرورگر واقعی آزموده شود.

**مرز قلم.** نمودار رشد چندجلسه‌ای، ذخیره lineage روی سرور، انتخاب خودکار
نماد/تاریخ، و شروع خودکار جلسه بیرون این برش‌اند.

## محدودیت‌های باز

داده تاریخی محلی برای پیمودن کامل پنج مرحله موجود نیست. نوار هشدار موبایل،
سرریز `capacity-panel` و پیمایش تاریخچه واقعی نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و روی همین شاخه پوش کنید. سپس بدون انتظار برای
مرج، قلم بعدی فاز ۷ را روی همین شاخه آغاز کنید.
