# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### هشدار قید ریسک در مسیر — برش دهم فاز ۵

**چرا.** قیود ریسک فقط **هنگام ثبت** سنجیده می‌شوند. ولی جلسه حالا در
زمان جلو می‌رود، و قیدی که موقع ثبت رعایت شده بود می‌تواند دو روز بعد
شکسته باشد — بی‌آنکه کاربر کاری کرده باشد. قیمت پایه تکان می‌خورد، وجه
تضمین بالا می‌رود، و کسی خبر نمی‌دهد.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-watch.mjs            سنجش قیود در لحظهٔ جاری
tests/suites/149-portfolio-watch.mjs
```

`node tools/next.mjs map ledgerRoomFor`. دفتر سرمایه از قبل قیود را
می‌سنجد (`core/portfolio-ledger.mjs`) — **همان را مصرف کنید**، ولی
دقت کنید که آن روی **سرمایهٔ ثبت‌شده** حساب می‌کند نه ارزش جاری. تفاوت
همین است و همین قلم را لازم کرده.

**پذیرش:**

1. هر قید با وضعیتِ **لحظهٔ جاری** سنجیده شود، نه لحظهٔ ثبت.
2. تفاوت با لحظهٔ ثبت دیده شود — «چه چیزی عوض شد» نه فقط «الان بد است».
3. قیدی که هنوز شکسته نشده ولی فاصله‌اش کم شده، **جدا** از قید شکسته
   گزارش شود؛ هشدارِ یکسان برای هر دو یعنی کاربر فوریت را نمی‌فهمد.
4. نبودِ ارزش‌گذاری، هشدارِ کاذب نسازد — «نمی‌دانیم» با «خوب است» یکی
   نیست.
5. سقف زیان مأموریت هم روی منحنیِ **فعلی** سنجیده شود، نه سند ثبت.
6. جلسهٔ بی‌موقعیت، هشدارِ خالی ندهد.

راستی‌آزمایی مرورگر لازم **نیست** (این برش رابط ندارد).

## محدودیت باز از قلم‌های قبلی

**ویزارد با دادهٔ واقعی هنوز پیموده نشده.** `data/` فقط `settings.json`
و یک پیش‌نویس مأموریت دارد؛ هیچ بایگانی تاریخی نیست، پس تقویم خالی است
و مرحلهٔ انتخاب تاریخ رد نمی‌شود. مسیر زندهٔ ساخت عکس در مرورگر واقعی
با فهرست ساختگی سنجیده شد (دستهٔ ۱۴۶ و پروب مرورگر) و درست کار کرد، ولی
پیمودن کامل ویزارد روی نصبی با تاریخچهٔ واقعی هنوز مانده. اگر روی چنین
نصبی کار می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و
نتیجه را همین‌جا ثبت کنید.

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سبد ربطی ندارد (همه در ۱۴۰۰×۹۰۰ و
۳۹۰×۸۴۴ سنجیده شدند)، ولی یک بار باید دیده شود که ظرف اسکرول خودش را
دارد یا واقعاً سرریز است.

`portfolioEntryPlan` علت را به شکل `{code,label}` برمی‌گرداند در حالی که
بقیهٔ ماژول‌ها رشته می‌دهند. دستهٔ ۱۲۶ رویش تکیه دارد، پس عوض‌کردنش یک
قلم جدا لازم دارد.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
