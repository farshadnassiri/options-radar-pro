# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### کارت ضعف‌های مستند در پرونده — برش هشتم فاز ۶

**چرا.** استخراج‌گر ضعف و مدل نمایش آن آماده‌اند، اما یافته‌ها هنوز در
پرونده دیده نمی‌شوند. کاربر باید عنوان، شدت، شرح و شاهد هر یافته را کنار
نتیجه مالی ببیند؛ پرونده آرام نیز باید صریح بگوید شاهد سنجیده‌شده ضعفی
نساخته است.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
ui/tabs/portfolio-time.mjs
ui/style.css
tests/suites/160-portfolio-dossier-weakness-ui.mjs
```

از `portfolioDossierWeaknesses` و `portfolioDossierWeaknessView` موجود مصرف
کنید. متن یا شدت تازه در تب اختراع نشود.

**پذیرش:**

1. همان `paintDossier` مشترک برای پرونده زنده و بازیابی‌شده کارت یافته‌ها
   را رسم کند؛ هر ردیف عنوان، شدت، شرح و شاهدهایش را داشته باشد.
2. شدت فوری/بازبینی/داده ناکافی با سه لحن توکنی متمایز دیده شوند و رنگ
   سخت‌کدشده نباشد.
3. پرونده آرام یک پیام کوتاه نشان دهد و جدول/کارت خالیِ شبیه خطا نسازد.
4. شاهد مالی تومان، درصد با واحد خودش و همه رقم‌ها فارسی باشند؛ تب روی
   شاهد خام حساب نکند.
5. کدهای فنی یافته به کاربر نشان داده نشوند، اما به‌عنوان `data-code` برای
   ممیزی DOM باقی بمانند.
6. کارت در موبایل یک‌ستونه و بدون اسکرول افقی باشد؛ کنترل تعاملی ندارد.

راستی‌آزمایی مرورگر لازم است، ولی اتصال مرورگر داخلی از قلم ۱۵۵ با خطای
kernel assets خراب است. اگر درست شد حالت چندشدتی و آرام را در عرض ۳۹۰
پیکسل ببینید؛ وگرنه محدودیت را ثبت کنید.

## محدودیت‌های باز

مقایسه چند پرونده آخرین قلم فاز ۶ است. نوار هشدار موبایل، سرریز
`capacity-panel` و پیمایش تاریخچه واقعی نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
