# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پذیرش انتخاب پیشنهاد و تشکیل سبد پس از refresh — قلم پنجم فاز ۸

**چرا.** سفر یک‌میلیاردی تا جلسه active و بازیابی آن پذیرفته شد. اکنون
باید ثابت شود پیشنهاد قابل اجرا واقعاً از snapshot قفل‌شده انتخاب می‌شود،
بودجه خانواده و عمق دفتر را رعایت می‌کند، موقعیت/رویداد پایدار می‌سازد و
پس از refresh همان سبد برمی‌گردد؛ نه اینکه فقط در حافظه تب دیده شود.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
ui/tabs/portfolio-time.mjs
ui/portfolio-proposals.mjs
core/portfolio-commit.mjs
server/portfolio-mission-store.mjs
tests/suites/132-portfolio-commit.mjs
tests/suites/174-portfolio-build-refresh.mjs
```

**پذیرش:**

1. snapshot کافی و هم‌لحظه فقط پیشنهادهای نماد، خانواده‌های دارای بودجه و
   قراردادهای پذیرفته‌شده همان جلسه را نشان دهد؛ هیچ قیمت یا قرارداد از
   نماد/لحظه دیگر وارد نشود.
2. انتخاب کاربر و حجم صریح او دوباره از عمق واقعی دفتر، VWAP پاها، کارمزد،
   وجه تضمین و سقف بودجه خانواده عبور کند؛ کمبود ظرفیت با علت رد شود.
3. ثبت موفق، Position/Transaction/Execution/Lot و رویداد immutable با
   شناسه‌های پایدار همان Session/Portfolio بسازد و دفتر سرمایه همان لحظه
   سرمایه درگیر و آزاد را به‌روز کند.
4. تخصیص ۳۰/۴۰/۳۰ مستقل بماند؛ مصرف یک خانواده از بودجه خانواده دیگر قرض
   نگیرد و کلیک/درخواست تکراری رویداد یا موقعیت دوم ناخواسته نسازد.
5. ذخیره موفق سرور پیش از اعلام ثبت نهایی لازم باشد؛ شکست، تعارض یا قطع
   شبکه نباید موقعیت فقط‌در-مرورگر را موفق نشان دهد یا فرم را قفل کند.
6. refresh و ادامه همان جلسه active، موقعیت، دفتر سرمایه، طرح ثبت‌شده،
   شمارنده‌ها و همه شناسه‌ها را عیناً بازسازی کند و پیشنهاد مصرف‌شده را
   دوباره قابل ثبت نشان ندهد.
7. داده ناکافی یا دفتر ناقص هیچ ثبت ساختگی نسازد. آزمون یکپارچگی و مرورگر
   واقعی دسکتاپ مسیر پیشنهاد تا refresh، نبود سرریز و صفر خطای کنسول را
   قفل کنند و فایل دقیق آزمون پاک شود.

**مرز قلم.** حرکت ساعت، تغییر حجم، آفست و بستن موقعیت در قلم بعدی پذیرش
می‌شوند.

## محدودیت‌های باز

- universe تاریخی هنوز فقط قراردادهای موجود در آرشیو محلی را می‌شناسد؛
  قرارداد منقضی‌شده بدون عکس روزانه قابل بازسازی نیست.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم پنجمین
وظیفه از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق ابزار پیشرفت بنویسید.
دروازه کامل، مرورگر واقعی، کامیت دارای بدنه و پوش روی همین شاخه لازم‌اند؛
سپس بدون انتظار برای مرج قلم ششم فاز ۸ را آغاز کنید.
