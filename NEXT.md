# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### قرارداد تداوم سرمایه بین دو جلسه — برش سوم فاز ۷

**چرا.** سرمایه نهایی پرونده کامل اکنون قطعی است، اما جلسه بعد همیشه از
ورودی دستی تازه شروع می‌شود و زنجیره سرمایه گم می‌شود. پیش از اتصال فرم،
یک قرارداد خالص لازم است که فقط سرمایه قطعی را با منشأ پرونده حمل کند و
پرونده ناقص را به عدد ساختگی تبدیل نکند.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
core/portfolio-capital-continuity.mjs
tests/suites/164-portfolio-capital-continuity.mjs
```

از `portfolioDossierAnalysis` موجود استفاده کنید؛ سرمایه نهایی را دوباره
حساب نکنید. این برش فرم یا ذخیره سرور را تغییر نمی‌دهد.

**پذیرش:**

1. فقط جلسه بسته و پرونده نسخه‌دار هم‌هویت با تحلیل `complete` پذیرفته شود؛
   تعهد باز، تحقق‌یافته نامعلوم یا حسابداری ناقص سرمایه انتقالی نسازد.
2. خروجی نسخه، شناسه جلسه/سبد منشأ، نماد پایه، لحظه بستن، سرمایه شروع،
   تحقق‌یافته و سرمایه نهایی را جدا نگه دارد.
3. سرمایه نهایی صفر صریحاً حالت `exhausted` باشد و برای ساخت جلسه مثبت جا
   زده نشود؛ مقدار منفی یا نامعتبر رد شود.
4. خروجی پس از JSON round-trip پایدار و از ورودی مستقل باشد؛ هیچ mutation
   روی جلسه یا پرونده رخ ندهد.
5. دو پرونده متوالی بتوانند منشأ زنجیره را حمل کنند، ولی این برش هنوز
   جلسه بعد یا شناسه تازه نسازد.
6. هیچ قیمت، fetch، DOM یا محاسبه مالی موازی وارد نشود.

این برش خالص است و راستی‌آزمایی مرورگر ندارد.

## محدودیت‌های باز

اتصال این قرارداد به فرم جلسه بعد و نمودار رشد سرمایه هنوز در قلم‌های بعد
فاز ۷ هستند. نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه
واقعی نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
