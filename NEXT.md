# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## گزارشِ هجده‌ایرادیِ رصد زنده — بسته شد

صاحب پروژه `47bce1e` را در ساعت بازار سنجید و هجده ایراد نوشت. همه بسته
شد؛ شرحش در بالای `WORKLOG.md`. خلاصه‌اش برای عاملِ سرد:

```text
۱  اعلانِ زنده بی قیمتِ زنده          ✔ حلقه روی liveKeys، نه روی rows
۲  سود/زیان/بازده با قیمت زنده         ✔ comboMetrics در هر تیک
۳  شرطِ قیمت نماد پایه                 ✔ پایه یک خانه از سهمیه می‌گیرد
۴  «کف/سقف امروز» = کف بازه            ✔ core/day-range.mjs
۵  رصد فقط روی منطبق‌های پیش‌نمایش      ✔ استخر = کلِ دامنه
۶  بن‌بستِ «عبور از آستانه»             ✔ previewCross در watch-rule
۷  پیش‌نمایشِ کهنه اجازهٔ رصد می‌داد     ✔ stalePreview + قفلِ دکمه
۸  فقط آخرین قاعده رصد می‌شد            ✔ watchRules جمع است
۹  حذف قاعده رصد را نمی‌خواباند         ✔ حذف، مجموعه را از نو می‌سازد
۱۰ توقف هنگام پاسخ کند                 ✔ نسل (liveGen/watchGen)
۱۱ درخواست‌های روی هم                   ✔ قفلِ busy + AbortController
۱۲ همهٔ ردیف‌ها برچسب «زنده»            ✔ live از liveKeys هر ردیف
۱۳ سقف ۲۴ و پوششِ ناقص                 ✔ سهمیه به ترکیب، نه به پا
۱۴ اولویتِ ادعاشده اجرا نشده            ✔ چهار اولویتِ انتخابیِ کاربر
۱۵ «زنده» = آخرین معاملهٔ هر پا         ✔ هم‌زمانی و سنِ پاها سنجیده می‌شود
۱۶ رصد زنده روی بازهٔ تاریخی            ✔ دروازهٔ «روز سنجش = امروز»
۱۷ علتِ نبودِ مورب اشتباه بود           ✔ expiryShortfall
۱۸ موجِ اعلان و دفترچهٔ مشترک            ✔ دفترِ جدا + deliverBurst
```

نقشهٔ کدِ تازه: `core/live-quote.mjs` (سهمیه و تعریفِ «زنده») ·
`core/day-range.mjs` (کف و سقفِ امروز) · `expiryShortfall` در
`core/radar-history.mjs` · `deliverBurst` در `ui/gap-alarm.mjs`.
آزمونشان دستهٔ ۲۲۹ و ۲۳۰ است؛ ۲۲۴ و ۲۲۸ هم بازنویسی شدند.

**بازطراحی ظاهر لغو شد** — صاحب پروژه پس‌گرفتش. سراغش نروید.

## کار — قلم بعدی را از اینجا بردارید

### الف. دانه‌بندی درون‌روزی روی بیش از یک روز

امروز فقط یک روز است و علتش در `ui/tabs/spread-radar.mjs` نوشته شده:
ریزمعاملهٔ هر پا برای هر روز یک درخواست است. راهِ درست، کش‌کردنِ
ریزمعامله روی دیسک است — مثل کاری که `data/roster` برای دفتر قراردادها
می‌کند — نه بالا بردن سقف درخواست.

**و حالا یک انگیزهٔ دوم دارد:** «کف و سقفِ امروز» فعلاً فقط از
مشاهده‌های خودِ رصد ساخته می‌شود (`core/day-range.mjs`)، چون کف و سقفِ
واقعیِ امروزِ یک **ترکیب** ریزمعاملهٔ همهٔ پاها را می‌خواهد. با کشِ
دیسکی، همان مرجع می‌تواند کفِ واقعیِ جلسه شود و نامش هم از «مشاهده‌شده»
دربیاید.

### ب. مظنهٔ قابل اجرا، نه آخرین معامله

`comboLiveQuote` حالا صادقانه می‌گوید پاها هم‌زمان‌اند یا نه، ولی منبعش
هنوز آخرین **معامله** است. `/Trade/GetTrade` دفترِ سفارش نمی‌دهد، پس
«بهترین خرید/فروش» از این مسیر درنمی‌آید. اگر مسیرِ دیگری در تابلو هست،
پیدا کردنش قلمِ بعدی است؛ اگر نیست، همین صداقتِ فعلی درست است و چیزی
اضافه نشود.

### ج. سهمیهٔ زنده، فراتر از ۲۴

سقف ۲۴ سقفِ `/api/live-trades` است و سرور آن را برای نکوبیدن بالادست
گذاشته. حالا سهمیه هدر نمی‌رود و اولویتش دستِ کاربر است، ولی با ۱۳۲۰
ترکیب باز هم بیشترشان بیرون می‌مانند. راهِ درست، چند تیکِ چرخشی است
(هر تیک یک دستهٔ تازه، با نگه‌داشتنِ آخرین مظنهٔ هر ترکیب و سنِ آن) —
نه بالا بردن سقفِ سرور.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی). این‌ها با دادهٔ
مسیریافته در مرورگر واقعی اجرا شدند، نه با تابلوی واقعی:

۱. **عددها را با تابلو بخوان.** اسپرد عمودی در مقیاس «قیمت خام»؛ بعد
مقیاس را عوض کن و ببین درصدِ پرشدگی **تکان نمی‌خورد**.
۲. **استرانگل فروش.** ستون «لنگر» باید «دهانهٔ اعمال» بدهد، «حداکثر
زیان» باید «نامحدود» بنویسد نه عدد، و «حداکثر سود ٪» باید بر وجه تضمین
باشد.
۳. **دنبالهٔ امروز.** در ساعت بازار، تیکِ «رصد زندهٔ بازار» را بزن و
ببین خطِ نقطه‌چینِ امروز روی روند گذشته می‌نشیند و برچسب «اکنون» دارد.
۴. **هشدارِ زنده، این بار با تابلو.** هجده ایرادِ بالا با پاسخِ ساختگی
بسته شدند و آزمون دارند؛ خودِ ساعتِ بازار هنوز ندیده‌شان. سه چیز را
مشخصاً نگاه کن: نوارِ وضعیت باید بگوید چند ترکیب «مظنهٔ هم‌زمان» دارد و
چندتا به‌خاطر ناهم‌زمانی کنار رفته · با «۰ ترکیب» هیچ اعلانی نباید ثبت
شود · و «حداکثر سود ٪» باید با هر تیک تکان بخورد.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. پای بی‌قیمت فاصله را باطل می‌کند، پاهای
ناهم‌زمان ترکیبِ زنده نمی‌سازند، «کف امروز» بی مشاهدهٔ امروز عدد ندارد،
قیمتِ پایهٔ نداشته `NaN` می‌ماند، توزیعِ بی‌پراکندگی حکم نمی‌گیرد،
بازدهی که مخرجِ مدافع‌پذیر ندارد ادعا نمی‌شود، زیانِ نامحدود عددِ بزرگ
نمی‌شود، و ترکیبِ بی‌قیمتِ زنده با عددِ روزانه سنجیده نمی‌شود.

اگر روزی یکی از این‌ها شکست، یکی از دسته‌های ۲۱۸ تا ۲۳۰ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون

پورت جداگانه (۸۷۹۴) و پس از کار بسته شود؛ ۸۷۸۷ دستِ صاحب پروژه است.
کرومیوم در `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` و
Playwright سراسری: از پوشهٔ موقت با
`ln -s /opt/node22/lib/node_modules node_modules`.
