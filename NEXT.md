# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## دورهٔ جاری

دو ممیزیِ «کامل‌بودنِ داده» بسته شد. دورهٔ ظاهری (تیرهٔ اسپاتیفای‌وار)
روتینِ ساعتی **ندارد** و نوبت‌به‌نوبت جلو می‌رود.

**هارنسِ شاهد.** `cdn.tsetmc.com` از این ماشین در دسترس نیست؛ راهِ
جواب‌داده: کپیِ مخزن + `data/settings.json` به استابِ محلی + پاک‌کردنِ
`data/option-roster.json`، **پورتِ تازه برای هر اجرا**. سرور با بازارِ
بسته هم بالا می‌آید. playwright همین‌جاست
(`/opt/node22/lib/node_modules/playwright/index.mjs`، کروم
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`)؛ تب با
`page.click('#rail-list >> text=<نام تب>')`.

**سه تلهٔ همین هارنس:** `pkill -f` نزنید، الگو پوستهٔ خودتان را هم
می‌کشد · **تبِ باز‌شده اثباتِ اجرا نیست** — مسیر را با
`page.evaluate(() => import(…))` واقعاً صدا بزنید · **ادعای ظاهری
بی‌اندازه‌گیری نکنید**، `getComputedStyle` را بپرسید.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۷/۰۱) — دروازه‌های ورودِ داده

**هر چهار مسیرِ ورودِ داده دروازه دارند**، هر کدام با نگهبانِ خودش
(دسته‌های ۲۹۱–۲۹۴): `tape-intake` (ریزمعامله) · `daily-intake` (تابلوی
روزانه) · `hist-intake` (`/api/hist`) · `quote-intake` (مظنهٔ زنده).
همه در `ui/`؛ شرحشان در `WORKLOG.md`.

**نگهبان را پیش از مهاجرت بنویسید، و به الگو ببندید نه به مسیر.**
نگهبانِ مسیر دو مصرف‌کننده‌ای را گرفت که فهرستِ دستی‌ام جا انداخته بود.
ولی نگهبانِ **الگو** — `fetch(url).then((r) => r.json())` بی بررسیِ
پاسخ — هفت فایلِ دیگر را هم گرفت که هیچ‌کدام در فهرست نبودند.

**سه قاعده که از این چهار دور ماند:** خالیِ تأییدشده و خالیِ بی‌تأیید
یک چیز نیستند (`core/upstream-shape.mjs`) · پاسخی که `ok` نیست داده
نیست — یک ۵۰۰ که به‌جای دفتر بنشیند «بازارِ بی‌عمق» می‌سازد · فازِ بازار
پیش از هر حکمی، وگرنه هشدار هر شب دروغ می‌گوید و خاموش می‌شود.

**و آنچه عمداً نشد:** برای `book`/`state`/`threshold` بررسیِ مهرِ تاریخ
ننوشتم — نامِ میدانِ تاریخشان را با پاسخ واقعی ندیده‌ام و حدسش همان
پسرفتِ R4-01 است. **جدول، نه حدس.**

**سهمیهٔ بالادست** (بسته): با **`HTTP 200` و `{"tradeHistory":[]}`**
می‌بندد — همان شکلِ روزِ بی‌معامله. معیارها در `core/throttle-watch.mjs`.

## قلم بعدی

۱. **بدهیِ ثبت‌شدهٔ دستهٔ ۲۹۴.** هفت فایل هنوز الگوی «json بگیر، پاسخ
   را نبین» را روی `/api/health` · `/api/positions` · `/api/daily`
   دارند. فهرستشان در خودِ دستهٔ ۲۹۴ است و **فقط کوتاه می‌شود** — ادعای
   کنارش، فهرستِ کهنه را هم قرمز می‌کند.

۲. **نمایشِ وضعیت در تب‌های باقی‌مانده:** رول · موقعیت‌های من · تاریخچه.
   حکم‌ها حالا می‌رسند؛ جای نشان‌دادنشان همه‌جا ساخته نشده.

۳. **صفِ کم‌داشته بینِ نشست‌ها بماند.** با سهمیه‌ای که پس از ~۵۰۰
   درخواست می‌بندد، بازهٔ سه‌ماهه بی این هرگز تمام نمی‌شود. **تصمیمِ
   باز:** کشِ دیسکیِ سرور یا IndexedDB مرورگر — پیشنهاد: دیسکیِ سرور،
   فقط برای روزِ بسته‌شده، چون آن روز دیگر عوض نمی‌شود.

۴. **بازارِ باز.** همهٔ ممیزی‌ها با بازارِ بسته اجرا شدند.

۵. **سه تبِ بی‌ریل** (`bereket` · `portfolio-time` · `spread-radar`) روی
   دیسک‌اند و اصلاحاتشان دیده نمی‌شود: یا ریل شوند یا برداشته.

## صفِ ظاهری (دورهٔ قبل، دست‌نخورده)

قلمِ بعدی **جدول‌ها و نوار بالا**ست: سرستونِ جدول روی کارت یک پله
روشن‌تر نه تیره‌تر · راه‌راهِ ردیفِ زوج یا دیده شود یا نباشد · نوار وضعیتِ
هدر «—»ها را بی‌گروه‌بندی می‌چیند. بعد: حالتِ خالیِ درون‌کارتی، فرم‌های
بلند، کنتراستِ دکمهٔ اصلی در پوستهٔ روشن (۳٫۸:۱، زیر کف). **ایموجیِ
عنوانِ دو تب عمداً می‌ماند** — دستهٔ ۸۶ قفلش می‌کند.

## قلم‌های باز (غیرظاهری، دست‌نخورده)

- **عددی که در دو جا به دو راه حساب می‌شود، آزمونی می‌خواهد که همان دو
  راه را کنار هم بگذارد.** (دسته‌های ۲۶۵ و ۲۶۶؛ و باز در R5-05.)
- **نرخ بدون ریسک سراسری ۳۰٪** — تصمیم صاحب پروژه (شرح در `WORKLOG.md`).
- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸. اول
  `dedupeTrades` را ببینید؛ اگر تکراری نبود، `conflicts` جایش است.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.
- **پذیرش قلم‌های بازارِ باز**: موتورها در دسته‌های ۲۵۳ و ۲۵۵–۲۶۳
  قفل‌اند؛ فهرستش همان‌جاست.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.
- **توکنی که مقدارش `transparent` است، لبهٔ زیرِ خودش را پاک می‌کند.** اگر
  تصمیمی برگشت، قاعده‌اش را هم بردارید — نه فقط مقدارش را.
- **هر پرچمِ تازه‌ای که روی رکوردِ نوار می‌نشیند، مسیرِ `keepBetterTape`
  را هم می‌خواهد.** `attempts` و `throttled` دو بار آنجا گم شدند.
- **ابزارِ تشخیص را با همان هدرهای برنامه بزنید** — بی هدر، ۴۰۳ِ فیلترِ
  ساده می‌گیرید و «IP مسدود شده» تشخیص می‌دهید.
- **آزمونِ کم‌زور بدتر از نبودنش است**: سبزِ بی‌پشتوانه می‌دهد.

**دروازه:** `node tools/check.mjs`
