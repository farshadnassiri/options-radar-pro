# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. **تنها ورودی اجباری
> شماست.** وقتی کارتان تمام شد، همین فایل را برای عامل بعدی بازنویسی کنید.

---

## ۰. پروتکل کم‌مصرف — بخوانید، بعد شروع کنید

کار **ترتیبی** است: در هر لحظه یک عامل. این تغییری نکرده. آنچه تغییر کرده
هزینهٔ هر جلسه است — پیش از این هر عامل ۵۷۴ هزار نویسه می‌خواند تا بفهمد
کجاست، و بیشترِ ظرفیتش پیش از نوشتن اولین خط کد تمام می‌شد.

**۱. فقط این دو را بخوانید:** همین فایل، و خروجی `node tools/next.mjs`.
دو تا با هم زیر ۱۰ کیلوبایت‌اند.

**۲. این‌ها را باز نکنید مگر جایی صریح ارجاعتان بدهد:**

```text
docs/worklog/        بایگانی — ۴۰۰ کیلوبایت تاریخچه، برای کار امروز بی‌مصرف
docs/status/         بایگانی خواسته‌های بسته‌شده
README.md            ۴۵ کیلوبایت راهنمای کاربر، نه راهنمای عامل
PORTFOLIO_TIME_TRAVEL.md   نقشهٔ کامل ۸ فاز — بندهای مربوطه در «کار» زیر آمده
AGENTS.md            قواعد فنی. §۲ را یک‌بار بخوانید و بس؛ خلاصه‌اش زیر است
tests/run.mjs        بارگذار است، ادعا ندارد
```

**۳. گشتن ممنوع، پرسیدن آزاد.** به‌جای `grep` و باز کردن فایل برای پیدا
کردن چیزی:

```bash
node tools/next.mjs map margin      # کدام ماژول این را دارد + صادراتش
node tools/next.mjs suites وجه      # آزمونم را کجا بنویسم
```

**۴. آزمون‌ها شکسته شده‌اند.** `tests/run.mjs` دیگر ۷۵۱ کیلوبایت نیست؛ یک
بارگذار ۱.۴ کیلوبایتی است و ۱۲۱ دستهٔ مستقل در `tests/suites/` دارد
(میانگین ۶ کیلوبایت). آزمون تازه را در دستهٔ مربوط بنویسید یا دستهٔ تازه
بسازید — بارگذار خودش برش می‌دارد. **هرگز به `tests/run.mjs` ادعا اضافه
نکنید**؛ نگهبان ۹ قرمز می‌شود.

**۵. همیشه با `--quiet` اجرا کنید.** گزارش کامل ۲۶۶۰ خط است که تقریباً
همه‌اش «✔» است و مستقیم در بافتار شما می‌ریزد.

```bash
node tools/check.mjs          # نحو + هر سه نگهبان، خلاصه. دروازهٔ پیش از پوش
node tests/run.mjs 042 --quiet  # فقط یک دسته، حین کار
```

**۶. قواعد فنی که بیشترین PR را رد کرده‌اند** (کامل در `AGENTS.md` §۲):
صفر وابستگی npm · فقط `.mjs` و ESM، بدون مرحلهٔ ساخت · هر عدد نمایشی از
`ui/fmt.mjs` با رقم فارسی · ریال داخل، تومان در نمایش · دادهٔ نداشته
«نداشته» بماند، هیچ عددی ساخته نشود · رنگ فقط از توکن CSS · **هر اصلاح
رفتاری یک آزمون رگرسیون دارد**.

**۷. تمام‌کردن یعنی:** `node tools/check.mjs` سبز · کامیت با بدنهٔ واقعی ·
یک ردیف در `WORKLOG.md` · **بازنویسی همین فایل برای عامل بعدی** · پوش به
شاخهٔ خودتان. PR باز نکنید مگر صاحب پروژه بخواهد.

---

## کار

### اتصال UI ادامهٔ مأموریت سفر زمانی — قلم هفتم و آخر فاز ۲

**چرا.** قرارداد مأموریت، پنج مرحلهٔ فرم، و ذخیره‌سازِ نسخه‌دار سرور هر سه
ساخته و ادغام شده‌اند. آنچه نیست، اتصالشان است: کاربر هنوز نمی‌تواند یک
پیش‌نویس نیمه‌کاره را ببندد و بعد ادامه دهد. با بسته‌شدن این قلم، فاز ۲
کامل و پیشرفت کل ۳۰٪ می‌شود.

**شاخه.** `claude/portfolio-mission-resume-ui` از آخرین `main`.

**فایل‌ها.** اتصال است، نه موتور تازه — نباید از این‌ها فراتر برود:

```text
ui/portfolio-mission-form.mjs     کنترل ادامه، وضعیت ذخیره، بازسازی مرحله
ui/tabs/portfolio-time.mjs        فهرست جلسه‌ها و باز کردن جلسهٔ فعال
core/portfolio-mission.mjs        فقط اگر بازسازی به تابع تازه نیاز داشت
```

**دستهٔ آزمون.** `tests/suites/122-portfolio-mission-resume.mjs` (تازه).
دسته‌های همسایه برای دیدن سبک: `120-portfolio-mission-form.mjs` و
`121-portfolio-mission-store.mjs`.

**پذیرش** — هر پنج بند، وگرنه قلم بسته نیست:

1. پیش‌نویس معتبر هر مرحله با PUT در ذخیره‌ساز سرور ثبت شود و از GET فهرست
   جلسه‌ها قابل انتخاب باشد. `localStorage` منبع حقیقت نشود.
2. ادامهٔ پیش‌نویس دقیقاً آخرین مرحلهٔ معتبر و همهٔ ورودی‌های صریح را
   بازسازی کند. دادهٔ ناقص یا نسخهٔ ناشناخته **صریح رد شود**، نه اینکه با
   پیش‌فرض پر شود.
3. جلسهٔ فعال با مأموریت و snapshot قفل‌شده فقط‌خواندنی باز شود؛ ادامه‌دادن
   نتواند تصمیم‌های پیش از شروع را تغییر دهد.
4. وضعیت ذخیره، زمان آخرین ثبت و خطای شبکه کنار خودِ کنترل ادامه دیده شود.
   **ذخیرهٔ ناموفق هرگز موفق نمایش داده نشود** — این همان قاعدهٔ صداقت
   عددی است، در لباس شبکه.
5. پیشنهاد استراتژی و تشکیل سبد وارد این برش نشوند. آن‌ها فاز ۳ و ۴ هستند
   و باید مأموریت قفل‌شده را مصرف کنند.

**راستی‌آزمایی مرورگر واقعی اجباری است**: هر پنج مرحله، یک refresh وسط
کار و ادامه، و هر دو حالت دسکتاپ و موبایل.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
از فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید — **این تنها جایی است که
باز کردن آن فایل‌ها لازم است**، و کار شماست نه کارِ نفر بعد. برای نفر بعد
همان چهار چیز را بنویسید که خودتان لازم داشتید: چرا، کدام فایل‌ها، کدام
دستهٔ آزمون، و پذیرش قابل‌سنجش.
