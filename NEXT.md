# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## یازده بندِ «رصد زندهٔ هر استراتژی» — دو دور بسته شد

خواسته، سرِ همهٔ یازده بند: **هر تبِ استراتژیِ سمت راست باید خودش یک
رادارِ زنده باشد، با پارامترهای مختصِ خودش.** دورِ اول بندهای ۱ و ۲ و ۷
را بست، دومی ۸ و ۹ را، سومی ۱۰ را، چهارمی ۴ و ۱۱ را — تا دو مقصد. شرح
در `WORKLOG.md`.

کدِ تازه: `core/strategy-radar.mjs` · `core/history-chain.mjs` ·
`core/session-trail.mjs` · `ui/strategy-history.mjs` ·
`strategyLinkTargets`/`watchConditionsFrom` در `ui/handoff.mjs` ·
`table.sortKey()`. آزمون: ۵۱، ۲۳۲، ۲۳۳ و ۲۳۴.

**دو قاعده‌ای که نباید شکسته شوند:**

۱ تفاوتِ سی‌وشش استراتژی **داده** است نه کد. برای استراتژی تازه نمایه‌اش
  را در `GROUP_PROFILES` یا `ID_PROFILES` بنویسید — نه تبِ دست‌نویس.

۲ زیرتب فقط **منبعِ** جدول را عوض می‌کند. انتخابگر نماد بالای زیرتب‌هاست
  و شاخص‌ها و فیلترها و جدول و جزئیات پایینشان — مشترک. جدولِ دومی
  ساخته نشود، وگرنه دو عدد از دو مسیر با هم مقایسه می‌شوند.

## کار — قلم بعدی را از اینجا بردارید

### الف. مقصدهای بیشتر برای نوار پیوند (بندهای ۴ و ۱۱)

قرارداد هست و دو مقصد دارد (🔬 آزمایشگاه، 🔔 دیده‌بان شرطی). مقصدِ تازه
= یک ردیف در `STRATEGY_LINK_TARGETS` **به‌علاوهٔ** پذیرشِ `state.handoff`
در `mount` همان تب — الگویش `applyStrategyLink` در
`ui/tabs/watchtower.mjs`. نامزدها: 📐 رصد یونانی · 📏 رادار فاصله ·
دیده‌بان زنجیره · تحلیل تاریخی استراتژی · موقعیت‌های من.

⚠ مقصد را وقتی اضافه کن که پذیرشش نوشته شده باشد، و `strategyLinkTargets`
باید شرطِ واقعیِ آن تب را بداند — مثل `GAP_STRATEGY_IDS` برای دیده‌بان.
دکمه‌ای که باز می‌شود ولی دامنه‌اش خالی می‌ماند، بدترین حالت است.

### ب. هزینهٔ واقعیِ هر تیک (بندِ ۵)

ستونِ «سنِ مظنه» و «وضعیت نماد» آمدند (دستهٔ ۲۳۲). آنچه نیامد: سوارکردنِ
تبِ استراتژی روی چرخشِ `core/live-quote.mjs`.

⚠ **پیش از بازنویسی، اندازه بگیر.** مرحلهٔ یک از عکسِ مشترکِ دیده‌بان
تغذیه می‌شود و شبکه نمی‌زند؛ مرحلهٔ دو فقط برای کاندیداهای برتر عمق
می‌گیرد (`ui/scanner.mjs`، سقف ۱۸۰ کد). با تابلوی باز ببین هر تیک چند
درخواست می‌شود. اگر سنگین بود `live-quote` را سوار کن و «جای چرخش» را
روی نوار نشان بده؛ اگر نبود، همین بماند.

### پ. مبنای قیمتِ «دستی» (بندِ ۱)

«دفتر سفارش، آخرین، پایانی، **دستی**» خواسته شد؛ «دستی» در `priceBasis`
نیست. قاعدهٔ صداقت: قیمتِ دستی فرضِ کاربر است نه دادهٔ بازار، پس ردیفِ
دستی هرگز «قابل اجرا» شمرده نشود و نشانِ خودش را داشته باشد.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی)، پس جدولِ رادارِ
استراتژی در این نوبت فقط **خالی** دیده شد. با دادهٔ واقعی ببینید:

۱. **کاورد کال.** با «بازده ایستا» مرتب بالا می‌آید؟ «فاصله تا سربه‌سری»
   و «دارایی قفل‌شده» عدد دارند؟
۲. **فیلتر.** «حداقل بیشترین سود» بگذار؛ گزارش باید بگوید چند ردیف افتاد
   و چندتاش به‌خاطرِ **نداشتنِ داده**.
۳. **ترتیب در رصد زنده.** روی ستونی مرتب کن، رصد زنده را روشن کن، یک دوره
   صبر کن — ترتیب نباید عوض شود. دورهٔ ۵ و ۶۰ را هم امتحان کن.
۴. **شاخص‌های کلیدی.** با فیلترِ روشن باید ردیف‌های **نمایش‌داده‌شده** را
   بشمارند، نه کلِ اسکن.
۵. **چهار خانواده.** پروانه، تقویمی، استرانگلِ فروش، باکس — ستون‌ها و
   فیلترها باید فرق کنند.
۶. **رصد تاریخی.** تقویم باید فقط روزهای داده‌دارِ همان نماد را فعال
   نشان بدهد. یک روز بگیر: **همان ستون‌ها** با قیمتِ آن روز، و جملهٔ زیرش
   بگوید چند پا از چند پا قیمت داشت. «دفتر سفارش» نباید در مبناها باشد و
   هیچ ردیفِ تاریخی نباید «قابل اجرا» بخورد.
۷. **خوابیدنِ زنده.** رصد زنده را روشن کن و برو روی زیرتب تاریخی — باید
   خودش خاموش شود، وگرنه جدولِ تاریخی چند ثانیه بعد پاک می‌شود.
۸. **نوار پیوند.** روی یک اسپرد «دیده‌بان شرطی» را بزن: **صفحهٔ جدا**،
   شرط‌ها با عددِ همان ردیف، و تیکِ استراتژی و نماد. روی کاورد کال این
   دکمه نباید باشد.
۹. **تازگی.** «سنِ مظنه» برای نمادِ فعال عدد، برای بی‌داده «—» نه صفر؛ و
   روی نمادِ متوقف «وضعیت نماد» باید «متوقف» بگوید.
۱۰. **ردِ جلسه.** ردیفی را کلیک کن، برو زیرتب سوم و مسیر را بکش: نمودار
   باید آنجا که پایی هنوز معامله نکرده شکاف داشته باشد، و ستونِ «فاصلهٔ
   پاها» عددِ غیرصفر بدهد.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. ردیفی که عددِ فیلترشده را ندارد از فیلتر
نمی‌گذرد و جدا شمرده می‌شود؛ «نامحدود» عددِ نداشته نیست و مثل عدد سنجیده
می‌شود؛ کادرِ خالیِ فیلتر شرطِ «حداقل صفر» نمی‌سازد؛ و ستونی که در
قرارداد ستونیِ موتور نباشد در هیچ نمایه‌ای نمی‌نشیند.

تاریخی: روزِ دیگری جانشینِ روزِ بی‌معامله نمی‌شود، دفترِ سفارشِ گذشته
ساخته نمی‌شود، پایهٔ بی‌قیمت ترکیب نمی‌سازد. ردِ جلسه: پایِ بی‌معامله
نقطه نمی‌سازد، لحظهٔ نرسیده ستون نمی‌شود، ناهم‌زمانی روی هر نقطه نوشته
می‌شود. تازگی: سنِ نداشته صفر نمی‌شود و وضعیتِ نداشته «مجاز» نیست.

اگر یکی شکست، یکی از دسته‌های ۲۳۲ تا ۲۳۴ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`
## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، بعد بسته شود (۸۷۸۷ دستِ صاحب پروژه). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`؛ Playwright سراسری،
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`waitUntil: 'domcontentloaded'` نه `networkidle` — تابلو بسته است و شبکه
آرام نمی‌گیرد. کارِ ظاهری: اول عکس، بعد آزمون.
