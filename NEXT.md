# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### گزارش چندجلسه‌ای فشرده در Excel — برش هفتم و پایانی فاز ۷

**چرا.** پرونده دانلودی اکنون جلسه فعلی، حسابداری و یافته‌ها را در Excel
دارد و کارت مرورگر نیز روند lineage را نشان می‌دهد؛ اما فایل Excel هنوز
سفرهای قبلی و تغییرهای قطعی رشد را حمل نمی‌کند. تحلیل بیرون برنامه نباید
برای بازسازی روند به JSONهای جدا یا محاسبه دستی وابسته باشد.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
core/portfolio-capital-growth.mjs
ui/portfolio-dossier-export.mjs
ui/portfolio-capital-growth-view.mjs
ui/tabs/portfolio-time.mjs
tests/suites/162-portfolio-dossier-export.mjs
tests/suites/169-portfolio-capital-growth-export.mjs
```

`portfolioCapitalGrowth` تنها منبع تغییر سفر و تجمعی است؛ exporter حق
بازمحاسبه سرمایه یا ساخت lineage موازی ندارد.

**پذیرش:**

1. workbook پرونده‌ای که continuity معتبر دارد یک برگ نسخه‌دار روند با
   یک ردیف برای هر سفر قدیم به جدید بسازد.
2. شناسه جلسه/سبد/نماد، لحظه بستن، سرمایه شروع، تحقق‌یافته، نهایی، تغییر
   سفر و تغییر تجمعی در ستون‌های جدا و با عدد خام ریال/درصد بمانند.
3. رشد، افت و بدون‌تغییر با کد پایدار صادر شوند؛ درصد نامعلوم خانه خالی
   بماند و علت سرمایه شروع صفر در ستون مجاور حفظ شود.
4. پرونده مستقل یک ردیف و پرونده دوم بازیابی‌شده دو ردیف صادر کند؛ ترتیب
   sheet دقیقاً ترتیب lineage معتبر باشد.
5. پرونده بدون continuity همچنان Excel معتبر فعلی را بگیرد و برگ روند یا
   صریحاً «در دسترس نیست» باشد یا حذف شود؛ هیچ ردیف ساختگی مجاز نیست.
6. continuity خراب fail-closed باشد و دانلود موفقِ ظاهری با برگ ناقص
   نسازد؛ خطا باید پیش از تولید blob به مدل دانلود برسد.
7. نویسنده xlsx، sheet splitting و نام فایل موجود دوباره پیاده نشوند؛
   قرارداد داده و دانلود فعلی مصرف شود و فایل واقعی با parser موجود آزموده
   شود.

**مرز قلم.** نمودار تصویری داخل Excel، پیش‌بینی سرمایه آینده، رتبه‌بندی
سفرها و فشرده‌سازی snapshot مأموریت بیرون این برش‌اند.

## محدودیت‌های باز

- ثبت مرحله `active` مأموریت واقعی با snapshot بزرگ هنوز از سقف عمومی یک
  مگابایت رد می‌شود؛ افزایش سراسری مجاز نیست و قرارداد جدا لازم دارد.
- نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه واقعی در چند
  نماد هنوز بازند.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم فاز ۷ را
از ۶ از ۷ به ۷ از ۷ و سهم آن را از ۶٪ به ۷٪ می‌رساند؛ پیشرفت کل باید از
۹۴٪ به ۹۵٪ برسد. دروازه کامل، کامیت دارای بدنه و پوش روی همین شاخه
لازم‌اند؛ سپس بدون انتظار برای مرج نخستین قلم فاز ۸ را آغاز کنید.
