# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پذیرش موبایل نوار هشدار و دفتر ظرفیت — قلم دوم فاز ۸

**چرا.** مسیر اصلی در دسکتاپ کار می‌کند، اما راستی‌آزمایی قبلی دو مرز
موبایل را باز گذاشت: نوار هشدار قیود می‌تواند محتوای بعدی را بپوشاند و
`capacity-panel` با جدول عریض، عرض کل صفحه را زیاد کند. در بازی سفر زمانی
هشدار و سرمایه آزاد باید هم‌زمان خوانا و قابل پیمایش بمانند.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
ui/tabs/portfolio-time.mjs
ui/style.css
tests/suites/153-portfolio-closeout-ui.mjs
tests/suites/171-portfolio-mobile-risk-layout.mjs
```

ابتدا با viewport واقعی ۳۹۰×۸۴۴ علت را بازتولید کنید؛ CSS باید از توکن‌های
موجود استفاده کند و راه‌حل به تغییر عدد مالی یا حذف ستون منجر نشود.

**پذیرش:**

1. در عرض ۳۹۰ پیکسل خود صفحه اسکرول افقی نگیرد؛ جدول ظرفیت اگر لازم است
   فقط در پوسته خودش افقی پیمایش شود.
2. نوار هشدار risk/capacity روی عنوان، دکمه بستن، ساعت یا جدول بعدی
   نیفتد و متن بلند فارسی بدون بریدن معنایی خوانده شود.
3. سرمایه درگیر/آزاد، حد، فاصله و حکم هیچ‌کدام برای جا شدن حذف یا خلاصه
   ساختگی نشوند؛ همان مدل فعلی مصرف شود.
4. شناسه‌ها و علت‌های بلند در کارت/جدول، عرض والد را نشکنند و تمرکز
   صفحه‌کلید کنترل‌های پیمایش دیده شود.
5. دسکتاپ ۱۴۰۰×۹۰۰ چیدمان چندستونه و جدول فعلی را نگه دارد؛ اصلاح موبایل
   نباید همه عرض‌ها را یک‌ستونه کند.
6. رنگ تازه سخت‌کد نشود؛ حالت هشدار/شکست فقط از توکن‌های موجود بیاید.
7. آزمون ساختاری selectorها و آزمون مرورگر واقعی در دو viewport،
   `scrollWidth <= clientWidth` صفحه، امکان پیمایش پوسته جدول و صفر خطای
   کنسول را قفل کنند.

**مرز قلم.** بازطراحی کامل کارت‌ها، تغییر محتوای هشدار و پیمایش تاریخچه
چندنمادی بیرون این برش‌اند.

## محدودیت‌های باز

- پیمایش تاریخچه واقعی در چند نماد هنوز باز است.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم دومین وظیفه
از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق `node tools/progress.mjs`
بنویسید. دروازه کامل، مرورگر واقعی، کامیت دارای بدنه و پوش روی همین شاخه
لازم‌اند؛ سپس بدون انتظار برای مرج قلم سوم فاز ۸ را آغاز کنید.
