# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### سرمایه نهایی و فاصله از هدف — برش پنجم فاز ۶

**چرا.** پرونده اکنون بسته، ذخیره و دوباره خوانده می‌شود، ولی هنوز فقط
دفتر و سود و زیان تحقق‌یافته را نشان می‌دهد. کاربر نمی‌بیند سرمایه نهایی
چه شد و نسبت به هدف مأموریت چقدر فاصله دارد. این دو عدد فقط وقتی صادق‌اند
که جلسه تخت و همه اجزای تحقق‌یافته معلوم باشند؛ در غیر این صورت باید
نامعلوم بمانند.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
core/portfolio-dossier-analysis.mjs       تحلیل خالص سرمایه و هدف
ui/portfolio-dossier-analysis-view.mjs    فقط تبدیل ریال به تومان و رقم فارسی
tests/suites/157-portfolio-dossier-analysis.mjs
```

داده فقط از `{session,dossier}` معتبر می‌آید. قیمت بازار، mark امروز،
آخرین قیمت و جایگزین تخمینی در این تحلیل وارد نشوند.

**پذیرش:**

1. اگر `positions.open === 0` و `realized.totalRial` معلوم است، سرمایه
   نهایی برابر سرمایه شروع به‌علاوه تحقق‌یافته باشد؛ اجزا و مبنا در خروجی
   جدا بمانند.
2. اگر موقعیت باز، تحقق‌یافته نامعلوم، حسابداری ناقص یا سرمایه شروع نامعلوم
   است، سرمایه نهایی و بازده `null` باشند و علت دقیق گفته شود؛ صفر ممنوع.
3. مبنای هدف از `lockedMission.objective.returnBase` خوانده شود: سرمایه
   کل یا سرمایه قابل تخصیص. مبنای ناشناخته حدس زده نشود.
4. بازده تحقق‌یافته، هدف درصدی و فاصله از هدف جدا گزارش شوند. نتیجهٔ
   «رسید/نرسید» فقط وقتی هر سه عدد معلوم‌اند ساخته شود.
5. تحلیل به جلسه و پرونده ناهم‌هویت یا نسخه ناشناخته عدد ندهد.
6. لایه نمایش هیچ حسابی جز تقسیم ریال بر ده نکند؛ رقم فارسی، تومان و «—»
   برای نامعلوم رعایت شوند.

این برش تابع خالص و مدل نمایش است؛ اتصال کارت تحلیل به تب قلم بعدی است و
مرورگر واقعی لازم نیست.

## محدودیت‌های باز

مرورگر داخلی در دو قلم قبلی با `failed to write kernel assets` در مرحله
اتصال شکست خورد. نمایش کارت تحلیل، مقایسه چند پرونده و استخراج ضعف‌های
رفتاری هنوز بیرون این برش‌اند. نوار هشدار و سرریز موبایل نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
