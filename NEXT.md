# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پذیرش سفر یک‌میلیاردی تا refresh جلسه active — قلم چهارم فاز ۸

**چرا.** اجزای فرم، snapshot بزرگ و ذخیره سرور جداگانه سبزند؛ اما هدف
واقعی کاربر یک مسیر پیوسته است: یک میلیارد تومان، نماد و تاریخ آشکار،
انتظار/ریسک/تخصیص ۳۰-۴۰-۳۰، قفل مأموریت و بازگشت همان جلسه پس از refresh.
پذیرش نهایی باید ثابت کند این اتصال‌ها روی داده واقعی یکدیگر را نمی‌شکنند.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
ui/tabs/portfolio-time.mjs
ui/portfolio-mission-data.mjs
server/portfolio-mission-store.mjs
tests/suites/122-portfolio-mission-resume.mjs
tests/suites/173-portfolio-billion-active-refresh.mjs
```

**پذیرش:**

1. ورودی ۱٬۰۰۰٬۰۰۰٬۰۰۰ تومان دقیقاً ۱۰٬۰۰۰٬۰۰۰٬۰۰۰ ریال بماند؛ ذخیره،
   تخصیص و refresh حق تغییر واحد یا گردکردن ندارد.
2. نماد، شروع/پایان، انتظار، ریسک، نقدشوندگی، هدف و سه تخصیص ۳۰/۴۰/۳۰ از
   setup تا active و JSON/server round-trip بدون تغییر بمانند.
3. snapshot واقعی بزرگ‌تر از یک مگابایت با همان کیفیت/علت‌ها ذخیره شود و
   پاسخ موفق سرور پیش از اعلام «ثبت شد» لازم باشد.
4. refresh فهرست جلسه را با مرحله active نشان دهد و ادامه، فرم قفل‌شده،
   ساعت شروع، دفتر سرمایه و پیشنهادهای همان snapshot را بازسازی کند.
5. داده ناکافی پیشنهاد یا قیمت ساختگی نسازد؛ وضعیت ناکافی و علت‌ها پس از
   refresh همان بمانند.
6. شناسه Session/Portfolio و سرمایه اولیه پیش و پس از refresh عیناً یکسان
   باشند؛ رکورد تازه یا جلسه دوم ناخواسته ساخته نشود.
7. آزمون یکپارچگی و مرورگر واقعی دسکتاپ مسیر کامل، autosave، reload،
   نبود سرریز و صفر خطای کنسول را قفل کنند و فایل دقیق آزمون پاک شود.

**مرز قلم.** انجام معامله، آفست و بستن جلسه در قلم‌های پذیرش بعدی‌اند.

## محدودیت‌های باز

- universe تاریخی هنوز فقط قراردادهای موجود در آرشیو محلی را می‌شناسد؛
  قرارداد منقضی‌شده بدون عکس روزانه قابل بازسازی نیست.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم چهارمین
وظیفه از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق ابزار پیشرفت بنویسید.
دروازه کامل، مرورگر واقعی، کامیت دارای بدنه و پوش روی همین شاخه لازم‌اند؛
سپس بدون انتظار برای مرج قلم پنجم فاز ۸ را آغاز کنید.
