# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### اعلام درست باز و بسته بودن پنل ستون‌های جدول

**چرا.** دکمه «ستون‌ها» یک پنل تاشونده را کنترل می‌کند، اما اکنون با
`aria-pressed` مثل یک کلید روشن/خاموش معرفی می‌شود. فناوری کمکی باید بداند
محتوای وابسته باز است یا بسته؛ قرارداد درست برای این الگو
`aria-expanded` و یک رابطه صریح با پنل کنترل‌شده است.

**شاخه.** پس از ادغام شاخه جاری، از آخرین `origin/main` شاخه کوتاه‌عمر
تازه بسازید. تاریخچه را بازنویسی یا force-push نکنید.

**فایل‌های محتمل:**

```text
ui/table.mjs
tests/suites/180-column-panel-accessibility.mjs
BACKLOG.md
```

**پذیرش:**

1. دکمه «ستون‌ها» هنگام بسته بودن پنل `aria-expanded="false"` و هنگام باز
   بودن `aria-expanded="true"` داشته باشد.
2. دکمه با `aria-controls` به شناسه یکتای پنل ستون‌های همان جدول متصل شود؛
   وجود چند جدول در یک صفحه شناسه تکراری نسازد.
3. بستن پنل از هر مسیر موجود، مقدار ARIA را نیز بی‌درنگ همگام کند.
4. `aria-pressed` از این دکمه حذف شود و رفتار کلیک، انتخاب ستون‌ها و ذخیره
   ترجیحات تغییری نکند.
5. آزمون رگرسیون مستقل قرارداد بسته/باز و یکتایی رابطه را قفل کند.

**مرز قلم.** فقط معنای دسترسی‌پذیری و اتصال دکمه به پنل؛ بازطراحی جدول،
افزودن ستون یا تغییر ظاهر خارج از دامنه است.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md` ثبت و `NEXT.md` برای قلم بعدی بازنویسی شود. دروازه
کامل، کامیت دارای بدنه و پوش روی شاخه کوتاه‌عمر لازم‌اند.
