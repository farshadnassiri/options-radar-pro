# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### اعمال فقط وقتی به‌صرفه است — رفع تناقض بیشترین زیان در موتور مشترک

**چرا.** برش ششم فاز ۳ (`core/portfolio-evaluation.mjs`) یک تناقض در خودِ
موتور مشترک بیرون آورد و عمداً آن را پین کرد به‌جای اینکه بی‌صدا ردش کند.
با یک کال ساده و کارمزد واقعی:

```text
analyzePayoff(...).maxLoss   →  ۶٬۵۳۲٬۹۲۰   با atMaxLoss = ۹۵۰۰
pnlAtExpiry(..., ۹۵۰۰, ...)  → −۲٬۹۲۲٬۹۲۰
```

یعنی موتور بیشترین زیان را به قیمتی نسبت می‌دهد که تابع نقطه‌ای خودش آن
عدد را آنجا رد می‌کند. علتش `isItm` نیست، فرض پشت آن است: هر اختیارِ در
سود «حتماً اعمال‌شده» فرض می‌شود، حتی اگر یک‌صدم ریال در سود باشد. آن‌وقت
کارمزد اعمال و کارمزد فروش سهم روی **کل ارزش اسمی** می‌خورد تا چند ریال
ارزش ذاتی گرفته شود.

این عدد بی‌ضرر نیست. `portfolioPlanEvaluation` همان را در
`risk.missionLossCap` می‌گذارد، و برای طرحی که کل سرمایهٔ در خطرش
۲٬۹۲۲٬۹۲۰ ریال است اعلام می‌کند سقف زیان مأموریت شکسته شده. امتیازدهی و
رتبه‌بندی — قلم‌های بعدی فاز ۳ — روی همین عدد بنا می‌شوند، پس پیش از
آن‌ها باید درست شود.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/payoff.mjs                          تصمیم اعمال، در affineOn
tests/suites/0??-payoff*.mjs             دستهٔ موجود payoff — با suites پیدایش کنید
tests/suites/128-portfolio-evaluation.mjs  ادعای پین‌شده باید برداشته شود
```

`node tools/next.mjs suites payoff` و `node tools/next.mjs map analyzePayoff`.

**پذیرش:**

1. اختیاری که اعمالش پس از کارمزد اعمال و کارمزد سهم **بدتر** از رها کردن
   باشد، اعمال‌شده فرض نشود. تصمیم باید صریح و قابل خواندن باشد، نه
   جانبی‌اثرِ یک مقایسهٔ عددی.
2. `analyzePayoff(...).maxLoss` و `atMaxLoss` با
   `pnlAtExpiry(..., atMaxLoss, ...)` سازگار شوند. یک ادعا دقیقاً همین
   سازگاری را برای کال ساده، پوت ساده و استرنگل بسنجد.
3. برای کال ساده با کارمزد واقعی، بیشترین زیان دقیقاً برابر پرمیوم پرداختی
   به‌علاوهٔ کارمزد ورود شود — نه بیشتر.
4. سربه‌سری‌ها همچنان درست بمانند: بالای نقطه‌ای که اعمال به‌صرفه می‌شود،
   کارمزد اعمال و فروش سهم باید همچنان لحاظ شود. یک ادعا سربه‌سری کالِ
   کارمزددار را پیش و پس از اصلاح مقایسه کند.
5. ریسک نامحدود دست‌نخورده بماند: استرنگل فروش همچنان `unlimitedLoss`
   بدهد و `maxLoss` عدد نگیرد.
6. **هیچ‌کدام از ۲۵۸۶ ادعای موجود نشکند.** اگر ادعایی شکست، پیش از تغییرش
   بفهمید آن ادعا رفتار درست را پین کرده بود یا همین اشتباه را؛ و هرکدام
   بود در `WORKLOG.md` بنویسید. ادعای پین‌شده در دستهٔ ۱۲۸
   («تناقض شناخته‌شدهٔ موتور») باید با اصلاح **برداشته** شود، نه معکوس.

**مرز قلم.** امتیازدهی، رتبه‌بندی، توزیع احتمال و پیشنهاد UI هنوز
ممنوع‌اند. این قلم فقط یک تصمیم اشتباه در موتور را درست می‌کند.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
