# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۶/۲۶)

**تب موقعیت‌های من از «عکس» به «روند» رسید.** هر موقعیت باز سه سرچشمهٔ
روند دارد — روزانه (`/api/dailies`)، درون‌روزی (`/api/live-trades`)، و
دنبالهٔ همین جلسه از قیمت‌گیریِ ۱۵ثانیه‌ای. ستون «تغییر امروز» مبنایش آخرین
روزِ **پیش از** امروز است، نه `close` تابلو که در ساعت بازار خودِ امروز را
می‌گوید. موقعیتِ سررسیدگذشته دیگر قیمت نمی‌گیرد و در جدولِ خودش می‌نشیند.
ویرایش موقعیت (عنوان، تعداد، تاریخ، قیمت‌ها — نه ساختار ترکیب) اضافه شد، و
«در جست‌وجوی استراتژی‌ها» مقصد تازه‌ای گرفت: «افزودن به موقعیت‌های من»، که
فرم را پیش‌پر می‌کند و ثبت پس از تأیید کاربر انجام می‌شود.

موتور: `core/position-track.mjs`. نما: `ui/positions-track-view.mjs` ·
`ui/positions-edit.mjs` · `ui/positions-intake.mjs`. دسته‌های ۲۵۳ و ۲۵۴
و افزوده‌های ۰۵۲.

### ادامهٔ همین قلم — چیزهایی که جا ماند

۱. **بستنِ موقعیت.** امروز فقط «باز» و «سررسیدگذشته» داریم؛ موقعیتی که
کاربر پیش از سررسید ببندد راهی برای ثبتِ تاریخ و قیمتِ خروج ندارد.
لازمه‌اش `exit: { date, prices[] }` روی رکورد است و ستونِ «سود و زیانِ
تحقق‌یافته» جدا از جاری.
۲. **روند سبد.** جمعِ روندِ همهٔ موقعیت‌های باز روی یک محور — ولی فقط
روزهایی که **همه** نقطه دارند، وگرنه جمع نصفه است.

### قلمِ بازِ قدیمی — ماژول‌های یتیم

`ui/tabs/greeks-watch.mjs` · `spread-radar.mjs` · `bereket.mjs` ·
`portfolio-time.mjs` از **برنامه** بیرون رفتند ولی روی دیسک مانده‌اند و
آزمونشان سبز است — با `portfolio-time` حدود سی ماژولِ `ui/portfolio-*.mjs`
که تنها خواننده‌شان همان بود. حذفشان **تصمیم صاحب پروژه** است؛ تا تصمیم
نگرفته، دست نزنید.

## کار

### پذیرش تب موقعیت‌های من در جلسهٔ بازار باز

ادعاهای موتورش در `tests/suites/253-position-track.mjs` است؛ این بند
چیزهایی است که فقط با بازارِ باز سنجیده می‌شود.

۱. **ستون تغییر امروز** باید برای موقعیتی که دیروز قیمت داشته عدد بدهد و
راهنمای سلولش تاریخِ مبنا را بگوید؛ برای موقعیتِ امروزباز «—» و علت.
۲. **روند روزانه** باید روی روزهای بی‌معاملهٔ یک پا **قطع** شود، نه وصل —
با شمردن `null`های سری هم سنجیدنی است.
۳. **درون‌روزی** فقط با کلیک گرفته شود: تب شبکه باز باشد، رفرشِ دوره‌ای
نباید هیچ `live-trades`ای ببرد.
۴. **سررسیدگذشته**: هیچ `books/infos` برای قراردادِ منقضی نرود.

### تقویم و روز جاری

۵. با «تا همین لحظه» آخرین روز باید **امروز** باشد؛ با «تا آخرین روز
بسته‌شده» دیروز. ۶. پس از «دریافت تاریخچه» شمار `/api/live-trades` باید
`ceil(n/24)` باشد و قراردادِ بی‌معامله در `ins=` نیاید. ۷. پس از ۲۰:۳۰
گرینویچ سقف بازه باید روزِ تهران باشد. ۸. تابلوی قطع
(`source=watch-archive`): هیچ ردیف امروزی ساخته نشود و علت گفته شود.

### رصد زنده

۹. تب شبکه باز؛ نباید `history/universe` یا `dailies` برود. ۱۰. «ارزش خود
پایه» نباید صفر باشد. ۱۱. در بازار بسته ساعت «عکس … · N ثانیه پیش» بگوید و
نوار قرمز شود. ۱۲. سایهٔ «در سود» سمت کال زیر قیمت جاری و سمت پوت بالای
آن؛ دلتا کال مثبت، پوت منفی. ۱۳. رفرش پیوستهٔ ۲۰ دقیقه‌ای.

### گام سوم بک‌تست

۱۴. بازه‌ای که امروز را در بر بگیرد: یک POST دسته‌ای به‌ازای هر ۱۲۰۰ جفت
به‌علاوهٔ یک GET نوار زنده — نه بیشتر. ۱۵. روزِ جاری نباید در «بدون نقطهٔ
مشترک» بیفتد. ۱۶. با قطع عمدی شبکه، روز «خطای دریافت» بگیرد نه
«بی‌معامله». ۱۷. جدول پوشش دقیقاً به تعداد روزهای معاملاتیِ بازه ردیف
داشته باشد و خطِ نمودار روی روزهای بی‌داده **قطع** شود.

## قلم بعدی — نرخ بدون ریسک سراسری ۳۰٪ (تصمیم صاحب پروژه، دو بار تأیید شده)

اندازه‌گیری نشان داد **علتِ غالبِ ستون‌های خالیِ تلاطم همین است**:

```
نرخ ۳۰٪ → کف نظری ۶٬۲۲۸ → قیمت ۴٬۶۳۱ زیر کف  →  belowFloor
نرخ  ۰٪ → کف نظری ۴٬۵۷۲ → قیمت ۴٬۶۳۱ بالای کف →  IV ۱۳٫۸٪
```

کفِ بدون‌آربیتراژ وقتی معنی دارد که بشود پایه را فروش استقراضی کرد؛ در این
بازار نمی‌شود. سه راه، و انتخابش تصمیم صاحب پروژه است: (۱) نرخِ جداگانه
برای کفِ بدون‌آربیتراژ کنار نرخِ تنزیل، (۲) بازده نقدی به‌ازای نماد از
منبعی که برنامه ندارد، (۳) مدل سود نقدی گسسته. عددِ فرضیِ ساخته‌شده دقیقاً
همان چیزی است که قاعدهٔ ۲-۴ منع می‌کند.

## قلم‌های باز

- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوارِ پس از حذف
  باطل‌ها ۶٬۷۹۸. اول باید سنجید کدام endpoint مرجع است.
- **بارِ بالادست**: تصمیم دربارهٔ `byEndpoint` در `/api/health`.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» هم از آخرین معاملهٔ نوار ساخته نمی‌شود.
- روز جاری فقط در فاز `open`، `after` یا `ungated` **و** از منبع `watch` یا
  `snapshot` به تاریخ امروز منتسب شود.
- ورودیِ آزمایشی باید هم‌شکلِ پاسخ واقعی باشد. قاعده‌ای که فقط با دادهٔ
  ساختگی برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون باید در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن، از
  `Date.now()` نسنج.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.

**دروازه:** `node tools/check.mjs`
