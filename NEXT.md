# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## دورهٔ جاری

دو ممیزیِ پیاپیِ «کامل‌بودنِ داده» بسته شد. دورهٔ ظاهری (تیرهٔ
اسپاتیفای‌وار، صفحهٔ نمونه «موقعیت‌های من») روتینِ ساعتی **ندارد** و
نوبت‌به‌نوبت جلو می‌رود.

**شاهدِ مرورگر:** `PORT=8787 node server/server.mjs &` — رابط با بازارِ
بسته هم بالا می‌آید. playwright روی همین ماشین است
(`/opt/node22/lib/node_modules/playwright/index.mjs`، کروم
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`). تب را با
`page.click('#rail-list >> text=<نام تب>')` باز کن. **ادعای ظاهری
بی‌اندازه‌گیری نکن** — `getComputedStyle` را بپرس.

**شاهدِ بالادست:** `cdn.tsetmc.com` از این ماشین در دسترس نیست. راهی که
جواب داد: یک بالادستِ ساختگی که پاسخ‌های واقعیِ ذخیره‌شده را سرو کند و
`baseUrl` در `data/settings.json` یک کپیِ جدا به آن اشاره کند.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۶/۲۹) — آزمونِ عملیِ صاحب پروژه، هر چهار یافته

ممیزیِ نوبتِ قبل هفت بند داشت و بسته شد؛ بعد صاحب پروژه یک **آزمونِ عملی**
با HTTP واقعی روی شش ابزار/روز اجرا کرد و چهار ایرادِ تازه داد. دو تایشان
نشان دادند نوبتِ قبل **تشخیص** را درست کرده بود و **بازیابی** را نه.

- **F-01** (دستهٔ ۲۸۱) — دریافت روی پاسخِ «غیرخالی ولی بریده» می‌ایستاد.
  اهرم/۲۰۲۶۰۹۱۹: مسیر `true` ۲٬۵۲۱ معامله، مسیر `false` ۷٬۷۳۶ — برابرِ تابلو. `core/tape-choice.mjs` حالا با تابلوی روزانه
  می‌سنجد و فقط تطبیقِ کامل جلوی درخواستِ دوم را می‌گیرد.
- **F-04** (۲۸۱) — تلاشِ دوبارهٔ بی‌کش دادهٔ موجود را **پاک می‌کرد**
  (۲٬۵۲۱ ردیف → صفر). `keepBetterTape` فقط وقتی جایگزین می‌کند که بهتر
  باشد.
- **F-02** (۲۸۱) — ستونِ «بیرون از جلسه» پنجرهٔ انتخابی را نمی‌دید. حالا
  دو ستونِ صریح، به‌علاوهٔ «کسریِ نسبت به تابلو».
- **F-03** (۲۸۱) — `hist?kind=closing` بی وضعیتِ پوشش برمی‌گشت.
  `closingCoverage` آن را می‌گوید.

**قاعده‌هایی که از این نوبت ماندند:** غیرخالی‌بودن معیارِ کامل‌بودن نیست ·
جمعِ خامِ دو پاسخِ بالادست ممنوع است (خودشان ردیف تکراری می‌فرستند) ·
تلاشِ دوباره حق ندارد دادهٔ سالم را ببرد · نگه‌داشتنِ دادهٔ قبلی یعنی «از
دستش ندادیم»، نه «کامل است» · `dEven:0` به‌تنهایی مدرکِ خرابی نیست.

## قلم بعدی

۱. **همان سه چیزی که ممیزیِ قبل نبست** و این نوبت هم لمس نشد: نمایشِ
   وضعیتِ پوشش در «نگاه باز»، «تاریخچه» و «رول» · کنترلِ تطبیقِ روز برای
   `hist/book`، `state`، `threshold`، `instrument`، `clientType` ·
   قراردادِ منقضی با روزانهٔ تاریخ‌دار.
۲. **هزینهٔ درخواستِ مرجع.** حالا هر ابزار/روزِ تاریخی یک درخواستِ
   روزانهٔ کوچک هم می‌برد. کش جذبش می‌کند (در آزمونِ محلی سه مسیر فقط ۴
   درخواستِ بالادست ساختند)، ولی برای بازهٔ بزرگ باید با سهمیهٔ واقعی
   اندازه گرفته شود.
۳. **بازارِ باز.** هر دو ممیزی با بازارِ بسته اجرا شدند. رفتارِ حینِ
   ورودِ لحظه‌ایِ معاملات هنوز سنجیده نشده.

## صفِ ظاهری (دورهٔ قبل، دست‌نخورده)

قلمِ ظاهریِ بعدی **جدول‌ها و نوار بالا**ست: سرستونِ جدول روی کارت یک پله
روشن‌تر، نه تیره‌تر · راه‌راهِ ردیفِ زوج یا دیده شود یا نباشد · نوار وضعیتِ
هدر «—»ها را بی‌گروه‌بندی می‌چیند. بعد از آن: حالتِ خالیِ درون‌کارتی،
فرم‌های بلند، و کنتراستِ دکمهٔ اصلی در پوستهٔ روشن (۳٫۸:۱، زیر کف).
**ایموجیِ عنوانِ دو تب عمداً می‌ماند** — دستهٔ ۸۶ قفلش می‌کند.

## قلم‌های باز (غیرظاهری، دست‌نخورده)

- **خروجی دیتا — درسِ آخرین ممیزی.** «کامل خروجی نمی‌سازه»: کفِ عمرِ
  قرارداد حالا از سری می‌آید نه از یک سمت (`dataExportListingFloors`)، و
  چهار ایرادِ ممیزیِ m15 بسته شد. درسِ قابل‌تعمیم: عددی که در دو برگِ یک
  فایل به دو راه حساب می‌شود، آزمونی می‌خواهد که همان دو راه را کنار هم
  بگذارد؛ و پرچمی که هسته می‌سازد (`lifeFromTrades`) اگر سرِ راه دور
  ریخته شود، مصرف‌کننده «مشاهده» را «رسمی» می‌خواند. دسته‌های ۲۶۵ و ۲۶۶.
- **نرخ بدون ریسک سراسری ۳۰٪** — تصمیم صاحب پروژه (شرح در `WORKLOG.md`).
- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸. حالا
  که `dedupeTrades` هست، اول همان را روی این نمونه ببینید: اختلافِ ۲
  می‌تواند دو ردیفِ تکراری باشد، و اگر نبود، تضادِ هم‌شماره در
  `conflicts` می‌نشیند.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.
- **پذیرش قلم‌های جلسهٔ بازار باز**: موتورها در دسته‌های ۲۵۳ و ۲۵۵ تا ۲۶۳
  قفل‌اند؛ آنچه فقط با بازارِ باز سنجیدنی است همان‌جا فهرست شده.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.
- **توکنی که مقدارش `transparent` است، لبهٔ زیرِ خودش را پاک می‌کند.** اگر
  تصمیمی برگشت، قاعده‌اش را هم بردارید — نه فقط مقدارش را.

**دروازه:** `node tools/check.mjs`
