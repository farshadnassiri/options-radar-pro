# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### سفر یکپارچه مرورگر تا جلسه بعد — قلم نهم و پایانی فاز ۸

**چرا.** همه قطعات از فرم یک میلیارد تومانی تا تشکیل سبد، حرکت زمان،
افزایش/کاهش، آفست، پایان، refresh و Excel جداگانه پذیرفته شده‌اند. پذیرش
نهایی باید ثابت کند این قطعات در یک سفر واقعی مرورگر به هم متصل‌اند و
هویت یا عددی بین مرزهای HTTP، refresh، دانلود و شروع سفر بعد گم نمی‌شود.

**شاخه.** با اجازه صریح صاحب پروژه، کامیت مستقل بعدی را روی همین شاخه
`codex/portfolio-final-xlsx-refresh` ادامه دهید. پیش از قلم تازه
`origin/main` را بررسی کنید؛ تاریخچه را بازنویسی یا force-push نکنید.

**فایل‌های محتمل:**

```text
ui/tabs/portfolio-time.mjs
ui/portfolio-dossier-export.mjs
server/server.mjs
tests/suites/178-portfolio-final-browser-journey.mjs
```

**پذیرش:**

1. کاربر در رابط با یک میلیارد تومان، نماد و زمان‌های صریح مأموریت معتبر
   می‌سازد؛ autosave و active فقط پس از پاسخ موفق سرور نهایی می‌شوند.
2. پیشنهاد واقعاً اجراپذیر با حجم صریح ثبت می‌شود؛ سپس ساعت جلو می‌رود،
   یک تغییر حجم و آفست کامل با دفتر همان لحظه انجام می‌شود.
3. refreshهای میانی همان Session/Portfolio/Position/Transaction/Execution/
   Lot، شمارنده‌ها، snapshotها و ساعت را بدون تولید هویت تازه برمی‌گردانند.
4. پایان بازی سرمایه نهایی، رتبه و صدک انتخاب، بهترین/بدترین و گزینه‌های
   بی‌رتبه را با علت نشان می‌دهد و ذخیره تکراری پرونده نمی‌سازد.
5. دانلود XLSX از همان پرونده بازیابی‌شده انجام می‌شود؛ فایل بازشدنی، زیر
   سقف دسته ۱۷۷ و شامل شناسه‌ها و عددهای قطعی سفر است.
6. «شروع جلسه بعد» فقط از سرمایه نهایی قطعی استفاده می‌کند، lineage را به
   پرونده قبلی متصل نگه می‌دارد و نماد/تاریخ‌ها را برای انتخاب تازه خالی
   می‌گذارد؛ refresh دوم این اتصال را هم حفظ می‌کند.
7. خطاهای HTTP یا داده گمشده هیچ حالت خوش‌بینانه یا عدد صفر ساختگی وارد
   رابط نمی‌کنند و فایل‌های آزمون دقیقاً پاک می‌شوند.
8. سفر در مرورگر واقعی در عرض دسکتاپ و موبایل پیموده شود؛ کنسول بی‌خطا،
   صفحه بدون overflow افقی و دکمه‌های مرحله جاری دسترس‌پذیر باشند.

**مرز قلم.** با سبزشدن این سفر، فاز ۸ و خواسته استودیوی سفر زمانی کامل
می‌شود. کار بعدی از `BACKLOG.md` انتخاب می‌شود، نه افزودن قابلیت مالی
حدسی به همین فاز.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. فاز ۸ را فقط پس از
آزمون یکپارچه و مشاهده واقعی ۹ از ۹ و کامل اعلام کنید. دروازه کامل، کامیت
دارای بدنه و پوش روی همین شاخه لازم‌اند؛ سپس قلم بعد را از بک‌لاگ بردارید.
