# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## فهرست یازده‌تایی صاحب پروژه — وضعیت هر قلم

این فهرست، کارِ جاری است. ترتیبش اولویت است؛ اولین قلمی که تمام نشده را
بردار. وضعیت‌ها با ممیزی روی خودِ کد در ۱۴۰۵/۰۶/۱۴ به‌روز شدند.

```text
۱  نام نماد در جدول‌ها و نمودارها               ✔ تمام (۱۴۰۵/۰۶/۱۴)
۲  همهٔ ستون‌های تأثیرگذار + حذف و افزودن ستون   ▲ نیمه — ستون‌ها ثابت‌اند
۳  نمودارها بزرگ‌تر                              ✔ تمام
۴  کشویی خانواده → نام استراتژی                 ✔ تمام (۱۴۰۵/۰۶/۱۴)
۵  جدول ویژوال و رنگی، نمودار داخل سلول         ✔ تمام در رادار
۶  «ارزش معامله» در فیلترها                      ✘ شروع نشده
۷  استرانگل فروش — نمایش و صحت محاسبه            ✔ کد و آزمون؛ تابلو نه
۸  رصد لحظه‌ای: سری تاریخی کنار سری زنده         ✘ شروع نشده
۹  تب مستقل سیستم اعلان                          ▲ نیمه — زیرتب است نه تب
۱۰ شرط روی چند نماد و روی پنجرهٔ N روزه          ▲ نیمه — تک‌نماد است
۱۱ رصدگر لحظه‌ای از شروع بازار تا اکنون          ▲ در تب رصد بله، در رادار نه
```

## قلم بعدی — ۲: ستون‌های جدول رادار، با حذف و افزودن

**این را بردار.** جدول «اکنون» در `ui/tabs/spread-radar.mjs` ده ستونِ
دست‌نویس دارد (سرستون‌ها حوالی خط ۱۵۰، ردیف‌ها در `paintTable`). آنچه
`measureGap` حساب می‌کند ولی هیچ‌جا نشان داده نمی‌شود:

```text
maxProfit   maxLoss   filledPct   gained   underwater   entry   size   mult
```

خواستهٔ صاحب پروژه صریح بود: «همهٔ آیتم‌های تأثیرگذار در جدول، با قابلیت
حذف و اضافهٔ ستون (حداکثر سود، زیان، درصدهایشان) با الهام از سایر جداول
برنامه (`ui/table.mjs`)».

**چرخ را از نو نساز.** `ui/table.mjs` خط ۱۵۶ تابع `makeTable` را دارد که
`opts.all` و `storeKey` می‌گیرد و پنل انتخاب ستون را خودش می‌سازد
(خط‌های ۳۱۵–۴۰۰)، و `moveColumn`/`insertColumn` در خط‌های ۴۳–۷۹. مسیر
کم‌ریسک: جدول رادار را به همان `makeTable` منتقل کن و ستون‌های تازه را
اضافه کن — نه اینکه پنل ستون دومی بنویسی.

دو نکته که موقع انتقال می‌شکنند:

- سلول‌های `fillBar` و `sparkline` (از `ui/gap-charts.mjs`) HTML خام
  می‌دهند؛ `makeTable` باید بتواند سلولِ HTML بگیرد، وگرنه قلم ۵ که
  تمام شده پس می‌رود.
- ستون تازهٔ «نمادها» از `symbolCell(row.legs)` می‌آید و باید بماند.

`maxProfit` و `maxLoss` برای استرانگلِ خرید عمداً `NaN`اند (سقف ندارد).
`—` بنویس، عدد نساز — دستهٔ ۲۱۸ همین را نگه می‌دارد.

## قلم پس از آن — ۶: «ارزش معامله» در فیلترها

`core/combo-filter.mjs` خط‌های ۲۹–۸۵ همین حالا `entryValue` («ارزش
معاملهٔ ورود») و `maxProfit` و `maxLoss` را دارد و در
`ui/tabs/portfolio-backtest.mjs` خط‌های ۸۱۷–۸۷۶ رابطش هست. رادار از هیچ‌کدام
استفاده نمی‌کند: `buildRadarHistory` در `core/radar-history.mjs` خط ۷۶ با
`liquidity: {}` صدا می‌زند، پس `passesLiquidity` بی‌اثر است. کار، وصل‌کردن
همان صافیِ موجود است، نه نوشتن صافی تازه.

## آنچه در این نوبت تمام شد

قلم ۱ و قلم ۴. `comboSymbols`/`comboSymbolText` در `core/spread-gap.mjs`؛
ستون «نمادها»، شناسنامهٔ بالای نمودارهای تاریخچه، و نام نماد در کشویی،
کارت‌ها و برچسب اعلان. کشویی خانواده به کشویی استراتژی با `optgroup`
تبدیل شد و صافی روی `def.id` افتاد. دسته‌های ۲۲۲ و ۲۲۳ تازه‌اند.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی؛ در مرورگر
`ERR_TUNNEL_CONNECTION_FAILED`). این چهار مورد هنوز روی دادهٔ واقعی
سنجیده نشده‌اند:

1. **عددها را با تابلو بخوان.** یک اسپرد عمودی در مقیاس «قیمت خام
   قرارداد» بگیر و ببین «تفاضل اکنون» همان تفریقی است که خودت می‌کنی.
   بعد مقیاس را عوض کن؛ درصدِ پرشدگی نباید تکان بخورد.
2. **استرانگل فروش، در سود و در زیان.** دومی باید نوار قرمزِ «در زیان»
   بگیرد و ستون باقی‌مانده‌اش «ارزشی که باید آب شود» بنویسد — نه «سود».
3. **نمودار «دو نرخ و فاصله‌شان».** در اسپرد دو خط و فضای رنگیِ میانشان؛
   در استرانگل دو ناحیهٔ روی‌هم با خطِ چینِ جمعِ روز ورود.
4. **هشدار زنده در ساعت بازار.** تیکِ «رصد زندهٔ بازار»، یک شرط «عبور»
   روی فاصلهٔ اکنون، و دیدن اینکه اعلان می‌رسد یا نه.

## پس از یازده قلم — بازطراحی ظاهر

خواستهٔ صریح: «حتی css را هم تغییر بده و ظاهر همهٔ المان‌ها و آیتم‌های
سایت را بهبود بده». کارِ چند نوبت است، هر نوبت **یک لایه**: اول توکن‌های
`ui/style.css` (رنگ، فاصله، شعاع، سایه، مقیاس قلم)، بعد المان‌های پایه،
بعد جدول‌ها، بعد نمودارها، بعد حالت‌های خالی. آخرش هر ۴۸ تب با Playwright
باز و نماگرفت شود.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. پای بی‌قیمت فاصله را باطل می‌کند، دو پای هم‌قیمت
ردیف نمی‌سازند، توزیعِ بی‌پراکندگی حکم نمی‌گیرد، استرانگلِ بی قیمت ورود
درصد نمی‌گیرد، بازدهی که مخرجِ مدافع‌پذیر ندارد ادعا نمی‌شود، و **نامِ
نداشتهٔ یک پا با شناسهٔ ابزار پر می‌شود نه با نامی سرِ هم‌شده.**

اگر روزی یکی از این شش شکست، دستهٔ ۲۱۸ یا ۲۲۲ قرمز می‌شود.

## مرورگر — چطور اجرا شد

Playwright سراسری است و `node_modules` مخزن ندارد؛ از پوشهٔ موقت با
`ln -s /opt/node22/lib/node_modules node_modules` کار می‌کند. مسیر واقعی
کرومیوم `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` است (نه
`/opt/pw-browsers/chromium`). ریلِ تب‌ها کلیک‌کردنی نیست؛ مسیرِ مطمئن
`goto(BASE)` و بعد `goto(BASE + '/#spread-radar')` است. سرور آزمون روی
پورت جداگانه (۸۷۹۴) و بعدش بسته شود؛ ۸۷۸۷ دستِ صاحب پروژه است.

**دروازه:** `node tools/check.mjs`
