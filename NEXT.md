# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## وضعیت رصد لحظه‌ای

نقشه اکنون ورودی اصلی است و این مسیر را کامل دارد:

`کل بازار ← نقشه پایه‌ها ← نقشه قراردادهای پایه ← سررسید ← بازه روزانه + زنجیره ← قرارداد`

- نقشهٔ پایه: ارزش کال، ارزش پوت، جمع هر دو، ارزش پایه، حجم اختیار و تغییر پایه.
- نقشهٔ قرارداد: ارزش، حجم، موقعیت باز و تغییر خود قرارداد.
- بازهٔ سررسید: کمترین/بیشترین، اولین/آخرین و پایانی واقعی از `/api/infos`؛
  دریافت دسته‌ای فقط برای سررسید باز و کش مرورگر ۳۰ ثانیه.
- زنجیره: ۳۱ ستون قابل افزودن/حذف و جابه‌جایی، مرتب‌سازی و خروجی.
- همهٔ ۶۸ نمای قبلی و دو تب کامل در «تحلیل‌های تکمیلی» حفظ شده‌اند.

فایل‌های محوری: `ui/live-market-map.mjs`، `ui/tabs/live-market-dashboard.mjs`،
`core/decision-dashboard.mjs`. قفل رفتاری: دستهٔ ۲۴۵.

## پذیرش بعدی در بازار باز

۱. روی دو نماد پرقرارداد و کم‌قرارداد، تعویض نقشهٔ پایه/قرارداد و کلیک
روی خانهٔ کوچک قرارداد را بسنج.

۲. زمان دریافت بازهٔ یک سررسید بزرگ را ثبت کن؛ اگر بیش از چرخهٔ تازه‌سازی
است، سقف نمایش یا صف درخواست باید با دادهٔ واقعی تصمیم‌گیری شود، نه حدس.

۳. تطابق پنج قیمت نمودار بازه با تابلوی رسمی یک کال و یک پوت را بررسی کن.

۴. با پوستهٔ «دفتر» و «تابلو» و عرض موبایل، خوانایی نام خانه‌های کوچک و
نشان‌های اولین/آخرین/پایانی را بازبینی کن.

## کارهای بازِ قبلی

- مقصدهای بیشتر برای نوار پیوند: دیده‌بان زنجیره، تحلیل تاریخی استراتژی و
  موقعیت‌های من؛ پذیرش `state.handoff` باید پیش از افزودن مقصد انجام شود.
- بار بالادست: ابتدا `/api/health.byEndpoint` را در بازار باز اندازه بگیر؛
  بعد دربارهٔ TTL بلند یا کش دیسکی تصمیم بگیر.
- اختلاف دو معاملهٔ `طهرم7061` میان شمارندهٔ رسمی و ریزمعامله هنوز نیاز به
  تعیین منبع مرجع دارد.
- ریزدانگی درون‌روزی بیش از یک روز، تصمیم ذخیره‌سازی صاحب پروژه می‌خواهد.

## قواعدی که نباید شکسته شوند

- عدد گمشده با صفر یا قیمت دیگر پر نشود؛ نمودار بازه فقط پنج قیمت معتبر را
  می‌پذیرد.
- رنگ نقشه معنی جهت دارد و فقط از توکن CSS می‌آید؛ اندازه معنی سنجه دارد.
- اطلاعات پایه، اختیار، سررسید و قرارداد با هم جمع یا جایگزین نشوند.
- تحلیل‌های قدیمی حذف نشوند؛ مسیر اصلی ساده بماند و جزئیات تخصصی تنبل باز شود.

**دروازه:** `node tools/check.mjs`
