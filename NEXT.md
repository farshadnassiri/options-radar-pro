# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### نمایش صادقانه سود و زیان ناموجود در ماشین زمان استراتژی

**چرا.** پس از رفع شبکه شوک، همان الگوی خطرناک در جدول «ماشین زمان» تب
استراتژی باقی مانده است: شرط دستی `x.pnl >= 0`، مقدار ناموجود را زیان قرمز
می‌کند. این جدول نیز از موتور شبیه‌سازی می‌آید و اگر داده یا محاسبه ناقص
بود، باید «—» بی‌رنگ نشان دهد؛ نه سود یا زیان ساختگی.

**شاخه.** با اجازه صریح صاحب پروژه، پس از کامیت قلم جاری همین شاخه
`codex/form-label-accessibility` ادامه داده شود؛ تاریخچه بازنویسی و
force-push نشود.

**فایل‌های محتمل:**

```text
ui/tabs/strategy.mjs
tests/suites/182-strategy-timemachine-missing-pnl.mjs
```

**پذیرش:**

1. سود و زیان متناهی مثبت/منفی/صفر همان رنگ مشترک `signTone` را بگیرد.
2. مقدار `NaN`، بی‌نهایت یا ناموجود با «—» و بدون کلاس/رنگ سود یا زیان
   نمایش داده شود؛ صفر مالی ساخته نشود.
3. جدول ماشین زمان از همان مقدار موتور بخواند و هیچ fallback قیمت یا سود و
   زیان تازه‌ای وارد نشود.
4. آزمون رگرسیون حالت متناهی و ناموجود را قفل کند.

**مرز قلم.** فقط صداقت نمایش سلول سود و زیان ماشین زمان؛ تغییر تحلیل
چندسررسیدی یا فرمول شبیه‌سازی خارج از دامنه است.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md` ثبت و `NEXT.md` برای قلم بعدی بازنویسی شود. دروازه
کامل، کامیت دارای بدنه و پوش روی شاخه کوتاه‌عمر لازم‌اند.
