# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پذیرش پرونده واقعی بزرگ در سرور — اصلاح رگرسیون ذخیره پایان

**چرا.** راستی‌آزمایی مرورگر برش ۱۶۵ پنج مرحله را با تاریخچه واقعی پیمود،
اما بستن جلسه روی `PUT /api/portfolio/dossier` شکست خورد: session دارای
snapshot کامل از سقف عمومی `1048576` بایت بزرگ‌تر بود. در نتیجه پرونده روی
سرور ثبت نمی‌شود و مسیر بازیابی و میان‌بر جلسه بعد، با وجود قرارداد درست،
در داده واقعی قابل استفاده نیست.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/portfolio-time-travel-continuation` ادامه می‌دهد. پیش از قلم تازه
`origin/main` را بررسی کنید، اما تا وقتی تعارضی نیست شاخه را عوض نکنید.

**فایل‌ها:**

```text
server/server.mjs
tests/suites/166-portfolio-dossier-large-body.mjs
```

از `readBody` و `BodyTooLarge` موجود استفاده کنید. نگهبان عمومی یک مگابایت
حذف نشود؛ فقط endpoint پرونده حد جدا، محدود و مستند بگیرد.

**پذیرش:**

1. `PUT /api/portfolio/dossier` بدنه معتبر بیشتر از یک مگابایت را تا سقف
   مخصوص پرونده بپذیرد؛ مقدار سقف نام‌دار و علتش کنار ثابت ثبت شود.
2. سقف تازه از اندازه معقول snapshot واقعی بزرگ‌تر و همچنان محدود باشد؛
   بدنه بزرگ‌تر از آن همان `413` و پیام فارسی را بگیرد.
3. `/api/settings`، مأموریت‌ها، trades و endpointهای دیگر همچنان `MAX_BODY`
   عمومی را مصرف کنند و مجوز سراسری بزرگ نشود.
4. نسخه، شناسه و اعتبارسنجی کامل store پس از خواندن بدنه بدون تغییر باقی
   بمانند؛ افزایش سقف راه دورزدن قرارداد نیست.
5. آزمون HTTP یا هم‌ارز رفتاری نشان دهد پرونده معتبر بزرگ ذخیره و بازیابی
   می‌شود و artifact آزمون را پاک کند.
6. پس از اصلاح، سناریوی مرورگر پرونده واقعی دوباره بسته شود؛ کارت تداوم
   نمایش یابد و کلیک آن سرمایه را پر، نماد و تاریخ را خالی کند.

**مرز قلم.** فشرده‌سازی snapshot، حذف داده از پرونده، افزایش سقف همه
endpointها، نمودار رشد و ذخیره lineage بیرون این اصلاح‌اند.

## محدودیت‌های باز

پس از پذیرش پرونده بزرگ، نوار هشدار موبایل، سرریز `capacity-panel` و
پیمایش تاریخچه واقعی در چند نماد هنوز بازند.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ چون اصلاح رگرسیون است شمار
وظایف فاز را بی‌دلیل زیاد نکنید. دروازه، مرورگر واقعی، کامیت دارای بدنه و
پوش روی همین شاخه لازم‌اند؛ سپس بدون انتظار برای مرج قلم بعد فاز ۷ را آغاز
کنید.
