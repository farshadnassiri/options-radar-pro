# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## فاز ۱۱ — نمودارهای پیشرفته و دانه‌بندی درون‌روزی (خواستهٔ ۱۴۰۵/۰۶/۰۸)

```text
۱  عدسی جمع‌شونده                    ✔ ۲۹ پیکسل بسته، ۲۲۳ باز، با خلاصهٔ یک‌خطی
۲  بیست نمودار تازه + درصد در تولتیپ  ✔ ۳۳ نمودار روی هم؛ shareLine در همه
۳  کلیک روی نمودار ← جدول جزئیات      ✔ کشوی مشترک با سه نما
۴  نقشهٔ حرارتی چندمدله               ✔ ۵ حالت خانه، ۵ ترتیب سطر، ۳ طیف، نقشهٔ افق
۵  تب روند                            ✔ باند چارک، افت مسیر، نرخ برد، رودخانه
۶  تب توزیع                           ✔ توزیع تجمعی، تندی حرکت، جدول چارک‌ها
۷  خروجی اکسل کامل‌تر                 ✔ سیزده برگ، دستهٔ ۲۰۲
۸  دانه‌بندی ۱ تا ۶۰ دقیقه            ✔ core/intraday-grid.mjs، دستهٔ ۲۰۳
۹  سبد چندنمادی                       ✔ کتابخانهٔ اجرا + تقویم مشترک، دستهٔ ۲۰۰-ب
۱۰ پایداری، تندی، پرتعدادی            ✔ چهار سنجه و ده سرخط، دستهٔ ۱۹۹-ب
۱۱ نمودار و جدول بیشتر با کاوش        ✔ در همان تب‌های بازچینی‌شده
۱۲ تب رتبه‌بندی منظم                  ✔ جدول اول، بعد مسابقه، بعد پایداری
۱۳ تب کل به جزء                       ✔ ده نمودار و سه جدول
```

سه نقص را فقط مرورگر پیدا کرد، و هر سه از جنسی بودند که آزمون منبع
نمی‌گیردشان:

  • حلقهٔ ناظر اندازه: `resize()` خودش بوم را عوض می‌کرد و ناظر دوباره
    شلیک می‌شد. با بیست‌وچند نمودار، نخِ اصلی چنان اشغال شد که هر فرمان
    بعدی بیش از نود ثانیه بی‌پاسخ ماند — و صفحه ظاهراً سالم بود.
  • نمودار نیرومحور، انیمیشن تمام‌نشدنی داشت و در پنل بسته هم می‌چرخید.
  • فهرست بازه در حالت درون‌روزی «—» می‌داد، چون کلیدِ لحظه چهارده‌رقمی
    است و تقویم جلالی رویش برچسبی ندارد.

راه‌انداز CDP هم درست شد: هر اجرا برگهٔ تازه باز می‌کند و هر فرمان مهلت
دارد. برگهٔ به‌جامانده از اجرای پیشین، `Runtime.enable` را بی‌صدا تا ابد
منتظر می‌گذاشت.

سایت نمونه‌های ECharts از این ماشین بسته است؛ فهرست انواع نمودار از خودِ
بستهٔ ۶٫۱٫۰ درآمد که از گالری کامل‌تر است.

قلم بعدی: همان قلم فاز ۱۰ — اجرای واقعی با نماد و بازهٔ خود کاربر.

## مرز ادامه

در دامنهٔ فعلی کار دیگری روی تب‌های دیگر انجام نشود.

**دروازه:** `node tools/check.mjs`
