# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### چیدمان مشترک آزمون سبد — بدهی فنی که دارد بزرگ می‌شود

**چرا.** دسته‌های ۱۲۸، ۱۲۹ و ۱۳۰ هر سه یک چیدمان را کپی کرده‌اند: همان
جلسه، همان قراردادها، همان دفتر سفارش، همان مأموریت. هر تغییری در شکل
جلسه باید سه جا انجام شود و اولین باری که یکی جا بماند، دو دسته دو چیز
متفاوت می‌سنجند و هیچ‌کدام قرمز نمی‌شود. قلم بعدیِ فاز ۳ (پیشنهاد UI) هم
همین چیدمان را لازم دارد و کپی چهارم را نباید ساخت.

این قلم عمداً **پیش از** ادامهٔ فاز ۳ آمده: بدهی چیدمان با هر دستهٔ تازه
گران‌تر می‌شود، نه ارزان‌تر.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
tests/fixtures/portfolio.mjs             چیدمان مشترک — تازه
tests/suites/128-portfolio-evaluation.mjs
tests/suites/129-portfolio-score.mjs
tests/suites/130-portfolio-ranking.mjs
tests/guards.mjs                         نگهبان تازه، بند ۵ زیر
```

**پذیرش:**

1. یک ماژول چیدمان که جلسه، قراردادها، دفتر سفارش، مدرک اجراپذیری،
   مأموریت (با دید و ریسک قابل تنظیم) و طرح‌های ورود/سرمایه را **یک بار**
   می‌سازد و صادر می‌کند. تابع باشد نه شیء ثابت، تا هر دسته بتواند دید
   بازار و قیود ریسکش را عوض کند بدون اینکه روی دستهٔ دیگر اثر بگذارد.
2. هر سه دستهٔ ۱۲۸ و ۱۲۹ و ۱۳۰ از همین ماژول بخوانند و **هیچ‌کدام شمار
   ادعایش کم نشود**: ۳۴ و ۲۰ و ۲۲، جمعاً ۷۶. اگر ادعایی افتاد، یعنی
   چیدمان معادل نیست.
3. خروجی `node tools/check.mjs` پیش و پس از تغییر همان ۲۶۳۷ ادعای سبز
   باشد. این قلم رفتار عوض نمی‌کند؛ فقط تکرار را برمی‌دارد.
4. چیدمان مشترک نباید به `tests/harness.mjs` اضافه شود — harness ابزار
   ادعاست، نه انبار داده. `tests/fixtures/` جای تازه و صریح است.
5. یک نگهبان تازه در `tests/guards.mjs` که اگر دسته‌ای دوباره چیدمان سبد
   را کپی کرد قرمز شود. ساده‌ترین شکلش: هیچ دسته‌ای جز خودِ fixture نباید
   `createPortfolioMission` و `portfolioCandidates` را با هم import کند.
   نگهبان باید علتش را در متن پیامش بگوید، نه فقط رد شود.
6. بارگذار و بودجهٔ خواندن دست‌نخورده بمانند: `tests/run.mjs` همچنان فقط
   `tests/suites/` را می‌خواند و `tests/fixtures/` را دسته حساب نکند.

**مرز قلم.** هیچ رفتار محصولی عوض نمی‌شود. اگر وسط کار به باگی خوردید،
یادداشتش کنید و در `NEXT.md` بگذارید — همین‌جا اصلاحش نکنید.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
