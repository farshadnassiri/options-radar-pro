# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### مدل رشد سرمایه چندجلسه‌ای و کارت روند — برش ششم فاز ۷

**چرا.** پرونده دوم اکنون lineage معتبر و بازیابی‌پذیر هر دو سفر را دارد،
اما کارت فقط ردیف‌های خام شروع و پایان را فهرست می‌کند. کاربر هنوز روند رشد،
افت یا سرمایه بدون تغییر را در یک قرارداد تحلیلی واحد نمی‌بیند و مقایسه دستی
اعداد می‌تواند به محاسبه اشتباه منجر شود.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
core/portfolio-capital-continuity.mjs
core/portfolio-capital-growth.mjs
ui/portfolio-capital-growth-view.mjs
ui/tabs/portfolio-time.mjs
ui/style.css
tests/suites/168-portfolio-capital-growth.mjs
```

همان `validatePortfolioCapitalContinuity` منبع حقیقت ورودی است؛ parser یا
lineage موازی نسازید.

**پذیرش:**

1. مدل هسته فقط continuity معتبر را بپذیرد و خروجی نسخه‌دار با یک ردیف برای
   هر سفر، سرمایه شروع، تحقق‌یافته و نهایی همان ردیف بسازد.
2. تغییر هر سفر و تغییر تجمعی نسبت به سرمایه شروع نخست در هسته محاسبه شود؛
   UI حق جمع، تفریق یا محاسبه درصد مالی ندارد.
3. وضعیت رشد، افت و بدون‌تغییر صریح باشد؛ سرمایه شروع صفر یا مقدار نامعتبر
   درصد ساختگی نسازد و علت را نگه دارد.
4. ترتیب lineage قدیم به جدید، شناسه جلسه/سبد/نماد و لحظه بستن در مدل و
   کارت حفظ شود؛ هیچ dossier دیگری با حدس زمانی جایگزین نشود.
5. پرونده مستقل یک نقطه معتبر می‌سازد؛ پرونده دوم بازیابی‌شده دو نقطه و
   continuity سه‌سفره سه نقطه بسازد.
6. کارت روند در هر دو پوسته و موبایل خوانا باشد، رنگ فقط از توکن بیاید و
   همه عددهای نمایشی با formatter مشترک و رقم فارسی چاپ شوند.
7. continuity خراب fail-closed باشد و کارت یا درصد نیمه‌معتبر نسازد؛ آزمون
   هسته، مدل نمایش و اتصال پرونده بازیابی‌شده را پوشش دهد.

**مرز قلم.** رتبه‌بندی بین سفرها، پیش‌بینی سرمایه آینده، تغییر سقف مأموریت
active و فشرده‌سازی snapshot بیرون این برش‌اند.

## محدودیت‌های باز

- ثبت مرحله `active` مأموریت واقعی با snapshot بزرگ هنوز از سقف عمومی یک
  مگابایت رد می‌شود؛ افزایش سراسری مجاز نیست و قرارداد جدا لازم دارد.
- نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه واقعی در چند
  نماد هنوز بازند.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم ششمین وظیفه
از هفت وظیفه فاز ۷ است؛ پس از پذیرش سهم فاز ۷ باید از ۵٪ به ۶٪ و پیشرفت کل
از ۹۳٪ به ۹۴٪ برسد. دروازه کامل، کامیت دارای بدنه و پوش روی همین شاخه
لازم‌اند؛ سپس بدون انتظار برای مرج قلم بعد را آغاز کنید.
