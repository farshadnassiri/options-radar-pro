# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### کارت سرمایه و هدف در پرونده — برش ششم فاز ۶

**چرا.** موتور تحلیل سرمایه نهایی و فاصله از هدف ساخته شده، اما پروندهٔ
تب هنوز آن را نشان نمی‌دهد. عددهای صادق باید کنار پرونده زنده و پرونده
خوانده‌شده از سرور یکسان دیده شوند و حالت نامعلوم علت خودش را داشته باشد.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
ui/tabs/portfolio-time.mjs
ui/style.css
ui/portfolio-dossier-analysis-view.mjs    فقط اگر قرارداد نمایش کم دارد
tests/suites/158-portfolio-dossier-analysis-ui.mjs
```

از `portfolioDossierAnalysis` و `portfolioDossierAnalysisView` موجود مصرف
کنید؛ تب حق محاسبه سرمایه، بازده یا فاصله را ندارد.

**پذیرش:**

1. پرونده زنده و پرونده بازشده از سرور، کارت یکسان سرمایه شروع، تحقق‌یافته،
   سرمایه نهایی، مبنای هدف، بازده، هدف و فاصله نشان دهند.
2. سرمایه نهایی یا بازده نامعلوم «—» باشد و علت‌های ساختاری تحلیل زیر کارت
   دیده شوند؛ صفر یا «هدف محقق نشد» برای حالت نامعلوم ساخته نشود.
3. لحن فاصله مثبت/منفی فقط از مدل نمایش بیاید و رنگ فقط از توکن‌های موجود
   باشد.
4. همه رقم‌ها فارسی و واحد پول تومان باشد؛ تب روی عدد ریالی حساب نکند.
5. کارت در موبایل بدون اسکرول افقی ستون‌شکن شود و کنترل تعاملی تازه نسازد.
6. همان `paintDossier` مشترک هر دو مسیر را رسم کند؛ مسیر موازی برای پرونده
   بازیابی‌شده ممنوع است.

راستی‌آزمایی مرورگر لازم است، ولی مرورگر داخلی در قلم‌های ۱۵۵ و ۱۵۶ با
`failed to write kernel assets` وصل نشد. اگر محیط درست شد، حالت کامل و
نامعلوم را در دسکتاپ و عرض ۳۹۰ پیکسل ببینید؛ وگرنه محدودیت را ثبت کنید.

## محدودیت‌های باز

استخراج ضعف‌های رفتاری و مقایسه چند پرونده هنوز ساخته نشده‌اند. نوار هشدار
موبایل، سرریز `capacity-panel` و پیمایش ویزارد روی تاریخچه واقعی نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
