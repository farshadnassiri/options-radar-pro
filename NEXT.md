# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### گام زمانی در تب — برش ششم فاز ۵

**چرا.** موتورها آماده‌اند: ساعت جلو می‌رود، عکسِ هر لحظه ساخته می‌شود،
و همهٔ موتورها روی لحظهٔ دلخواه کار می‌کنند (دستهٔ ۱۴۴). ولی هیچ‌کدام از
رابط صدا زده نمی‌شوند — تب هنوز جلسه را در لحظهٔ شروع میخ‌کوب نگه داشته.
این آخرین حلقه است تا سفر زمانی واقعاً کار کند.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
ui/tabs/portfolio-time.mjs          نوار گام زمانی بالای بخش‌ها
ui/portfolio-clock-view.mjs         مدل نمایش پله‌ها و لحظهٔ جاری
tests/suites/145-portfolio-clock-ui.mjs
```

`node tools/next.mjs map stepPortfolioSession`. پله‌ها را از
`PORTFOLIO_STEPS` بگیرید نه فهرست دستی. **ساختنِ عکسِ لحظهٔ تازه لازم
است** — `portfolioMomentSnapshot` داده می‌خواهد و تب باید از همان
`gateLoaders()` بگیرد که برای عکس شروع می‌گیرد.

**پذیرش:**

1. لحظهٔ جاری جلسه همیشه دیده شود، با تاریخ و ساعت فارسی.
2. هر پله دکمهٔ خودش را داشته باشد؛ پله‌ای که ممکن نیست **غیرفعال** شود
   با علت، نه اینکه بزند و خطا بدهد.
3. پس از گام، هر چهار بخش (سرمایه، موقعیت‌ها، پیشنهادها، حکم اجراپذیری)
   با جلسه و عکسِ تازه دوباره رسم شوند.
4. شکستِ گام با علت خوانا دیده شود؛ «تقویم تمام شد» و «از پایان جلسه رد
   می‌شود» دو پیام جدا بگیرند.
5. اگر عکسِ لحظهٔ تازه ناکافی باشد، **صریح گفته شود** — نه اینکه
   جدول‌های خالی شبیه «هیچ فرصتی نیست» دیده شوند.
6. رقم فارسی؛ یک ادعا نبود رقم لاتین را بسنجد.

**نام‌گذاری:** یکتایی شناسه‌ها را پیش از انتخاب بسنجید (درسِ برش دوم
فاز ۴).

**راستی‌آزمایی مرورگر لازم است** (۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴).

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سرمایه و پیشنهاد ربطی ندارد
(هر دو در ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ سنجیده شدند و سرریز ندارند)، ولی یک بار
باید دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

بخش دفتر سرمایه در مرورگر با بازکردن دستی `#pt-review-step` سنجیده شد،
چون بدون تاریخچهٔ واقعی نمی‌شود جلسه را فعال کرد. چیدمان و سرریز واقعی
سنجیده شدند؛ مسیر رسیدنِ کاربر به آن هنوز با دادهٔ واقعی دیده نشده.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
