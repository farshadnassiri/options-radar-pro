# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### قرارداد نسخه‌دار دادهٔ Excel پرونده — برش نخست فاز ۷

**چرا.** پرونده اکنون روی صفحه کامل و مقایسه‌پذیر است، ولی خروجی Excel
نباید با خواندن DOM یا ساخت دوباره حسابداری ساخته شود. نخست باید یک مدل
نسخه‌دار و خالص از برگ‌ها تثبیت شود تا دانلود، آزمون و مصرف هوش مصنوعی
همگی دقیقاً یک قرارداد را بخوانند.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
ui/portfolio-dossier-export.mjs
tests/suites/162-portfolio-dossier-export.mjs
```

از `sheet` و `sheetParts` موجود در `ui/xlsx.mjs` استفاده کنید. این برش فقط
توصیف برگ‌های دفترکار را می‌سازد؛ دکمه و دانلود در قلم بعد می‌آیند.

**پذیرش:**

1. سازنده فقط جلسه بسته و پرونده نسخه‌دار هم‌هویت را بپذیرد و خروجی خودش
   شماره نسخه، شناسه جلسه و زمان ساخت صریح داشته باشد.
2. برگ سرشناسه، مأموریت قفل‌شده، خلاصه سرمایه/حسابداری، ردیف‌های تحقق‌یافته،
   تعهدهای باز، هشدارها و یافته‌های مستند جدا باشند؛ داده خام از قلم نیفتد.
3. عددهای مالی در خانه‌های عددی ریال بمانند و واحد در سرستون نوشته شود؛
   درصد نیز عدد خام با سرستون درصد باشد، نه رشته قالب‌شده فارسی.
4. مقدار نامعلوم به خانه خالی (`NaN` در قرارداد xlsx موجود) تبدیل شود، نه
   صفر یا خط تیره. علت نامعلوم در ستون متنی مجاور بماند.
5. تحلیل سرمایه و ضعف فقط از ماژول‌های موجود مصرف شوند؛ قیمت تازه، fetch،
   DOM و محاسبه مالی موازی ممنوع است.
6. ترتیب برگ‌ها، سرستون‌ها و ردیف یافته‌ها پایدار باشد تا دو فایل قابل diff
   باشند؛ کدهای فنی برای مصرف ماشین در خروجی حفظ شوند.

این برش خالص و بدون UI است و راستی‌آزمایی مرورگر ندارد. خود ساختار برگ‌ها
و خانه‌های خالی را با آزمون مستقیم بسنجید.

## محدودیت‌های باز

دکمه دانلود، نمودار رشد سرمایه و انتقال سرمایه نهایی پرونده به جلسه بعدی
هنوز در قلم‌های بعد فاز ۷ هستند. نوار هشدار موبایل، سرریز `capacity-panel`
و پیمایش تاریخچه واقعی نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
