# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### دفتر سرمایه در تب — برش دوم فاز ۴

**چرا.** `core/portfolio-ledger.mjs` می‌گوید چقدر سرمایه درگیر است، چقدر
آزاد مانده، و کدام قید ریسک نزدیک شکستن است — ولی کاربر هیچ‌کدام را
نمی‌بیند. بدتر: وقتی ثبتی به‌خاطر شکستن قید رد می‌شود، فقط یک جملهٔ خطا
می‌بیند بدون اینکه بداند چقدر فاصله داشت.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
ui/portfolio-ledger-view.mjs        مدل نمایش از خروجی دفتر
ui/tabs/portfolio-time.mjs          نوار وضعیت سرمایه بالای پیشنهادها
tests/suites/134-portfolio-ledger-view.mjs
```

`node tools/next.mjs map portfolioCapitalLedger`. چیدمان آزمون را از
`tests/fixtures/portfolio.mjs` بگیرید — نگهبان ۱۰ کپی‌اش را قرمز می‌کند.
الگوی لایهٔ نمایش را از `ui/portfolio-proposals.mjs` بردارید، از جمله
ادعایی که هر حساب روی عدد ریالی جز تقسیم بر ده را ممنوع می‌کند.

**پذیرش:**

1. مدل نمایش فقط از خروجی معتبر `portfolioCapitalLedger` ساخته شود و
   **هیچ عدد مالی تازه‌ای** محاسبه نکند. تنها حسابِ مجاز تقسیم بر ده است.
2. رقم فارسی و تومان در همه‌جا؛ یک ادعا نبود رقم لاتین در متن نمایشی را
   بسنجد.
3. برای هر قید ریسک، **فاصله تا شکستن** دیده شود نه فقط وضعیت فعلی —
   کاربر باید بفهمد چقدر جا مانده، پیش از اینکه ثبتش رد شود.
4. اجزای سرمایه (بدهکار، کارمزد، وجه تضمین) و تفکیک خانواده جدا نمایش
   داده شوند، با نام خوانا نه شناسهٔ خام.
5. رویدادهای بی‌عدد (`unpriced`) صریح دیده شوند. پنهان‌کردنشان یعنی جمعِ
   نمایش‌داده‌شده کمتر از واقعیت است و کسی نمی‌فهمد.
6. جلسهٔ بدون ثبت یا بدون مأموریت، پیام صریح بدهد نه نوار خالی یا صفر.

**راستی‌آزمایی مرورگر لازم است** (۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴): بدون سرریز افقی
صفحه و بدون خطای کنسول.

---

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش پیشنهادها ربطی ندارد، ولی یک بار باید
دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
