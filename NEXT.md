# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## وضعیت رصد لحظه‌ای

مسیر اصلی اکنون چنین است:

`کل بازار ← نقشه پایه‌ها ← پایه ← نقشه قراردادها ← سررسید ← بازه تعاملی + زنجیره ← قرارداد`

بلافاصله زیر این مسیر، «نبض زنده بازار» هفت نمای فشرده دارد:

۱. محدوده تغییر آخرین معامله همه پایه‌ها با انتخاب نقطه و خلاصه همان نماد.
۲. روند ارزش پنج ردیف سفارش خرید/فروش پایه‌ها؛ فقط با پوشش کامل دفتر.
۳. روند تعداد پایه‌های مثبت و منفی معامله‌شده.
۴. تراز موقعیت باز کال/پوت و تغییر نسبت به دیروز.
۵. شدت معاملات اختیار و جمع ارزش کال، پوت و کل امروز.
۶. روند ارزش بهترین سفارش خرید/فروش اختیار به تفکیک کال و پوت.
۷. روند موقعیت باز کال و پوت اختیار سهام.

شدت از اختلاف عکس‌های متوالی ساخته می‌شود، تاریخچه کوتاه در `sessionStorage`
می‌ماند و درخواست دفتر پایه بیشتر از هر ۳۰ ثانیه تکرار نمی‌شود. فایل‌های محوری:
`core/live-market-pulse.mjs`، `ui/live-market-pulse.mjs`،
`ui/tabs/live-market-dashboard.mjs` و `ui/live-market-map.mjs`. قفل رفتاری:
دسته ۲۴۶؛ نقشه و مسیر قبلی همچنان با دسته ۲۴۵ قفل است.

## پذیرش بعدی در بازار باز

۱. طی دست‌کم ۲۰ دقیقه بازار باز، تغییر تعداد نقاط، شدت ارزش کال/پوت و تغییر
واقعی سفارش‌ها را بسنج؛ هیچ شدت منفی ناشی از ریست شمارنده نباید دیده شود.

۲. جمع پنج ردیف سفارش پایه را برای دو پایه پرمعامله با تابلوی رسمی تطبیق بده
و زمان تکمیل ۲۳ دفتر را از `/api/health.byEndpoint` ثبت کن. اگر به چرخه‌های
دیگر فشار می‌آورد، چرخش پایه‌ها باید با برچسب «پوشش جزئی» طراحی شود؛ جمع جزئی
نباید به‌عنوان کل نمایش داده شود.

۳. تعریف «ارزش سفارش اختیار» را با نیاز صاحب پروژه قطعی کن: پیاده‌سازی فعلی
`قیمت × تعداد × اندازه قرارداد` برای بهترین سفارش هر قرارداد است. اگر مقصود
ارزش هر پنج ردیف باشد، منبع عمق قراردادها و بودجه درخواست باید پیش از گسترش
اندازه‌گیری شود.

۴. تغییر موقعیت باز کال و پوت را روی روزی که `yesterdayOP` واقعاً تغییر دارد
با منبع رسمی یک کال و یک پوت تطبیق بده.

۵. بازه تعاملی را روی یک قرارداد مثبت، یک منفی و یک بازه تک‌قیمتی در هر دو
پوسته «دفتر» و «تابلو» بررسی کن؛ پنجره جزئیات نباید از کارت بیرون بزند.

## کارهای باز قبلی

- مقصدهای بیشتر برای نوار پیوند: دیده‌بان زنجیره، تحلیل تاریخی استراتژی و
  موقعیت‌های من؛ پذیرش `state.handoff` باید پیش از افزودن مقصد انجام شود.
- اختلاف دو معامله `طهرم7061` میان شمارنده رسمی و ریزمعامله هنوز نیاز به
  تعیین منبع مرجع دارد.
- نگهداری تاریخچه نبض پس از بسته‌شدن مرورگر یا برای بیش از یک روز، تصمیم
  ذخیره‌سازی صاحب پروژه می‌خواهد؛ فعلاً تاریخچه فقط جلسه مرورگر است.

## قواعدی که نباید شکسته شوند

- اعداد نمونه سایت مرجع یا عدد گمشده هاردکد نشوند؛ داده ناقص باید «—» بماند.
- رنگ‌ها فقط از توکن CSS بیایند و جمع پایه، اختیار و موقعیت باز قاطی نشوند.
- درخواست دفتر سفارش نباید سهمیه بازار را اشباع کند؛ اول اندازه‌گیری، بعد سقف.
- همه نماهای قدیمی در «تحلیل‌های تکمیلی» حفظ شوند و مسیر اصلی ساده بماند.

**دروازه:** `node tools/check.mjs`
