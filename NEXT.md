# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## سه دور گزارش — هر ۲۴ ایرادِ رصد زنده، و جدولِ ناخوانا

هجده ایرادِ دورِ اول (`47bce1e`)، شش ایرادِ دورِ دوم (`c03e06d`)، و سه
علتِ «جدول قابل خوندن نیست» در دورِ سوم. همه بسته شد؛ شرحِ کاملشان در سه
ورودیِ بالای `WORKLOG.md`.

نقشهٔ کدِ تازه: `core/live-quote.mjs` (سهمیه، چرخش، دو منبعِ مظنه،
تعریفِ «زنده») · `core/day-range.mjs` (کف و سقفِ امروز) ·
`ruleCoverage`/`ruleScopeUnion`/`watchDistance` در `core/watch-rule.mjs` ·
`expiryShortfall` در `core/radar-history.mjs` · `deliverBurst` در
`ui/gap-alarm.mjs` · `rowHeightFrom` در `ui/table.mjs`.
آزمون: دسته‌های ۲۱۹، ۲۲۴، ۲۲۸، ۲۲۹، ۲۳۰ و ۲۳۱.

**بازطراحی ظاهر دوباره خواسته شد** — یادداشتِ «لغو شد، سراغش نروید» دیگر
معتبر نیست. پوسته از شیشه به کاغذ رفت (شرح در `WORKLOG.md`). آنچه عمداً
دست‌نخورده ماند: **مقیاسِ قلم** — بزرگ‌بودنش تصمیمِ خوانایی بوده نه
سلیقه، و کوچک‌کردنش باید با گزارشِ خوانایی بیاید نه با میلِ «مینیمال».

## درسِ دو نوبتِ گذشته — پیش از نوشتنِ آزمون بخوانید

دورِ دوم شش ایراد داشت و دورِ سوم سه‌تا، در حالی که دروازه هر بار سبز
بود. دو ریشه داشت:

**۱. ادعای منبع، آزمونِ رفتار نیست.** `check('…', src.includes('x'))`
وقتی هم سبز می‌ماند که `x` غلط ساخته شده باشد. هر جا شد، منطق را به
تابعی خالص در `core/` ببرید و رفتار را بسنجید.

**۲. هیچ آزمونی چشم ندارد.** «جدول ناخوانا» را هیچ ادعای متنی نمی‌گرفت —
شکستنِ فونت، برشِ عدد و ارتفاعِ غلطِ ردیف فقط با **عکس گرفتن در مرورگر**
دیده شدند. برای هر کارِ ظاهری، اول در مرورگر عکس بگیرید و نگاه کنید؛
آزمون بعد از دیدن نوشته می‌شود، نه به‌جای آن.

## کار — قلم بعدی را از اینجا بردارید

### الف. دانه‌بندی درون‌روزی روی بیش از یک روز

امروز فقط یک روز است؛ علتش در `ui/tabs/spread-radar.mjs` نوشته شده.
راهِ درست، کش‌کردنِ ریزمعامله روی دیسک است — مثل `data/roster` — نه بالا
بردن سقف درخواست. انگیزهٔ دوم: «کف و سقفِ امروز» فعلاً فقط از مشاهده‌های
خودِ رصد می‌آید (`core/day-range.mjs`)؛ با کشِ دیسکی می‌تواند کفِ واقعیِ
جلسه شود و نامش از «مشاهده‌شده» دربیاید.

### ب. پوستهٔ مینیمال، دورِ دوم

دورِ اول در سطح توکن بود، پس همه‌جا یکدست عوض شد: سطحِ مات، سایهٔ فقط
برای چیزِ شناور، گردیِ جمع‌تر، دکمهٔ گوشه‌دار، رنگِ داغِ یک‌چهارم. آنچه
هنوز جای کار دارد و توکن حلش نمی‌کند:

* **چگالی.** `body` روی `--fs-xl` است (۱۸px) — برای صفحهٔ پرجدول بزرگ.
  راهِ درست یک توکنِ «چگالی» است که فقط صفحه‌های جدولی بخوانند، نه
  پایین‌آوردنِ کلِ مقیاس.
* **`.gap-skin` و `.pb-skin`.** حالا که پوستهٔ پایه مینیمال است، بیشترِ
  قاعده‌های این دو تکراری‌اند — ولی پاک‌کردنشان یکی‌یکی و با عکسِ
  قبل و بعد.

### ج. کهنگیِ دفترِ سفارش

منبعِ دفتر «قابل اجرا» است ولی **زمان ندارد**: تابلو برای سطوحِ دفتر مهرِ
زمان نمی‌دهد، پس نمادِ متوقف هم سفارشِ باقی‌مانده نشان می‌دهد.
`/api/infos` برای هر ابزار `state` و `staleSec` می‌دهد؛ وصل‌کردنش به
حلقهٔ رصد تنها ادعای بی‌پشتوانهٔ این منبع را می‌بندد.

### د. عمقِ بیش از سطح اول

`comboBookQuote` فقط سطح اول را می‌خواند. برای حجم بزرگ، میانگینِ اجرا از
چند سطح درمی‌آید و `/api/books` هر پنج سطح را می‌دهد. ریاضی‌اش در
`core/book-history.mjs` هست؛ همان باید استفاده شود، نه نسخهٔ دوم.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی). دو دورِ گذشته
نشان داد بازپخشِ واقعی چیزی می‌بیند که دروازه نمی‌بیند:

۱. **مقیاس.** مقیاس را عوض کن؛ درصدِ پرشدگی نباید تکان بخورد.
۲. **استرانگل فروش.** «لنگر» = دهانهٔ اعمال، «حداکثر زیان» = نامحدود نه
عدد، «حداکثر سود ٪» بر وجه تضمین.
۳. **دنبالهٔ امروز.** خطِ نقطه‌چینِ امروز روی روند گذشته، با برچسب «اکنون».
۴. **منبعِ دفتر.** عددِ فاصله را با دفترِ تابلو بسنج: پای خریدنی روی
عرضه، پای فروختنی روی تقاضا. «کمینه عمق» را ده بگذار.
۵. **چرخش.** با چند صد ترکیب، «ترکیب دارای عددِ زنده» باید تیک‌به‌تیک تا
کلِ دامنه بالا برود و با طولِ دورِ اعلام‌شده بخواند.
۶. **جدول، با چشمِ خودت.** بازپخشِ این نوبت دادهٔ ساختگی داشت. با دادهٔ
واقعی ببین «سود در برابر زیان» و «پر شده / باقی‌مانده» در عرضِ ستونِ
خودت خوانا می‌مانند و اسکرول تا ردیف آخر می‌رسد.
۷. **چند قاعده، چند دامنه.** دو قاعده روی دو نماد بساز و دامنهٔ یکی را
بساز: نوار باید «۱ از ۲ قاعده» بگوید و کارتِ آن یکی «در این ساخت داده
ندارد».

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. پای بی‌قیمت فاصله را باطل می‌کند، پاهای
ناهم‌زمان ترکیبِ زنده نمی‌سازند، پایی که یک سمتِ دفترش خالی است در آن
جهت اجرا نمی‌شود، عددِ زندهٔ پوسیده به عددِ روز سنجش برمی‌گردد، «کف
امروز» بی مشاهدهٔ امروز عدد ندارد، قیمتِ پایهٔ نداشته `NaN` می‌ماند، و
قاعده‌ای که داده ندارد «در حال رصد» خوانده نمی‌شود.

اگر یکی شکست، یکی از دسته‌های ۲۱۸ تا ۲۳۱ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴ و پس از کار بسته شود؛ ۸۷۸۷ دستِ صاحب پروژه است. کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`، Playwright سراسری:
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
برای کارِ ظاهری، صفحهٔ آزمایشیِ کوچکی با ردیف‌های ساختگی بساز، عکس بگیر و
نگاه کن — بعد آزمون بنویس.
