# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### دانلود XLSX پرونده از کارت پایان — برش دوم فاز ۷

**چرا.** قرارداد هشت برگ آماده و آزمون‌شده است، اما کاربر هنوز راهی برای
گرفتن فایل ندارد. دکمه باید همان سند خامی را که `paintDossier` مشترک برای
پرونده زنده و بازیابی‌شده دارد به سازنده بدهد و خروجی را با نویسنده xlsx
موجود دانلود کند؛ مسیر دوم گزارش ممنوع است.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
ui/portfolio-dossier-export.mjs
ui/tabs/portfolio-time.mjs
ui/style.css
tests/suites/163-portfolio-dossier-export-ui.mjs
```

یک تابع کوچک دانلود کنار سازنده اضافه کنید که فقط `downloadXlsx` موجود را
مصرف کند. مدل برگ‌ها را در تب بازسازی نکنید.

**پذیرش:**

1. دکمه «دانلود Excel پرونده» فقط وقتی مدل پرونده معتبر رسم شده فعال باشد
   و برای پرونده زنده و بازیابی‌شده از همان `paintDossier` مشترک کار کند.
2. کلیک دقیقاً `portfolioDossierWorkbook` و سپس `downloadXlsx` موجود را
   مصرف کند؛ تب برگ، حساب مالی یا JSON موازی نسازد.
3. نام فایل پایدار و امن، شامل نماد پایه و تاریخ بستن باشد؛ پسوند را
   `downloadXlsx` اضافه کند و شناسه خام مسیر فایل نشود.
4. هنگام ساخت فایل دکمه غیرفعال و متن وضعیت روشن باشد؛ شکست ساخت/دانلود
   پرونده روی صفحه را خراب نکند و تلاش دوباره ممکن بماند.
5. هیچ fetch تازه‌ای برای خروجی انجام نشود؛ همه داده از `view.session` و
   `view.dossier` همان پرونده بیاید.
6. کنترل در موبایل عرض کامل، دسترس‌پذیر و بدون سرریز باشد.

راستی‌آزمایی مرورگر لازم است: پرونده بازیابی‌شده را در عرض ۳۹۰ پیکسل باز و
فعال‌شدن دکمه را ببینید. اگر اتصال داخلی همچنان با خطای kernel assets
شکست خورد، محدودیت را ثبت کنید و آزمون DOM/منبع را نگه دارید.

## محدودیت‌های باز

نمودار رشد سرمایه و انتقال سرمایه نهایی پرونده به جلسه بعدی هنوز در قلم‌های
بعد فاز ۷ هستند. نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه
واقعی نیز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
