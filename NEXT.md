# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## دورهٔ جاری: ارتقای ظاهر (خواستهٔ صاحب پروژه، ۱۴۰۵/۰۶/۲۸)

«مهارت ui/ux را استفاده کن و پروژه را از نظر ظاهری ارتقا بده؛ خیلی بیسیک
است.» این کار **ساعتی** ادامه دارد: هر نوبت یک قلمِ ظاهری، با شاهدِ
مرورگر، آزمون رگرسیون، دروازهٔ سبز و پوش.

### چطور شاهد بگیرید (هیچ وابستگی npm لازم نیست)

```bash
PORT=8787 node server/server.mjs &         # بازار بسته باشد هم رابط بالا می‌آید
```

سپس یک اسکریپت کوتاه با playwright که روی همین ماشین هست:

```js
const { chromium } = await import('/opt/node22/lib/node_modules/playwright/index.mjs');
const browser = await chromium.launch({ executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome' });
const page = await browser.newPage({ viewport: { width: 1600, height: 1000 } });
await page.goto('http://127.0.0.1:8787/ui/index.html', { waitUntil: 'networkidle' });
await (await page.$('#rail-list >> text=موقعیت‌های من')).click();   // نام تب
await page.screenshot({ path: '/tmp/shot.png' });
```

**قاعدهٔ این دوره:** ادعای ظاهری بی‌اندازه‌گیری نکنید. «به نظر تخت
می‌آید» کافی نیست؛ `getComputedStyle` را بپرسید. قلمِ قبلی دقیقاً همین‌طور
پیدا شد: کادرِ هر `.ghost` در مرورگر `rgba(0, 0, 0, 0)` بود در حالی که
شیوه‌نامه کادر داشت.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۶/۲۸)

**لایهٔ مردهٔ «پوستهٔ شیشه‌ای» برداشته شد.** توکن‌های `--glass-*` روزی خنثی
شدند ولی قاعده‌هایشان — که عمداً آخرِ شیوه‌نامه بودند — ماندند و لبهٔ پایه
را بی‌صدا می‌پوشاندند. نتیجه همان «بیسیک» بودن بود: هیچ دکمهٔ ثانویه‌ای
کادر نداشت، و `box-shadow: var(--glow-accent), inset …` چون با `none`
شروع می‌شد کلِ اعلان را بی‌اعتبار می‌کرد. دستهٔ ۲۷۱ کلاس خطا را قفل
می‌کند، دستهٔ ۲۱۹ وارونه شد. شرحِ کامل در `WORKLOG.md`.

## قلم بعدی — ردیفِ کارتِ عدد در «موقعیت‌های من»

نماگرفت (۱۶۰۰ پیکسل، تب «موقعیت‌های من») سه ایراد نشان می‌دهد و هر سه
اندازه‌پذیرند:

۱. **برچسبِ دوخطی، عددِ پلکانی.** هفت کارت در یک ردیف جا داده شده‌اند، پس
   «سرمایه روز ورود» و «وجه تضمین امروز» دو خط می‌شوند و کارت‌ها ارتفاع
   نابرابر می‌گیرند. کمینهٔ عرضِ ستون را بالا ببرید تا ردیف بشکند، نه
   برچسب. (`.kpi` خودش برای همین `grid-template-rows: auto 1fr` دارد —
   مشکل در ظرفِ ردیف است، نه در کارت.)
۲. **کارتِ «قیمت‌گیری»** متنِ «بی‌قیمت — قیمت‌گیری نشد» را در جای عدد و با
   اندازهٔ عدد می‌نشاند و از کارت بیرون می‌زند. متنِ وضعیت، عدد نیست:
   جایش `.kpi .s` است یا یک نشانِ وضعیت.
۳. **صفرِ بی‌واحد.** «۰» تنها، بدون «ریال»، کنار کارتی که واحدش را
   می‌گوید. واحد یا همه‌جا هست یا هیچ‌جا.

قاعده‌ها سرِ جایشان: رنگ فقط از توکن CSS، هر عدد نمایشی از `ui/fmt.mjs`
با رقم فارسی، و یک آزمون رگرسیون برای هر اصلاح.

## صفِ ظاهریِ بعد از آن (به ترتیب اثر)

- ~~**آیکونِ ناهمگونِ ایموجی**~~ — بسته شد (دستهٔ ۲۷۳): دکمه‌های پیوندِ
  استراتژی و خانهٔ «شرط» جدول موقعیت‌ها آیکونِ خانواده گرفتند. **ایموجیِ
  عنوانِ دو تب عمداً ماند**: خواستهٔ صریح صاحب پروژه است و دستهٔ ۸۶ قفلش
  می‌کند. اگر روزی تصمیمش عوض شد، اول آن دسته باید عوض شود.
- **نوار وضعیتِ هدر.** «—» های کنار هم بی‌گروه‌بندی‌اند و جداکننده ندارند؛
  در عرضِ کم هم می‌شکنند.
- **حالتِ خالیِ درون‌کارتی.** جعبهٔ خاکستریِ «در این بازه نماد پایه‌ای پیدا
  نشد» نه نشان دارد نه کاری پیشنهاد می‌دهد — برخلاف `.empty` سراسری که
  الگوی درستش را دارد.
- **فرم‌های بلند.** «افزودن موقعیت» ده ورودی را در یک شبکهٔ تخت می‌ریزد،
  بی دسته‌بندی و بی خلاصهٔ پیش از ثبت.

## قلم‌های باز (غیرظاهری، دست‌نخورده)

- **نرخ بدون ریسک سراسری ۳۰٪** — تصمیم صاحب پروژه (شرح در `WORKLOG.md`).
- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.
- **پذیرش قلم‌های جلسهٔ بازار باز**: موتورها در دسته‌های ۲۵۳ و ۲۵۵ تا ۲۶۳
  قفل‌اند؛ آنچه فقط با بازارِ باز سنجیدنی است همان‌جا فهرست شده.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.
- **توکنی که مقدارش `transparent` است، لبهٔ زیرِ خودش را پاک می‌کند.** اگر
  تصمیمی برگشت، قاعده‌اش را هم بردارید — نه فقط مقدارش را.

**دروازه:** `node tools/check.mjs`
