# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## فاز ۱۳ — دفتر قراردادهای تاریخی (خواستهٔ ۱۴۰۵/۰۶/۱۰ و ۱۱)

```text
۱  قرارداد منقضی در بازهٔ انتخابی بیاید   ✔ core/option-roster.mjs، دستهٔ ۲۰۷
۲  دو سال گذشته هم قابل بررسی باشد        ✔ universe?from=&to= اجتماع بازه
۳  فعال یا منقضی به کاربر گفته شود        ✔ در همان کنترل بازه، نه تب جدا
۴  انقضا در طول بازه رخ می‌دهد            ✔ activeFrom/activeTo و «داخل بازه»
۵  چیزی از گذشته جا نماند                 ✔ شمارِ ناخوانا و غیر-اختیار
۶  دفتر بی‌دستور دستی ساخته شود           ✔ ساخت پس‌زمینه، اولویت ۹
۷  همهٔ تب‌های تاریخ‌دار از بازه بخوانند   ✔ پنج تب، یک کنترل مشترک
۸  تب جدا حذف شود                         ✔ وضعیت همان‌جای انتخاب تاریخ
۹  پوشش با نوشتنِ ناموفق کم نشود          ✔ بازه یکی می‌شود، نه جایگزین
۱۰ قراردادِ بی‌معامله هم پیدا شود          ✔ پاس کاتالوگ، دستهٔ ۲۱۱
۱۱ شناسهٔ ۱۷ رقمی گرد نشود                ✔ core/json-safe.mjs
۱۲ عمر از مشخصات رسمی بیاید               ✔ contractLife با beginDate/endDate
۱۳ جفت کال و پوت کنترل شود، بی‌حدس        ✔ pairAudit — گزارش، نه ترمیم
۱۴ اسکن ناقص «موفق» ثبت نشود              ✔ rosterHealth.complete
```

**دو درسِ این فاز:**

  • ابزاری که برای کار کردن به یک مرحلهٔ دستی نیاز دارد، برای کاربر خراب
    است — هرچند همهٔ آزمون‌هایش سبز باشد.
  • «سابقهٔ معاملات» با «فهرست ابزارها» یکی نیست. فرضی که این دو را یکی
    می‌گیرد جهت‌دار است: بی‌معامله‌ها همان دورافتاده‌هایند.

## آنچه این ماشین نتوانست بسنجد

`cdn.tsetmc.com` از این ماشین بسته است (۴۰۳ از پراکسی). سه مسیر تازه —
`GetInstrumentSearch`، `GetInstrumentInfo`،
`GetInstrumentOptionByInstrumentID` — شکلِ پاسخشان از مستندات عمومی
استنتاج شده و پارسرشان عمداً دفاعی است (`unwrapSearch` هر شکلی را باز
می‌کند)، ولی **روی پاسخ واقعی سنجیده نشده**.

مسیرِ شکست سنجیده شد و درست کار می‌کند: ۲۸۸ جست‌وجو، هر ۲۸۸ خطا، دفترِ
۷۹۳۳ ردیفی دست‌نخورده، `complete` همچنان false.

نخستین اجرای واقعی با صاحب پروژه است:

```bash
node tools/roster-scan.mjs --from 20240901 --to <امروز>
node tools/roster-verify.mjs --base اهرم --expiry 1404/01/27
```

معیار پذیرش همان است که خودش گذاشت: ۱۴ کال، ۱۴ پوت، جفتِ ناقص صفر. اگر
شکلِ پاسخ فرق داشت، عددِ «کاتالوگ ۰ موفق» در گزارش پایانی همان لحظه
می‌گویدش.

## قلم بعدی

پس از نخستین اسکنِ موفق، `tools/roster-verify.mjs` را ببینید. اگر جفتِ
ناقص ماند، `--pairs 40` فهرستشان را می‌دهد؛ هر ردیفش یک سؤال واقعی است:
آن سمت واقعاً وجود نداشته، یا جست‌وجو پیدایش نکرده؟ پاسخِ دسته‌ای این
سؤال، قلم بعدی است.

## مرز ادامه

در دامنهٔ فعلی کار دیگری روی تب‌های دیگر انجام نشود.

**دروازه:** `node tools/check.mjs`
