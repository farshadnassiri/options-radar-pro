# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### ارزیابی بازده و ریسک طرح سرمایه‌دار — برش ششم فاز ۳

**چرا.** طرح ورود اکنون قیمت اجرایی، ظرفیت و مبنای سرمایه قابل حسابرسی
دارد، اما هنوز نمی‌گوید در قیمت هدف یا بازه‌ای که کاربر در مأموریت قفل
کرده چه سود/زیانی می‌سازد. پیش از هر امتیاز یا رتبه باید بازده خام و
محدودیت ریسک با موتور مشترک payoff و بدون احتمال‌سازی محاسبه شوند.

**شاخه.** `codex/portfolio-payoff-evaluation` از آخرین `main`، فقط پس از
ادغام قلم قبل.

**فایل‌ها:**

```text
core/portfolio-evaluation.mjs       ارزیابی بازده/ریسک یک طرح سرمایه‌دار
core/portfolio-capital.mjs          فقط اگر ارجاع ممیزی عمومی کم دارد
core/payoff.mjs                     فقط اگر primitive عمومی کم دارد
tests/suites/128-portfolio-evaluation.mjs
```

با `node tools/next.mjs map analyzePayoff`،
`node tools/next.mjs map pnlAtExpiry` و
`node tools/next.mjs map validateMissionOutlook` مسیرهای مشترک را پیدا کنید.

**دستهٔ آزمون.** `tests/suites/128-portfolio-evaluation.mjs` (تازه).

**پذیرش:**

1. فقط خروجی معتبر `portfolioCapitalRequirement` برای همان جلسه، همان
   snapshot و همان طرح ورود پذیرفته شود؛ خروجی دست‌ساز یا ناهم‌لحظه بازده
   نگیرد.
2. پاها با VWAP ورود و حجم اجرایی خود طرح به `analyzePayoff` مشترک برسند؛
   جریان نقد ورود پس از کارمزد صریح باشد و محاسبه موازی payoff ساخته نشود.
3. برای دید جهت‌دار، سود/زیان در `targetPriceRial` قفل‌شده؛ برای دید خنثی،
   هر دو کران `rangeLowRial` و `rangeHighRial` و بدترین آن‌ها؛ و برای دید
   پرنوسان، فقط نقاط قیمتی صریح مأموریت گزارش شوند. هیچ قیمت هدف یا احتمال
   پنهان ساخته نشود.
4. بیشترین سود، بیشترین زیان، نقاط سربه‌سری و ریسک نامحدود از خروجی موتور
   مشترک حفظ شوند. سقف زیان مأموریت جدا سنجیده شود و مقدار نامتناهی با
   عدد بزرگ ساختگی جایگزین نشود.
5. کارمزد تسویه/اعمال و نرخ سهم، هرجا لازم‌اند، باید صریح و هم‌snapshot
   باشند. داده یا کیفیت گمشده کل عدد وابسته را `null` نگه دارد؛ کیفیت
   `estimated` و علتش تا خروجی بماند.
6. بازده بر سرمایه فقط با جمع `portfolioCapitalRequirement` محاسبه و مبنا
   کنار آن نگه داشته شود. این ارزیابی نه بودجه را تغییر دهد، نه ظرفیت ورود
   را کوچک کند.

**مرز قلم.** ساخت توزیع احتمال، `scoreCandidate`، رتبه‌بندی، پیشنهاد UI،
انتخاب بهترین‌ها و ثبت موقعیت هنوز ممنوع‌اند. این برش فقط داده خام و
قابل حسابرسی لازم برای امتیازدهی بعدی را می‌سازد.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
