# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پذیرش snapshot بزرگ مأموریت فعال — قلم نخست فاز ۸

**چرا.** پرونده بزرگ پایان فقط در endpoint خودش تا سقف محدود پذیرفته
می‌شود، اما autosave مأموریت واقعی در مرحله `active` همان snapshot تاریخی
را حمل می‌کند و هنوز از سقف عمومی یک مگابایت رد می‌شود. در نتیجه کاربر
می‌تواند جلسه را فعال کند اما refresh همان مرحله را از سرور برنمی‌گرداند.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
server/server.mjs
server/portfolio-mission-store.mjs
tests/suites/121-portfolio-mission-store.mjs
tests/suites/170-portfolio-mission-large-body.mjs
```

حد بزرگ باید نام‌دار، محدود و فقط روی `PUT /api/portfolio/session` باشد؛
افزایش `MAX_BODY` عمومی یا دورزدن اعتبارسنج مأموریت مجاز نیست.

**پذیرش:**

1. `PUT /api/portfolio/session` یک سقف نام‌دار و محدود برای بدنه مأموریت
   داشته باشد و مقدار آن مستقل از سقف عمومی تعریف شود.
2. رکورد `active` معتبر با snapshot واقعی بزرگ‌تر از یک مگابایت از HTTP
   ذخیره و با `GET` بدون تغییر JSON بازیابی شود.
3. بدنه بالاتر از سقف مخصوص مأموریت با ۴۱۳ و پیام فارسی رد شود و فایل
   نیمه‌نوشته بر جا نگذارد.
4. تنظیمات، trades و endpointهای عمومی همچنان بالاتر از یک مگابایت را رد
   کنند؛ سقف پرونده پایان ۱۶ مگابایتی نیز تغییر نکند.
5. نسخه، مرحله، سرمایه، continuity و قفل snapshot همچنان از همان
   `restorePortfolioMissionSave` عبور کنند؛ بزرگ‌بودن مجوز داده خراب نیست.
6. آزمون HTTP واقعی سرور را روی پورت آزاد بالا بیاورد، فایل دقیق خودش را
   پاک کند و پس از پایان پردازه‌ای باقی نگذارد.
7. خطای بیش‌ازحد بودن بدنه از خطای JSON/اعتبارسنجی قابل تشخیص بماند؛ عدد
   یا snapshot جایگزین و فشرده‌سازی مخفی ساخته نشود.

**مرز قلم.** فشرده‌سازی snapshot، تغییر schema مأموریت و افزایش سقف سایر
endpointها بیرون این برش‌اند.

## محدودیت‌های باز

- نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه واقعی در چند
  نماد هنوز بازند.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم نخستین
وظیفه از نه وظیفه فاز ۸ است؛ سهم محقق‌شده فاز را مطابق ابزار پیشرفت ثبت
کنید. دروازه کامل، کامیت دارای بدنه و پوش روی همین شاخه لازم‌اند؛ سپس بدون
انتظار برای مرج قلم دوم فاز ۸ را آغاز کنید.
