# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعدی بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### دروازهٔ اجراپذیری نامزدها — برش نخست فاز ۳

**چرا.** فاز ۲ کامل شد: مأموریت قفل می‌شود، عکس شروع بریده می‌شود، و
پیش‌نویس روی سرور ذخیره و ادامه داده می‌شود. فاز ۳ «پیشنهاد و اجراپذیری»
است و ۰ از ۱۰ قلمش انجام شده. پیش از هر پیشنهادی، باید بتوان گفت **کدام
قرارداد اصلاً قابل معامله است** — با همان قیود نقدشوندگی و ریسکی که کاربر
در مأموریت قفل کرده.

این برش عمداً موتور خالص است و رابط ندارد. اگر پیشنهاددهنده پیش از دروازه
ساخته شود، ردیف‌هایی پیشنهاد می‌دهد که کاربر هرگز نمی‌تواند با آن حجم
بخرد — و همان لحظه کل بازپخش بی‌معنا می‌شود.

**شاخه.** `claude/portfolio-eligibility` از آخرین `main`.

**فایل‌ها.** موتور خالص، بدون DOM و بدون شبکه:

```text
core/portfolio-eligible.mjs    ماژول تازه — دروازهٔ اجراپذیری
```

برای خواندن قیود و عبور از دفتر سفارش، این‌ها از قبل هست (دوباره نسازید):
`core/portfolio-mission.mjs` قیود قفل‌شده را دارد، `core/exec.mjs`
`walkBook`/`maxSize`/`bookCapacity` را، و `core/data-quality.mjs`
قرارداد کیفیت را. با `node tools/next.mjs map exec` ببینیدشان.

**دستهٔ آزمون.** `tests/suites/123-portfolio-eligible.mjs` (تازه).
سبک کار را از `122-portfolio-mission-resume.mjs` بردارید.

**پذیرش** — هر شش بند، وگرنه قلم بسته نیست:

1. تابع، مأموریت قفل‌شده و فهرست نامزدها را در همان لحظهٔ جاری می‌گیرد و
   برای هر نامزد یک حکم صریح می‌دهد: پذیرفته، یا ردشده **با علت نام‌بُرده**.
   حکمِ بی‌علت وجود ندارد.
2. هر شش قید نقدشوندگیِ مأموریت اعمال شوند: حداقل ارزش روزانهٔ نماد پایه،
   حداقل ارزش روزانهٔ اختیار، حداقل موقعیت باز، حداکثر اسپرد، حداکثر مصرف
   عمق دفتر، و الزام پنج سطح دفتر.
3. حجم اجراپذیر **فقط** از عبور واقعی دفتر سفارش دربیاید (`core/exec.mjs`).
   اگر دفتر نیست یا ناقص است، حجم `null` بماند و نامزد رد شود — هیچ حجمی
   تخمین زده نشود.
4. سقف مصرف عمق رعایت شود: حجم پیشنهادی از `maxBookTakePct` دفتر بیشتر
   نشود، حتی اگر دفتر عمیق‌تر باشد.
5. کیفیت دادهٔ هر نامزد به حکم برسد: نامزدی که خوراکش `missing` یا کهنه
   است، پذیرفته نشود و علتش همان کیفیت باشد. عددی جایگزین دادهٔ نبوده
   ساخته نشود.
6. هیچ دادهٔ بعد از لحظهٔ جاری خوانده نشود — نه قیمت آینده، نه قرارداد
   تازه‌منتشرشده. یک ادعای آزمون باید صریح همین را بسنجد.

**رابط ندارد**، پس راستی‌آزمایی مرورگر لازم نیست. برش بعدی، این دروازه را
به تب وصل می‌کند.

---

## محدودیت باز از قلم قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. **اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.** شرح کامل آنچه دیده شد در `WORKLOG.md`، قلم بالا.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید — **این تنها جایی است که باز
کردن آن فایل‌ها لازم است**، و کار شماست نه کارِ نفر بعد. برای نفر بعد همان
چهار چیز را بنویسید که خودتان لازم داشتید: چرا، کدام فایل‌ها، کدام دستهٔ
آزمون، و پذیرش قابل‌سنجش.
