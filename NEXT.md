# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### ارزش جاری موقعیت — نخستین قلم فاز ۵

**فاز ۴ کامل شد** (۷ از ۷): ورود، خروج، دفتر سرمایه، موقعیت‌ها و خلاصهٔ
حسابداری. فاز ۵ «بازپخش و معامله در مسیر» است، ۱۳ قلم.

**چرا این اول.** تا اینجا هر ماژول عمداً از ارزش‌گذاری پرهیز کرده و در
هرکدام یک ادعا هست که می‌گوید «سود و زیان کار فاز ۵ است». حالا وقتش
است — و باید **یک بار** و درست ساخته شود، وگرنه هر بخش رابط فرمول خودش
را می‌سازد.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-valuation.mjs        ارزش جاری موقعیت از دفتر سفارش هم‌لحظه
tests/suites/140-portfolio-valuation.mjs
```

`node tools/next.mjs map portfolioSessionPositions`. قیمت بستن هر پا
همان قاعدهٔ `core/portfolio-close.mjs` است: **سمت وارونه** — ارزشِ پای
خریداری‌شده همان چیزی است که با فروختنش گرفته می‌شود، نه قیمت خرید.

**پذیرش:**

1. ارزش هر پا از دفتر سفارشِ هم‌لحظه و از **سمت وارونهٔ ورود** بیاید.
   یک ادعا سمت را قفل کند (جهش سمت باید قرمز شود).
2. مدرکِ کهنه یا پای بی‌حکم → ارزش `null` با علت، **نه** قیمت پایانی و
   نه صفر. نبودِ عدد هیچ‌وقت با عددی جایگزین نمی‌شود.
3. سود و زیان تحقق‌نیافته = ارزش جاری منهای نقدِ ورود، **منهای کارمزدِ
   فرضیِ خروج**. نادیده‌گرفتن کارمزد یعنی عددی که هیچ‌وقت گرفته نمی‌شود.
4. موقعیت بسته ارزش جاری ندارد؛ عددش `null` است نه صفر.
5. ارزشِ ناقص (یک پا نامعلوم) کل موقعیت را `null` کند — جمعِ نصفه بدتر
   از نبودِ عدد است.
6. جلسهٔ بدون موقعیت، فهرست خالی با علت بدهد.

راستی‌آزمایی مرورگر لازم **نیست** (این برش رابط ندارد).

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سرمایه و پیشنهاد ربطی ندارد
(هر دو در ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ سنجیده شدند و سرریز ندارند)، ولی یک بار
باید دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

بخش دفتر سرمایه در مرورگر با بازکردن دستی `#pt-review-step` سنجیده شد،
چون بدون تاریخچهٔ واقعی نمی‌شود جلسه را فعال کرد. چیدمان و سرریز واقعی
سنجیده شدند؛ مسیر رسیدنِ کاربر به آن هنوز با دادهٔ واقعی دیده نشده.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
