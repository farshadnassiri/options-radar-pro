# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### رتبه‌بندی و کوتاه‌فهرست نامزدها — برش هشتم فاز ۳

**چرا.** هر طرح حالا امتیاز قابل حسابرسی دارد
(`core/portfolio-score.mjs`) ولی هیچ‌جا کنار هم چیده نمی‌شوند. کاربر
نمی‌تواند بگوید «سه طرح برترِ همین مأموریت کدام‌اند» و مهم‌تر، نمی‌تواند
بگوید چرا یکی بالاتر از دیگری است.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-ranking.mjs
tests/suites/130-portfolio-ranking.mjs
```

`node tools/next.mjs map portfolioPlanScore`.

**پذیرش:**

1. ورودی فهرستی از خروجی‌های معتبر `portfolioPlanScore` برای **همان**
   جلسه، همان snapshot و همان مأموریت باشد؛ امتیاز از جلسه یا لحظهٔ دیگر
   کل فراخوانی را رد کند، نه اینکه بی‌صدا از فهرست بیفتد.
2. ترتیب **قطعی** باشد: امتیاز برابر با شکنندهٔ نام‌بُرده و پایدار حل شود
   (نه ترتیب ورودی، نه ترتیب `Object.keys`). یک ادعا باید همان فهرست را
   دوبار با ترتیب ورودی متفاوت بدهد و خروجی یکسان بگیرد.
3. طرحِ `ineligible` و طرحِ با امتیاز `null` **رتبه نگیرند** و در بخش
   جدا با علتشان بمانند. امتیاز نامعلوم ته جدول یعنی «بد»، و این دروغ است.
4. برای هر ردیف، **علت جایگاهش** بیاید: کدام جزء بیشترین سهم را داشته و
   کدام بیشترین کاهش را. عددِ رتبه بدون علت، همان امتیازِ بی‌اجزاست.
5. کوتاه‌فهرست سقف صریح بگیرد و اگر تعداد واجدها کمتر از سقف بود، کم‌تر
   برگردد — با عدد ساختگی پر نشود.
6. هیچ عدد مالی تازه‌ای اینجا محاسبه نشود؛ فقط چیدن و توضیح دادن.

**مرز قلم.** پیشنهاد UI و ثبت موقعیت همچنان ممنوع‌اند.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
