# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## بک‌لاگ صاحب پروژه — هر یازده قلم بسته است

```text
۱  نام نمادها در جدول و نمودار            ✔ ستون، کشویی، شناسنامه، کارت‌ها، اعلان
۲  همهٔ آیتم‌ها، ستون‌ها انتخابی            ✔ ۴۶ ستون در ui/radar-columns.mjs
۳  نمودارها بزرگ‌تر                        ✔ ۳۴۰/۴۴۰/۵۲۰ پیکسل، ستون شبکه ۴۶۰
۴  «خانواده» برود، نامِ استراتژی بیاید     ✔ optgroup بر پایهٔ خانواده
۵  جدول ویژوال، نمودار در سلول            ✔ نوار پرشدگی، اسپارک‌لاین، نوار سود/زیان
۶  «ارزش معامله» در پالایه                 ✔ کمینهٔ ارزش و حجمِ نازک‌ترین پا
۷  short strangle درست شود                 ✔ لنگر شد دهانهٔ اعمال، بازده بر وجه تضمین
۸  رصد زنده + روند تاریخی، شکل متفاوت      ✔ joinLive، سه نشانهٔ تفکیک
۹  تب سیستم اعلان                          ✔ تب «دیده‌بان شرطی»
۱۰ شرط روی ترکیب خاص/عمومی و چند نماد      ✔ core/watch-rule.mjs
۱۱ رصد لحظه‌ای از شروع بازار تا اکنون       ✔ نوارِ امروز در هر تیک
```

**بازطراحی ظاهر لغو شد** — صاحب پروژه پس‌گرفتش. سراغش نروید.

## کار — قلم بعدی را از اینجا بردارید

بک‌لاگِ صریحی روی میز نیست. سه کارِ واقعی که خودِ کد نشان می‌دهد:

### الف. دانه‌بندی درون‌روزی روی بیش از یک روز

امروز فقط یک روز است و علتش در `ui/tabs/spread-radar.mjs` نوشته شده:
ریزمعاملهٔ هر پا برای هر روز یک درخواست است. راهِ درست، کش‌کردنِ
ریزمعامله روی دیسک است — مثل کاری که `data/roster` برای دفتر قراردادها
می‌کند — نه بالا بردن سقف درخواست.

### ب. سقفِ ۲۴ ابزارِ رصد زنده

`/api/live-trades` بیست‌وچهار ابزار می‌گیرد. با صد ترکیب، پاهای بعدی
بی‌قیمتِ زنده می‌مانند. الان شمارِ پوشش‌داده‌شده نوشته می‌شود و ترکیبِ
بی‌قیمت با عددِ روزانه سنجیده **نمی‌شود** — پس دروغی گفته نمی‌شود، ولی
کاربر هم نمی‌تواند بگوید کدام ترکیب‌ها اولویت بگیرند. انتخابِ صریحِ
اولویت (پرمعامله‌ترین؟ منطبق‌ترین؟) کارِ بعدی است.

### ج. دیده‌بان شرطی، در ساعت بازار

تب ساخته شد و با دادهٔ ساختگی کار کرد. حلقهٔ رصدش هرگز در ساعت بازار
اجرا نشده.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی). این‌ها با دادهٔ
مسیریافته در مرورگر واقعی اجرا شدند، نه با تابلوی واقعی:

۱. **عددها را با تابلو بخوان.** اسپرد عمودی در مقیاس «قیمت خام»؛ بعد
مقیاس را عوض کن و ببین درصدِ پرشدگی **تکان نمی‌خورد**.
۲. **استرانگل فروش.** ستون «لنگر» باید «دهانهٔ اعمال» بدهد، «حداکثر
زیان» باید «نامحدود» بنویسد نه عدد، و «حداکثر سود ٪» باید بر وجه تضمین
باشد. سربه‌سری‌ها را با ماشین‌حساب خودت بسنج.
۳. **دنبالهٔ امروز.** در ساعت بازار، تیکِ «رصد زندهٔ بازار» را بزن و
ببین خطِ نقطه‌چینِ امروز روی روند گذشته می‌نشیند و برچسب «اکنون» دارد.
۴. **هشدار زنده.** نه در رادار و نه در دیده‌بان، هیچ‌وقت در ساعت بازار
سنجیده نشده.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. پای بی‌قیمت فاصله را باطل می‌کند، دو پای
هم‌قیمت ردیف نمی‌سازند، توزیعِ بی‌پراکندگی حکم نمی‌گیرد، بازدهی که
مخرجِ مدافع‌پذیر ندارد ادعا نمی‌شود، نامِ نداشتهٔ یک پا با شناسهٔ ابزار
پر می‌شود، زیانِ نامحدود عددِ بزرگ نمی‌شود، و ترکیبِ بی‌قیمتِ زنده با
عددِ روزانه سنجیده نمی‌شود.

اگر روزی یکی از این‌ها شکست، یکی از دسته‌های ۲۱۸ تا ۲۲۸ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون

پورت جداگانه (۸۷۹۴) و پس از کار بسته شود؛ ۸۷۸۷ دستِ صاحب پروژه است.
کرومیوم در `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` و
Playwright سراسری: از پوشهٔ موقت با
`ln -s /opt/node22/lib/node_modules node_modules`.
