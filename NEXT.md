# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده — فهرست کامل نگاه باز چندروزه

پارسر `core/option-roster.mjs` اکنون «ت» را فقط وقتی جداکننده دارد نشان
قرارداد تعدیل‌شده می‌داند؛ بنابراین توان، تیام و تاصیکو کامل می‌مانند.
`repairRosterBaseNames` دفترهای قدیمی را هنگام خواندن ترمیم می‌کند و
`rosterChainRows` پیش از نام، نگاشت قطعی شناسهٔ قرارداد به شناسهٔ پایه را
به‌کار می‌برد. آزمون رگرسیون در دستهٔ ۲۰۷ است. ممیزی روی دفتر ۹٬۰۸۱ ردیفی
هر ۲۳ نماد پایهٔ آخرین بایگانی را نشان داد؛ ۲۹۰ قرارداد توان برگشتند.

## کار

### پذیرش رصد و نگاه باز در یک جلسهٔ بازار باز

کندل افقی فشرده شده، فیلتر زنجیرهٔ «هر دو / فقط کال / فقط پوت» اضافه شده
و کارت فعال نمودار بالای همسایه می‌آید تا tooltip پنهان نشود. «نگاه باز»
به‌صورت پیش‌فرض از اولین معامله امروز تا اکنون ساخته می‌شود، سطل تک‌نقطه‌ای
را رسم می‌کند و اگر دریافت یک قرارداد شکست بخورد، ریزمعامله‌های سالم را
نگه می‌دارد. دسته‌های ۶۵، ۲۱۰، ۲۴۵ و ۲۴۷ این رفتارها را قفل می‌کنند.

امروز جمعه بود و پذیرش دادهٔ زنده ممکن نبود. در نخستین جلسه بازار باز، روی
یک پایه و سررسید پرمعامله بررسی کن که نمودارهای درون‌روزی با هر عکس تازه از
اولین معامله همان روز تا آخرین لحظه جلو می‌روند. آخرین نقطه را با آخرین
ریزمعامله پایه، یک کال و یک پوت تطبیق بده؛ قیمت هیچ ابزار نباید از سطل قبلی
حمل شود. خرابی عمدی یا طبیعی یک قرارداد نباید کل نمودار را خالی کند.

## فزر و بازهٔ تاریخی

بازه ۱۴۰۵/۰۵/۰۳ تا ۱۴۰۵/۰۶/۱۸ شامل ۳۲ روز معاملاتی و ۸۶ قرارداد فزر است،
اما قراردادهای دارای قیمت در روز ورود در ۱۴۰۵/۰۵/۲۵ سررسید می‌شوند. منبع
روزانه، آخرین قیمت آن‌ها را بعد از سررسید با حجم و معامله صفر تکرار می‌کند؛
این ردیف دیگر قیمت تازه محسوب نمی‌شود. تقویم آزمون آخرین روز دارای قرارداد
مشترک را انتخاب و علت کوتاه‌شدن را کنار فرم می‌گوید. اجرای واقعی مرورگر تا
۲۵ مرداد، ۸۹۷ ترکیب معتبر از ۳۰ استراتژی و ۱۵ روز معتبر ساخت.

اگر هدف آینده مقایسهٔ کل بازه تا ۱۸ شهریور باشد، آن دیگر یک ورود ثابت نیست:
باید «غلتاندن سررسید» به‌عنوان مدل جدا و صریح طراحی شود تا خروج نسل قدیم و
ورود نسل تازه، هزینه و قاعدهٔ انتخاب مستقل داشته باشد؛ قرارداد منقضی نباید
بی‌صدا با قرارداد تازه جایگزین شود.

## پذیرش بعدی در بازار باز

۱. فیلتر هر دو/کال/پوت را روی زنجیره‌ای دارای هر دو سمت و پس از تعویض
سررسید کنترل کن؛ شمار ردیف و انتخاب ذخیره‌شده باید درست بماند.

۲. کندل مثبت، منفی و تک‌قیمتی را در عرض دسکتاپ و موبایل ببین؛ کادر هر نقطه
باید داخل ردیف بماند و کندل دوباره کشیده نشود.

۳. روی نمودار «روند روزانه سربه‌سر و پایه» و نمودارهای مجاور hover کن؛
راهنما باید بالای همه کارت‌ها و قابل خواندن باشد.

۴. طی دست‌کم ۲۰ دقیقه، شدت ارزش کال/پوت، روند سفارش‌ها و تغییر موقعیت باز
را بسنج؛ ریست شمارنده تجمعی نباید شدت منفی جعلی بسازد.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است.
- روز جاری فقط در فاز `open`، `after` یا `ungated` به تاریخ امروز منتسب شود.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل معامله
  یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.
- همه نماهای قدیمی در «تحلیل‌های تکمیلی» حفظ شوند.

**دروازه:** `node tools/check.mjs`
