# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده — ستون‌های انتخابی زنجیرهٔ دوطرفه

فهرست ثابت شش‌تایی رفت؛ زنجیرهٔ دوطرفه از **همان کاتالوگ ستونِ جدول تخت**
می‌خواند (`contractColumns`)، با انتخابگر گروه‌بندی‌شده، جست‌وجو، «نمای
آماده» و «همه». انتخاب در `options-radar:market-map-paired-cols` می‌ماند و
ترتیب نمایش از کاتالوگ می‌آید نه از ترتیب تیک‌زدن.

چهارده سنجهٔ تازه در `contractAnalytics` (`core/decision-dashboard.mjs`):

- **یونانی‌ها** — دلتا، گاما، تتا (ریال در روز)، وگا، رو، احتمال در سود.
  از همان `ivPct` عکس بازار، نه حلِ دوباره.
- **اهرم** — ساده و مؤثر (× قدرمطلق دلتا).
- **فرسایش** — ارزش زمانی بر روز (ریال و ٪ پریمیوم) و سالانه‌شده روی پایه.
- **بازده سناریو** — «اگر پایه تکان نخورد چند درصد»؛ برای اختیار بی‌ارزشِ
  ذاتی دقیقاً ‎−۱۰۰‎.
- به‌علاوه گردش به موقعیت باز و پریمیوم ٪ اعمال.

یونانی‌ها با `state.settings` خودِ کاربر ساخته می‌شوند تا با تب «رصد
یونانی» بخوانند. دستهٔ ۲۴۶ (۶۲ ادعا) همه را قفل می‌کند.

## کار

### پذیرش صفحهٔ رصد زنده در جلسهٔ بازار باز

پذیرش تا امروز فقط با دادهٔ ساختگی انجام شده. در نخستین جلسهٔ بازار باز:

۱. **نگاه باز لحظه‌ای**: از نقشه یک نماد انتخاب کن و به تب «تلاطم و
انتظارات» → نمای «نگاه باز چندروزه» برو. باید بدون هیچ کلیکِ دیگری نمودار
لحظه‌ای بسازد. تب شبکه را باز بگذار: نباید `dailies` برود. بعد «تاریخی
چندروزه» را بزن و ببین بازه و تاریخ‌ها برمی‌گردند.

۲. **زنجیرهٔ دوطرفه**: خط «قیمت جاری پایه» باید دقیقاً بین دو اعمالی باشد
که آن را در بر می‌گیرند. سایهٔ «در سود» سمت کال باید **زیر** قیمت جاری و
سمت پوت **بالای** آن باشد — این دو وارونهٔ هم‌اند و اشتباهشان آسان است.

۳. **نقشهٔ هم‌اندازه** را روی یک روز پرمعامله ببین؛ نمادهای کم‌معامله باید
همان‌قدر قابل کلیک باشند و رنگشان همچنان تغییر واقعی خودشان.

۴. **ستون سربه‌سر** را برای یک کال و یک پوت با محاسبهٔ دستی تطبیق بده؛ برای
پوتی که قیمت جاری زیر سربه‌سرش است عدد باید **منفی** باشد.

۵. **دریافت‌ها**: ۲ دقیقه روی تب «نبض» بمان؛ نباید `live-trades` یا `infos`
تازه‌ای برود. بعد نمای «ریزمعامله قرارداد» را باز کن؛ باید هر تیک یکی برود.

۶. **۵۰ نما** را یک دور باز کن و اگر نمایی هنوز تکراری یا بی‌فایده دیدی
بگو؛ هرس این دور محافظه‌کارانه بود.

۷. دو تب ادغام‌شده باید تنبل بار شوند و پس از ترک صفحه تایمرشان بخوابد.

۸. **ستون‌های تازهٔ زنجیره**: دلتای کال باید مثبت و پوت منفی باشد و با
عمق در سود به ±۱ میل کند. اهرم مؤثر باید نزدیک پول بیشینه شود. «بازده اگر
پایه تکان نخورد» برای هر اختیارِ خارج از سود باید دقیقاً ‎−۱۰۰٪‎ باشد.
اگر عددی با تب «رصد یونانی» نخواند، اختلافِ فرضِ مدل را گزارش کن.

## قلم بعدی — اختلاف شمارش ریزمعامله

برای طهرم۷۰۶۱ عدد رسمی تابلو ۶٬۷۹۶ معامله بود و نوار ریزمعامله پس از حذف
معاملات باطل ۶٬۷۹۸ ردیف فعال داد. اختلاف از ناسازگاری دو endpoint بالادست
می‌آید. اول باید سنجید کدام مرجع است، بعد تصمیم گرفت کدام عدد نمایش داده
شود — نه اینکه یکی بی‌صدا با دیگری جایگزین شود.

## قلم‌های باز

- **بارِ بالادست**: تصمیم دربارهٔ `byEndpoint` در `/api/health` منتظر
  اندازه‌گیری صاحب پروژه است.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: اگر مقایسهٔ بازه‌ای فراتر از عمر یک نسل قرارداد لازم
  شد، باید مدل صریح شود؛ قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است.
- روز جاری فقط در فاز `open`، `after` یا `ungated` به تاریخ امروز منتسب شود.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل معامله
  یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.
- همه نماهای قدیمی صفحهٔ رصد زنده حفظ شوند — تب‌بندی جای `<details>` را
  گرفت، چیزی حذف نشد.

**دروازه:** `node tools/check.mjs`
