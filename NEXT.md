# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## یازده بندِ «رصد زندهٔ هر استراتژی» — هر یازده بسته شد

خواسته: **هر تبِ استراتژیِ سمت راست خودش یک رادارِ زنده باشد، با
پارامترهای مختصِ خودش.** شرحش در `WORKLOG.md`.

کدِ تازه: `core/` → `strategy-radar` · `history-chain` · `session-trail` ·
`manual-price` · `row-trust` · `upstream-tally` · `watch-health`؛ `server/`
→ `job-queue` · `atomic-json`؛ `ui/` → `strategy-history` ·
`strategyLinkTargets` در `handoff.mjs` · `table.sortKey()` و `demote`.
آزمون: ۵۱ و ۲۳۲ تا ۲۳۹.

**سه قاعده‌ای که نباید شکسته شوند:**

۱ تفاوتِ سی‌وشش استراتژی **داده** است نه کد — نمایه در `GROUP_PROFILES`
  یا `ID_PROFILES`، نه تبِ دست‌نویس.

۲ زیرتب فقط **منبعِ** جدول را عوض می‌کند؛ بقیه مشترک است. جدولِ دومی
  ساخته نشود، وگرنه دو عدد از دو مسیر مقایسه می‌شوند.

۳ **انتخابِ موتورِ بازده یک قاعده است، نه چند جا.** هر جا پاها هم‌سررسید
  نیستند، `analyzeMixed` — وگرنه دو بخشِ یک صفحه دو عدد می‌گویند.

## کار — قلم بعدی را از اینجا بردارید

### الف. مقصدهای بیشتر برای نوار پیوند (بندهای ۴ و ۱۱)

چهار مقصد دارد: آزمایشگاه، دیده‌بان شرطی، رصد یونانی، رادار فاصله. مقصدِ
تازه = یک ردیف در `STRATEGY_LINK_TARGETS` **به‌علاوهٔ** پذیرشِ
`state.handoff` در `mount` همان تب. نامزدها: دیده‌بان زنجیره · تحلیل
تاریخی استراتژی · موقعیت‌های من.

⚠ چهار قاعده: پذیرش پیش از افزودن · `strategyLinkTargets` شرطِ واقعیِ آن تب
را بداند · `ACCEPTING` در دستهٔ ۵۱ به‌روز شود · مسیرِ «ترکیب پیدا نشد» حالتِ
کهنه را پاک کند (همان P1 آزمایشگاه).

### ب. بارِ بالادست — حالا قابل اندازه‌گیری است

بازآزمایی: ۳۳ خطا از ۱۲۶۷ درخواست (پیش‌تر ۱۵۷ از ۲۰۸۲). ابزارِ سنجش ساخته
شد نه سیاست: `/api/health` حالا `byEndpoint` و `worstEndpoint` می‌دهد.

⚠ **اول اندازه بگیر، بعد تصمیم.** با تابلوی باز چند مسیر تاریخی باز کن و
`curl /api/health | jq .byEndpoint` بگیر. اگر `GetClosingPriceDailyList`
صدر بود، مسئله «سریِ روزانهٔ روزِ گذشته» است که هرگز عوض نمی‌شود —
کاندیدای TTL بلند یا کشِ دیسکی. اگر `BestLimits` بود، چرخشِ زنده است و
`core/live-quote.mjs` جوابش. پیش از دیدنِ عدد هیچ‌کدام را نساز.

### پ. قراردادهایی که باید راستی‌آزمایی شوند

⚠ **ادعای متنی فقط وقتی بی‌ضرر است که راست باشد** — سه کامنت می‌گفتند «زیان
منفی نوشته می‌شود» در حالی که `maxLoss` مثبت است. پیش از تکیه بر جملهٔ
«موتور فلان می‌کند» با `node -e` بسنجش. نامزدها: `maxLossPct`،
`instantClosePnl`، و `quality.level` در برابر `executable`.

### ت. ریزدانگیِ درون‌روزی بیش از یک روز

تصمیمِ ذخیره‌سازیِ صاحب پروژه می‌خواهد (کشِ دیسکی). پیشنهاد: ۹۰ روز، فقط
نمادهای درخواست‌شده، سقف ۵۰۰ مگابایت. تا تصمیم نیامده شروع نشود.

## چهار دورِ پذیرش انجام شد

`652e819` (چهار ایراد) · `bd0a618` بازارِ بسته (هشت، هفت اصلاح) · `bd0a618`
بازارِ باز (پنج، هر پنج) · `10557f8` بازآزمایی (دو P1، هر دو). قفلشان در
دسته‌های ۲۳۶ تا ۲۳۹.

⚠ **چهار درس که نباید تکرار شوند:**

۱ **عددِ غلط با ظاهرِ معتبر، از خانهٔ خالی خطرناک‌تر است.** حالتِ شکست
  باید **خالی** باشد نه پیش‌فرضِ قبلی.

۲ **آزمونِ تابع، جای‌نگه‌دار و مرزِ نمایش را نمی‌بیند.** «سنِ مظنه صفر» از
  جای‌نگه‌دارِ `buildChain` آمد، «تاریخِ لاتین» و «— ثانیه کهنه است» از مرزِ
  نمایش — هر سه با آزمونِ سبز. **کارِ ظاهری: اول عکس.**

۳ **هشداری که روی تصمیم اثر ندارد، برچسب است نه هشدار.** ردیفِ «بازده
  ماهانه ۲٬۰۲۶٬۳۹۶٪» هشدارِ کامل داشت و رتبهٔ اول بود.

۴ **شمارنده‌ها با هم، یک وضعیت نمی‌سازند.** عکس ۲۹۰ ثانیه کهنه بود با
  `paused=false` و خطای صفر؛ هیچ‌کدام دروغ نمی‌گفتند. اگر سؤالی هست که
  کاربر می‌پرسد، یک نفر باید صریح جوابش را بدهد. و **خروجِ زودهنگام،
  حالتِ کهنه را پاک نمی‌کند مگر صریح پاکش کنی**.

## پذیرش — دورِ پنجم، در بازارِ باز

مانده از دورِ سوم: **بلیتِ داده** (وسطِ اسکن برو تاریخی؛ جدول نباید عوض
شود، و برعکس) · **ردِ جلسه روی اسپردِ پرگردش** (مسیر کشیده شود و «فاصلهٔ
پاها» غیرصفر).

تازه‌های این دور — هر دو P1 بازآزمایی:

۱. **پیوندِ آزمایشگاه.** از یک اسپردِ زنده آزمایشگاه را چند بار پشتِ هم باز
   کن، هم وقتی ترکیب در آن روز هست و هم وقتی نیست. تب نباید بیفتد؛ حالتِ
   «نیست» باید کادرِ قیمتِ خالی و پیامِ «ترکیب معتبر انتخاب کن» بدهد، نه
   عددِ ترکیبِ قبلی.
۲. **گرسنگیِ حلقهٔ زنده.** آزمایشگاه و رصد یونانی و رادار فاصله را هم‌زمان
   باز کن و `watchAgeSec` را در `/api/health` ببین: باید زیر
   `watchLimitSec` بماند. اگر نماند، نشانِ قرمزِ «عکس تابلو … کهنه است»
   باید بالای صفحه بیاید — سکوت پذیرفته نیست.
۳. **دفتر قراردادها.** حین ساختِ دفتر، مسیرهای تاریخی را باز کن:
   «Unterminated string in JSON» نباید تکرار شود.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود: ردیفِ بی‌عدد از فیلتر نمی‌گذرد و جدا شمرده
می‌شود؛ کادرِ خالی شرطِ «حداقل صفر» نمی‌سازد؛ عددِ دستی هرگز «قابل اجرا»
نیست؛ سنِ نداشته صفر نمی‌شود؛ دفترِ سفارشِ گذشته ساخته نمی‌شود.

ردیفِ مشکوک **حذف نمی‌شود** — فقط صدر را نمی‌گیرد. بی‌کرانی **ساختاری**
خوانده می‌شود نه از شیبِ عددی. و پروندهٔ JSON که خوانندهٔ هم‌زمان دارد،
اتمیک نوشته می‌شود.

اگر یکی شکست، یکی از دسته‌های ۲۳۲ تا ۲۳۹ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، بعد بسته شود (۸۷۸۷ دستِ صاحب پروژه). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`؛ Playwright سراسری،
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`domcontentloaded` نه `networkidle`.
