# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### بستن موقعیت در تب — برش ششم فاز ۴

**چرا.** `core/portfolio-close.mjs` خروج را می‌سازد ولی هیچ‌جا از رابط
صدا زده نمی‌شود. جدول موقعیت‌ها موقعیت باز را نشان می‌دهد بدون هیچ راهی
برای بستنش. بدتر: وقتی دفتر سفارش کم‌عمق است، ماژول عددِ ممکن را
برمی‌گرداند — و کاربر باید همان را ببیند، نه یک «نشد».

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
ui/portfolio-positions-view.mjs     ستون «بستن» و متن علتِ رد
ui/tabs/portfolio-time.mjs          دستگیرهٔ بستن روی جدول موقعیت‌ها
tests/suites/138-portfolio-close-ui.mjs
```

`node tools/next.mjs map closePortfolioPosition`. الگوی دستگیره را از
دکمهٔ «انتخاب و ثبت» در `#pt-proposals` بردارید — از جمله اینکه شکست
هیچ‌وقت شبیه موفقیت نشان داده نمی‌شود و جلسهٔ برگشتی جایگزین می‌شود تا
رسم بعدی درست دربیاید.

**پذیرش:**

1. هر موقعیت **باز** دکمهٔ بستن داشته باشد؛ موقعیت بسته نداشته باشد.
2. پس از بستن، جدول موقعیت‌ها و نوار سرمایه هر دو با جلسهٔ تازه دوباره
   رسم شوند — نه فقط یکی.
3. شکستِ بستن با علت خوانا دیده شود. برای `insufficientBook` **عددِ
   ممکن** هم دیده شود تا کاربر بتواند همان را بخواهد.
4. رقم فارسی و تومان؛ یک ادعا نبود رقم لاتین در متن نمایشی را بسنجد.
5. نقد و کارمزدِ خروج پس از بستن دیده شوند، با علامت درست.
6. قفل ویرایشگر مأموریت این بخش را غیرفعال نکند.

**نام‌گذاری:** پیش از انتخاب شناسه و نام تابع، یکتایی‌شان را در
`ui/tabs/portfolio-time.mjs` بسنجید (درسِ برش دوم فاز ۴).

**راستی‌آزمایی مرورگر لازم است** (۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴).

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سرمایه و پیشنهاد ربطی ندارد
(هر دو در ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ سنجیده شدند و سرریز ندارند)، ولی یک بار
باید دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

بخش دفتر سرمایه در مرورگر با بازکردن دستی `#pt-review-step` سنجیده شد،
چون بدون تاریخچهٔ واقعی نمی‌شود جلسه را فعال کرد. چیدمان و سرریز واقعی
سنجیده شدند؛ مسیر رسیدنِ کاربر به آن هنوز با دادهٔ واقعی دیده نشده.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
