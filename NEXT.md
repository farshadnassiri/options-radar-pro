# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعدی بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### سرمایه لازم و وجه تضمین طرح ورود — برش پنجم فاز ۳

**چرا.** طرح ورود اکنون ظرفیت مشترک، VWAP هر پا و جریان نقد واقعی را دارد،
اما برای ساختار بستانکار دریافت پریمیوم به معنی سرمایه لازم صفر نیست. باید
بدهکار خالص، کارمزد و وجه تضمین از موتورهای مشترک به یک مبنای سرمایه
ممیزی‌پذیر برسند؛ داده یا پارامتر گمشده نباید با صفر یا پیش‌فرض پنهان پر
شود.

**شاخه.** `codex/portfolio-capital-requirement` از آخرین `main`، فقط پس
از ادغام قلم قبل.

**فایل‌ها:**

```text
core/portfolio-capital.mjs          مبنای سرمایه هر طرح ورود
core/portfolio-entry.mjs            فقط اگر ارجاع ممیزی عمومی کم دارد
core/margin.mjs                     فقط اگر primitive عمومی کم دارد
core/payoff.mjs                     فقط اگر primitive عمومی کم دارد
tests/suites/127-portfolio-capital.mjs
```

با `node tools/next.mjs map strategyMargin`،
`node tools/next.mjs map capitalBase` و `node tools/next.mjs suites وجه`
مسیرهای مشترک را پیدا کنید.

**دستهٔ آزمون.** `tests/suites/127-portfolio-capital.mjs` (تازه).

**پذیرش:**

1. فقط طرح ورود معتبر همان جلسه و همان snapshot پذیرفته شود؛ طرح دست‌ساز،
   ناهم‌لحظه یا دارای پای ناقص سرمایه نگیرد.
2. ساختار بدهکار، بدهکار خالص واقعی به‌علاوه کارمزد ورود را سرمایه لازم
   بداند؛ دریافت پریمیوم یا پای فروش نباید هزینه خرید پاها را محو کند.
3. ساختار بستانکار، وجه تضمین را از موتور مشترک `strategyMargin` و
   پارامترهای صریح همان snapshot بگیرد. پریمیوم دریافتی به‌تنهایی سرمایه
   لازم صفر یا منفی نسازد.
4. اگر وجه تضمین یا یکی از پارامترهای لازم تخمینی است، کیفیت `estimated`
   و علتش تا خروجی بماند. اگر داده لازم موجود نیست، سرمایه لازم `null`
   بماند و صفر جایگزین نشود.
5. کارمزد از تنظیم صریح و موتور مشترک بیاید؛ نبود نرخ، کارمزد صفرِ فرضی
   نسازد. همه اجزا—بدهکار، وجه تضمین، کارمزد و جمع—جدا و در ریال بمانند.
6. بودجه خانواده با سرمایه لازم سنجیده شود و حداکثر حجم بودجه جدا گزارش
   شود؛ ظرفیت دفتر `portfolioEntryPlan` پنهانی کوچک نشود.

**مرز قلم.** امتیازدهی، رتبه‌بندی، پیشنهاد UI، انتخاب بهترین‌ها و ثبت
موقعیت هنوز ممنوع‌اند. این برش فقط مبنای سرمایه قابل حسابرسی می‌سازد.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
