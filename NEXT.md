# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## کار

### پذیرش زنده «نگاه باز» و نبض بازار در یک روز معاملاتی

در یک جلسه بازار باز، حالت «تا همین لحظه» نگاه باز را دست‌کم روی یک پایه و
یک سررسید پرمعامله بسنج: ردیف امروز باید با هر عکس تازه به‌روز شود، حالت
درون‌روزی باید از آخرین معامله هر سطل ساخته شود و هیچ قیمت پایه یا قرارداد
از سطل قبلی حمل نشود. هم‌زمان پذیرش بیست‌دقیقه‌ای نبض زنده را کامل کن.

## وضعیت رصد لحظه‌ای

مسیر اصلی:

`کل بازار ← نقشه پایه‌ها ← پایه ← نقشه قراردادها ← سررسید ← کندل امروز + زنجیره ← قرارداد`

- کندل قرارداد: سایه کمینه/بیشینه، بدنه اولین/آخرین، لوزی پایانی و نشان
  آخرین. پنج قیمت با hover دیده می‌شوند؛ درصد آخرین و پایانی نسبت به دیروز
  مستقل‌اند و مثبت/منفی سبز/قرمز است.
- «نگاه باز» دو دامنه دارد: روزهای بسته‌شده و تا همین لحظه. در جزئیات، منبع
  میان روز تاریخی و امروز زنده عوض می‌شود. زنده، ریزمعامله را در دسته‌های
  حداکثر ۲۴ کد می‌گیرد و قیمت آخرین معامله هر سطل را با وزن ارزش واقعی
  قراردادها ترکیب می‌کند.
- چرخه رفرش داشبورد، نمای زنده بازشده را هر ۱۲ ثانیه یا بیشتر تازه می‌کند؛
  نمای بسته درخواست اضافه ندارد. پنج‌شنبه/تعطیل و پیش از بازار به امروز مهر
  نمی‌خورند.
- هفت نمای «نبض زنده بازار» بین نقشه و ۶۸ نمای تکمیلی حفظ شده‌اند.

فایل‌های محوری این قلم: `core/open-view.mjs`، `core/live-day.mjs`،
`ui/tabs/open-view.mjs`، `ui/live-market-map.mjs` و
`ui/tabs/live-market-dashboard.mjs`. قفل‌ها: دسته‌های ۶۵، ۷۱، ۲۴۵ و ۲۴۶.

## پذیرش بعدی در بازار باز

۱. روی یک پایه پرمعامله، آخرین نقطه ریز زنده را با آخرین ریزمعامله پایه و
دو قرارداد (یک کال و یک پوت) تطبیق بده؛ VWAP نباید به‌جای آخرین قیمت بنشیند.

۲. با رفرش‌های پیاپی، اضافه/به‌روزرسانی ردیف امروز در نمودار چندروزه را
بررسی کن و مطمئن شو با تغییر سررسید، قرارداد سررسید قبلی درخواست نمی‌شود.

۳. طی دست‌کم ۲۰ دقیقه، شدت ارزش کال/پوت، روند سفارش‌ها و تغییر موقعیت باز
را بسنج؛ ریست شمارنده تجمعی نباید شدت منفی جعلی بسازد.

۴. جمع پنج ردیف سفارش پایه را برای دو پایه پرمعامله با تابلوی رسمی تطبیق
بده و زمان تکمیل دفترها را از `/api/health.byEndpoint` ثبت کن.

۵. کندل را روی قرارداد مثبت، منفی و تک‌قیمتی در هر دو پوسته و عرض موبایل
بررسی کن؛ tooltip نباید از کارت بیرون بزند.

## کارهای باز قبلی

- مقصدهای بیشتر برای نوار پیوند: دیده‌بان زنجیره، تحلیل تاریخی استراتژی و
  موقعیت‌های من؛ پذیرش `state.handoff` باید پیش از افزودن مقصد انجام شود.
- اختلاف دو معامله `طهرم7061` میان شمارنده رسمی و ریزمعامله هنوز نیاز به
  تعیین منبع مرجع دارد.
- تاریخچه نبض فعلاً فقط در جلسه مرورگر است؛ ماندگاری بیش از یک روز تصمیم
  ذخیره‌سازی صاحب پروژه می‌خواهد.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است.
- روز جاری فقط در فاز `open`، `after` یا `ungated` به تاریخ امروز منتسب شود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.
- همه نماهای قدیمی در «تحلیل‌های تکمیلی» حفظ شوند.

**دروازه:** `node tools/check.mjs`
