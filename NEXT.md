# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پایان بازی، سرمایه نهایی و رتبه استراتژی‌ها پس از refresh — قلم هفتم فاز ۸

**چرا.** جلسه اکنون با snapshot هم‌لحظه جلو می‌رود، حجم همان Position را
کم یا زیاد می‌کند و آفست را با Lotهای FIFO، سود تحقق‌یافته و دفتر سرمایه
پایدار ثبت می‌کند. قدم بعدی باید پاسخ اصلی بازی را بدهد: در تاریخ پایان
سرمایه چقدر شد، انتخاب کاربر بین گزینه‌های قابل اجرای همان شروع چه رتبه‌ای
گرفت و بهترین/بدترین انتخاب قابل مقایسه چه بود.

**شاخه.** با اجازه صریح صاحب پروژه، کامیت‌های مستقل را روی
`codex/portfolio-build-refresh` ادامه دهید. پیش از قلم تازه `origin/main` را
بررسی کنید؛ تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
core/portfolio-closeout.mjs
core/portfolio-dossier-analysis.mjs
core/portfolio-dossier-compare.mjs
core/portfolio-capital-growth.mjs
ui/portfolio-closeout-view.mjs
ui/tabs/portfolio-time.mjs
server/portfolio-dossier-store.mjs
tests/suites/176-portfolio-final-ranking-refresh.mjs
```

**پذیرش:**

1. پایان بازی فقط در تاریخ/لحظه پایان قفل‌شده یا با تأیید صریح پایان زودتر
   انجام شود؛ موقعیت باز بی‌صدا صفر یا بسته فرض نشود.
2. هر خروج نهایی از دفتر سفارش همان لحظه و کارمزد واقعی عبور کند. داده یا
   عمق ناکافی، پرونده را با علت ناقص نگه دارد و سرمایه نهایی ساختگی نسازد.
3. سرمایه پایان از سرمایه آزاد، نقد خروج‌ها، کارمزدها و سود تحقق‌یافته دفتر
   immutable بازسازی شود و با جمع مسیر سرمایه و وضعیت Positionها سازگار باشد.
4. انتخاب کاربر فقط با استراتژی‌هایی رتبه‌بندی شود که در snapshot شروع واقعاً
   اجراپذیر بوده‌اند و برای کل بازه داده کافی دارند؛ حذف‌شده‌ها با علت در
   «بدون رتبه» بمانند، نه اینکه بازده صفر بگیرند.
5. رتبه کاربر، تعداد رقیب، صدک، بهترین و بدترین استراتژی با بازده ریالی و
   درصدی هم‌مبنا نمایش داده شود؛ مقایسه از آینده برای تصمیم شروع استفاده نکند.
6. پرونده نهایی ابتدا روی سرور ثبت شود و بعد رابط session را بسته و
   فقط‌خواندنی نشان دهد. شکست/تعارض، بازی active و قابل تلاش دوباره بماند.
7. refresh همان پرونده، سرمایه نهایی، رتبه، شواهد اجرا، شناسه‌ها و علت‌های
   بدون‌رتبه را عیناً بازگرداند؛ درخواست تکراری پرونده دوم نسازد.
8. آزمون یکپارچگی `176-portfolio-final-ranking-refresh.mjs` مسیر پایان و
   بازیابی را قفل کند. آزمون بصری فقط با دستور تازه صاحب پروژه انجام شود.

**مرز قلم.** پذیرش فایل Excel کم‌حجم ولی پرجزئیات و سفر کامل کاربر در
مرورگر، در دو قلم پایانی فاز ۸ انجام می‌شوند.

## محدودیت‌های باز

- universe تاریخی هنوز فقط قراردادهای موجود در آرشیو محلی را می‌شناسد؛
  قرارداد منقضی‌شده بدون عکس روزانه قابل بازسازی نیست.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم هفتمین
وظیفه از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق ابزار پیشرفت بنویسید.
دروازه کامل، کامیت دارای بدنه و پوش روی همین شاخه لازم‌اند؛ سپس بدون انتظار
برای مرج قلم هشتم فاز ۸ را آغاز کنید.
