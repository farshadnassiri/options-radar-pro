# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## بک‌لاگ — یازده قلمِ صاحب پروژه

```text
۱  نام نمادها در جدول و نمودار            ✔ جدول تمام · نمودار: فقط «دو نرخ» دارد
۲  همهٔ آیتم‌ها در جدول، ستون‌ها انتخابی    ✔ ۴۶ ستون در ui/radar-columns.mjs
۳  نمودارها بزرگ‌تر                        ✔ ۳۴۰/۴۴۰/۵۲۰ پیکسل، ستون شبکه ۴۶۰
۴  «خانواده» برود، نامِ استراتژی بیاید     ✔ optgroup بر پایهٔ خانواده
۵  جدول ویژوال و رنگی، نمودار در سلول      ✔ نوار پرشدگی، اسپارک‌لاین، نوار سود/زیان
۶  «ارزش معامله» در پالایه                 ✔ کمینهٔ ارزش و حجمِ نازک‌ترین پا
۷  short strangle درست شود                 ✔ لنگر شد دهانهٔ اعمال، بازده بر وجه تضمین
۸  رصد زنده + روند تاریخی، با شکل متفاوت   ◐ joinLive نوشته شد · رابط نمانده
۹  تب سیستم اعلان                          ◐ موتور watch-rule تمام · تب نساخته
۱۰ شرط روی ترکیب خاص/عمومی و چند نماد      ◐ موتور تمام · رابط نمانده
۱۱ رصد لحظه‌ای از شروع بازار تا اکنون       ◐ joinLive آماده · واکشیِ نوارِ امروز نمانده
```

بعد از این یازده: **بازطراحی ظاهر** — «حتی css را هم تغییر بده و ظاهر
همهٔ المان‌ها و آیتم‌های سایت را بهبود بده»، با تغییرات مینیمالِ بنیادی.

## کار — از اینجا شروع کن

### الف. رابطِ قلم‌های ۸ و ۱۱ (نیم‌ساعت)

`core/spread-gap-series.mjs` تابع `joinLive(daily, intraday)` را دارد:
دنبالهٔ درون‌روزیِ امروز را به سریِ روزانه می‌چسباند، نقاطِ امروز را
`live: true` می‌زند و مرز را در `liveFrom` می‌گذارد. `hasLiveTail(series)`
هم هست.

مانده:

1. در `ui/gap-charts.mjs`، `gapPathChart` و `gapBandChart` سریِ زنده را
   جدا بکشند — خطِ چین یا رنگِ `--accent-2` — و نقطهٔ مرزی در هر دو
   بماند تا پیوسته دیده شود.
2. در `ui/tabs/spread-radar.mjs`، وقتی «رصد زندهٔ بازار» روشن است، نوارِ
   ریزمعاملهٔ **امروز** برای پاهای ترکیب انتخاب‌شده و نماد پایه واکشی
   شود (`/api/trades/batch` با تاریخِ امروزِ تهران از
   `tehranDateNumber()` در `core/live-day.mjs`) و با `joinLive` به سری
   روزانه بچسبد. تایم‌فریم روی سریِ مخلوط اعمال نمی‌شود (`grain: 'mixed'`).
3. آزمون رگرسیون در دستهٔ ۲۲۲.

### ب. تب «دیده‌بان شرطی» — قلم‌های ۹ و ۱۰

موتورش کامل است: `core/watch-rule.mjs` با ۳۶ ادعا در دستهٔ ۲۲۴.
`WATCH_METRICS` (۲۰ سنجه، سه گروه)، `WATCH_REFS` (مطلق، ٪ از میانگین/
کمینه/بیشینهٔ N روز گذشته، ٪ از کف و سقف امروز)، `normalizeWatchRule`،
`checkRule`، `evaluateWatch` و `watchSnapshot`.

مانده: خودِ تب. یک فایل تازه در `ui/tabs/`، ثبتش در `TABS` در
`ui/app.mjs`، و **به‌روزکردن شمار تب در `README.md` و `AGENTS.md`** —
نگهبان ۸ اگر عدد کهنه بماند قرمز می‌شود.

شکلِ پیشنهادی: انتخاب چند استراتژی و چند نماد پایه (یا یک ترکیبِ مشخص از
رادار) · افزودن شرط‌ها یکی‌یکی · دکمهٔ «چه چیزی همین حالا منطبق است»
که با `evaluateWatch` پیش‌نمایش می‌دهد (از `matched`، نه `fired`) و
همان ستون‌های `RADAR_ALL_COLS` را در `makeTable` نشان می‌دهد · و
«شروع رصد» که حلقهٔ زنده را روشن می‌کند و از `ui/gap-alarm.mjs` اعلان
می‌فرستد.

### ج. رابط در مرورگر دیده نشده

تغییرات این نوبت (جدولِ تازه، ستون‌ها، مقیاس نمودار) فقط دروازه را رد
کرده‌اند. با Playwright و دادهٔ مسیریافته اجرا کن: جدول باید ۴۶ ستون در
پنل «ستون‌ها» بدهد، نوار پرشدگی و اسپارک‌لاین در سلول رسم شوند، و
مرتب‌سازی روی هر ستون کار کند. صفر خطای کنسول.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی).

۱. **عددها را با تابلو بخوان.** اسپرد عمودی در مقیاس «قیمت خام»؛ بعد
مقیاس را عوض کن و ببین درصدِ پرشدگی **تکان نمی‌خورد**.
۲. **استرانگل فروش.** ستون لنگر باید «دهانهٔ اعمال» بدهد، «حداکثر زیان»
باید «نامحدود» بنویسد نه عدد، و «حداکثر سود ٪» باید بر وجه تضمین باشد.
۳. **هشدار زنده در ساعت بازار.** هنوز در هیچ نوبتی سنجیده نشده.
