# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم‌های بسته‌شده (۱۴۰۵/۰۶/۲۴)

چهار دور ممیزی، همه در یک روز. الگوی مشترکشان: **آزمونِ سبزِ بی‌فایده** —
ادعایی که متنِ کد را می‌سنجید، نه رفتار را.

- **ردیف نماد پایه** به جایگاهِ خط قیمت جاری گره خورده بود، پس با مرتب‌سازی
  روی هر ستون دیگر ناپدید می‌شد. `spotRowPlacement` حالا تصمیم می‌گیرد.
- **گام سوم بک‌تست**: روز جاری از `/api/trades` تاریخی خوانده می‌شد که تا
  پایان روز خالی است. `core/trades-source.mjs` روز جاری را به نوار زنده
  می‌برد؛ روزهای بسته‌شده از `/api/trades/batch` می‌آیند (۴۵ روز = یک
  درخواست، نه ۱۳۵)؛ سقف «کل بازه» ۴۵ → ۴۰۰؛ پیشرفت و خطا داخل همان تب؛ و
  «دریافت نشد» از «معامله نشده» جدا شمرده می‌شود.
- **یونانی‌های اجرایی**: `midGreeks` همه را می‌ساخت و فقط `delta` برمی‌گشت.
  حالا هر شش‌تا به‌علاوهٔ `effectiveLeverageMid`، و `ivMidWhyText` ستون دارد.
  مظنهٔ یک‌طرفه دیگر «بدون مظنه» گزارش نمی‌شود (`oneSided`).

## کار

### پذیرش روز جاری در جلسهٔ بازار باز

۱. **تقویم**: با دامنهٔ «تا همین لحظه» آخرین روز باید **امروز** باشد؛ با
«تا آخرین روز بسته‌شده» دیروز. هر دو را ببین. ۲. **هزینه**: پس از «دریافت
تاریخچه»، شمار `/api/live-trades` باید `ceil(n/24)` باشد و قراردادِ
بی‌معامله در فهرست `ins=` نیاید. ۳. **مبنای اولین/کمترین/بیشترین** روی روز
جاری عدد بدهد؛ اگر «—» داد، جملهٔ دامنه باید گفته باشد چند ردیف ناقص است.
۴. **پس از ۲۰:۳۰ گرینویچ** سقف بازه باید روزِ تهران باشد. ۵. **تابلوی قطع**
(`source=watch-archive`): هیچ ردیف امروزی ساخته نشود و علت گفته شود.

### پذیرش صفحهٔ رصد زنده در جلسهٔ بازار باز

۶. **نگاه باز لحظه‌ای**: تب شبکه باز باشد؛ نباید هیچ `history/universe` یا
`dailies` برود. ۷. **«ارزش خود پایه»** دیگر نباید صفر باشد. ۸. **ساعت** در
بازار بسته «عکس … · N ثانیه پیش» بگوید و نوار قرمز شود. ۹. **زنجیرهٔ
دوطرفه**: سایهٔ «در سود» سمت کال زیر قیمت جاری و سمت پوت بالای آن؛ دلتا کال
مثبت، پوت منفی. ۱۰. **یونانی‌های اجرایی**: ستون‌های تازه (گاما/تتا/وگا/رو
اجرایی) باید هرجا «تلاطم اجرایی» عدد دارد پر باشند. ۱۱. **پایداری**: رفرش
پیوستهٔ ۲۰ دقیقه‌ای.

### پذیرش گام سوم بک‌تست

۱۲. بازه‌ای که امروز را در بر بگیرد: شمار درخواست‌ها باید یک POST دسته‌ای
به‌ازای هر ۱۲۰۰ جفت به‌علاوهٔ یک GET نوار زنده باشد — نه بیشتر. ۱۳. روزِ
جاری نباید در شمارندهٔ «بدون نقطهٔ مشترک» بیفتد. ۱۴. با قطع شبکهٔ عمدی، روز
باید در «دریافت نشد» بیفتد، نه «بی‌معامله».

## قلم بعدی — نرخ بدون ریسک سراسری ۳۰٪ (تصمیم صاحب پروژه، دو بار تأیید شده)

ممیزی ردیف ۹ (دور قبل) درست بود و اندازه‌گیری نشان داد **علتِ غالبِ
ستون‌های خالیِ تلاطم همین است**، نه ناهم‌زمانی قیمت:

```
نرخ ۳۰٪ → کف نظری ۶٬۲۲۸ → قیمت ۴٬۶۳۱ زیر کف  →  belowFloor
نرخ  ۰٪ → کف نظری ۴٬۵۷۲ → قیمت ۴٬۶۳۱ بالای کف →  IV ۱۳٫۸٪
```

کفِ بدون‌آربیتراژ وقتی معنی دارد که بشود پایه را فروش استقراضی کرد؛ در این
بازار نمی‌شود. ستون‌های «کف نظری قیمت» و «فاصله قیمت از کف نظری» اضافه
شدند تا فرض دیده شود، ولی **انتخاب خودِ فرض تصمیم صاحب پروژه است**:

۱. نرخِ جداگانه برای کفِ بدون‌آربیتراژ (مثلاً صفر) در کنار نرخِ تنزیل.
۲. بازده نقدی به‌ازای نماد، از منبع داده‌ای که برنامه الان ندارد.
۳. مدل سود نقدی گسسته برای رویدادهای قطعی.

هیچ‌کدام بدون تصمیم صاحب پروژه پیاده نمی‌شود — عددِ فرضیِ ساخته‌شده دقیقاً
همان چیزی است که قاعدهٔ ۲-۴ منع می‌کند.

ممیزی دومِ همان روز (یونانی‌های خالی) مستقلاً به همین نتیجه رسید و آن را
«مهم‌ترین علت» نامید. پس این تنها قلمِ باقی‌مانده از هر دو گزارش است، و
تنها چیزی که لازم دارد یک جملهٔ صاحب پروژه است: **کدام یک از سه راه؟**

بقیهٔ ردیف‌های آن گزارش یا اصلاح شدند (۴، ۵، ۶) یا توصیفِ درستِ رفتارِ
عمدی بودند (۲، ۳، ۷) — و آن رفتار عمدی است چون جایگزینش ساختنِ عدد است.

## قلم باز — اختلاف شمارش ریزمعامله

برای طهرم۷۰۶۱ عدد رسمی تابلو ۶٬۷۹۶ معامله بود و نوار ریزمعامله پس از حذف
معاملات باطل ۶٬۷۹۸ ردیف فعال داد. اول باید سنجید کدام endpoint مرجع است،
بعد تصمیم گرفت کدام عدد نمایش داده شود — نه اینکه یکی بی‌صدا جایگزین شود.

## قلم‌های باز

- **بارِ بالادست**: تصمیم دربارهٔ `byEndpoint` در `/api/health` منتظر
  اندازه‌گیری صاحب پروژه است.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: اگر مقایسهٔ بازه‌ای فراتر از عمر یک نسل قرارداد لازم
  شد، باید مدل صریح شود؛ قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» هم از آخرین معاملهٔ نوار ساخته نمی‌شود.
- روز جاری فقط در فاز `open`، `after` یا `ungated` **و** از منبع `watch` یا
  `snapshot` به تاریخ امروز منتسب شود.
- ورودیِ آزمایشی باید هم‌شکلِ پاسخ واقعی باشد. قاعده‌ای که فقط با دادهٔ
  ساختگی برقرار می‌شود، آزمونِ سبزِ بی‌فایده است — همین یک بار ده ردیف
  ممیزی ساخت.
- ادعای آزمون باید در **همهٔ** ساعت‌ها بگزد، نه فقط در بعضی‌شان. ساعت را
  تزریق کن، از `Date.now()` نسنج.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.

**دروازه:** `node tools/check.mjs`
