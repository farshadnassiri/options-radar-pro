# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### ساختن عکس شروع واقعی — برش هفتم فاز ۵

**چرا — و این مهم‌ترین شکاف پروژه است.** در برنامهٔ زنده،
`session.startSnapshot.contracts` را **هیچ‌کس نمی‌سازد**. عکسی که ویزارد
می‌سازد شکل دیگری دارد (`universe`/`daily`/`intraday`/`book`)، ولی همهٔ
موتورهای سبد شکل `{ at, spot, contracts, capitalInputs }` را مصرف
می‌کنند. یعنی حکم اجراپذیری، ترکیب‌ها، پیشنهادها، ثبت، بستن و
ارزش‌گذاری — همه — امروز فقط زیر آزمون زنده‌اند و در برنامهٔ واقعی
داده‌ای نمی‌بینند.

`core/portfolio-snapshot.mjs` سازندهٔ خالص را دارد (دستهٔ ۱۴۳)؛ آنچه
نیست، لایه‌ای است که قراردادهای تاریخی یک لحظه را از سرور می‌گیرد و به
آن می‌دهد.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
ui/portfolio-snapshot-data.mjs      گرفتن قراردادهای یک لحظه از سرور
ui/tabs/portfolio-time.mjs          وصل‌کردنش به قفل مأموریت
tests/suites/146-portfolio-snapshot-data.mjs
```

`node tools/next.mjs map portfolioMomentSnapshot`. الگوی واکشی را از
`startSnapshot` در `ui/tabs/portfolio-time.mjs` بردارید (`gateLoaders`،
`createTimeGate`، `/api/history/universe`). `repriceAt` در همان فایل
همین کار را برای لحظهٔ تازه می‌کند — احتمالاً باید یکی شوند.

**پذیرش:**

1. فهرست قراردادها از `/api/history/universe` همان تاریخ بیاید، نه
   دیده‌بان امروز.
2. هر قرارداد از دروازهٔ زمان قیمت بگیرد؛ نبودِ داده `missing` شود، نه
   قیمت روز قبل.
3. خروجی مستقیم به `portfolioMomentSnapshot` داده شود — شکل دوم ساخته
   نشود.
4. شکستِ واکشی، عکسِ ناقص را **صریح** علامت بزند؛ ادامهٔ خاموش ممنوع.
5. تعداد واکشی‌ها کران‌دار باشد — زنجیرهٔ واقعی صدها قرارداد دارد و
   واکشیِ همه، تب را می‌بندد. کرانه صریح و گفته‌شده باشد.
6. پس از قفل مأموریت، جلسه باید `startSnapshot.contracts` واقعی داشته
   باشد — یک ادعا این را از سر تا ته بسنجد.

**راستی‌آزمایی مرورگر لازم است** — و این بار **با دادهٔ واقعی**: اگر
`data/` تاریخچه دارد، ویزارد را تا قفل مأموریت بپیمایید و ببینید
جدول‌ها پر می‌شوند یا نه. این همان محدودیتِ بازِ چند قلم گذشته است.

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سرمایه و پیشنهاد ربطی ندارد
(هر دو در ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ سنجیده شدند و سرریز ندارند)، ولی یک بار
باید دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

بخش دفتر سرمایه در مرورگر با بازکردن دستی `#pt-review-step` سنجیده شد،
چون بدون تاریخچهٔ واقعی نمی‌شود جلسه را فعال کرد. چیدمان و سرریز واقعی
سنجیده شدند؛ مسیر رسیدنِ کاربر به آن هنوز با دادهٔ واقعی دیده نشده.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
