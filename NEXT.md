# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### حرکت ساعت، تغییر حجم و آفست پایدار پس از refresh — قلم ششم فاز ۸

**چرا.** پیشنهاد با حجم صریح اکنون فقط پس از تأیید سرور وارد سبد می‌شود و
جلسه active پس از refresh با همان موقعیت و دفتر سرمایه بازمی‌گردد. قدم بعدی
این است که بازی از لحظه ورود عبور کند: زمان جلو برود، کاربر حجم را کم یا زیاد
کند و بخشی یا تمام موقعیت را آفست کند، بدون قیمت ساختگی یا از دست رفتن تاریخچه.

**شاخه.** با اجازه صریح صاحب پروژه، کامیت‌های مستقل را روی
`codex/portfolio-build-refresh` ادامه دهید. پیش از قلم تازه `origin/main` را
بررسی کنید؛ تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
ui/tabs/portfolio-time.mjs
core/portfolio-clock.mjs
core/portfolio-session.mjs
core/portfolio-close.mjs
server/portfolio-mission-store.mjs
tests/suites/137-portfolio-close.mjs
tests/suites/142-portfolio-clock.mjs
tests/suites/175-portfolio-path-adjust-refresh.mjs
```

**پذیرش:**

1. ساعت جلسه active فقط روی گام‌های مجاز تقویم جلو برود و snapshot همان نماد
   و لحظه را مصرف کند؛ نبود داده نباید با آخرین قیمت یا قیمت آینده پر شود.
2. افزایش یا کاهش حجم صریح کاربر دوباره از دفتر سفارش هم‌لحظه، VWAP پاها،
   کارمزد، وجه تضمین، بودجه خانواده و ریسک عبور کند؛ مقدار نامعتبر یا بیش از
   ظرفیت رد شود و سیستم آن را پنهانی کوچک نکند.
3. کاهش جزئی، lotها را با قاعده FIFO مصرف کند و آفست کامل موقعیت را ببندد؛
   Transaction/Execution/Lot و رویدادهای immutable شناسه پایدار و قابل ردگیری
   داشته باشند و سود تحقق‌یافته از تحقق‌نیافته جدا بماند.
4. اعلام حرکت ساعت، تغییر حجم یا آفست فقط پس از ذخیره موفق سرور انجام شود؛
   شکست یا تعارض، آخرین وضعیت ثبت‌شده را نگه دارد و امکان تلاش دوباره بدهد.
5. refresh دقیقاً لحظه جاری، حجم باز، lotها، دفتر سرمایه، شواهد سود و زیان،
   شمارنده‌ها و شناسه‌ها را بازسازی کند و تکرار درخواست رویداد دوم نسازد.
6. دفتر ناقص یا نقدشوندگی ناکافی هیچ معامله یا سود و زیان ساختگی نسازد و علت
   رد در رویداد و رابط قابل مشاهده بماند.
7. آزمون یکپارچگی `175-portfolio-path-adjust-refresh.mjs` مسیر ورود، حرکت،
   تغییر حجم، آفست جزئی/کامل و refresh را قفل کند. مرورگر واقعی نیز در صورت
   دسترس بودن، نبود خطای کنسول و یکپارچگی رابط را بررسی کند.

**مرز قلم.** بستن کل جلسه، رتبه‌بندی نهایی، پرونده معامله و خروجی اکسل در
قلم‌های بعدی پذیرش می‌شوند.

## محدودیت‌های باز

- universe تاریخی هنوز فقط قراردادهای موجود در آرشیو محلی را می‌شناسد؛
  قرارداد منقضی‌شده بدون عکس روزانه قابل بازسازی نیست.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم ششمین وظیفه
از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق ابزار پیشرفت بنویسید. دروازه
کامل، کامیت دارای بدنه و پوش روی همین شاخه لازم‌اند؛ سپس بدون انتظار برای
مرج قلم هفتم فاز ۸ را آغاز کنید.
