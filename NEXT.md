# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## دو دور گزارشِ رصد زنده — هر ۲۴ ایراد بسته شد

هجده ایرادِ دورِ اول روی `47bce1e`، و شش ایرادِ دورِ دوم روی `c03e06d`.
شرحشان در دو ورودیِ بالای `WORKLOG.md`. خلاصهٔ دورِ دوم:

```text
۱  ۸ قاعده نشان می‌داد، ۳تا رصد می‌شد   ✔ ruleCoverage + «۳ از ۸» + دکمهٔ اجتماع
۲  شرطِ قیمت پایه در دیده‌بان کار نمی‌کرد ✔ یک‌سومِ سهمیه برای نمادهای پایه
۳  تغییر بازه، رصد را نمی‌خواباند        ✔ stalePreview→stopWatch + دروازه در هر تیک
۴  «ترتیب جدول» از جدول پیروی نمی‌کرد    ✔ listedOrderScore(table.get())
۵  ستون قیمت پایه زنده نبود              ✔ row.spot با هر تیک
۶  تیک/وضعیت/توضیح ناسازگار می‌شدند      ✔ هر سه با هم در stopLive
```

و دو محدودیتی که محدودیت نبودند: **مظنهٔ قابل اجرا** (`/api/books` از
روز اول در مخزن بود؛ حالا «منبع مظنه» انتخابی است و در حالتِ دفتر پای
خریدنی با بهترین عرضه و پای فروختنی با بهترین تقاضا قیمت می‌خورد، با
قیدِ عمق) و **چرخشِ سهمیه** (`startAt`/`nextStart`، پس ترکیبی برای
همیشه بیرون نمی‌ماند؛ هشدار فقط روی ترکیب‌های همین تیک، و عددِ زندهٔ
کهنه‌تر از سه دقیقه به عددِ روز سنجش برمی‌گردد).

نقشهٔ کد: `core/live-quote.mjs` (سهمیه، چرخش، دو منبع، تعریفِ «زنده») ·
`core/day-range.mjs` · `ruleCoverage`/`ruleScopeUnion`/`watchDistance` در
`core/watch-rule.mjs` · `expiryShortfall` در `core/radar-history.mjs` ·
`deliverBurst` در `ui/gap-alarm.mjs`. آزمون: دستهٔ ۲۲۹ و ۲۳۰.

**بازطراحی ظاهر لغو شد** — صاحب پروژه پس‌گرفتش. سراغش نروید.

## درسِ نوبتِ گذشته — پیش از نوشتنِ آزمون بخوانید

دورِ دوم شش ایراد داشت در حالی که دروازه سبز بود. علتش یک الگوی غلط در
آزمون‌ها بود: **ادعای منبع، آزمونِ رفتار نیست.**

```js
check('...', src.includes("watchRules = active"));   // فقط می‌گوید رشته هست
```

چنین ادعایی وقتی هم سبز می‌ماند که `active` خودش غلط ساخته شده باشد. هر
جا شد، منطق را به یک تابعِ خالص در `core/` ببرید و **رفتار** را بسنجید؛
ادعای منبع فقط برای «این سیم به آن سیم وصل است» بماند، و همان را هم روی
شکلِ کد ننویسید که با یک بازآرایی بی‌صدا بشکند یا بی‌صدا سبز بماند.

## کار — قلم بعدی را از اینجا بردارید

### الف. دانه‌بندی درون‌روزی روی بیش از یک روز

امروز فقط یک روز است و علتش در `ui/tabs/spread-radar.mjs` نوشته شده:
ریزمعاملهٔ هر پا برای هر روز یک درخواست است. راهِ درست، کش‌کردنِ
ریزمعامله روی دیسک است — مثل `data/roster` — نه بالا بردن سقف درخواست.
انگیزهٔ دوم: «کف و سقفِ امروز» فعلاً فقط از مشاهده‌های خودِ رصد ساخته
می‌شود (`core/day-range.mjs`)؛ با کشِ دیسکی می‌تواند کفِ واقعیِ جلسه شود
و نامش از «مشاهده‌شده» دربیاید.

### ب. کهنگیِ دفترِ سفارش

منبعِ دفتر «قابل اجرا» است ولی **زمان ندارد**: تابلو برای سطوحِ دفتر
مهرِ زمان نمی‌دهد، پس نمادِ متوقف هم سفارشِ باقی‌مانده نشان می‌دهد.
`/api/infos` برای هر ابزار `state` و `staleSec` می‌دهد. وصل‌کردنش به
حلقهٔ رصد (یک درخواست برای همان ابزارهای همان تیک) اجازه می‌دهد ترکیبی
که نمادش متوقف است «قابل اجرا» خوانده نشود. این تنها ادعای باقی‌ماندهٔ
منبعِ دفتر است که پشتوانه ندارد.

### ج. عمقِ بیش از سطح اول

`comboBookQuote` فقط سطح اول را می‌خواند. برای حجم‌های بزرگ، قیمتِ
میانگینِ اجرا از چند سطح درمی‌آید و `/api/books` هر پنج سطح را می‌دهد.
ماژول `core/book-history.mjs` این ریاضی را برای مسیر تاریخی دارد؛ همان
باید اینجا هم استفاده شود، نه نسخهٔ دوم.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی). دو دورِ گذشته
نشان داد بازپخشِ واقعی چیزی می‌بیند که دروازه نمی‌بیند:

۱. **مقیاس.** اسپرد عمودی در «قیمت خام»؛ مقیاس را عوض کن و ببین درصدِ
پرشدگی تکان نمی‌خورد.
۲. **استرانگل فروش.** «لنگر» = دهانهٔ اعمال، «حداکثر زیان» = نامحدود نه
عدد، «حداکثر سود ٪» بر وجه تضمین.
۳. **دنبالهٔ امروز.** در ساعت بازار، خطِ نقطه‌چینِ امروز روی روند گذشته
با برچسب «اکنون».
۴. **منبعِ دفتر.** «بهترین خرید/فروش» را بزن و عددِ فاصله را با دفترِ
تابلو بسنج: پای خریدنی روی عرضه، پای فروختنی روی تقاضا. «کمینه عمق» را
ده بگذار و ببین ترکیبِ کم‌عمق کنار می‌رود.
۵. **چرخش.** با چند صد ترکیب، شمارِ «ترکیب دارای عددِ زنده» باید
تیک‌به‌تیک تا کلِ دامنه بالا برود و با طولِ دورِ اعلام‌شده بخواند.
۶. **چند قاعده، چند دامنه.** دو قاعده روی دو نماد بساز، دامنهٔ یکی را
بساز: نوار باید «۱ از ۲ قاعده» بگوید و کارتِ آن یکی «در این ساخت داده
ندارد». بعد «دامنه را برای همهٔ قاعده‌ها بچین» را بزن.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. پای بی‌قیمت فاصله را باطل می‌کند، پاهای
ناهم‌زمان ترکیبِ زنده نمی‌سازند، پایی که یک سمتِ دفترش خالی است در آن
جهت اجرا نمی‌شود، عددِ زندهٔ پوسیده به عددِ روز سنجش برمی‌گردد، «کف
امروز» بی مشاهدهٔ امروز عدد ندارد، قیمتِ پایهٔ نداشته `NaN` می‌ماند، و
قاعده‌ای که داده ندارد «در حال رصد» خوانده نمی‌شود.

اگر روزی یکی از این‌ها شکست، یکی از دسته‌های ۲۱۸ تا ۲۳۰ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون

پورت جداگانه (۸۷۹۴) و پس از کار بسته شود؛ ۸۷۸۷ دستِ صاحب پروژه است.
کرومیوم در `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` و
Playwright سراسری: از پوشهٔ موقت با
`ln -s /opt/node22/lib/node_modules node_modules`.
