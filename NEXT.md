# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### موقعیت‌های جلسه در تب — برش چهارم فاز ۴

**چرا.** `core/portfolio-positions.mjs` می‌گوید چه چیزی در دست است — پاها،
اعمال، حجم، قیمت ورود، کیفیت — ولی کاربر هیچ‌کدام را نمی‌بیند. بعد از
ثبت هنوز فقط یک شناسهٔ موقعیت به او نشان داده می‌شود.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
ui/portfolio-positions-view.mjs     مدل نمایش از خروجی موقعیت‌ها
ui/tabs/portfolio-time.mjs          بخش موقعیت‌ها زیر نوار سرمایه
tests/suites/136-portfolio-positions-view.mjs
```

`node tools/next.mjs map portfolioSessionPositions`. الگوی لایهٔ نمایش را
از `ui/portfolio-ledger-view.mjs` بردارید — از جمله ادعایی که هر حساب روی
عدد ریالی جز تقسیم بر ده را ممنوع می‌کند. چیدمان آزمون از
`tests/fixtures/portfolio.mjs`؛ نگهبان ۱۰ کپی‌اش را قرمز می‌کند.

**پذیرش:**

1. هر موقعیت، پاهایش را در یک ردیف خوانا نشان دهد: نوع و سمت و اعمال و
   سررسید و حجم و `vwap` — همه با رقم فارسی و تومان.
2. تنها حسابِ مجاز تقسیم بر ده است؛ یک ادعا آن را قفل کند.
3. موقعیت بی‌سند **دیده شود** با علتش، نه حذف و نه شبیه یک موقعیت سالم.
4. باز و بسته از هم جدا؛ شمارش‌ها با فهرست‌ها یکی باشند.
5. کیفیت برآوردی روی همان ردیف دیده شود، نه در جای دیگر.
6. جلسهٔ بدون موقعیت، پیام صریح بدهد نه جدول خالی.

**نام‌گذاری:** پیش از انتخاب شناسه و نام تابع، یکتایی‌شان را در
`ui/tabs/portfolio-time.mjs` بسنجید. برش دوم فاز ۴ با `pt-capital` و
`paintCapital` تصادم کرد و تابع بی‌صدا رونویسی می‌شد. ادعای «شناسه‌ها
تصادم ندارند» در دستهٔ ۱۳۴ الگویش را دارد.

**راستی‌آزمایی مرورگر لازم است** (۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴): بدون سرریز افقی
صفحه و بدون خطای کنسول.

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سرمایه و پیشنهاد ربطی ندارد
(هر دو در ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ سنجیده شدند و سرریز ندارند)، ولی یک بار
باید دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

بخش دفتر سرمایه در مرورگر با بازکردن دستی `#pt-review-step` سنجیده شد،
چون بدون تاریخچهٔ واقعی نمی‌شود جلسه را فعال کرد. چیدمان و سرریز واقعی
سنجیده شدند؛ مسیر رسیدنِ کاربر به آن هنوز با دادهٔ واقعی دیده نشده.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
