# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## دورهٔ جاری

دو ممیزیِ «کامل‌بودنِ داده» بسته شد. دورهٔ ظاهری (تیرهٔ اسپاتیفای‌وار)
روتینِ ساعتی **ندارد** و نوبت‌به‌نوبت جلو می‌رود.

**شاهدِ مرورگر:** `PORT=8787 node server/server.mjs &` — با بازارِ بسته
هم بالا می‌آید. playwright همین‌جاست:
`/opt/node22/lib/node_modules/playwright/index.mjs` + کروم
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`؛ تب با
`page.click('#rail-list >> text=<نام تب>')`. **ادعای ظاهری
بی‌اندازه‌گیری نکن** — `getComputedStyle` را بپرس.

**شاهدِ بالادست:** `cdn.tsetmc.com` از این ماشین در دسترس نیست؛ راهِ
جواب‌داده استابِ محلی است (پایین‌تر، «هارنسِ مرورگر»).

## آخرین قلم بسته‌شده (۱۴۰۵/۰۷/۰۱) — دروازه‌های ورودِ داده

دو دروازه بسته شد: `ui/tape-intake.mjs` (ریزمعامله) و
`ui/daily-intake.mjs` (تابلوی روزانه). هر کدام یک نگهبان دارند —
دسته‌های ۲۹۱ و ۲۹۲ — که صدازدنِ مستقیمِ آن مسیر را از هر فایلِ دیگری رد
می‌کنند.

**درسِ اصلی: نگهبان را پیش از مهاجرت بنویسید.** فهرستِ دستیِ من دو
مصرف‌کننده کم داشت و نگهبان همان دو را گرفت. `grep`ِ من ناقص بود —
نگهبان نبود که ناقص بماند.

**تلهٔ شکلی:** هشت جا `Object.entries(payload)`ِ `/api/dailies` را خام
می‌پیمودند؛ افزودنِ کلیدِ خلاصهٔ `__meta` بی جداسازی خودش باگِ تازه
می‌شد. دروازه جداش می‌کند.

### سهمیهٔ بالادست (خلاصه)

بالادست سهمیه را با **`HTTP 200` و `{"tradeHistory":[]}`** می‌بندد — همان
شکلی که روزِ بی‌معامله دارد. سه معیارِ تشخیص و سقف‌هایشان در
`core/throttle-watch.mjs`. سومی (نسبت در پنجرهٔ کشویی) لازم بود چون بستن
**تدریجی** است و هر پاسخِ موفق رشته را صفر می‌کند.

**سه درسِ تکرارشونده:** آزمونِ کم‌زور بدتر از نبودنش است («نرخ مقصر
نیست» را از ۳۰ درخواست گرفتم، در برابر ۱٬۵۱۰ درخواستِ واقعی) · ابزارِ
تشخیص را با همان هدرهای برنامه بزن، وگرنه ۴۰۳ِ فیلترِ ساده را «IP مسدود
شده» می‌خوانی · هر پرچمِ تازه روی رکورد، مسیرِ `keepBetterTape` را هم
می‌خواهد — `attempts` و `throttled` دو بار آنجا گم شدند.

**هارنسِ مرورگر:** کپیِ مخزن + `data/settings.json` به استابِ محلی +
پاک‌کردنِ `data/option-roster.json`، **پورتِ تازه برای هر اجرا**.
**`pkill -f` نزنید** — الگو پوستهٔ خودتان را هم می‌کشد. و **تبِ باز‌شده
اثباتِ اجرا نیست**: مسیر را در صفحه با `page.evaluate(() => import(…))`
واقعاً صدا بزنید.

## قلم بعدی

۱. **بقیهٔ مسیرهای ورودِ داده.** ریزمعامله و تابلوی روزانه دروازه
   دارند. این‌ها هنوز ندارند و پاسخِ خالی‌شان بی‌صدا «نداشت» ترجمه
   می‌شود:

   | مسیر | مصرف‌کننده‌ها |
   |---|---|
   | `/api/daily` (تکی) | تقویمِ تاریخچهٔ استراتژی |
   | `/api/hist` (book/state/threshold) | آزمایشگاه، بازپخش |
   | `/api/live-trades` · `/api/book` | دیده‌بان، رصد لحظه‌ای |

   الگویش همان است: یک دروازه، و یک نگهبان که دور زدنش را ببندد.

۲. **نمایشِ وضعیت در تب‌های باقی‌مانده:** رول · موقعیت‌های من · تاریخچه.

۳. **صفِ کم‌داشته بینِ نشست‌ها بماند.** با سهمیه‌ای که پس از ~۵۰۰
   درخواست می‌بندد، بازهٔ سه‌ماهه بی این هرگز تمام نمی‌شود. **تصمیمِ
   باز:** کشِ دیسکیِ سرور یا IndexedDB مرورگر — پیشنهاد: دیسکیِ سرور،
   فقط برای روزِ بسته‌شده، چون آن روز دیگر عوض نمی‌شود.

۴. **بازارِ باز.** همهٔ ممیزی‌ها با بازارِ بسته اجرا شدند.

## صفِ ظاهری (دورهٔ قبل، دست‌نخورده)

قلمِ ظاهریِ بعدی **جدول‌ها و نوار بالا**ست: سرستونِ جدول روی کارت یک پله
روشن‌تر، نه تیره‌تر · راه‌راهِ ردیفِ زوج یا دیده شود یا نباشد · نوار وضعیتِ
هدر «—»ها را بی‌گروه‌بندی می‌چیند. بعد از آن: حالتِ خالیِ درون‌کارتی،
فرم‌های بلند، و کنتراستِ دکمهٔ اصلی در پوستهٔ روشن (۳٫۸:۱، زیر کف).
**ایموجیِ عنوانِ دو تب عمداً می‌ماند** — دستهٔ ۸۶ قفلش می‌کند.

## قلم‌های باز (غیرظاهری، دست‌نخورده)

- **عددی که در دو جا به دو راه حساب می‌شود، آزمونی می‌خواهد که همان دو
  راه را کنار هم بگذارد.** (دسته‌های ۲۶۵ و ۲۶۶؛ و دوباره در R5-05.)
- **نرخ بدون ریسک سراسری ۳۰٪** — تصمیم صاحب پروژه (شرح در `WORKLOG.md`).
- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸. حالا
  که `dedupeTrades` هست، اول همان را روی این نمونه ببینید: اختلافِ ۲
  می‌تواند دو ردیفِ تکراری باشد، و اگر نبود، تضادِ هم‌شماره در
  `conflicts` می‌نشیند.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.
- **پذیرش قلم‌های جلسهٔ بازار باز**: موتورها در دسته‌های ۲۵۳ و ۲۵۵ تا ۲۶۳
  قفل‌اند؛ آنچه فقط با بازارِ باز سنجیدنی است همان‌جا فهرست شده.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.
- **توکنی که مقدارش `transparent` است، لبهٔ زیرِ خودش را پاک می‌کند.** اگر
  تصمیمی برگشت، قاعده‌اش را هم بردارید — نه فقط مقدارش را.

**دروازه:** `node tools/check.mjs`
