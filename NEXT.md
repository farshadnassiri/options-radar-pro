# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### مقایسهٔ دو پروندهٔ پیاپی — برش نهم و پایانی فاز ۶

**چرا.** یک پرونده نتیجه و ضعف خودش را نشان می‌دهد، اما تحلیل رفتار به
روند نیاز دارد. آخرین قلم فاز باید پرونده بازشده را با نزدیک‌ترین پرونده
قدیمی‌تر مقایسه کند و فقط تغییرهای مستند را بگوید؛ تفاوت به معنی علت یا
بهبود قطعی نیست.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
core/portfolio-dossier-compare.mjs
ui/portfolio-dossier-compare-view.mjs
ui/tabs/portfolio-time.mjs
ui/style.css
tests/suites/161-portfolio-dossier-compare.mjs
```

از تحلیل سرمایه و یافته‌های مستند موجود برای هر دو پرونده استفاده کنید؛
محاسبه موازی یا قیمت بازار تازه ممنوع است.

**پذیرش:**

1. مقایسه فقط با دو پرونده معتبر و ترتیب زمانی قدیمی→جدید ساخته شود؛ هویت
   هر دو و اینکه نماد پایه یکسان است یا نه در خروجی بماند.
2. بازده تحقق‌یافته، فاصله درصدی از هدف و شمار شدت‌ها برای هر طرف و delta
   جدا باشند. مقدار نامعلوم delta نگیرد و صفر فرض نشود.
3. کد یافته‌های تازه، برطرف‌شده و مشترک به‌صورت مجموعه‌های پایدار گزارش
   شوند؛ تفاوت متن یا ترتیب، یافته تازه نسازد.
4. متن از «علت»، «بهتر شد» یا «بدتر شد» بدون شاهد پرهیز کند و فقط افزایش،
   کاهش، افزوده‌شدن یا دیگر ثبت‌نشدن را بگوید.
5. هنگام بازکردن پرونده، نزدیک‌ترین پرونده قدیمی‌تر از فهرست پیدا و با
   `loadDossier` خوانده شود. نبود قبلی یا خطای شبکه، خود پرونده فعلی را
   خراب نکند و پیام مستقل داشته باشد.
6. کارت مقایسه رقم فارسی و درصد داشته باشد؛ روی مقدار نامعلوم «—» و در
   موبایل یک ستون باشد. تب مستقیم fetch یا حساب مالی نکند.

راستی‌آزمایی مرورگر لازم است، ولی اتصال مرورگر داخلی از قلم ۱۵۵ با خطای
kernel assets خراب است. اگر درست شد دو پرونده و حالت بدون پرونده قبلی را
در عرض ۳۹۰ پیکسل ببینید؛ وگرنه محدودیت را ثبت کنید.

## محدودیت‌های باز

پس از این قلم فاز ۶ کامل است و قلم بعد باید از فاز ۷ (Excel و تداوم سرمایه)
برداشته شود. نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه
واقعی هنوز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
