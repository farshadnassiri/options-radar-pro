# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### پذیرش پیمایش تاریخ واقعی پس از تغییر نماد — قلم سوم فاز ۸

**چرا.** کاربر باید نماد و تاریخ را آشکار انتخاب کند، اما پذیرش مرورگر تا
امروز عمدتاً با یک نماد انجام شده است. تغییر سریع نماد می‌تواند پاسخ تاریخ
نماد قبلی را دیرتر برگرداند و تقویم یا snapshot را با پایه تازه مخلوط کند؛
این نشت در بک‌تست تصمیم تاریخی قابل قبول نیست.

**شاخه.** با اجازه صریح صاحب پروژه، Codex کامیت‌های مستقل را روی
`codex/ongoing` ادامه می‌دهد. پیش از قلم تازه `origin/main` را بررسی کنید؛
تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
ui/tabs/portfolio-time.mjs
ui/portfolio-snapshot-data.mjs
core/trading-calendar.mjs
tests/suites/146-portfolio-snapshot-data.mjs
tests/suites/172-portfolio-multi-symbol-history.mjs
```

داده امروز یا تقویم نماد قبلی fallback نماد تازه نیست. درخواست‌های هم‌زمان
باید با هویت نماد/نسل پاسخ بسته شوند، نه با زمان رسیدن شبکه.

**پذیرش:**

1. انتخاب دست‌کم سه نماد واقعی، روزهای قابل انتخاب و عنوان نماد همان پایه
   را نشان دهد؛ تاریخ یا شناسه نماد قبلی در رابط نماند.
2. پاسخ دیررس نماد اول پس از انتخاب نماد دوم نتواند dates، chain یا وضعیت
   کیفیت نماد دوم را بازنویسی کند.
3. شروع و پایان انتخاب‌شده پس از تغییر نماد پاک و دوباره صریح انتخاب شوند؛
   draft قفل‌شده حق تعویض پایه ندارد.
4. snapshot شروع فقط universe/daily/intraday/book هم‌نماد و هم‌لحظه را
   بپذیرد؛ داده بیگانه fail-closed و با علت بماند.
5. نبود تاریخ برای یک نماد، تاریخ نماد دیگر یا امروز را جایگزین نکند و
   پیام صادقانه قابل تلاش دوباره نشان دهد.
6. آزمون رفتاری مسابقه پاسخ‌ها و آزمون مرورگر واقعی با سه نماد، هویت
   تقویم، نبود اسکرول افقی و صفر خطای کنسول را قفل کنند.
7. هیچ درخواست اضافی هنگام تب بسته یا نماد تغییرنکرده ساخته نشود.

**مرز قلم.** ساخت آرشیو قراردادهای منقضی‌شده و رفع سوگیری بقا، داده‌ای
است و تا تصمیم ذخیره‌سازی جدا بیرون این قلم می‌ماند.

## محدودیت‌های باز

- universe تاریخی هنوز فقط قراردادهای موجود در آرشیو محلی را می‌شناسد؛
  قرارداد منقضی‌شده بدون عکس روزانه قابل بازسازی نیست.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم سومین وظیفه
از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق `node tools/progress.mjs`
بنویسید. دروازه کامل، مرورگر واقعی، کامیت دارای بدنه و پوش روی همین شاخه
لازم‌اند؛ سپس بدون انتظار برای مرج قلم چهارم فاز ۸ را آغاز کنید.
