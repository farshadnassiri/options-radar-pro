# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## یازده بندِ «رصد زندهٔ هر استراتژی» — دو دور بسته شد

خواسته، سرِ همهٔ یازده بند: **هر تبِ استراتژیِ سمت راست باید خودش یک
رادارِ زنده باشد، با پارامترهای مختصِ خودش.** دورِ اول بندهای ۱ و ۲ و ۷
را بست، دومی ۸ و ۹ را، سومی ۱۰ را، چهارمی ۴ و ۱۱ را — تا دو مقصد. شرح
در `WORKLOG.md`.

کدِ تازه: `core/strategy-radar.mjs` · `core/history-chain.mjs` ·
`core/session-trail.mjs` · `ui/strategy-history.mjs` ·
`strategyLinkTargets`/`watchConditionsFrom` در `ui/handoff.mjs` ·
`table.sortKey()`. آزمون: ۵۱، ۲۳۲، ۲۳۳ و ۲۳۴.

**دو قاعده‌ای که نباید شکسته شوند:**

۱ تفاوتِ سی‌وشش استراتژی **داده** است نه کد. برای استراتژی تازه نمایه‌اش
  را در `GROUP_PROFILES` یا `ID_PROFILES` بنویسید — نه تبِ دست‌نویس.

۲ زیرتب فقط **منبعِ** جدول را عوض می‌کند. انتخابگر نماد بالای زیرتب‌هاست
  و شاخص‌ها و فیلترها و جدول و جزئیات پایینشان — مشترک. جدولِ دومی
  ساخته نشود، وگرنه دو عدد از دو مسیر با هم مقایسه می‌شوند.

## کار — قلم بعدی را از اینجا بردارید

### الف. مقصدهای بیشتر برای نوار پیوند (بندهای ۴ و ۱۱)

قرارداد هست و دو مقصد دارد (🔬 آزمایشگاه، 🔔 دیده‌بان شرطی). مقصدِ تازه
یعنی یک ردیف در `STRATEGY_LINK_TARGETS` **به‌علاوهٔ** پذیرشِ
`state.handoff` در `mount` همان تب — الگویش در `ui/tabs/watchtower.mjs`
(`applyStrategyLink`) و `ui/tabs/backtest.mjs`.

نامزدها: 📐 رصد یونانی · 📏 رادار فاصله · دیده‌بان زنجیره · تحلیل تاریخی
استراتژی · موقعیت‌های من.

⚠ مقصد را وقتی اضافه کن که پذیرشش نوشته شده باشد، و `strategyLinkTargets`
باید شرطِ واقعیِ آن تب را بداند — همان‌طور که برای دیده‌بان
`GAP_STRATEGY_IDS` را می‌داند. دکمه‌ای که باز می‌شود ولی دامنه‌اش خالی
می‌ماند، بدترین حالت است.

### ب. «زنده» یعنی چه، در تبِ استراتژی (بندِ ۵)

رصدِ زندهٔ تبِ استراتژی هنوز هر دوره یک **اسکنِ دومرحله‌ای کامل** می‌زند —
با چند ده ترکیب یعنی صف و backoff. تعریفِ «زنده» از قبل هست:
`core/live-quote.mjs` (سهمیه، چرخش، دو منبعِ مظنه، پوسیدگی). تبِ
استراتژی باید سوارِ همان شود. کنارش: ستون **سنِ عدد** (از
`state`/`staleSec` در `/api/infos`) و **جای چرخش** روی نوار.

### پ. مبنای قیمتِ «دستی» (بندِ ۱)

«دفتر سفارش، آخرین، پایانی، **دستی**» خواسته شد؛ سه‌تای اول در
`priceBasis` هستند، «دستی» نیست. قاعدهٔ صداقت عددی: قیمتِ دستی **فرضِ
کاربر** است نه دادهٔ بازار، پس ردیفِ دستی هرگز «قابل اجرا» شمرده نشود و
نشانِ خودش را داشته باشد.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی)، پس جدولِ رادارِ
استراتژی در این نوبت فقط **خالی** دیده شد. با دادهٔ واقعی ببینید:

۱. **کاورد کال.** با «بازده ایستا» مرتب بالا می‌آید؟ «فاصله تا سربه‌سری»
   و «دارایی قفل‌شده» عدد دارند؟
۲. **فیلتر.** «حداقل بیشترین سود» بگذار؛ گزارش باید بگوید چند ردیف افتاد
   و چندتاش به‌خاطرِ **نداشتنِ داده** بود.
۳. **ترتیب در رصد زنده.** روی یک ستون مرتب کن، رصد زنده را روشن کن، یک
   دوره صبر کن — ترتیب نباید عوض شود.
۴. **دورهٔ نوسازی.** ۵ و ۶۰ را امتحان کن.
۵. **شاخص‌های کلیدی.** با فیلترِ روشن باید ردیف‌های **نمایش‌داده‌شده** را
   بشمارند، نه کلِ اسکن.
۶. **چهار خانواده.** پروانه، تقویمی، استرانگلِ فروش و باکس — ستون‌ها و
   فیلترها باید آشکارا فرق کنند.
۷. **رصد تاریخی.** زیرتب را باز کن؛ تقویم باید فقط روزهای داده‌دارِ همان
   نماد را فعال نشان بدهد. یک روز بگیر: جدول باید **همان ستون‌ها** را با
   قیمتِ آن روز پر کند، و جملهٔ زیرش بگوید چند پا از چند پا قیمت داشت.
۸. **مرزِ تاریخی.** «دفتر سفارش» نباید در فهرستِ مبناهای تاریخی باشد، و
   هیچ ردیفِ تاریخی نباید «قابل اجرا» بخورد.
۹. **خوابیدنِ زنده.** رصد زنده را روشن کن و بعد برو روی زیرتب تاریخی —
   باید خودش خاموش شود، وگرنه چند ثانیه بعد جدولِ تاریخی پاک می‌شود.
۱۰. **نوار پیوند.** روی یک اسپرد، «دیده‌بان شرطی» را بزن: باید در
   **صفحهٔ جدا** باز شود، شرط‌ها با عددِ همان ردیف پیش‌پر باشند و تیکِ
   استراتژی و نماد خورده باشد. روی کاورد کال، این دکمه نباید باشد.
۱۱. **ردِ جلسه.** ردیفی را کلیک کن، برو زیرتب سوم و مسیر را بکش: نمودار
   باید شکاف داشته باشد آنجا که پایی هنوز معامله نکرده، و ستونِ «فاصلهٔ
   پاها» باید عددِ غیرصفر بدهد — اگر همه‌جا صفر است یعنی چیزی درست
   خوانده نشده.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. ردیفی که عددِ فیلترشده را ندارد از فیلتر
نمی‌گذرد و جدا شمرده می‌شود؛ «نامحدود» عددِ نداشته نیست و مثل عدد سنجیده
می‌شود؛ کادرِ خالیِ فیلتر شرطِ «حداقل صفر» نمی‌سازد؛ و ستونی که در
قرارداد ستونیِ موتور نباشد در هیچ نمایه‌ای نمی‌نشیند.

در حالت تاریخی: روزِ دیگری جانشینِ روزِ بی‌معامله نمی‌شود، دفترِ سفارشِ
گذشته ساخته نمی‌شود، و پایهٔ بی‌قیمت ترکیب نمی‌سازد. در ردِ جلسه: پایِ
بی‌معامله نقطه نمی‌سازد، لحظهٔ نرسیده ستون نمی‌شود، و ناهم‌زمانیِ پاها
روی هر نقطه نوشته می‌شود.

اگر یکی شکست، یکی از دسته‌های ۲۳۲ تا ۲۳۴ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، پس از کار بسته شود (۸۷۸۷ دستِ صاحب پروژه است). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`، Playwright سراسری:
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`waitUntil: 'domcontentloaded'` بگذارید نه `networkidle` — تابلو بسته
است و شبکه آرام نمی‌گیرد. کارِ ظاهری: اول عکس، بعد آزمون.
