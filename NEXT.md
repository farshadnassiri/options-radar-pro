# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### پیشنهاد اجراپذیر در تب سفر زمانی — برش نهم فاز ۳

**چرا.** زنجیرهٔ موتور کامل است: حکم اجراپذیری، ترکیب، قیمت ورود، سرمایهٔ
لازم، ارزیابی بازده، امتیاز، و رتبه‌بندی. هیچ‌کدام هنوز به چشم کاربر
نمی‌رسند. تب سفر زمانی جلسهٔ فعال را نشان می‌دهد ولی نمی‌گوید «با این
مأموریت، این سه طرح در دسترس‌اند و این‌ها هم چرا کنار گذاشته شدند».

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
ui/portfolio-proposals.mjs        ساخت مدل نمایش از خروجی رتبه‌بندی
ui/tabs/portfolio-time.mjs        اتصال به تب
tests/suites/131-portfolio-proposals.mjs
```

`node tools/next.mjs map rankPlanScores` و `node tools/next.mjs map faDigits`.
چیدمان آزمون را از `tests/fixtures/portfolio.mjs` بگیرید — نگهبان ۱۰
کپی‌اش را قرمز می‌کند.

**پذیرش:**

1. مدل نمایش فقط از خروجی معتبر `rankPlanScores` ساخته شود و **هیچ عدد
   مالی تازه‌ای** محاسبه نکند. هر عددی که نشان داده می‌شود باید در ورودی
   عیناً موجود باشد.
2. هر عدد نمایشی از `ui/fmt.mjs` و با رقم فارسی؛ ریال داخل، تومان در
   نمایش. یک ادعا باید نبود رقم لاتین در خروجی را بسنجد.
3. ردیف‌های کوتاه‌فهرست با رتبه، امتیاز، و **علت جایگاه** (جزء بالابرنده و
   پایین‌کشنده) نشان داده شوند. امتیاز بدون علت نمایش داده نشود.
4. طرح‌های ردشده و بی‌امتیاز در بخش جدا با علت خواندنی بمانند و با
   ردیف‌های رتبه‌دار قاطی نشوند. «نامعلوم» نباید شبیه «بد» دیده شود.
5. کیفیت داده کنار هر ردیف دیده شود؛ کیفیت `estimated` و علتش پنهان
   نشود.
6. جلسهٔ غیرفعال یا نبودِ نامزد، پیام صریح بدهد نه جدول خالی. هیچ عدد
   جای‌گزینی ساخته نشود.

**راستی‌آزمایی مرورگر.** این برش رابط دارد، پس یک بار در Chromium با
`Emulation.setDeviceMetricsOverride` روی ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ باز شود:
بدون سرریز افقی و بدون خطای کنسول. محدودیت دادهٔ زیر را هم بخوانید.

---

## محدودیت باز از قلم‌های قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
