# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### دفتر سرمایهٔ جلسه — برش نخست فاز ۴

**چرا.** فاز ۳ کامل شد و طرح‌ها حالا در دفتر رویداد ثبت می‌شوند
(`core/portfolio-commit.mjs`). ولی هیچ‌جا نمی‌شود پرسید «الان چقدر از
سرمایه درگیر است، چقدر آزاد مانده، و آیا قیود ریسکِ مأموریت هنوز رعایت
می‌شوند؟» بدون این، هر ثبت تازه کورکورانه است — `commitPortfolioPlan` فقط
بودجهٔ **یک خانواده** را می‌بیند، نه کل سرمایه و نه سقف وجه تضمین.

`replayPortfolioSession` موقعیت‌ها و lotها را می‌دهد و
`core/portfolio-commit.mjs` سرمایهٔ هر ثبت را در `data.capitalRial` نگه
داشته. این برش فقط همان‌ها را جمع می‌بندد — **هیچ ارزش‌گذاری جاری و هیچ
سود و زیانی اینجا نیست**؛ آن فاز بعد است.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-ledger.mjs                 دفتر سرمایه از روی رویدادها
core/portfolio-commit.mjs                 فقط اگر باید قید تازه را مصرف کند
tests/suites/133-portfolio-ledger.mjs
```

`node tools/next.mjs map replayPortfolioSession` و
`node tools/next.mjs map familyBudgetState`. چیدمان آزمون را از
`tests/fixtures/portfolio.mjs` بگیرید — نگهبان ۱۰ کپی‌اش را قرمز می‌کند.

**پذیرش:**

1. همه‌چیز فقط از **دفتر رویداد و مأموریت قفل‌شده** درآید. هیچ عددی از
   جای دیگر و هیچ شمارندهٔ موازی‌ای ساخته نشود؛ شمارندهٔ موازی روزی با
   دفتر اختلاف پیدا می‌کند و آن‌وقت هیچ‌کدام سند نیستند.
2. به تفکیک گزارش شود: سرمایهٔ درگیر کل، به تفکیک خانواده، وجه تضمین
   درگیر، کارمزد پرداختی، و سرمایهٔ آزاد. هر جزء جدا بماند — جمعِ درهم
   نمی‌گوید کدام قید دارد فشار می‌آورد.
3. دو قید ریسکِ مأموریت سنجیده شوند: `minFreeCapitalPct` و
   `maxMarginUsePct`. برای هرکدام مقدار جاری، حد، و شکسته‌شدن یا نشدن
   صریح بیاید — نه فقط یک پرچم.
4. رویدادی که `data.capitalRial` ندارد (ثبت‌های دستی یا قدیمی) کل گزارش
   را `null` نکند، ولی **صریح شمرده و نام‌بُرده شود**. عددی برایش ساخته
   نشود و بی‌صدا صفر هم حساب نشود.
5. `commitPortfolioPlan` پیش از ثبت این قیود را هم بسنجد و ثبتی که
   `minFreeCapitalPct` یا `maxMarginUsePct` را بشکند رد شود، با علت
   نام‌بُرده — همان‌طور که بودجهٔ خانواده را رد می‌کند. حجم کوچک نشود.
   ادعاهای موجود دستهٔ ۱۳۲ نباید بشکنند.
6. هیچ ارزش‌گذاری جاری، هیچ سود و زیان، و هیچ قیمت تازه‌ای اینجا محاسبه
   نشود. این دفتر فقط می‌گوید چه پولی درگیر شده، نه اینکه چقدر می‌ارزد.

**رابط ندارد**، پس راستی‌آزمایی مرورگر لازم نیست. برش بعدی این دفتر را به
تب وصل می‌کند.

---

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش پیشنهادها ربطی ندارد، ولی یک بار باید
دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
