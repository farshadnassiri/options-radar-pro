# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### اتصال ذخیره پرونده به بستن جلسه — برش سوم فاز ۶

**چرا.** انبار نسخه‌دار، دو مسیر API و کلاینت شبکه در برش قبل ساخته شدند،
ولی دستگیرهٔ بستن هنوز فقط حافظهٔ تب را عوض می‌کند. تا `saveDossier` در
همان مسیر واقعی صدا زده نشود، refresh همچنان پرونده را پاک می‌کند و
زیرساخت تازه عملاً مصرف‌کننده ندارد.

**شاخه.** از آخرین `main` شاخهٔ کوتاه‌عمر تازه بسازید؛ شاخهٔ این قلم
`codex/portfolio-dossier-store` است و نباید ادامه پیدا کند.

**فایل‌ها:**

```text
ui/portfolio-closeout-view.mjs       سند خام موتور را کنار مدل نمایش نگه دارد
ui/tabs/portfolio-time.mjs           ذخیره را پیش از اعلام موفقیت انجام دهد
ui/portfolio-dossier-data.mjs        فقط اگر قرارداد خطا به اصلاح کوچک نیاز داشت
tests/suites/155-portfolio-dossier-ui.mjs
```

از `closeoutView` سند خام را بازسازی نکنید؛ خروجی معتبر
`closeoutPortfolioSession` همان منبع است. از `saveDossier` موجود استفاده
کنید و fetch تازه‌ای در تب نسازید.

**پذیرش:**

1. خروجی موفق `closeoutView` علاوه بر متن نمایشی، `dossier` خام و همان
   جلسهٔ بسته‌شده را بدون دستکاری در اختیار مسیر ذخیره بگذارد.
2. دستگیرهٔ بستن، ذخیره را `await` کند و فقط پس از تأیید معتبر سرور جلسه
   محلی را بسته، پرونده را نمایش داده و کنترل‌ها را خاموش کند.
3. شکست شبکه، پاسخ ناقص یا تعارض، متن موفقیت و پروندهٔ ذخیره‌شده نشان
   ندهد؛ جلسهٔ محلی فعال و دکمه برای تلاش دوباره قابل استفاده بمانند.
4. هنگام درخواست، دوبار کلیک نتواند دو ذخیره هم‌زمان بسازد و وضعیت روشن
   «در حال ذخیره» نشان داده شود.
5. تب مستقیم `fetch` نزند و خطای فارسی کلاینت را بدون تبدیل به پیام موفق
   در `pt-closeout-state` نشان دهد.

راستی‌آزمایی مرورگر لازم است: یک بار موفقیت و یک بار خطای عمدی شبکه را
در مرورگر واقعی ببینید؛ در حالت خطا دکمه باید دوباره قابل کلیک باشد.

## محدودیت باز از قلم‌های قبلی

فهرست و خواندن پرونده روی سرور آماده است، ولی نمایش پروندهٔ ذخیره‌شده پس
از refresh هنوز قلم جداست؛ این برش فقط تضمین می‌کند خودِ بستن واقعاً آن
را ذخیره می‌کند.

نوار هشدار هنوز «بدون اسکرول» نیست. جمع‌شدن ویزارد فاصله را در دسکتاپ و
موبایل حدود ۶۱ تا ۶۴ درصد کم کرد، ولی موبایل هنوز چند صفحه اسکرول دارد.

ویزارد با تاریخچهٔ واقعی هنوز پیموده نشده چون نصب فعلی بایگانی تاریخی
ندارد. همچنین در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` نیاز به بررسی سرریز
دارد.

`portfolioEntryPlan` علت را `{code,label}` برمی‌گرداند، در حالی که بقیه
ماژول‌ها رشته می‌دهند؛ دستهٔ ۱۲۶ روی آن تکیه دارد و تغییرش قلم جداست.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد روشن بنویسید:
چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
