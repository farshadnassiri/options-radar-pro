# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## دورهٔ جاری: ارتقای ظاهر (خواستهٔ صاحب پروژه، ۱۴۰۵/۰۶/۲۸)

صاحب پروژه ظاهر را با «یک سایت مدرن مثل اسپاتیفای/آمازون/فیسبوک» سنجید و
جهت را انتخاب کرد: **تیرهٔ اسپاتیفای‌وار**، و صفحهٔ نمونه «موقعیت‌های من».
هر نوبت یک قلمِ ظاهری، با شاهدِ مرورگر، آزمون رگرسیون، دروازهٔ سبز و پوش.

> این دوره روتینِ ساعتی **ندارد** — صاحب پروژه لغوش کرد. کار دستی و
> نوبت‌به‌نوبت جلو می‌رود.

### چطور شاهد بگیرید (بی هیچ وابستگی npm)

`PORT=8787 node server/server.mjs &` — رابط با بازارِ بسته هم بالا می‌آید.
playwright روی همین ماشین هست:
`/opt/node22/lib/node_modules/playwright/index.mjs` و کروم
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`. تب را با
`page.click('#rail-list >> text=<نام تب>')` باز کن.

**قاعدهٔ این دوره:** ادعای ظاهری بی‌اندازه‌گیری نکن. «تخت به نظر می‌آید»
کافی نیست؛ `getComputedStyle` را بپرس. دو قلمِ اول دقیقاً همین‌طور پیدا
شدند.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۶/۲۸) — پوستهٔ تیره، پیش‌فرض

سه کامیت پشتِ سر هم، هر سه با شاهدِ مرورگر (شرح کامل در `WORKLOG.md`):

۱. **لایهٔ مردهٔ «پوستهٔ شیشه‌ای» برداشته شد.** توکن‌های `--glass-*` روزی
   خنثی شدند ولی قاعده‌هایشان — که عمداً آخرِ شیوه‌نامه بودند — ماندند و
   لبهٔ پایه را بی‌صدا می‌پوشاندند: کادرِ محاسبه‌شدهٔ هر `.ghost`
   `rgba(0, 0, 0, 0)` بود. دستهٔ ۲۷۱.
۲. **ایموجی → آیکونِ خانواده** در دکمه‌های پیوند و خانهٔ «شرط». دستهٔ ۲۷۳.
۳. **پوستهٔ تیره، پیش‌فرض.** سه سطحِ واقعی (`#0a0b0d` / `#15171b` /
   `#1e2127`)، پلهٔ برند روی زغال از ۶۰۰ به ۴۰۰ (۳٫۱:۱ → ۸٫۹۶:۱، اندازه‌گیری
   در مرورگر)، دکمهٔ قرصی، و کاشیِ عددِ بازنویسی‌شده. در راهش یک باگ واقعی:
   تماسِ اولِ `applyTheme` در بوت پیش‌فرض را در `localStorage` می‌نوشت، پس
   تنظیمِ ذخیره‌شدهٔ پوسته هرگز غالب نمی‌شد. دستهٔ ۲۷۴.

## قلم بعدی — جدول‌ها و نوار بالا، با همان زبان

کاشی و کارت و دکمه به پوستهٔ تازه رسیده‌اند؛ **جدول** نرسیده — و جدول
بیشترِ سطحِ این برنامه است. سه چیزِ اندازه‌پذیر:

۱. **سرستونِ جدول** هنوز زمینهٔ `--ground` می‌گیرد، یعنی در پوستهٔ تیره
   از خودِ صفحه تیره‌تر است و «فرورفته» دیده می‌شود؛ روی کارتِ روشن‌تر
   باید یک پله **روشن‌تر** باشد نه تیره‌تر.
۲. **ردیفِ زوج** با `color-mix(--panel-2 35%)` رنگ می‌گیرد که روی زمینهٔ
   تازه تقریباً نامرئی است. راه‌راه یا باید دیده شود یا اصلاً نباشد.
۳. **نوار وضعیتِ هدر** «—»های کنار هم را بی‌گروه‌بندی و بی‌جداکننده
   می‌چیند و در عرضِ کم می‌شکند.

## صفِ ظاهریِ بعد از آن (به ترتیب اثر)

- ~~کاشیِ عددِ «موقعیت‌های من»~~ و ~~آیکونِ ناهمگونِ ایموجی~~ بسته شدند.
  **ایموجیِ عنوانِ دو تب عمداً ماند**: خواستهٔ صریح صاحب پروژه است و دستهٔ
  ۸۶ قفلش می‌کند؛ اگر تصمیمش عوض شد، اول آن دسته باید عوض شود.
- **حالتِ خالیِ درون‌کارتی.** جعبهٔ «در این بازه نماد پایه‌ای پیدا نشد» نه
  نشان دارد نه کاری پیشنهاد می‌دهد — برخلاف `.empty` سراسری.
- **فرم‌های بلند.** «افزودن موقعیت» ده ورودی را در یک شبکهٔ تخت می‌ریزد،
  بی دسته‌بندی و بی خلاصهٔ پیش از ثبت.
- **کنتراستِ دکمهٔ اصلی در پوستهٔ روشن.** سفید روی تیلِ ۶۰۰ فقط ۳٫۸:۱ است،
  زیر کف ۴٫۵. پلهٔ ۷۰۰ آن را به ۵٫۳ می‌برد. پوستهٔ روشن دیگر پیش‌فرض نیست،
  ولی هنوز یک کلیک فاصله دارد.

## قلم‌های باز (غیرظاهری، دست‌نخورده)

- **خروجی دیتا — درسِ آخرین ممیزی.** «کامل خروجی نمی‌سازه»: کفِ عمرِ
  قرارداد حالا از سری می‌آید نه از یک سمت (`dataExportListingFloors`)، و
  چهار ایرادِ ممیزیِ m15 بسته شد. درسِ قابل‌تعمیم: عددی که در دو برگِ یک
  فایل به دو راه حساب می‌شود، آزمونی می‌خواهد که همان دو راه را کنار هم
  بگذارد؛ و پرچمی که هسته می‌سازد (`lifeFromTrades`) اگر سرِ راه دور
  ریخته شود، مصرف‌کننده «مشاهده» را «رسمی» می‌خواند. دسته‌های ۲۶۵ و ۲۶۶.
- **نرخ بدون ریسک سراسری ۳۰٪** — تصمیم صاحب پروژه (شرح در `WORKLOG.md`).
- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.
- **پذیرش قلم‌های جلسهٔ بازار باز**: موتورها در دسته‌های ۲۵۳ و ۲۵۵ تا ۲۶۳
  قفل‌اند؛ آنچه فقط با بازارِ باز سنجیدنی است همان‌جا فهرست شده.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.
- **توکنی که مقدارش `transparent` است، لبهٔ زیرِ خودش را پاک می‌کند.** اگر
  تصمیمی برگشت، قاعده‌اش را هم بردارید — نه فقط مقدارش را.

**دروازه:** `node tools/check.mjs`
