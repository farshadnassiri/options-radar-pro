# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### ثبت طرح انتخاب‌شده در دفتر رویداد — برش دهم و پایانی فاز ۳

**چرا.** کاربر حالا کوتاه‌فهرست را می‌بیند ولی نمی‌تواند کاری با آن بکند.
فاز ۴ (تشکیل سبد و حسابداری) روی «موقعیت ثبت‌شده» بنا می‌شود و تا وقتی
انتخاب کاربر جایی ثبت نشود، شروع نمی‌شود.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-commit.mjs          ثبت طرح در دفتر رویداد جلسه
ui/tabs/portfolio-time.mjs         دکمهٔ انتخاب روی هر ردیف کوتاه‌فهرست
tests/suites/132-portfolio-commit.mjs
```

`node tools/next.mjs map recordPortfolioEvent` — دفتر رویداد از فاز ۱
هست و ساختار تازه‌ای لازم ندارد. چیدمان آزمون را از
`tests/fixtures/portfolio.mjs` بگیرید؛ نگهبان ۱۰ کپی‌اش را قرمز می‌کند.

**پذیرش:**

1. فقط طرحی ثبت شود که در همان لحظه **رتبه‌دار** باشد. طرح ردشده یا
   بی‌امتیاز حتی با شناسهٔ درست ثبت نشود، با علت نام‌بُرده.
2. رویداد ثبت‌شده باید طرح را **کامل و قابل بازسازی** نگه دارد: شناسهٔ
   نامزد، پاها با VWAP، حجم اجرایی، مبنای سرمایه، امتیاز و اجزایش، و
   لحظهٔ ثبت. رویدادی که نشود از رویش طرح را بازساخت، سند نیست.
3. ثبت **دوباره** همان نامزد در همان لحظه رد شود؛ ثبت دو طرح مختلف مجاز
   باشد. تکرار بی‌صدا یعنی دو موقعیت به‌جای یکی.
4. بودجهٔ خانواده پس از ثبت به‌روز شود و ثبتی که از بودجهٔ قفل‌شده بگذرد
   رد شود — نه اینکه حجم را بی‌خبر کوچک کند.
5. کیفیت داده و پرچم `estimated` در خود رویداد بماند. سندی که کیفیتش را
   نبرد، فردا شبیه دادهٔ قطعی خوانده می‌شود.
6. دکمهٔ انتخاب فقط روی ردیف‌های رتبه‌دار باشد و پس از ثبت، وضعیت ردیف
   صریح عوض شود. رابط باید بگوید چه ثبت شد، نه فقط ساکت بماند.

**راستی‌آزمایی مرورگر لازم است** (۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴).

---

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش پیشنهادها ربطی ندارد، ولی یک بار باید
دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
