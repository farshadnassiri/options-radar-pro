# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## آنچه همین حالا تمام شد — پنج خواستهٔ صاحب پروژه (۱۴۰۵/۰۶/۱۳)

شاخه: `claude/option-radar-improvements-sa6xuq`

```text
۱  سقف ترکیب برداشته شود، بی پرسش از کاربر   ✔ توقف و پیشرفت جایش نشست
۲  تاریخ پیش از نماد پایه                    ✔ هر پنج تبِ تاریخ‌دار
۳  بازطراحی کامل ظاهر                        ✔ پوستهٔ شیشه‌ای، هر دو پوسته
۴  فاصلهٔ اسپرد: اکنون، تاریخ، نمودار، هشدار   ✔ تب «رادار فاصله»
۵  همان منطق برای استرانگل                    ✔ همان موتور، لنگرِ «دهانه»
```

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

هر سه بند زیر فقط با دادهٔ واقعی سنجیده می‌شوند. `cdn.tsetmc.com` از ماشین
عامل بسته است (۴۰۳ از پراکسی)، پس اینجا با دادهٔ ساختگی سنجیده شده‌اند و
درست کار کرده‌اند — ولی «درست کار کرد» با دادهٔ ساختگی، ادعای کوچکی است.

**۱. اجرای بی‌سقف روی یک نماد پهن.** در «آزمون همه استراتژی‌ها» یک نماد با
زنجیرهٔ پهن (اهرم، سررسید دور) بردارید و اجرا کنید. سه چیز را ببینید:

- نوار پیشرفت هر دو عدد را می‌گوید (استراتژی چندم، و ترکیب چندم درونش)
- دکمهٔ «توقف» نتیجهٔ تا آن لحظه را نگه می‌دارد و گزارش «ناتمام» می‌خورد
- ستون «کنارگذاشته» در برگ «پنجره قیمت اعمال» اکسل **صفر** است

اگر اجرا آن‌قدر طول کشید که غیرقابل استفاده شد، عددِ واقعیِ زمان را بنویسید —
قلم بعدی همان است: موازی‌سازی ریسه، نه برگرداندن سقف.

**۲. فاصله با قیمت واقعی.** در «رادار فاصله» یک نماد و یک بازه بگیرید و
جدول «اکنون» را با تابلو مقایسه کنید. آنچه باید بخواند:

- «فاصلهٔ اعمال» = اختلاف دو قیمت اعمال × اندازهٔ قرارداد
- «فاصلهٔ اکنون» = همان عددی که برای بستنِ موقعیت می‌دهید یا می‌گیرید
- در استرانگل فروش، «فاصلهٔ اکنون» همان پرمیوم است — «۳۰۰» و «۳۱۰» خودتان

**۳. هشدار زنده.** تیکِ «رصد زندهٔ بازار» را در ساعت بازار بزنید، یک شرط
«عبور» روی فاصلهٔ اکنون بگذارید و ببینید اعلان می‌رسد یا نه. اجازهٔ اعلان
مرورگر باید یک بار داده شود؛ اگر ندادید، کارت درون‌صفحه جایش می‌آید و همان
هم باید دیده شود.

## قلم بعدی، اگر پذیرش سبز بود

**دانه‌بندی درون‌روزی روی بیش از یک روز.** امروز فقط یک روز است و علتش در
کد نوشته شده: ریزمعاملهٔ هر پا برای هر روز یک درخواست است. برای دیدن روند
دقیقه‌ایِ فاصله در یک هفته، راهِ درست کش کردنِ ریزمعامله روی دیسک است —
مثل همان کاری که `data/roster` برای دفتر قراردادها می‌کند — نه بالا بردن
سقف درخواست.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. پای بی‌قیمت فاصله را باطل می‌کند، دو پای هم‌قیمت
ردیف نمی‌سازند، توزیعِ بی‌پراکندگی حکم نمی‌گیرد، و خانهٔ خالیِ تاریخچه با
قیمت روز قبل پر نمی‌شود. اگر روزی یکی از این چهار شکست، دستهٔ ۲۱۸ قرمز
می‌شود.

**دروازه:** `node tools/check.mjs`
