# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعد بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### موقعیت‌های باز جلسه — برش سوم فاز ۴

**چرا.** دفتر سرمایه می‌گوید **چقدر** پول درگیر است. هیچ‌جا نمی‌گوید
**چه چیزی** در دست است. `replayPortfolioSession` موقعیت را با شناسه و
حجم می‌سازد ولی پاها، اعمال، و قیمت ورود در `data` رویدادِ ثبت مانده‌اند
و هیچ‌کس آن دو را کنار هم نمی‌گذارد. کاربر بعد از ثبت فقط یک شناسهٔ
موقعیت می‌بیند.

**شاخه.** `claude/portfolio-dev` (شاخهٔ ماندگار؛ بند ۱۱ پروتکل). پیش از
شروع `git merge origin/main`.

**فایل‌ها:**

```text
core/portfolio-positions.mjs        اتصال بازپخش به سند ثبت
tests/suites/135-portfolio-positions.mjs
```

`node tools/next.mjs map replayPortfolioSession`. شکل سند ثبت را از
`core/portfolio-commit.mjs` بردارید (`data.legs`، `data.capital`،
`data.quality`). چیدمان آزمون از `tests/fixtures/portfolio.mjs` — نگهبان
۱۰ کپی‌اش را قرمز می‌کند.

**پذیرش:**

1. هر موقعیت باز، پاهایش را با نوع، سمت، اعمال، حجم و `vwap` ثبت‌شده
   نشان دهد — همان اعدادِ سند، نه بازمحاسبه از دفتر سفارشِ امروز.
2. موقعیتی که رویداد ثبتش پیدا نشود، **حذف نشود**: با علت صریح بماند.
   حذفش یعنی جمعِ موقعیت‌ها کمتر از واقعیت است و کسی نمی‌فهمد.
3. **هیچ ارزش‌گذاری‌ای نه.** سود و زیان جاری کار فاز ۵ است. یک ادعا
   بسنجد که ماژول قیمت لحظه‌ای نمی‌خواند و P&L نمی‌سازد.
4. موقعیت بسته و باز از هم جدا بمانند؛ `openQty` صفر با «بسته» یکی نیست
   مگر خودِ بازپخش گفته باشد.
5. کیفیت داده‌ی هر موقعیت از همان سند بیاید و `estimated` پنهان نشود.
6. جلسهٔ بدون ثبت، فهرست خالی با علت بدهد نه خطا.

راستی‌آزمایی مرورگر لازم **نیست** (این برش رابط ندارد).

---

## محدودیت باز از قلم‌های قبلی

در موبایل ۳۹۰×۸۴۴، `DIV.capacity-panel` از عرض دید بیرون می‌زند. خود
صفحه افقی اسکرول نمی‌شود و به بخش‌های سرمایه و پیشنهاد ربطی ندارد
(هر دو در ۱۴۰۰×۹۰۰ و ۳۹۰×۸۴۴ سنجیده شدند و سرریز ندارند)، ولی یک بار
باید دیده شود که آیا ظرف اسکرول خودش را دارد یا واقعاً سرریز است.

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

بخش دفتر سرمایه در مرورگر با بازکردن دستی `#pt-review-step` سنجیده شد،
چون بدون تاریخچهٔ واقعی نمی‌شود جلسه را فعال کرد. چیدمان و سرریز واقعی
سنجیده شدند؛ مسیر رسیدنِ کاربر به آن هنوز با دادهٔ واقعی دیده نشده.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز
را روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
