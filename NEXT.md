# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده — ردیفی که هیچ‌وقت ساخته نمی‌شد

ممیزی صاحب پروژه روی `98cebbd` دوازده ردیف داشت. ده‌تا یک علتِ مشترک
داشتند: `boardRow` برای ساختن ردیفِ امروز مدرکِ معامله می‌خواست
(`qTotTran5J_*` یا `zTotTran_*`) و دیده‌بان اختیار آن دو را برای **نماد
پایه** نمی‌فرستد. پس هر قرارداد ردیف امروز می‌گرفت و هیچ پایه‌ای نمی‌گرفت؛
و چون تقویم‌ها روزهایشان را از سریِ پایه می‌سازند، امروز در هیچ انتخابگری
نبود. آزمون‌ها سبز بودند چون ورودی آزمایشی آن فیلدها را ساخته بود.

- **دو منبع**: تابلوی اختیار برای قراردادها، نوار معامله برای پایه‌ها.
  `liveTapeCodes` فهرست را کوچک نگه می‌دارد؛ `tapeFor` هزینه را صریح
  می‌کند (`'bases'` پیش‌فرض، `'all'` در چهار تبِ تک‌نمادی) با سقف ۱۴۰ ابزار.
- **منبع عکس** هم بررسی می‌شود، نه فقط فاز بازار: `watch` و `snapshot`
  مهر امروز می‌گیرند، بایگانی نه.
- **«امروز» یک تعریف دارد**: `core/tehran-day.mjs`.
- **سه مسیرِ بی‌اتصال** وصل شدند: تاریخچهٔ استراتژی، تحلیل زمانی سبد،
  آزمایشگاه بک‌تست.

دستهٔ ۸۷ از پایه بازنویسی شد (۸۲ ادعا)، دستهٔ ۸۸ به هفت مسیر گسترش یافت.

## کار

### پذیرش روز جاری در جلسهٔ بازار باز

تا امروز فقط با ساختگیِ هم‌شکلِ پاسخ واقعی سنجیده شده.

۱. **تقویم**: در «تحلیل تاریخی استراتژی» با دامنهٔ «تا همین لحظه»، آخرین
روزِ قابل انتخاب باید **امروز** باشد. با «تا آخرین روز بسته‌شده» باید دیروز
بماند. هر دو را ببین.

۲. **هزینه**: پس از کلیک «دریافت تاریخچه» شمار درخواست‌های
`/api/live-trades` باید `ceil(n/24)` باشد، نه بیشتر. قراردادِ بی‌معامله
نباید در فهرست `ins=` بیاید.

۳. **مبنای اولین/کمترین/بیشترین** روی روز جاری باید عدد بدهد، نه «—».
اگر «—» داد، جملهٔ دامنه باید گفته باشد چند ردیف ناقص است.

۴. **پس از ۲۰:۳۰ به وقت گرینویچ** (بامداد تهران) سقف بازهٔ تحلیل باید
همان روزِ تهران باشد، نه روز قبل.

۵. **تابلوی قطع**: اگر `/api/history/universe` با `source=watch-archive`
پاسخ داد، هیچ ردیف امروزی نباید ساخته شود و جمله باید علت را بگوید.

### پذیرش صفحهٔ رصد زنده در جلسهٔ بازار باز

۶. **نگاه باز لحظه‌ای**: تب شبکه را باز بگذار و نمای «نگاه باز چندروزه» را
باز کن؛ نباید هیچ `history/universe` یا `dailies` برود.

۷. **ستون «علت نبود تلاطم»** را روشن کن و ببین چند درصد `belowFloor`
می‌گیرند — عددِ لازم برای تصمیم نرخ (قلم بعدی).

۸. **«ارزش خود پایه»** در کارت نماد و دیده‌بان دیگر نباید صفر باشد.

۹. **ساعت**: در بازار بسته باید «عکس … · N ثانیه پیش» بگوید و نوار قرمز شود.

۱۰. **زنجیرهٔ دوطرفه**: خط قیمت جاری بین دو اعمالِ در بر گیرنده؛ سایهٔ «در
سود» سمت کال زیر قیمت جاری و سمت پوت بالای آن. دلتا: کال مثبت، پوت منفی.

۱۱. **پایداری**: رفرش پیوستهٔ ۲۰ دقیقه‌ای، در بازار باز.

## قلم بعدی — نرخ و بازده نقدی به‌ازای نماد (تصمیم صاحب پروژه)

ممیزی ردیف ۹ (دور قبل) درست بود و اندازه‌گیری نشان داد **علتِ غالبِ
ستون‌های خالیِ تلاطم همین است**، نه ناهم‌زمانی قیمت:

```
نرخ ۳۰٪ → کف نظری ۶٬۲۲۸ → قیمت ۴٬۶۳۱ زیر کف  →  belowFloor
نرخ  ۰٪ → کف نظری ۴٬۵۷۲ → قیمت ۴٬۶۳۱ بالای کف →  IV ۱۳٫۸٪
```

کفِ بدون‌آربیتراژ وقتی معنی دارد که بشود پایه را فروش استقراضی کرد؛ در این
بازار نمی‌شود. ستون‌های «کف نظری قیمت» و «فاصله قیمت از کف نظری» اضافه
شدند تا فرض دیده شود، ولی **انتخاب خودِ فرض تصمیم صاحب پروژه است**:

۱. نرخِ جداگانه برای کفِ بدون‌آربیتراژ (مثلاً صفر) در کنار نرخِ تنزیل.
۲. بازده نقدی به‌ازای نماد، از منبع داده‌ای که برنامه الان ندارد.
۳. مدل سود نقدی گسسته برای رویدادهای قطعی.

هیچ‌کدام بدون تصمیم صاحب پروژه پیاده نمی‌شود — عددِ فرضیِ ساخته‌شده دقیقاً
همان چیزی است که قاعدهٔ ۲-۴ منع می‌کند.

## قلم باز — اختلاف شمارش ریزمعامله

برای طهرم۷۰۶۱ عدد رسمی تابلو ۶٬۷۹۶ معامله بود و نوار ریزمعامله پس از حذف
معاملات باطل ۶٬۷۹۸ ردیف فعال داد. اول باید سنجید کدام endpoint مرجع است،
بعد تصمیم گرفت کدام عدد نمایش داده شود — نه اینکه یکی بی‌صدا جایگزین شود.

## قلم‌های باز

- **بارِ بالادست**: تصمیم دربارهٔ `byEndpoint` در `/api/health` منتظر
  اندازه‌گیری صاحب پروژه است.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: اگر مقایسهٔ بازه‌ای فراتر از عمر یک نسل قرارداد لازم
  شد، باید مدل صریح شود؛ قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» هم از آخرین معاملهٔ نوار ساخته نمی‌شود.
- روز جاری فقط در فاز `open`، `after` یا `ungated` **و** از منبع `watch` یا
  `snapshot` به تاریخ امروز منتسب شود.
- ورودیِ آزمایشی باید هم‌شکلِ پاسخ واقعی باشد. قاعده‌ای که فقط با دادهٔ
  ساختگی برقرار می‌شود، آزمونِ سبزِ بی‌فایده است — همین یک بار ده ردیف
  ممیزی ساخت.
- ادعای آزمون باید در **همهٔ** ساعت‌ها بگزد، نه فقط در بعضی‌شان. ساعت را
  تزریق کن، از `Date.now()` نسنج.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.

**دروازه:** `node tools/check.mjs`
