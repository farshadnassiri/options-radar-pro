# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده (۱۴۰۵/۰۶/۲۶)

**بیست قلمِ پیشنهادی، در هفت تب:**

- **موقعیت‌های من** — بستن موقعیت و سود تحقق‌یافته · نگاه سبد (یونانیِ کل،
  تقویم سررسید، منحنی سبد) · اتاق سربه‌سر · شرط و نوار هشدار · دکمهٔ رول
- **تحلیل رول** — اصطکاک اجرا (کارمزد + نیم‌اسپرد) و جبران با تتا ·
  جست‌وجو در همهٔ سررسیدها · پذیرش موقعیت از تب موقعیت‌ها
- **جست‌وجوی استراتژی‌ها** — پالایهٔ اجراپذیری · «بهترینِ هر ساختار»
- **رصد زنده** — صدک فاصلهٔ مظنه در تابلوی امروز · نرخ معامله‌شدن هر سررسید
- **دیده‌بان شرطی** — «یا» بین شرط‌ها · تاریخچهٔ شلیک با حرکتِ پایه پس از آن
- **آزمایشگاه** — اثر نرخ بدون ریسک بر تلاطم · بایگانی و مقایسهٔ اجراها
- **سراسری** — تب **دفترچهٔ معاملات** · پشتیبان و بازیابی · سلامت داده

موتورها: `position-close` · `positions-portfolio` · `position-alert` ·
`roll-cost` · `iv-rate-scan` · `backtest-runs` · `backup-bundle` ·
`journal`، و افزوده‌های `scan` و `watch-rule`. دسته‌های ۲۵۵ تا ۲۶۳.

سه پیشنهاد در راه عوض شدند؛ علتش در دفتر کار است.

### ادامهٔ همین کار — چیزهایی که جا ماند

۱. **ردیف دفترچه از تب‌های دیگر.** الان فقط تب موقعیت‌ها می‌نویسد؛ رول و
آزمایشگاه هم تصمیم‌اند.
۲. **دفترچه در پشتیبان نیست** — افزودنش یک بخش تازه در `BACKUP_PARTS` است.
۳. **حرکتِ پس از هشدار** فقط تا وقتی جلسه باز است سنجیده می‌شود؛
نگه‌داشتنِ قیمتِ پایه بین جلسه‌ها همان قلمِ بازِ «کشِ دیسکی» است.

### قلمِ بازِ قدیمی — ماژول‌های یتیم

`greeks-watch` · `spread-radar` · `bereket` · `portfolio-time` از برنامه
بیرون رفتند ولی روی دیسک مانده‌اند و آزمونشان سبز است — با آخری حدود سی
ماژولِ `ui/portfolio-*.mjs`. حذفشان **تصمیم صاحب پروژه** است.

## کار

### پذیرش قلم‌های تازه در جلسهٔ بازار باز

موتورها در `tests/suites/253-position-track.mjs` و ۲۵۵ تا ۲۶۳ قفل‌اند.

۱. **ستون تغییر امروز** باید برای موقعیتی که دیروز قیمت داشته عدد بدهد و
راهنمای سلولش تاریخِ مبنا را بگوید؛ برای موقعیتِ امروزباز «—» و علت.
۲. **روند روزانه** باید روی روزهای بی‌معاملهٔ یک پا **قطع** شود، نه وصل —
با شمردن `null`های سری هم سنجیدنی است.
۳. **درون‌روزی** فقط با کلیک گرفته شود: تب شبکه باز باشد، رفرشِ دوره‌ای
نباید هیچ `live-trades`ای ببرد.
۴. **سررسیدگذشته و بسته‌شده**: هیچ `books/infos` برایشان نرود.
۵. **شرط موقعیت**: با آستانه‌ای که همین حالا برقرار است، نوار بالای صفحه
باید بزند و سلولِ «شرط» زنگ بگیرد.
۶. **اصطکاک رول**: ستونِ اصطکاک در جدول نامزدها برای پای یک‌طرفه باید «؟»
بگیرد، نه عددِ بی‌اسپرد.
۷. **بهترینِ هر ساختار**: ساختاری که ردیف ندارد باید ته جدول با علتش
بنشیند، نه غایب شود.
۸. **دفترچه**: بستن یک موقعیت باید بی هیچ کلیکی یک ردیف در `#journal`
بسازد، با همان سود تحقق‌یافتهٔ جدول.

### تقویم، رصد زنده و گام سوم بک‌تست

۹. با «تا همین لحظه» آخرین روز **امروز**، با «تا آخرین روز بسته‌شده»
دیروز. ۱۰. شمار `/api/live-trades` = `ceil(n/24)` و قراردادِ بی‌معامله در
`ins=` نیاید. ۱۱. پس از ۲۰:۳۰ گرینویچ سقف بازه روزِ تهران باشد.
۱۲. `source=watch-archive`: هیچ ردیف امروزی ساخته نشود و علت گفته شود.
۱۳. رصد زنده: هیچ `history/universe` یا `dailies` نرود؛ «ارزش خود پایه»
صفر نباشد. ۱۴. گام سوم: یک POST به‌ازای هر ۱۲۰۰ جفت به‌علاوهٔ یک GET نوار
زنده — نه بیشتر؛ جدول پوشش به تعداد روزهای معاملاتی ردیف داشته باشد و خط
روی روزهای بی‌داده **قطع** شود.

## قلم بعدی — نرخ بدون ریسک سراسری ۳۰٪ (تصمیم صاحب پروژه)

```
نرخ ۳۰٪ → کف نظری ۶٬۲۲۸ → قیمت ۴٬۶۳۱ زیر کف  →  belowFloor
نرخ  ۰٪ → کف نظری ۴٬۵۷۲ → قیمت ۴٬۶۳۱ بالای کف →  IV ۱۳٫۸٪
```

علتِ غالبِ ستون‌های خالیِ تلاطم همین است، و حالا در پنل جزئیات هر ترکیب
**دیدنی** شده (`core/iv-rate-scan.mjs`) — ولی تصمیمش گرفته نشده. کفِ
بدون‌آربیتراژ وقتی معنی دارد که بشود پایه را فروش استقراضی کرد؛ در این
بازار نمی‌شود. سه راه: (۱) نرخِ جداگانه برای کفِ بدون‌آربیتراژ کنار نرخِ
تنزیل، (۲) بازده نقدی به‌ازای نماد از منبعی که برنامه ندارد، (۳) مدل سود
نقدی گسسته. عددِ فرضیِ ساخته‌شده همان چیزی است که قاعدهٔ ۲-۴ منع می‌کند.

## قلم‌های باز

- **اختلاف شمارش ریزمعامله**: طهرم۷۰۶۱ — تابلو ۶٬۷۹۶، نوار ۶٬۷۹۸. کدام
  endpoint مرجع است؟
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: قرارداد منقضی نباید بی‌صدا جایگزین شود.

(«بارِ بالادست» بسته شد: `byEndpoint` حالا در دفتر خطاها جدول دارد.)

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است. «قیمت پایانی» از آخرین معاملهٔ نوار ساخته نمی‌شود.
- **جمعِ نصفه ساخته نمی‌شود**، و نامِ آنچه کم است گفته می‌شود.
- روز جاری فقط در فاز `open`/`after`/`ungated` **و** از منبع `watch` یا
  `snapshot` به امروز منتسب شود.
- ورودیِ آزمایشی هم‌شکلِ پاسخ واقعی باشد؛ قاعده‌ای که فقط با دادهٔ ساختگی
  برقرار می‌شود، آزمونِ سبزِ بی‌فایده است.
- ادعای آزمون در **همهٔ** ساعت‌ها بگزد. ساعت را تزریق کن.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل
  معامله یا مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs`.

**دروازه:** `node tools/check.mjs`
