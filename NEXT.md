# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### نمایش صادقانه سود و زیان ناموجود در موقعیت‌های من

**چرا.** دو سلول سود و زیان کل موقعیت و پای جدا در تب «موقعیت‌های من» با
مقایسه دستی `>= 0` رنگ می‌گیرند. اگر ارزش‌گذاری ناقص باشد، مقدار ناموجود
نباید به‌دروغ زیان قرمز نمایش داده شود.

**شاخه.** با اجازه صریح صاحب پروژه، پس از کامیت قلم جاری همین شاخه
`codex/form-label-accessibility` ادامه داده شود؛ تاریخچه بازنویسی و
force-push نشود.

**فایل‌های محتمل:**

```text
ui/tabs/positions.mjs
tests/suites/185-positions-missing-pnl.mjs
```

**پذیرش:**

1. سود و زیان متناهی مثبت/منفی/صفر همان رنگ مشترک `signTone` را بگیرد.
2. مقدار `NaN`، بی‌نهایت یا ناموجود با «—» و بدون کلاس/رنگ سود یا زیان
   نمایش داده شود؛ صفر مالی ساخته نشود.
3. هر دو مقدار تسویه مستقیماً از نتیجه موجود بخوانند و هیچ fallback قیمت یا
   سود و زیان تازه‌ای وارد نشود.
4. آزمون رگرسیون حالت متناهی و ناموجود را قفل کند.

**مرز قلم.** فقط صداقت نمایش دو مقدار تسویه؛ تغییر فرمول تسویه یا منبع قیمت
خارج از دامنه است.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md` ثبت و `NEXT.md` برای قلم بعدی بازنویسی شود. دروازه
کامل، کامیت دارای بدنه و پوش روی شاخه کوتاه‌عمر لازم‌اند.
