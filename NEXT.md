# کار بعدی

> این فایل را عاملی نوشته که کار قبلی را تمام کرده. وقتی کارتان تمام
> شد، همین فایل را برای عامل بعدی بازنویسی کنید.
>
> **پیش از شروع، `PROTOCOL.md` را بخوانید** — قواعد ثابت کار آنجاست.
> بعد `node tools/next.mjs`.

---

## کار

### اتصال حکم اجراپذیری به جلسهٔ فعال — برش دوم فاز ۳

**چرا.** موتور خالص `core/portfolio-eligible.mjs` اکنون هر قرارداد را با
شش قید مأموریت، کیفیت و دفتر همان لحظه حکم می‌دهد. کاربر هنوز این حکم را
نمی‌بیند و عکس شروع قفل‌شده هنوز به شکل فهرست نامزدهای قابل سنجش تبدیل
نمی‌شود. پیش از ساخت پیشنهاد استراتژی، باید همین مدرک خام و علت رد هر
قرارداد در تب دیده شود.

**شاخه.** `codex/portfolio-eligibility-ui` از آخرین `main`، فقط پس از
ادغام قلم قبل.

**فایل‌ها.** از موتور موجود استفاده کنید؛ منطق دفتر را در رابط تکرار نکنید:

```text
ui/portfolio-eligibility.mjs       آداپتر خالص snapshot/universe به نامزد
ui/tabs/portfolio-time.mjs         جدول حکم‌های جلسه فعال
ui/style.css                       چیدمان دسکتاپ و موبایل
```

با `node tools/next.mjs map portfolio` قراردادهای موجود و با
`node tools/next.mjs suites portfolio` دسته‌های نزدیک را ببینید.

**دستهٔ آزمون.** `tests/suites/124-portfolio-eligibility-ui.mjs` (تازه).

**پذیرش:**

1. فقط جلسهٔ `active` و snapshot قفل‌شده‌اش قابل سنجش باشند؛ draft یا
   snapshot تازه از شبکه، حکم ساخته‌شده نشان ندهد.
2. آداپتر هیچ قیمت، ارزش، موقعیت باز یا کیفیتی را حدس نزند. قرارداد ناقص
   به همان حکم رد موتور برسد.
3. جدول برای هر قرارداد نام، سمت، حکم، همه علت‌های رد، کیفیت و سقف حجم
   اجراپذیر را نشان دهد؛ حجم `null` با خط تیره بماند، نه صفر.
4. لحظهٔ سنجش همان لحظه snapshot باشد و تغییر ساعت رابط نتواند دادهٔ آینده
   وارد حکم کند.
5. فیلتر پذیرفته/ردشده فقط نمایش را عوض کند و حکم موتور را بازنویسی نکند.
6. رابط فارسی، رقم فارسی، بدون اسکرول افقی در ۱۴۴۰×۱۰۰۰ و ۳۹۰×۸۴۴، و
   کنسول بدون warning/error در مرورگر داخلی واقعی تأیید شود.

**مرز قلم.** رتبه‌بندی، ساخت ترکیب، پیشنهاد استراتژی و تشکیل سبد هنوز
ممنوع است. این برش فقط مدرک اجراپذیری قراردادها را قابل مشاهده می‌کند.

---

## محدودیت باز از قلم قبلی

پیمودن هر پنج مرحلهٔ wizard در مرورگر با دادهٔ واقعی ممکن نشد — `data/`
تاریخچه‌ای ندارد، پس تقویم خالی است. اگر روی نصبی با تاریخچهٔ واقعی کار
می‌کنید، یک بار پنج مرحله را با یک refresh وسط کار بپیمایید و نتیجه را
همین‌جا ثبت کنید.

---

## وقتی تمام شد

`## کار` بالا را با قلم بعدی جایگزین کنید. قلم بعدی را از `BACKLOG.md` یا
فاز جاری `PORTFOLIO_TIME_TRAVEL.md` بردارید. برای نفر بعد همان چهار چیز را
روشن بنویسید: چرا، فایل‌ها، دسته آزمون و پذیرش قابل‌سنجش.
