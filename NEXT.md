# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## یازده بندِ «رصد زندهٔ هر استراتژی» — هر یازده بسته شد

خواسته: **هر تبِ استراتژیِ سمت راست خودش یک رادارِ زنده باشد، با
پارامترهای مختصِ خودش.** شرحِ هفت دور در `WORKLOG.md`.

کدِ تازه: `core/` → `strategy-radar` · `history-chain` · `session-trail` ·
`manual-price` · `row-trust` · `upstream-tally`؛ `ui/` →
`strategy-history` · `strategyLinkTargets` در `handoff.mjs` ·
`table.sortKey()` و گزینهٔ `demote`. آزمون: ۵۱ و ۲۳۲ تا ۲۳۸.

**سه قاعده‌ای که نباید شکسته شوند:**

۱ تفاوتِ سی‌وشش استراتژی **داده** است نه کد — نمایه در `GROUP_PROFILES`
  یا `ID_PROFILES`، نه تبِ دست‌نویس.

۲ زیرتب فقط **منبعِ** جدول را عوض می‌کند؛ بقیه مشترک است. جدولِ دومی
  ساخته نشود، وگرنه دو عدد از دو مسیر مقایسه می‌شوند.

۳ **انتخابِ موتورِ بازده یک قاعده است، نه چند جا.** هر جا پاها
  هم‌سررسید نیستند، `analyzeMixed`. هر بار که تکرار نشد، دو بخشِ یک صفحه
  دو عدد گفتند (`core/manual-price.mjs`).

## کار — قلم بعدی را از اینجا بردارید

### الف. مقصدهای بیشتر برای نوار پیوند (بندهای ۴ و ۱۱)

چهار مقصد دارد: آزمایشگاه، دیده‌بان شرطی، رصد یونانی، رادار فاصله.
مقصدِ تازه = یک ردیف در `STRATEGY_LINK_TARGETS` **به‌علاوهٔ** پذیرشِ
`state.handoff` در `mount` همان تب. نامزدها: دیده‌بان زنجیره · تحلیل
تاریخی استراتژی · موقعیت‌های من.

⚠ سه قاعده: پذیرش پیش از افزودن · `strategyLinkTargets` باید شرطِ واقعیِ
آن تب را بداند (مثل `GAP_STRATEGY_IDS`) · `ACCEPTING` در دستهٔ ۵۱ به‌روز
شود. و هیچ مسیرِ شکستی `return` خالی نداشته باشد.

### ب. بارِ بالادست — حالا قابل اندازه‌گیری است

گزارشِ ۱۴۰۵/۰۶/۱۶: در ۱۸ دقیقه ۲۰۸۲ درخواست، ۵۶۷ انتظار سهمیه، ۱۵۷ خطای
۵۰۲. ابزارِ سنجش ساخته شد نه سیاست: `/api/health` حالا `byEndpoint` و
`worstEndpoint` می‌دهد.

⚠ **اول اندازه بگیر، بعد تصمیم.** با تابلوی باز چند مسیر تاریخی باز کن و
`curl /api/health | jq .byEndpoint` بگیر. اگر `GetClosingPriceDailyList`
صدر بود، مسئله «سریِ روزانهٔ روزِ گذشته» است که **هرگز عوض نمی‌شود** —
کاندیدای TTL بلند یا کشِ دیسکی. اگر `BestLimits` بود، مسئله چرخشِ زنده
است و `core/live-quote.mjs` جوابش. پیش از دیدنِ عدد هیچ‌کدام را نساز.

### پ. قراردادهایی که باید راستی‌آزمایی شوند

⚠ **ادعای متنی فقط وقتی بی‌ضرر است که راست باشد** — سه کامنت می‌گفتند
«زیان منفی نوشته می‌شود» در حالی که `maxLoss` مثبت است. پیش از تکیه بر هر
جملهٔ «موتور فلان می‌کند» با `node -e` بسنجش. نامزدها: علامت `maxLossPct`
و `instantClosePnl`، و `quality.level` در برابر `executable`.

### ت. ریزدانگیِ درون‌روزی بیش از یک روز

تصمیمِ ذخیره‌سازیِ صاحب پروژه می‌خواهد (کشِ دیسکی مثل `data/roster`).
پیشنهاد: ۹۰ روز، فقط نمادهای درخواست‌شده، سقف ۵۰۰ مگابایت، JSON فشرده.
تا تصمیم نیامده شروع نشود.

## سه دورِ پذیرش انجام شد

`652e819` (چهار ایراد) · `bd0a618` بازارِ بسته (هشت مورد، هفت اصلاح) ·
`bd0a618` بازارِ باز (پنج مورد، هر پنج). قفلشان در دسته‌های ۲۳۶ تا ۲۳۸.

⚠ **سه درس که نباید تکرار شوند:**

۱ **عددِ غلط با ظاهرِ معتبر، از خانهٔ خالی خطرناک‌تر است.** حالتِ شکست
  باید **خالی** باشد نه پیش‌فرضِ قبلی.

۲ **آزمونِ تابع، جای‌نگه‌دار و مرزِ نمایش را نمی‌بیند.** «سنِ مظنه صفر»
  از جای‌نگه‌دارِ `buildChain` آمد و «تاریخِ لاتین» از مرزِ نمایش — هر دو
  با آزمونِ سبز.

۳ **هشداری که روی تصمیم اثر ندارد، برچسب است نه هشدار.** ردیفِ «بازده
  ماهانه ۲٬۰۲۶٬۳۹۶٪» هشدارِ کامل داشت و رتبهٔ اول بود.

## پذیرش — دورِ چهارم، در بازارِ باز

مانده از دورِ سوم: **بلیتِ داده** (وسطِ اسکن برو تاریخی؛ جدول نباید عوض
شود، و برعکس) · **ردِ جلسه روی اسپردِ پرگردش** (مسیر کشیده شود و «فاصلهٔ
پاها» غیرصفر) · **پیوند** (هر چهار مقصد **همان** ترکیب را بگیرند، وگرنه
بگویند «در این روز نیست»).

تازه‌های این دور:

۱. **تقویمی.** جدول، نمودار و کادرِ «اگر با قیمت دستی پر شود» **یک عدد**
   بدهند، و «بیشترین زیان» متناهی باشد نه «نامحدود». کالِ برهنه و نسبتِ
   ۱:۲ باید همچنان نامحدود بمانند.
۲. **ردیفِ غیرقابل اتکا.** ردیفی با «بازده نامتعارف» یا «آفست ناممکن»
   باید **ته** جدول باشد (هر دو جهتِ مرتب‌سازی)، در «بهترین بازده ماهانه»
   و «ردیف قابل اجرا» شمرده نشود، و کارتِ «غیرقابل اتکا» تعدادش را
   بگوید. تیکِ آخرِ کارتِ فیلتر باید کاملاً پنهانش کند.
۳. **اسکنِ صفرنتیجه.** روی استراتژی‌ای که ردیفی نمی‌سازد، جملهٔ آخر باید
   «اسکن تمام شد — مرحله دو اجرا نشد: …» باشد، نه وعدهٔ «مرحله دو…».
۴. **دفتر خطا.** خطای بالادست باید نامِ مسیر و نشانیِ کامل بنویسد، نه
   «[object Object]»؛ و تولتیپِ شمارِ خطا نامِ پرخطاترین سرویس را بگوید.
۵. **پنلِ گروه.** پس از انتخاب استراتژی خودش بسته شود.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود: ردیفِ بی‌عدد از فیلتر نمی‌گذرد و جدا شمرده
می‌شود؛ کادرِ خالی شرطِ «حداقل صفر» نمی‌سازد؛ عددِ دستی هرگز «قابل اجرا»
نیست؛ سنِ نداشته صفر نمی‌شود؛ دفترِ سفارشِ گذشته ساخته نمی‌شود.

ردیفِ مشکوک **حذف نمی‌شود** — فقط صدر را نمی‌گیرد. و بی‌کرانی **ساختاری**
خوانده می‌شود نه از شیبِ عددی: کارمزد شیب می‌سازد ولی در معرض بودن نیست.

اگر یکی شکست، یکی از دسته‌های ۲۳۲ تا ۲۳۸ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، بعد بسته شود (۸۷۸۷ دستِ صاحب پروژه). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`؛ Playwright سراسری،
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`domcontentloaded` نه `networkidle`. اول عکس، بعد آزمون.
