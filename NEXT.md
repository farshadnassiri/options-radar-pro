# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## فاز ۱۴ — پنجرهٔ قیمت اعمال و سرشماری قرارداد (خواستهٔ ۱۴۰۵/۰۶/۱۲)

```text
۱  قرارداد فقط با اجبارِ سقف کنار برود   ✔ core/strike-window.mjs، حالت auto
۲  چهار حالت پنجره، با درصدِ پیشین       ✔ auto · pct · steps · all
۳  سهم برابر هر سررسید از سقف ردیف       ✔ fairShare، هیچ سهمِ صفری
۴  سرشماری قرارداد در روز ورود           ✔ contractCensus، دستهٔ ۲۱۲
۵  برگ «قراردادها» و «پنجره قیمت اعمال»  ✔ خروجی اکسل، خودش قابل قضاوت
۶  سرشماری پیش از هر عددِ ترکیبی         ✔ بالای پنل نتیجه، نه ته گزارش
۷  قراردادِ کنارمانده با نام خودش         ✔ برگ «قرارداد بی‌قیمت»، با علت
۸  قیمت ورود پیش از سقف، نه بعدش          ✔ enumBudget و برداشت نوبتی
۹  نشانیِ هر ابزار در خودِ فایل            ✔ برگ «منبع داده»، حتی بی‌داده
۱۰ قیمتِ قراردادِ سررسیدشده هم بیاید       ✔ منبع دوم، فقط وقتی اولی خالی است
۱۱ شناسهٔ گردشده در دفتر، ترمیم شود        ✔ safeId در ورودی + --repair
۱۲ ادغامی که تکراری بسازد، متوقف شود      ✔ blockingTwins، قاعده در هسته
۱۳ پالایهٔ ترکیب با ده قید                 ✔ core/combo-filter.mjs، دستهٔ ۲۱۵
```

**۱۹۹ سقف نیست:** C(12,2)+C(13,2)+C(11,2) = ۱۹۹، و «استراتژی سقف‌خورده»
صفر است. هر استراتژیِ دوقیمت‌اعمالی همین عدد را می‌سازد.

**سنجیده شد:** پنل پالایه روی ماشین عامل باز نمی‌شود (فقط پس از یک اجرای
کامل سوار می‌شود و اجرای کامل به بالادست نیاز دارد)، ولی صاحب پروژه
۱۴۰۵/۰۶/۱۲ روی ماشین خودش دیدش و درست کار می‌کرد.

## نخستین کارِ صاحب پروژه — ترمیم دفتر

دفترِ فعلی کدهای گردشده دارد (در فایل دو ساله: **۵۴۹۱ از ۷۹۳۳**). تا
ترمیم نشود، همان قراردادها «معامله نشده» ثبت می‌شوند:

```bash
node tools/roster-import.mjs tsetmc_historical_options_2years.xlsx --repair --merge
```

روی ماشین صاحب پروژه انجام شد: **۳۵۲۳ کد ترمیم شد**، دفتر ۱۰۶۷۱ ردیف، و
ضهرم۶۰۵۱ کدِ درستش را گرفت. بی `--repair` ادغام حالا **متوقف می‌شود** —
پیش از این ۵۳۶۴ ردیف تکراری می‌ساخت و چیزی نمی‌گفت.

مانده: اجرای دوباره روی همان بازه و دیدن اینکه «خالی برگشت» چقدر کم شده.

**سنجیده شده با دادهٔ واقعی:** سه فایل زنجیرهٔ اهرم که صاحب پروژه فرستاد،
مو به مو با برگ «قراردادها» می‌خوانند — ۱۲/۱۲/۱۲، ۱۳/۱۳/۱۳، ۱۱/۱۱/۱۱ و
همان نردبان‌ها. شناسایی بسته است.

**درسِ این فاز:** «شش ترکیب» بدون «از چند قرارداد» یک عدد نیست، یک ادعای
بی‌مبناست. دو خروجیِ صاحب پروژه هر دو درست بودند و هر دو بی‌فایده، چون
هیچ‌کدام ورودیِ خودشان را گزارش نمی‌کردند.

## دو کارِ صاحب پروژه

### ۱. نخستین اسکنِ واقعیِ دفتر (از فاز ۱۳، هنوز باز)

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی). سه مسیر
`GetInstrumentSearch`، `GetInstrumentInfo` و
`GetInstrumentOptionByInstrumentID` روی پاسخ واقعی سنجیده نشده‌اند.

```bash
node tools/roster-scan.mjs --from 20240901 --to <امروز>
node tools/roster-verify.mjs --base اهرم --expiry 1404/01/27
```

معیار پذیرش همان است که خودش گذاشت: ۱۴ کال، ۱۴ پوت، جفتِ ناقص صفر. اگر
شکلِ پاسخ فرق داشت، «کاتالوگ ۰ موفق» در گزارش پایانی همان لحظه می‌گویدش.

### ۲. سقف ترکیب را باز کن

ستون «کنارگذاشته» در برگ «پنجره قیمت اعمال» باید حالا صفر باشد — پنجره
دیگر پیش از آزمونِ قیمت ورود چیزی نمی‌بُرد.

استرانگل فروش باید از ۲۳ به شمارِ واقعیِ قابل‌بک‌تست برسد (در فایل قبلی
۷۵). اگر نرسید، عددِ «سقف ترکیب هر استراتژی» را بالا ببرید — تنها چیزی که
از این به بعد می‌بُرد، همان سقف است و در «محدودیت داده» گفته می‌شود.

و برگ تازهٔ «قرارداد بی‌قیمت» را ببینید: ۲۷ قراردادی که وارد نشدند، حالا
با نام و علت. اگر ردیفی «آن روز معامله نشد» بود ولی مطمئنید معامله شده،
همان ردیف یک باگ واقعی است — کد نمادش همان‌جا هست.

### ۳. کشویی مرتب و منحنی تازه را ببینید

در «سبد فرضی»، کشویی «ترکیب» حالا از **ارزان به گران** مرتب است و بهای هر
قرارداد در خودِ برچسب می‌آید. همان مجموعه‌ای است که جدول رتبه‌بندی نشان
می‌دهد — فقط ترتیبش به کار سبد می‌خورد نه به کار رتبه‌بندی.

در زیرتب «در برابر بازار»، نمودار تازهٔ «بازده سبد در برابر قیمت نماد
پایه». برای دیدنش در دانه‌بندی ریزتر: در مرحلهٔ راه‌اندازی «دانه‌بندی
زمان» را روی یک تا شصت دقیقه بگذارید و «اجرای درون‌روزی» را بزنید.

خبرِ زیر نمودار عددی دارد که ارزش نگاه‌کردن دارد: **حساسیت به پایه**. سبدی
که ادعای خنثی‌بودن دارد و شیبش نزدیک یک است، در عمل یک شرط جهت‌دار بوده.

## قلم بعدی

اگر پس از اسکنِ موفق جفتِ ناقص ماند، `tools/roster-verify.mjs --pairs 40`
فهرستشان را می‌دهد؛ هر ردیفش یک سؤال واقعی است: آن سمت واقعاً وجود
نداشته، یا جست‌وجو پیدایش نکرده؟ پاسخِ دسته‌ای این سؤال، قلم بعدی است.

## مرز ادامه

در دامنهٔ فعلی کار دیگری روی تب‌های دیگر انجام نشود.

**دروازه:** `node tools/check.mjs`
