# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### استخراج ضعف‌های مستند پرونده — برش هفتم فاز ۶

**چرا.** پرونده اکنون نتیجه مالی را نشان می‌دهد، اما «تحلیل ضعف» هنوز فقط
نام فاز است. نخست باید از شواهد خود پرونده گزاره‌های پایدار و قابل‌ردیابی
ساخته شوند؛ نه توصیه کلی و نه حدس درباره علت رفتار کاربر.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex کامیت‌های جدا را
روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه می‌دهد. پیش از کار
`origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
core/portfolio-dossier-weakness.mjs
ui/portfolio-dossier-weakness-view.mjs
tests/suites/159-portfolio-dossier-weakness.mjs
```

منابع مجاز: `dossier.early`، تعهد باز، علت‌های داده نامعلوم، هشدارهای ثبت‌شده
لحظه بستن و خروجی `portfolioDossierAnalysis`. قیمت یا رخداد بیرون پرونده
وارد نشود.

**پذیرش:**

1. هر یافته کد پایدار، شدت، عنوان، شرح و `evidence` صریح داشته باشد؛ ترتیب
   خروجی پایدار و کدها تکراری نباشند.
2. بستن زودهنگام، تعهد باز، قید شکسته، داده مالی نامعلوم و نرسیدن قطعی به
   هدف پنج یافته مستقل باشند؛ هرکدام فقط با شاهد خودش ساخته شود.
3. «نرسیدن به هدف» وقتی تحلیل ناقص است ساخته نشود؛ نامعلوم شکست نیست.
4. هشدار `near` با `breached` یکی نشود و شدت‌هایشان متفاوت باشد؛ حالت
   `clear` یافته نسازد.
5. متن علت‌سازی نکند: به‌جای «تصمیم بد» بگوید چه سندی چه وضعی را ثبت کرده.
6. جلسه/پرونده ناهم‌هویت یا نسخه ناشناخته رد شود و خروجی آرامِ بدون یافته
   برای پرونده سالم ممکن باشد.
7. مدل نمایش رقم فارسی داشته باشد و هر شاهد مالی را تومان نشان دهد؛ عدد
   نامعلوم «—» بماند.

این برش خالص است و رابط تازه ندارد؛ مرورگر واقعی لازم نیست. اتصال یافته‌ها
به کارت پرونده قلم بعدی خواهد بود.

## محدودیت‌های باز

مرورگر داخلی از قلم ۱۵۵ با خطای kernel assets وصل نمی‌شود. مقایسه چند
پرونده، نوار هشدار موبایل، سرریز `capacity-panel` و پیمایش تاریخچه واقعی
هنوز بازند.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
