# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### Excel کم‌حجم و پرجزئیاتِ بازی کامل پس از refresh — قلم هشتم فاز ۸

**چرا.** پایان بازی اکنون سرمایه نهایی و رتبه انتخاب کاربر را با بهترین و
بدترین گزینه، فقط از طرح‌های اجراپذیر شروع و دفتر واقعی پایان می‌سازد و
پرونده پس از refresh عیناً برمی‌گردد. حالا خروجی Excel باید همین سند را
برای تحلیل هوش مصنوعی و آرشیو، بدون جدول‌های حجیم و تکراری، کامل حمل کند.

**شاخه.** با اجازه صریح صاحب پروژه، کامیت‌های مستقل را روی
`codex/portfolio-build-refresh` ادامه دهید. پیش از قلم تازه `origin/main` را
بررسی کنید؛ تا وقتی تعارضی نیست شاخه را عوض نکنید و تاریخچه را بازنویسی نکنید.

**فایل‌های محتمل:**

```text
ui/portfolio-dossier-export.mjs
core/portfolio-dossier-analysis.mjs
core/portfolio-final-ranking.mjs
ui/tabs/portfolio-time.mjs
tests/suites/168-portfolio-dossier-xlsx.mjs
tests/suites/177-portfolio-final-xlsx-refresh.mjs
```

**پذیرش:**

1. Excel بازیابی‌شده از سرور همان Session/Portfolio/Position/Transaction/
   Execution/Lot و زمان‌های شروع، تغییر و خروج را بدون تولید شناسه تازه ببرد.
2. مأموریت، سرمایه و تخصیص‌ها، ورودی‌های ریسک و نقدشوندگی، snapshot شروع،
   اطلاعات نماد پایه و کیفیت/علت داده‌ها در شیت‌های روشن و نسخه‌دار باشند.
3. تراکنش‌ها، پاها، Lotهای FIFO، کارمزد، وجه تضمین، نقد ورود/خروج و سود
   تحقق‌یافته/تحقق‌نیافته با عدد خام ریالی و برچسب فارسی جدا بمانند.
4. مسیر سرمایه در تایم‌فریم انتخابی، نقاط تغییر حجم و آفست، سرمایه درگیر و
   آزاد را فشرده ثبت کند؛ داده گمشده سلول خالی و علت داشته باشد، نه صفر.
5. رتبه کاربر، صدک، بهترین/بدترین، همه گزینه‌های رتبه‌دار و بدون‌رتبه با
   علت، مبنای یکسان و زمان شروع/پایان در خروجی حضور داشته باشند.
6. یک شیت manifest کوچک برای هوش مصنوعی، نسخه ساختار، واحدها، روابط کلیدها،
   تعریف ستون‌ها و هشدار «قضاوت آموزشی، نه توصیه مالی» را توضیح دهد.
7. ردیف‌ها تکرار بی‌دلیل نداشته باشند و حجم فایل با fixture کامل زیر سقف
   صریح آزمون بماند؛ جزئیات خام دفتر سفارش فقط به‌صورت شواهد لازم نگه داشته شود.
8. آزمون `177-portfolio-final-xlsx-refresh.mjs` فایل را پس از load پرونده
   بسازد، ZIP/XML و روابط/اعداد/شناسه‌ها را بخواند و پاک‌سازی فایل دقیق را
   قفل کند. آزمون بصری فقط با دستور تازه صاحب پروژه انجام شود.

**مرز قلم.** سفر کامل مرورگر از یک میلیارد تومان تا پایان، دانلود و شروع
جلسه بعد، قلم نهم و پایانی فاز ۸ است.

## محدودیت‌های باز

- universe تاریخی هنوز فقط قراردادهای موجود در آرشیو محلی را می‌شناسد؛
  قرارداد منقضی‌شده بدون عکس روزانه قابل بازسازی نیست.

---

## وقتی تمام شد

یک ردیف `WORKLOG.md`، جدول‌های `TASK_STATUS.md` و
`PORTFOLIO_TIME_TRAVEL.md` و همین فایل را به‌روز کنید. این قلم هشتمین
وظیفه از نه وظیفه فاز ۸ است؛ سهم و جمع را فقط مطابق ابزار پیشرفت بنویسید.
دروازه کامل، کامیت دارای بدنه و پوش روی همین شاخه لازم‌اند؛ سپس بدون انتظار
برای مرج قلم نهم و پایانی فاز ۸ را آغاز کنید.
