# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## یازده بندِ «رصد زندهٔ هر استراتژی» — هر یازده بسته شد

خواسته: **هر تبِ استراتژیِ سمت راست خودش یک رادارِ زنده باشد، با
پارامترهای مختصِ خودش.** شرحش در `WORKLOG.md`.

کدِ تازه: `core/` → `strategy-radar` · `history-chain` · `session-trail` ·
`manual-price` · `row-trust` · `upstream-tally` · `watch-health`؛ `server/`
→ `job-queue` · `atomic-json`؛ `ui/` → `strategy-history` و `demote` در
`table.mjs`. آزمون: ۵۱ و ۲۳۲ تا ۲۴۲.

**سه قاعده‌ای که نباید شکسته شوند:**

۱ تفاوتِ سی‌وشش استراتژی **داده** است نه کد — نمایه در `GROUP_PROFILES`
  یا `ID_PROFILES`، نه تبِ دست‌نویس.

۲ زیرتب فقط **منبعِ** جدول را عوض می‌کند؛ بقیه مشترک است. جدولِ دومی
  ساخته نشود، وگرنه دو عدد از دو مسیر مقایسه می‌شوند.

۳ **انتخابِ موتورِ بازده یک قاعده است، نه چند جا.** هر جا پاها هم‌سررسید
  نیستند، `analyzeMixed` — وگرنه دو بخشِ یک صفحه دو عدد می‌گویند.

## کار — قلم بعدی را از اینجا بردارید

### الف. مقصدهای بیشتر برای نوار پیوند (بندهای ۴ و ۱۱)

چهار مقصد دارد: آزمایشگاه، دیده‌بان شرطی، رصد یونانی، رادار فاصله. مقصدِ
تازه = یک ردیف در `STRATEGY_LINK_TARGETS` **به‌علاوهٔ** پذیرشِ
`state.handoff` در `mount` همان تب. نامزدها: دیده‌بان زنجیره · تحلیل
تاریخی استراتژی · موقعیت‌های من.

⚠ چهار قاعده: پذیرش پیش از افزودن · `strategyLinkTargets` شرطِ واقعیِ آن تب
را بداند · `ACCEPTING` در دستهٔ ۵۱ به‌روز شود · مسیرِ «ترکیب پیدا نشد» حالتِ
کهنه را پاک کند (همان P1 آزمایشگاه).

### ب. بارِ بالادست — اندازه‌گیری‌اش آماده است، تصمیمش نه

`/api/health` حالا `byEndpoint` و `worstEndpoint` می‌دهد. آخرین گزارش: ۶۵
خطا که ۶۱ تایش روی تاریخچهٔ روزانه بود.

⚠ **اول اندازه بگیر، بعد تصمیم.** با تابلوی باز
`curl /api/health | jq .byEndpoint` بگیر. اگر `GetClosingPriceDailyList`
صدر بود، مسئله «سریِ روزانهٔ روزِ گذشته» است که هرگز عوض نمی‌شود —
کاندیدای TTL بلند یا کشِ دیسکی. اگر `BestLimits` بود، چرخشِ زنده است و
`core/live-quote.mjs` جوابش. پیش از دیدنِ عدد هیچ‌کدام را نساز.

### پ. قراردادهای نامزدشده — بسته شد (دستهٔ ۲۴۱)

⚠ **یک مشاهده که تصمیم می‌خواهد:** برای استراتژی‌های بدهکار، سرمایه برابرِ
`maxLoss` گرفته می‌شود، پس «بیشترین زیان ٪ سرمایه» همیشه دقیقاً ۱۰۰ است.
حساب درست است ولی ستون آنجا بی‌اطلاع است. نگهش داریم یا مبنای دیگری؟


### ت. ریزدانگیِ درون‌روزی بیش از یک روز

تصمیمِ ذخیره‌سازیِ صاحب پروژه می‌خواهد (کشِ دیسکی). پیشنهاد: ۹۰ روز، فقط
نمادهای درخواست‌شده، سقف ۵۰۰ مگابایت. تا تصمیم نیامده شروع نشود.

## شش دورِ پذیرش انجام شد

`652e819` · `bd0a618` بسته (هفت) و باز (پنج) · `10557f8` (دو P1) ·
`d6a89e9` بازآزمایی · `957bb24` (سه ایراد). قفلشان در ۲۳۶ تا ۲۴۲.

⚠ **درس‌هایی که نباید تکرار شوند** (شرحِ کاملشان در `WORKLOG.md`):

۱ **عددِ غلط با ظاهرِ معتبر، از خانهٔ خالی خطرناک‌تر است.** حالتِ شکست باید
  **خالی** باشد نه پیش‌فرضِ قبلی — و خروجِ زودهنگام، حالتِ کهنه را پاک
  نمی‌کند مگر صریح پاکش کنی.

۲ **آزمونِ تابع، جای‌نگه‌دار و مرزِ نمایش را نمی‌بیند.** «سنِ مظنه صفر»،
  «تاریخِ لاتین» و «رنگِ وارونهٔ زیان» همه با آزمونِ سبز رد شدند. **اول
  عکس.** و آنچه اصلاً وارد نمی‌شود، آزمون هم ندارد.

۳ **هشدار یا شمارنده‌ای که روی تصمیم اثر ندارد، برچسب است نه هشدار.**

۴ **نکتهٔ کناریِ گزارش را جدی بگیر، و فرضِ کامنت‌ها را دوباره بخوان.** «۶۱
  خطای ۵۰۲» ایراد شمرده نشده بود و به «قیمت پایانی ۰» رسید؛ «چون مبدأ
  ردیفِ زنده است» درست بود تا روزی که زیرتبِ تاریخی ساخته شد.


## پذیرش — در بازارِ باز

مانده از پیش: **بلیتِ داده** (وسطِ اسکن برو تاریخی؛ جدول نباید عوض شود، و
برعکس) · **ردِ جلسه روی اسپردِ پرگردش** (مسیر و «فاصلهٔ پاها» غیرصفر).

۱. **جدولِ تاریخی زیر خطای بالادست.** روزی با چند ۵۰۲: جمله بندِ «⚠ …
   دریافتشان از بالادست شکست خورد» بگیرد، لحنش زرد شود، و هیچ ردیفی با پای
   «قیمت پایانی ۰» نسازد — آن‌ها در سطلِ «بی‌مظنه» شمرده می‌شوند.
۲. **مبنای کمترین/بیشترین قیمت روز.** در مرحلهٔ یک عدد ندارند، پس جدول باید
   **خالی** باشد با سطلِ «بی‌مظنه» — نه جدولی از صفر. بعد از مرحلهٔ دو کار
   کنند.
۳. **رنگِ «بیشترین زیان ٪ سرمایه».** ردیفی با عددِ منفی (سودِ قطعی) کنارِ
   ردیف‌های عادی باید **سبز** باشد نه قرمز.
۴. **پیوندِ ردیفِ تاریخی.** از «رصد تاریخی» یک ردیف را به آزمایشگاه و رصد
   یونانی بفرست: هر دو روی **همان روز** بنشینند، نه تازه‌ترین روز.
۵. **تحلیل تاریخیِ دستی.** اندازهٔ پاها ۱۰۰۰ (یا پیش‌فرض تو) باشد نه «۱»، و
   جریان نقد و یونانی‌های موقعیت غیرصفر. دو پای اسپرد هم پیش‌فرض دو
   قراردادِ متفاوت بگیرند.


## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود: ردیفِ بی‌عدد از فیلتر نمی‌گذرد و جدا شمرده
می‌شود؛ کادرِ خالی شرطِ «حداقل صفر» نمی‌سازد؛ عددِ دستی هرگز «قابل اجرا»
نیست؛ سنِ نداشته صفر نمی‌شود؛ دفترِ سفارشِ گذشته ساخته نمی‌شود.

ردیفِ مشکوک **حذف نمی‌شود** — فقط صدر را نمی‌گیرد. بی‌کرانی **ساختاری**
خوانده می‌شود نه از شیبِ عددی. و پروندهٔ JSON که خوانندهٔ هم‌زمان دارد،
اتمیک نوشته می‌شود.

اگر یکی شکست، یکی از دسته‌های ۲۳۲ تا ۲۴۲ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، بعد بسته شود (۸۷۸۷ دستِ صاحب پروژه). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`؛ Playwright سراسری،
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`domcontentloaded` نه `networkidle`.
