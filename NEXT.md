# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## آخرین قلم بسته‌شده — انتظار، مرتب‌سازی زنجیره، و سمت یک‌طرفه

- **نشانهٔ بارگذاری** در `ui/busy.mjs`: `busyBlock` (اسکلت + متن) و
  `attachBusyBar` (نوار در جریان، بدون پاک‌کردن محتوا). کلاس `.skeleton`
  که از قبل در کد بود ولی **هیچ CSS نداشت** حالا دیدنی است.
- **مرتب‌سازی زنجیرهٔ دوطرفه** (`sortPairedChain`): چون هر ردیف دو مقدار
  دارد، سرستون سمتِ خودش را حمل می‌کند. ردیفِ بدون قرارداد در آن سمت در
  هر دو جهت آخر می‌ماند، و خط قیمت جاری فقط در ترتیب «قیمت اعمال» کشیده
  می‌شود.
- **فیلتر یک‌سمته** (`pairedSides`): «فقط کال» دیگر ستون‌های پوت را با «—»
  پر نمی‌کند؛ اصلاً رسم نمی‌شوند.
- **نقشهٔ سایت** در `docs/SITE-MAP.pdf` — برای معامله‌گر، نه برای عامل.

دستهٔ ۲۴۸ حالا ۵۳ ادعا دارد.

## کار

### پذیرش صفحهٔ رصد زنده در جلسهٔ بازار باز

پذیرش تا امروز فقط با دادهٔ ساختگی و در بازار بسته انجام شده.

۱. **نگاه باز لحظه‌ای**: تب شبکه را باز بگذار و نمای «نگاه باز چندروزه» را
باز کن؛ نباید هیچ `history/universe` یا `dailies` برود. فهرست نماد باید
فقط نمادهای امروزِ تابلو را داشته باشد.

۲. **ستون «علت نبود تلاطم»** را روشن کن و ببین چند درصد `belowFloor`
می‌گیرند؛ «فاصله قیمت از کف نظری» عددِ لازم برای تصمیم نرخ را می‌دهد.

۳. **تلاطم اجرایی** (میانه مظنه) باید جایی پر باشد که مشاهده‌ای خالی است.
دلتای اجرایی و مشاهده‌ای اگر خیلی فاصله گرفتند، یعنی قیمت مشاهده‌ای کهنه است.

۴. **«ارزش خود پایه»** در کارت نماد و دیده‌بان دیگر نباید صفر باشد.

۵. **ساعت**: در بازار بسته باید «عکس … · N ثانیه پیش» بگوید و نوار قرمز شود.

۶. **دایرهٔ جهت** باید «قرارداد معامله‌شده» بگوید، نه «نماد».

۷. **زنجیرهٔ دوطرفه**: خط قیمت جاری بین دو اعمالِ در بر گیرنده؛ سایهٔ «در
سود» سمت کال زیر قیمت جاری و سمت پوت بالای آن.

۸. **دلتا**: کال مثبت، پوت منفی، و با عمق در سود به ±۱ میل کند. اهرم مؤثر
نزدیک پول بیشینه شود. «بازده اگر پایه تکان نخورد» برای هر اختیار خارج از
سود دقیقاً ‎−۱۰۰٪‎.

۹. **پایداری**: رفرش پیوستهٔ ۲۰ دقیقه‌ای و صحت آخرین نقطهٔ نمودارهای
درون‌روزی، در بازار باز.

## قلم بعدی — نرخ و بازده نقدی به‌ازای نماد (تصمیم صاحب پروژه)

ممیزی ردیف ۹ درست بود و اندازه‌گیری نشان داد **علتِ غالبِ ستون‌های خالیِ
تلاطم همین است**، نه ناهم‌زمانی قیمت. روی نمونهٔ خودِ گزارش:

```
نرخ ۳۰٪ → کف نظری ۶٬۲۲۸ → قیمت ۴٬۶۳۱ زیر کف  →  belowFloor
نرخ  ۰٪ → کف نظری ۴٬۵۷۲ → قیمت ۴٬۶۳۱ بالای کف →  IV ۱۳٫۸٪
```

با نرخ ۳۰٪، کفِ بدون‌آربیتراژِ هر کالِ در سود ۱٬۶۵۶ ریال بالاتر می‌رود. آن
کف وقتی معنی دارد که بشود پایه را فروش استقراضی کرد؛ در این بازار نمی‌شود.

ستون‌های «کف نظری قیمت» و «فاصله قیمت از کف نظری» اضافه شدند تا فرض دیده
شود. ولی **انتخاب خودِ فرض تصمیم صاحب پروژه است**، و سه راه دارد:

۱. نرخِ جداگانه برای کفِ بدون‌آربیتراژ (مثلاً صفر) در کنار نرخِ تنزیل.
۲. بازده نقدی به‌ازای نماد، از یک منبع داده که برنامه الان ندارد.
۳. مدل سود نقدی گسسته برای رویدادهای قطعی.

هیچ‌کدام را بدون تصمیح صاحب پروژه پیاده نمی‌کنم — عددِ فرضیِ ساخته‌شده،
دقیقاً همان چیزی است که قاعدهٔ ۲-۴ منع می‌کند. بالا بردن کورکورانهٔ سقف IV
هم راه‌حل نیست؛ ممیزی خودش همین را گفت.

## قلم باز — اختلاف شمارش ریزمعامله

برای طهرم۷۰۶۱ عدد رسمی تابلو ۶٬۷۹۶ معامله بود و نوار ریزمعامله پس از حذف
معاملات باطل ۶٬۷۹۸ ردیف فعال داد. اختلاف از ناسازگاری دو endpoint بالادست
می‌آید. اول باید سنجید کدام مرجع است، بعد تصمیم گرفت کدام عدد نمایش داده
شود — نه اینکه یکی بی‌صدا با دیگری جایگزین شود.

## قلم‌های باز

- **بارِ بالادست**: تصمیم دربارهٔ `byEndpoint` در `/api/health` منتظر
  اندازه‌گیری صاحب پروژه است.
- **کشِ دیسکی درون‌روزی**: منتظر تصمیم دربارهٔ سیاست ذخیره‌سازی.
- **غلتاندن سررسید**: اگر مقایسهٔ بازه‌ای فراتر از عمر یک نسل قرارداد لازم
  شد، باید مدل صریح شود؛ قرارداد منقضی نباید بی‌صدا جایگزین شود.

## قواعدی که نباید شکسته شوند

- عدد گمشده با پایانی دیروز، VWAP یا قیمت مدل پر نشود؛ «—» بهتر از عدد
  ساختگی است.
- روز جاری فقط در فاز `open`، `after` یا `ungated` به تاریخ امروز منتسب شود.
- قرارداد پس از سررسید، حتی اگر بالادست آخرین قیمت را تکرار کرد، قابل معامله
  یا قابل مقایسه شمرده نشود.
- رنگ‌ها فقط از توکن CSS و همه اعداد نمایشی از `ui/fmt.mjs` بیایند.
- همه نماهای قدیمی صفحهٔ رصد زنده حفظ شوند — تب‌بندی جای `<details>` را
  گرفت، چیزی حذف نشد.

**دروازه:** `node tools/check.mjs`
