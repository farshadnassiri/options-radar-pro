# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## یازده بندِ «رصد زندهٔ هر استراتژی» — دورِ اول بسته شد

خواستهٔ صاحب پروژه، سرِ همهٔ یازده بند: **هر تبِ استراتژیِ سمت راست باید
خودش یک رادارِ زنده باشد، با پارامترهای مختصِ خودش.** دورِ اول بندهای ۱ و
۲ و ۷ را تا حدِ «جدولِ زنده با فیلترِ مختصِ خودش» بست؛ شرح در
`WORKLOG.md`.

کدِ تازه: `radarProfile`/`applyRadarFilters`/`filterLimit`/`passesFilter`/
`RADAR_FILTERS` در `core/strategy-radar.mjs` · `table.sortKey()` در
`ui/table.mjs`. آزمون: دستهٔ ۲۳۲.

**قاعده‌ای که نباید شکسته شود:** تفاوتِ سی‌وشش استراتژی **داده** است نه
کد. برای استراتژی تازه نمایه‌اش را در `GROUP_PROFILES` یا `ID_PROFILES`
بنویسید — نه تبِ دست‌نویس.

## کار — قلم بعدی را از اینجا بردارید

ترتیب از بالا به پایین است: هر قلم روی قلمِ پیش از خودش می‌نشیند.

### الف. زیرتب‌های صفحهٔ استراتژی (بندِ ۹)

«تب اصلی رصد بازاره و بقیه تبها باید برای کمک به رصد بازار استفاده بشن.»

`ui/subtabs.mjs` آماده است و `spread-radar` الگویش را دارد. چهار زیرتب:
**رصد زنده** (همین که هست) · **رصد تاریخی** (ب) · **ردِ جلسه** (پ) ·
**پیوندها** (ت). ⚠ قاعدهٔ خودِ `subtabs.mjs`: «تبی که به جایی نمی‌رسد
بدتر از نبودِ تب است» — زیرتبِ خالی نسازید.

### ب. رصد تاریخی روی همان جدول (بندِ ۸)

«اگه کاربر تاریخ داد جدول بر اساس تاریخ به روز بشه (روزانه، ۶۰ دقیقه تا
۱ دقیقه در ان روز خاص).» قطعه‌ها هستند و **دوباره نوشته نشوند**:
`core/radar-history.mjs` (`buildRadarHistory`, `radarDataReport`) ·
`core/intraday-grid.mjs` (`MOMENT_GRAINS` — روزانه تا یک‌دقیقه) ·
`ui/history-range.mjs` و `ui/datewheel.mjs` (انتخابگر بازه) ·
`ui/live-scope.mjs` (چسباندنِ روز جاری).

⚠ محدودیتِ واقعی، در خودِ `core/intraday-grid.mjs` نوشته شده: دانه‌بندی
درون‌روزی فقط روی **یک روز** کار می‌کند، چون ریزمعاملهٔ هر ابزار برای هر
روز یک درخواست است. راهِ درست کشِ دیسکی است مثل `data/roster`، نه بالا
بردن سقف درخواست. تا آن نیامده، «تاریخِ خاص + دانه‌بندی ریز» را فقط
برای **یک روز** بدهید و محدودیت را روی صفحه بنویسید.

### پ. ردِ جلسه برای یک ترکیب (بندهای ۱۰ و ۸)

«با انتخاب یک ترکیب خاص، رصد اون ترکیب از لحظه شروع بازار تا الان.»

یک ترکیب، نه کلِ جدول — و همین شدنی‌اش می‌کند: `/api/live-trades` برای
پاهای همان ترکیب. `core/day-range.mjs` امروز فقط مشاهده‌های خودِ رصد را
دارد؛ با ریزمعاملهٔ جلسه، «کف و سقفِ امروز» کفِ واقعیِ جلسه می‌شود.
نمودارش: `gapPathChart` در `ui/gap-charts.mjs`.

### ت. پیوند به بقیهٔ برنامه (بندهای ۴ و ۱۱)

«لینک کنه به قسمت ازمایشگاه یا رصد زنده … لینک در صفحه مجزا باز بشه.»

نیمه‌اش هست: `openHandoffPage` در `ui/handoff.mjs` صفحهٔ جدا را باز
می‌کند (`#tab!token`) و مسیریابِ `ui/app.mjs` هر شناسهٔ تبی را می‌پذیرد.
آنچه نیست: `handoffPlan` فقط مقصدِ `backtest` می‌سازد و هیچ تبِ دیگری
`state.handoff` را نمی‌خواند. لازم است قراردادِ پیوندِ عمومی، و پذیرشش
در: 📐 رصد یونانی · 📏 رادار فاصله · 🔔 دیده‌بان شرطی (قاعده از روی همین
ردیف، پیش‌پر) · موقعیت‌های من · تحلیل رول · دیده‌بان زنجیره.

### ث. «زنده» یعنی چه، در تبِ استراتژی (بندِ ۵)

رصدِ زندهٔ تبِ استراتژی هنوز هر دوره یک **اسکنِ دومرحله‌ای کامل** می‌زند —
با چند ده ترکیب یعنی صف و backoff. تعریفِ «زنده» از قبل نوشته شده:
`core/live-quote.mjs` (سهمیه، چرخش، دو منبعِ مظنه، پوسیدگی). تبِ
استراتژی باید سوارِ همان شود، نه `setInterval` روی اسکنِ کامل. کنارش:
ستون **سنِ عدد** (از `state`/`staleSec` در `/api/infos` — نمادِ متوقف
نباید عددِ زنده نشان بدهد) و **جای چرخش** روی نوار.

### ج. مبنای قیمتِ «دستی» (بندِ ۱)

«دفتر سفارش، آخرین، پایانی، **دستی**» خواسته شد؛ سه‌تای اول در
`priceBasis` هستند، «دستی» نیست. قاعدهٔ صداقت عددی: قیمتِ دستی **فرضِ
کاربر** است نه دادهٔ بازار، پس ردیفِ دستی هرگز «قابل اجرا» شمرده نشود و
نشانِ خودش را داشته باشد.

## پذیرشِ باقی‌مانده — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳ از پراکسی)، پس جدولِ رادارِ
استراتژی در این نوبت فقط **خالی** دیده شد. با دادهٔ واقعی ببینید:

۱. **کاورد کال.** با «بازده ایستا» مرتب بالا می‌آید؟ «فاصله تا سربه‌سری»
   و «دارایی قفل‌شده» عدد دارند؟
۲. **فیلتر.** «حداقل بیشترین سود» بگذار؛ گزارش باید بگوید چند ردیف افتاد
   و چندتاش به‌خاطرِ **نداشتنِ داده** بود.
۳. **ترتیب در رصد زنده.** روی یک ستون مرتب کن، رصد زنده را روشن کن، یک
   دوره صبر کن — ترتیب نباید عوض شود.
۴. **دورهٔ نوسازی.** ۵ و ۶۰ را امتحان کن.
۵. **شاخص‌های کلیدی.** با فیلترِ روشن باید ردیف‌های **نمایش‌داده‌شده** را
   بشمارند، نه کلِ اسکن.
۶. **چهار خانواده.** پروانه، تقویمی، استرانگلِ فروش و باکس — ستون‌ها و
   فیلترها باید آشکارا فرق کنند.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. ردیفی که عددِ فیلترشده را ندارد از فیلتر
نمی‌گذرد و جدا شمرده می‌شود؛ «نامحدود» عددِ نداشته نیست و مثل عدد سنجیده
می‌شود؛ کادرِ خالیِ فیلتر شرطِ «حداقل صفر» نمی‌سازد؛ و ستونی که در
قرارداد ستونیِ موتور نباشد در هیچ نمایه‌ای نمی‌نشیند.

اگر یکی شکست، دستهٔ ۲۳۲ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`

## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، پس از کار بسته شود (۸۷۸۷ دستِ صاحب پروژه است). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`، Playwright سراسری:
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`waitUntil: 'domcontentloaded'` بگذارید نه `networkidle` — تابلو بسته
است و شبکه آرام نمی‌گیرد. کارِ ظاهری: اول عکس، بعد آزمون.
