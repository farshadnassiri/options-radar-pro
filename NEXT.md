# کار بعدی

> اول `PROTOCOL.md`، بعد `node tools/next.mjs`.

## یازده بندِ «رصد زندهٔ هر استراتژی» — هر یازده بسته شد

خواسته، سرِ همه: **هر تبِ استراتژیِ سمت راست باید خودش یک رادارِ زنده
باشد، با پارامترهای مختصِ خودش.** شرحِ شش دور در `WORKLOG.md`.

کدِ تازه: `core/strategy-radar.mjs` · `core/history-chain.mjs` ·
`core/session-trail.mjs` · `core/manual-price.mjs` ·
`ui/strategy-history.mjs` · `strategyLinkTargets` در `ui/handoff.mjs` ·
`table.sortKey()`. آزمون: ۵۱ و ۲۳۲ تا ۲۳۵.

**دو قاعده‌ای که نباید شکسته شوند:**

۱ تفاوتِ سی‌وشش استراتژی **داده** است نه کد — نمایه در `GROUP_PROFILES`
  یا `ID_PROFILES`، نه تبِ دست‌نویس.

۲ زیرتب فقط **منبعِ** جدول را عوض می‌کند؛ شاخص‌ها و فیلترها و جدول و
  جزئیات مشترک‌اند. جدولِ دومی ساخته نشود، وگرنه دو عدد از دو مسیر با هم
  مقایسه می‌شوند.

## کار — قلم بعدی را از اینجا بردارید

### الف. مقصدهای بیشتر برای نوار پیوند (بندهای ۴ و ۱۱)

چهار مقصد دارد: آزمایشگاه، دیده‌بان شرطی، رصد یونانی، رادار فاصله.
مقصدِ تازه = یک ردیف در `STRATEGY_LINK_TARGETS` **به‌علاوهٔ** پذیرشِ
`state.handoff` در `mount` همان تب. نامزدها: دیده‌بان زنجیره · تحلیل
تاریخی استراتژی · موقعیت‌های من.

⚠ سه قاعده: پذیرش پیش از افزودن · `strategyLinkTargets` باید شرطِ واقعیِ
آن تب را بداند (مثل `GAP_STRATEGY_IDS`) · فهرستِ `ACCEPTING` در دستهٔ ۵۱
هم به‌روز شود. و هیچ مسیرِ شکستی `return` خالی نداشته باشد — رصد یونانی
مدت‌ها همین را داشت.

### ب. هزینهٔ واقعیِ هر تیک (بندِ ۵)

«سنِ مظنه» و «وضعیت نماد» آمدند (۲۳۲). نیامد: سوارکردنِ تبِ استراتژی روی
چرخشِ `core/live-quote.mjs`.

⚠ **اول اندازه بگیر.** مرحلهٔ یک از عکسِ مشترکِ دیده‌بان تغذیه می‌شود و
شبکه نمی‌زند؛ مرحلهٔ دو فقط برای کاندیداهای برتر عمق می‌گیرد
(`ui/scanner.mjs`، سقف ۱۸۰ کد). با تابلوی باز ببین هر تیک چند درخواست
می‌شود؛ اگر سنگین بود `live-quote` را سوار کن، اگر نه همین بماند.

### پ. قراردادهایی که باید راستی‌آزمایی شوند

دورِ دهم دید سه کامنت می‌گفتند «زیان منفی نوشته می‌شود» در حالی که
`maxLoss` مثبت است و نامحدودش `+Infinity`. کد درست بود، مستندات غلط — و
دو ادعای آزمون همان غلط را قفل کرده بودند.

⚠ **ادعای متنی فقط وقتی بی‌ضرر است که راست باشد.** پیش از تکیه بر هر
جملهٔ «موتور فلان می‌کند»، با یک `node -e` بسنجش. نامزدهای بعدی: علامت
`maxLossPct` و `instantClosePnl`، و `quality.level` در برابر
`executable`.

## پذیرش — نیاز به ماشینِ صاحب پروژه

`cdn.tsetmc.com` از ماشین عامل بسته است (۴۰۳)، پس جدول فقط **خالی** دیده
شد. با دادهٔ واقعی ببینید:

۱. **کاورد کال.** با «بازده ایستا» مرتب بالا می‌آید؟ «فاصله تا سربه‌سری»
   و «دارایی قفل‌شده» عدد دارند؟
۲. **فیلتر.** «حداقل بیشترین سود» بگذار؛ گزارش بگوید چند ردیف افتاد و
   چندتا به‌خاطرِ **نداشتنِ داده**.
۳. **ترتیب در رصد زنده.** روی ستونی مرتب کن، رصد زنده را روشن کن، یک دوره
   صبر کن — ترتیب نباید عوض شود. دورهٔ ۵ و ۶۰ را هم امتحان کن.
۴. **شاخص‌ها.** با فیلترِ روشن باید ردیف‌های **نمایش‌داده‌شده** را بشمارند.
۵. **چهار خانواده.** پروانه، تقویمی، استرانگلِ فروش، باکس — ستون‌ها و
   فیلترها باید فرق کنند.
۶. **رصد تاریخی.** تقویم فقط روزهای داده‌دارِ همان نماد. یک روز بگیر:
   **همان ستون‌ها** با قیمتِ آن روز، و جمله بگوید چند پا از چند پا قیمت
   داشت. «دفتر سفارش» نباید در مبناها باشد و هیچ ردیفِ تاریخی «قابل
   اجرا» نخورد.
۷. **خوابیدنِ زنده.** رصد زنده را روشن کن و برو روی زیرتب تاریخی — باید
   خودش خاموش شود، وگرنه جدولِ تاریخی چند ثانیه بعد پاک می‌شود.
۸. **نوار پیوند.** روی یک اسپرد باید چهار دکمه باشد و روی کاورد کال دو
   تا. هر چهار را بزن: **صفحهٔ جدا** باز شود و مقصد واقعاً روی همان نماد
   و همان ترکیب بنشیند — رادار فاصله باید زیرتب «تاریخچه» را با همان
   ترکیب باز کند.
۹. **قیمت دستی.** در ستون «قیمت دستی» یک پا را عوض کن: دو ستونِ زیرش
   باید فرق کنند و «بیشترین زیانِ» کمتر **سبز** باشد. کادر را خالی کن؛
   دو ستون دوباره یکی شوند.
۱۰. **تازگی.** «سنِ مظنه» برای نمادِ فعال عدد، برای بی‌داده «—» نه صفر؛ و
   روی نمادِ متوقف «وضعیت نماد» باید «متوقف» بگوید.
۱۱. **ردِ جلسه.** ردیفی را کلیک کن، برو زیرتب سوم و مسیر را بکش: نمودار
   باید آنجا که پایی هنوز معامله نکرده شکاف داشته باشد، و ستونِ «فاصلهٔ
   پاها» عددِ غیرصفر بدهد.

## مرزی که رد نشد

هیچ عددی ساخته نمی‌شود. ردیفِ بی‌عدد از فیلتر نمی‌گذرد و جدا شمرده
می‌شود؛ «نامحدود» عددِ نداشته نیست؛ کادرِ خالی شرطِ «حداقل صفر» نمی‌سازد
(نه در فیلتر، نه در قیمت دستی)؛ و ستونی که در قرارداد ستونیِ موتور نباشد
در هیچ نمایه‌ای نمی‌نشیند. عددِ دستی هرگز «قابل اجرا» نیست.

تاریخی: روزِ دیگری جانشینِ روزِ بی‌معامله نمی‌شود، دفترِ سفارشِ گذشته
ساخته نمی‌شود، پایهٔ بی‌قیمت ترکیب نمی‌سازد. ردِ جلسه: پایِ بی‌معامله
نقطه نمی‌سازد، لحظهٔ نرسیده ستون نمی‌شود، ناهم‌زمانی روی هر نقطه نوشته
می‌شود. تازگی: سنِ نداشته صفر نمی‌شود و وضعیتِ نداشته «مجاز» نیست.

اگر یکی شکست، یکی از دسته‌های ۲۳۲ تا ۲۳۵ قرمز می‌شود.

**دروازه:** `node tools/check.mjs`
## سرور آزمون و عکس‌گرفتن

پورت ۸۷۹۴، بعد بسته شود (۸۷۸۷ دستِ صاحب پروژه). کرومیوم در
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`؛ Playwright سراسری،
از پوشهٔ موقت با `ln -s /opt/node22/lib/node_modules node_modules`.
`waitUntil: 'domcontentloaded'` نه `networkidle` — تابلو بسته است و شبکه
آرام نمی‌گیرد. کارِ ظاهری: اول عکس، بعد آزمون.
