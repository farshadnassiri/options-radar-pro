# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید.

## فاز ۱۳ — دفتر قراردادهای تاریخی (خواستهٔ ۱۴۰۵/۰۶/۱۰)

```text
۱  قرارداد منقضی در بازهٔ انتخابی بیاید   ✔ core/option-roster.mjs، دستهٔ ۲۰۷
۲  دو سال گذشته هم قابل بررسی باشد        ✔ universe?date= از دفتر می‌خواند
۳  فعال یا منقضی به کاربر گفته شود        ✔ تب تازه + برچسب در هر ردیف
۴  انقضا در طول بازه رخ می‌دهد            ✔ activeFrom/activeTo و «داخل بازه»
۵  چیزی از گذشته جا نماند                 ✔ شمارِ ناخوانا و غیر-اختیار، هر دو دیده می‌شوند
```

روی ۷۹۷۴ ردیف واقعی: ۷۹۳۳ پذیرفته · ۳۲ صندوق و اوراق کنار گذاشته · ۹ نامِ
بریده که حدس زده نشد. قیمت اعمال و سررسید با آنچه اسکریپت خودِ صاحب پروژه
خوانده بود، روی هر ۷۵۱۱ ردیفِ مشترک مو به مو یکی درآمد — و ۴۲۲ قرارداد
بیشتر خوانده شد.

دو نقص را فقط دادهٔ واقعی و مرورگر پیدا کردند:

  • openpyxl سلولِ خالی را `<c ... />` می‌نویسد؛ ویرگولِ حریص سلولِ بعدی
    را می‌بلعید و ستون‌ها یکی‌درمیان جابه‌جا می‌شدند، بی‌هیچ خطایی.
  • `display:flex` روی `.field`، `display:none`ِ ضمنیِ `[hidden]` را
    می‌بازد. `el.hidden === true` بود و فیلد همچنان روی صفحه.

یک جهش زنده ماند و درس داد: ترتیب سه منبع را با «کجای فایل نوشته شده»
قفل کرده بودم و می‌شد خواندنِ بایگانی را بی‌اثر کرد بی‌آنکه ادعا بشکند.
سیاست به `pickUniverseSource` در هسته منتقل شد.

## آنچه این ماشین نتوانست بسنجد

`tools/roster-scan.mjs` هرگز روی شبکهٔ واقعی اجرا نشد — این ماشین به
`cdn.tsetmc.com` دسترسی ندارد. منطق استخراجش با پاسخِ ساختگیِ هم‌شکل
آزموده شده (دستهٔ ۲۰۹) و ورودی واقعی از مسیر xlsx گذشت. نخستین اجرای
واقعی با صاحب پروژه است:

```bash
node tools/roster-scan.mjs --from 20240901 --to <امروز>
```

اگر شکل پاسخ با آنچه فرض شده فرق داشت، `unwrapDay` و `instrumentFields`
در `core/roster-scan.mjs` عمداً دفاعی نوشته شده‌اند؛ ولی گزارشِ «ابزارِ
دیده‌شده» در پایان اجرا را ببینید — عددِ کوچک یعنی شکل عوض شده.

## قلم بعدی

اندازهٔ قرارداد در ردیف‌هایی که از دفتر می‌آیند صفر است و از پیش‌فرض
اعلامی کاربر پر می‌شود. `GetInstrumentOptionMarketWatch` اندازه را دارد
ولی فقط برای امروز؛ مسیر تاریخی هم شاید بدهد. اگر داد، همان ردیف‌ها
اندازهٔ واقعیِ همان روز را می‌گیرند و پرچم `assumed` پایین می‌آید. تا آن
موقع، نشان‌دار ماندن ردیف درست‌ترین کار است.

## مرز ادامه

در دامنهٔ فعلی کار دیگری روی تب‌های دیگر انجام نشود.

**دروازه:** `node tools/check.mjs`
