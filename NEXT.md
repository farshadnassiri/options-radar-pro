# کار بعدی

> پیش از شروع `PROTOCOL.md` و سپس `node tools/next.mjs` را بخوانید. پس
> از پایان، همین فایل را برای قلم بعد بازنویسی کنید.

---

## کار

### بازکردن پروندهٔ ذخیره‌شده پس از refresh — برش چهارم فاز ۶

**چرا.** بستن جلسه اکنون پرونده را واقعاً روی سرور ثبت می‌کند، اما پس از
refresh فقط پیش‌نویس/جلسهٔ مأموریت در فهرست ادامه دیده می‌شود. پرونده روی
دیسک هست ولی کاربر راهی برای بازکردن و دیدنش ندارد؛ ماندگاری بدون مسیر
خواندن در رابط نیمه‌کاره است.

**شاخه.** با اجازهٔ صریح صاحب پروژه در ۱۴۰۵/۰۶/۰۶، Codex چند قلم را با
کامیت‌های جدا روی شاخهٔ ماندگار `codex/portfolio-dossier-store` ادامه
می‌دهد. پیش از کار `origin/main` را بررسی کنید، اما شاخه را عوض نکنید.

**فایل‌ها:**

```text
ui/portfolio-closeout-view.mjs       ساخت مدل نمایش از پروندهٔ خام ذخیره‌شده
ui/tabs/portfolio-time.mjs           فهرست/بازکردن پرونده و قفل جلسه بسته
ui/portfolio-dossier-data.mjs        قرارداد فهرست و خواندن موجود است
tests/suites/156-portfolio-dossier-resume.mjs
```

مدل نمایش را از اعداد خام `dossier` بسازید؛ متن تومانِ ذخیره‌شده یا محاسبه
دوباره از قیمت بازار ممنوع است. `closeoutView` نیز باید همان سازندهٔ مشترک
را مصرف کند تا پروندهٔ زنده و پروندهٔ خوانده‌شده دو نمایش متفاوت نسازند.

**پذیرش:**

1. یک تابع خالص، `{session,dossier}` معتبر را به همان مدل نمایشی پروندهٔ
   زنده تبدیل کند؛ نسخه ناشناخته یا ناهمخوانی شناسه صریح رد شود.
2. فهرست پرونده‌های سرور در کنترل ادامه قابل انتخاب باشد و ردیف خراب با
   پیام روشن باز نشود.
3. بازکردن پرونده، `loadDossier` را صدا بزند، جلسهٔ بسته را در تب قرار دهد،
   پرونده را رسم کند و همهٔ کنترل‌های معامله/گام/بستن را خاموش نگه دارد.
4. 404، خطای شبکه یا رکورد ناقص شبیه پروندهٔ موفق نشان داده نشود و وضعیت
   فعلی تب را بی‌صدا عوض نکند.
5. تب مستقیم fetch نزند؛ همهٔ شبکه از `portfolio-dossier-data.mjs` بگذرد.
6. همهٔ عددهای نمایشی رقم فارسی و تومان باشند؛ دادهٔ مالی نامعلوم «—»
   بماند و صفر ساخته نشود.

راستی‌آزمایی مرورگر لازم است. اتصال مرورگر داخلی در قلم قبل با خطای محیطی
`failed to write kernel assets` دو بار شکست خورد؛ اگر محیط درست شد، هم
پروندهٔ معتبر و هم خطای خواندن را در مرورگر واقعی ببینید.

## محدودیت‌های باز

نمایش فهرست پرونده‌ها هنوز تحلیل مقایسه‌ای یا «ضعف‌ها» نیست؛ آن‌ها قلم‌های
بعدی فاز ۶ هستند. نوار هشدار موبایل همچنان چند صفحه اسکرول دارد و
`DIV.capacity-panel` در عرض ۳۹۰ پیکسل نیاز به بررسی سرریز دارد. ویزارد نیز
روی نصب دارای تاریخچهٔ واقعی هنوز از ابتدا تا انتها پیموده نشده است.

---

## وقتی تمام شد

پیشرفت وزن‌دار، یک ردیف `WORKLOG.md` و این فایل را به‌روز کنید؛ دروازه را
سبز، کامیت را دارای بدنه و پوش را روی همین شاخه انجام دهید.
