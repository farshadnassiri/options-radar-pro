# قواعد کار عامل‌ها

این فایل قرارداد کار روی این مخزن است. هر عامل — Claude، Codex، Antigravity
یا هر ابزار دیگری — پیش از اولین تغییر باید کل این فایل را بخواند.

خلاصه در یک خط: **کار ترتیبی، شاخه کوتاه‌عمر، آزمون سبز، ادغام فقط با
بازبینی.**

---

## ۱. پروژه چیست

سامانه رصد استراتژی آپشن. سرور محلی + موتور محاسبه + رابط وب فارسی با ۳۴ تب.

```bash
node server/server.mjs     # http://127.0.0.1:8787
node tests/run.mjs         # آزمون موتور
node tests/guards.mjs      # آزمون‌های نگهبان قواعد همین فایل
```

نقشه کد:

```text
core/        موتور محاسبه — توابع خالص، بدون DOM، بدون شبکه
strategies/  فهرست استراتژی‌ها؛ همه از یک موتور مشترک عبور می‌کنند
server/      سرور محلی، نگهبان ورودی، کش، backoff
worker/      ریسه‌های اسکن و بازپخش تاریخی
ui/          رابط کاربری؛ ui/tabs/ هر تب یک ماژول
tests/       اجراکننده آزمون دست‌ساز، بدون فریم‌ورک
data/        تنظیمات
```

---

## ۲. قواعد فنی — شکستن این‌ها یعنی PR رد می‌شود

**۲-۱. صفر وابستگی npm.** پروژه هیچ بسته‌ای نصب نمی‌کند و نباید بکند.
`npm install` نزنید. `dependencies` به `package.json` اضافه نکنید. فایل قفل
نسازید. هر `import` یا از ماژول‌های خود پروژه است یا از `node:` داخلی.
اگر برای کاری کتابخانه لازم شد، به‌جای افزودنش، همان کار را در چند ده خط
بنویسید یا مسئله را مطرح کنید.

**۲-۲. ESM خالص.** همه فایل‌ها `.mjs` با `import`/`export`. بدون
`require`، بدون مرحله build، بدون ترنسپایل. نود ۱۸ به بالا.

**۲-۳. فارسی بودن رابط.** متن رابط، برچسب‌ها و کامنت‌های کد فارسی‌اند.
هر عددی که به کاربر نشان داده می‌شود باید از `ui/fmt.mjs` عبور کند و با
رقم فارسی چاپ شود. رقم لاتین در خروجی نمایشی، ایراد است.

**۲-۴. صداقت عددی — مهم‌ترین قاعده.** این برنامه تصمیم مالی را تغذیه
می‌کند:

- بدون داده معتبر، عدد مالی اختراع نشود.
- قیمت گمشده با قیمت روز قبل یا قیمت مدل جایگزین نشود؛ وضعیت «فاقد داده»
  باید تا خروجی حفظ بماند.
- عددی که ادعای اجراپذیری دارد باید از دفتر سفارش آمده باشد، نه از میانگین
  یا حدس.
- اگر داده نیست، جای خالی نشان بده. جای خالی صادق است؛ عدد ساختگی نیست.

**۲-۵. یک موتور مشترک.** هیچ استراتژی محاسبه‌گر جداگانه ندارد. همه از
`core/` عبور می‌کنند. اگر استراتژی تازه‌ای مسیر محاسبه خودش را می‌خواهد،
یعنی موتور کم دارد — موتور را کامل کنید، نه اینکه دورش بزنید.

**۲-۶. رنگ از توکن.** هیچ رنگ سخت‌کدشده‌ای در جاوااسکریپت نوشته نشود و در
CSS بیرون از بلوک‌های توکن (`:root` و `body[data-theme="board"]`) نیاید.
رنگ از متغیر CSS بیاید تا هر دو پوسته هم‌زمان درست بمانند.
`tests/guards.mjs` سقف فعلی را نگه می‌دارد و اجازه بدترشدن نمی‌دهد.

**۲-۷. آزمون برای هر اصلاح رفتاری.** هر باگ که می‌بندید، یک آزمون رگرسیون
می‌خواهد که بدون اصلاح شما رد شود.

---

## ۳. گردش کار

کار **ترتیبی** است. در هر لحظه فقط یک عامل روی مخزن کار می‌کند. اگر شاخه
باز و ناتمامی هست، اول آن تمام می‌شود.

```text
۱  صاحب پروژه ایده را به یک عامل می‌دهد
۲  عامل از آخرین main شاخه می‌سازد
۳  کار می‌کند، آزمون‌ها را سبز نگه می‌دارد، روی شاخه خودش پوش می‌کند
۴  CI اجرا می‌شود
۵  Claude بازبینی می‌کند: دیف، آزمون‌ها، و تطابق با خواسته
۶  ادغام در main توسط Claude — نه توسط عاملی که کد را نوشته
۷  شاخه بسته می‌شود؛ تسک بعدی شاخه تازه می‌گیرد
```

### نام شاخه

به‌ازای **هر تسک**، نه هر عامل. شاخه پس از ادغام می‌میرد و تسک بعدی شاخهٔ
تازه از `main` می‌گیرد.

شاخهٔ دائمی به‌ازای هر عامل ممنوع است. سه دلیل، به‌ترتیب اهمیت:

- **پایه کهنه می‌شود.** شاخه کامیت ادغام خودش را ندارد؛ تسک بعدی روی نسخهٔ
  قدیمی `main` سوار می‌شود. تنها راه فرار، ریست‌کردن شاخه روی `main` است —
  یعنی همان force-push ممنوع.
- **دامنهٔ بازبینی گم می‌شود.** دیف PR یعنی «هرچه در شاخه هست و در main
  نیست». روی شاخهٔ انباشته، PR تسک دوم شامل تسک اول هم می‌شود.
- **رد کردن یک تسک ممکن نمی‌ماند.** الان PR بد بسته می‌شود و شاخه می‌میرد؛
  `main` اصلاً آن را ندیده. روی شاخهٔ انباشته باید cherry-pick کرد.

```text
ag/<کار>        Antigravity
codex/<کار>     Codex
claude/<کار>    Claude
```

### قواعد صریح

- **پوش مستقیم روی `main` ممنوع است.** بازبینی بعد از ورود به `main` دیگر
  دروازه نیست.
- **هیچ عاملی PR خودش را ادغام نمی‌کند.**
- **هر شاخه از آخرین `main` ساخته می‌شود.**
- **force-push، بازنویسی تاریخچه، و حذف شاخه دیگران ممنوع است.**
- **تغییر سطح دسترسی مخزن ممنوع است.** خصوصی یا عمومی بودنش تصمیم صاحب
  پروژه است، نه عامل.
- **PR باز نکنید مگر صاحب پروژه صریحاً بخواهد.**

### پیام کامیت

دیف می‌گوید **چه** عوض شد. تاریخچه تنها جایی است که می‌تواند بگوید **چرا**.
اگر «چرا» را ننویسید، شش ماه بعد هیچ‌کس — از جمله خودتان — نمی‌داند آن
تصمیم عمدی بوده یا تصادفی. پس بدنه اجباری است.

```text
<نوع>: <موضوع کوتاه، حداکثر ۷۲ نویسه>
                                        ← خط خالی، الزامی
چه عوض شد و چرا. تصمیم‌هایی که بدیهی نیستند را یک جمله توضیح دهید:
چرا این راه، نه راه واضح‌تر.

اگر رفتاری اصلاح شد، به آزمونی که آن را قفل می‌کند اشاره کنید.
```

نوع یکی از این‌هاست:

```text
feat      قابلیت تازه
fix       رفع نقص رفتاری
style     ظاهر و CSS، بدون تغییر رفتار
refactor  بازآرایی بدون تغییر رفتار
perf      کارایی
test      آزمون
docs      مستندات
chore     زیرساخت، CI، پیکربندی
```

یک کامیت = یک تغییر منسجم. اگر مجبورید در بدنه بنویسید «و همچنین»، احتمالاً
باید دو کامیت می‌بود.

نمونهٔ بد و خوب:

```text
✘  style: بهبود جدول

✔  style: کنارگذاشتن ردیف‌های نشان‌دار از راه‌راه جدول

   اولویت CSS در `tr:nth-child(even)` دقیقاً با `tr.atm` برابر است، پس
   نتیجه فقط به ترتیب نوشتن قواعد وابسته بود. با `:not(.atm):not(.picked)`
   صریح شد تا جابه‌جایی قواعد باگ دورهای ۲۴ و ۴۶ را برنگرداند.
```

این قاعده را `tests/commits.mjs` در CI می‌سنجد و فقط کامیت‌های تازهٔ شاخه را
می‌بیند. کامیت ادغام را گیت‌هاب می‌سازد و از سنجش معاف است.

### پیش از هر پوش

```bash
node tests/run.mjs        # باید سبز باشد
node tests/guards.mjs     # باید سبز باشد
node tests/commits.mjs    # باید سبز باشد
```

اگر تغییر رابط کاربری بود، سرور را بالا بیاورید و در مرورگر واقعی ببینید.
ادعای «باید کار کند» کافی نیست.

---

## ۴. حافظه پروژه

- `WORKLOG.md` — دفتر تغییرات. تازه‌ترین بالا. بعد از هر قلم کامل‌شده یک
  ردیف اضافه کنید: چه بود، چرا، کجا عوض شد.
- `TASK_STATUS.md` — وضعیت خواسته‌های کاربر. بعد از تغییر مهم به‌روز شود.
- `AUTOMATION.md` — راهنمای اجرای خودکار ساعتی. آن اجرا در ۱۴۰۵/۰۵/۲۴
  متوقف شده؛ آن فایل تاریخی است و در تعارض با همین فایل، همین فایل معتبر
  است.

---

## ۵. وقتی مطمئن نیستید

- تغییر را کوچک کنید، نه اینکه رهایش کنید.
- اگر داده کافی برای یک عدد مالی ندارید، آن عدد را نسازید.
- اگر خواسته مبهم بود، مفروض خود را در متن PR بنویسید تا بازبین ببیند.
- اگر برای انجام کار مجبورید یکی از قواعد بخش ۲ را بشکنید، نشکنید — بنویسید
  چرا لازم شد.
