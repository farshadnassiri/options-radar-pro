# استودیوی سفر زمانی سبد — نقشه اجرا و تحویل بین عامل‌ها

این فایل نقطه شروع عامل بعدی است. جزئیات تاریخی در `WORKLOG.md` و وضعیت
کل خواسته‌ها در `TASK_STATUS.md` است. قواعد شاخه، آزمون و push در
`AGENTS.md` مقدم‌اند.

## هدف

کاربر با سرمایه معلوم، نماد پایه و لحظه شروع و پایان را انتخاب می‌کند؛
سرمایه قابل تخصیص را بین خانواده‌های استراتژی تقسیم می‌کند؛ از میان
ترکیب‌های واقعاً قابل اجرا سبد می‌سازد؛ مسیر آن را روزانه و درون‌روزی
بازپخش می‌کند؛ در مسیر افزایش، کاهش، آفست و رول انجام می‌دهد؛ و در پایان
پرونده معاملات، سرمایه نهایی، نمودار رشد سرمایه و Excel فشرده می‌گیرد.

## قرارداد مالی

- واحد داخلی پول ریال است؛ تومان فقط نمایش است.
- سرمایه شروع، ذخیره و سرمایه قابل تخصیص سه قلم جدا هستند.
- درصد خانواده‌ها روی سرمایه قابل تخصیص اعمال می‌شود.
- برای موقعیت بستانکار، وجه تضمین و ریسک مبنای تخصیص‌اند، نه پریمیوم.
- قیمت اجرایی فقط از دفتر سفارش همان لحظه می‌آید.
- قیمت معامله یا پایانی می‌تواند mark مشاهده‌ای باشد، نه آفست اجرایی.
- داده گمشده تا خروجی گمشده می‌ماند؛ عدد جایگزین ساخته نمی‌شود.

## ممیزی داده فاز صفر

| خوراک | مسیر فعلی | وضعیت | کار باقی‌مانده |
|---|---|---|---|
| universe تاریخی | `core/watch-archive.mjs` و `/api/history/universe?date=` | fallback امروز بی‌صدا نیست و هشدار سوگیری بقا دارد | ساخت قرارداد کیفیت مشترک برای همه خوراک‌ها |
| روزانه پایه و اختیار | `core/time-gate.mjs` و `ui/bereket-data.mjs` | ردیف آینده بریده می‌شود | حمل کیفیت و منبع به مدل سبد |
| ریزمعامله | `core/time-gate.mjs` و `core/intraday-mark.mjs` | فقط تا ثانیه جاری پذیرفته می‌شود | برچسب کفایت مشترک |
| دفتر سفارش تاریخی | `core/book-history.mjs` و `core/bereket-exec.mjs` | دفتر لحظه بازسازی و صف/نبود مظنه جدا می‌شود | قرارداد کیفیت مشترک |
| وجه تضمین | `core/margin.mjs` و `core/bereket-value.mjs` | مسیر مشترک موجود؛ اسپرد بستانکار در برکت تخمینی است | برچسب تخمینی باید تا سبد و Excel بماند |
| تسویه | مسیر مقرراتی کامل ندارد | ناقص | بدون متن رسمی عدد مالی تازه ساخته نشود |

## وضعیت وزن‌دار

| فاز | وزن | پذیرفته | سهم محقق‌شده از کل | وضعیت |
|---:|---:|---:|---:|---|
| ۰ — ممیزی و اعتماد داده | ۸٪ | ۴ از ۵ | ۶٫۴٪ | قرارداد کیفیت مشترک مانده |
| ۱ — جلسه، سرمایه و دفتر رویداد | ۱۰٪ | ۶ از ۶ | ۱۰٪ | کامل |
| ۲ — فرم مأموریت و تخصیص | ۱۲٪ | ۰ از ۷ | ۰٪ | شروع نشده |
| ۳ — پیشنهاد و اجراپذیری | ۱۸٪ | ۰ از ۱۰ | ۰٪ | شروع نشده |
| ۴ — تشکیل سبد و حسابداری | ۱۲٪ | ۰ از ۷ | ۰٪ | شروع نشده |
| ۵ — بازپخش و معامله در مسیر | ۱۸٪ | ۰ از ۱۳ | ۰٪ | شروع نشده |
| ۶ — پرونده و تحلیل ضعف | ۱۰٪ | ۰ از ۹ | ۰٪ | شروع نشده |
| ۷ — Excel و تداوم سرمایه | ۷٪ | ۰ از ۷ | ۰٪ | شروع نشده |
| ۸ — پذیرش نهایی | ۵٪ | ۰ از ۹ | ۰٪ | شروع نشده |

**پیشرفت کل: ۱۶٫۴٪ — باقی‌مانده: ۸۳٫۶٪.**

درصد بر اساس وظیفه پذیرفته‌شده و آزمون سبز است، نه حجم کد. هر عامل پس از
push همین جدول و درصدها را به‌روز می‌کند.

## آنچه در برش نخست ساخته شد

`core/portfolio-session.mjs` قرارداد خالص زیر را فراهم می‌کند:

- ساخت جلسه با سرمایه، ذخیره، بازه و نماد؛
- تخصیص خانواده‌ها با جمع حداکثر صد درصد؛
- قفل snapshot شروع؛
- شناسه پایدار Session، Portfolio، Position، Transaction، Execution و Lot؛
- دفتر رویداد immutable؛
- ورود، افزایش، کاهش، آفست، رول، تسویه و اعمال؛
- مصرف FIFO lotها؛
- بازسازی وضعیت فقط از رویدادها؛
- پایداری کامل پس از JSON round-trip.

آزمون گروه ۱۱۲ در `tests/run.mjs` این قرارداد را با سناریوی یک میلیارد
تومان، ذخیره بیست درصد و تخصیص ۳۰/۴۰/۳۰ قفل می‌کند.

## نقطه دقیق ادامه برای عامل بعدی

پس از بازبینی و ادغام شاخه برش نخست، از آخرین `main` شاخه تازه بساز:

```text
codex/portfolio-time-data-quality
```

فقط این قلم را انجام بده:

1. یک قرارداد خالص و مشترک کیفیت داده بساز که `source`، `asOf`،
   `observed/executable/estimated/missing`، کفایت، کهنگی و دلیل را حمل کند.
2. آن را ابتدا به خروجی‌های universe تاریخی، دفتر سفارش و daily/intraday
   وصل کن، بدون تغییر عددهای مالی.
3. آزمون کن که کیفیت و هشدار تا snapshot جلسه سبد حفظ می‌شود.
4. فاز صفر را از ۴/۵ به ۵/۵ برسان؛ پیشرفت کل باید ۱۸٪ و باقی‌مانده ۸۲٪ شود.
5. `TASK_STATUS.md`، `WORKLOG.md` و همین فایل را پیش از push به‌روز کن.

بعد از تکمیل این قلم، عامل بعدی باید فاز ۲ را با فرم مأموریت و تخصیص آغاز
کند؛ مستقیم به رابط پیشنهادها نپر، چون آن رابط باید قرارداد کیفیت را
مصرف کند.

## قالب گزارش اجباری هر push

```markdown
### تحویل push

- شاخه و کامیت: ...
- وظایف پذیرفته‌شده این برش: ...
- پیشرفت کل: ...٪
- باقی‌مانده: ...٪
- آزمون‌ها: ...
- مرورگر واقعی: ...
- محدودیت‌های باز: ...
- اولین کار دقیق عامل بعدی: ...
```
