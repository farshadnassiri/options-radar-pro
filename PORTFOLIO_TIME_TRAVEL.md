# استودیوی سفر زمانی سبد — نقشه اجرا و تحویل بین عامل‌ها

این فایل نقطه شروع عامل بعدی است. جزئیات تاریخی در `WORKLOG.md` و وضعیت
کل خواسته‌ها در `TASK_STATUS.md` است. قواعد شاخه، آزمون و push در
`AGENTS.md` مقدم‌اند.

## هدف

کاربر با سرمایه معلوم، نماد پایه و لحظه شروع و پایان را انتخاب می‌کند؛
سرمایه قابل تخصیص را بین خانواده‌های استراتژی تقسیم می‌کند؛ از میان
ترکیب‌های واقعاً قابل اجرا سبد می‌سازد؛ مسیر آن را روزانه و درون‌روزی
بازپخش می‌کند؛ در مسیر افزایش، کاهش، آفست و رول انجام می‌دهد؛ و در پایان
پرونده معاملات، سرمایه نهایی، نمودار رشد سرمایه و Excel فشرده می‌گیرد.

## قرارداد مالی

- واحد داخلی پول ریال است؛ تومان فقط نمایش است.
- سرمایه شروع، ذخیره و سرمایه قابل تخصیص سه قلم جدا هستند.
- درصد خانواده‌ها روی سرمایه قابل تخصیص اعمال می‌شود.
- برای موقعیت بستانکار، وجه تضمین و ریسک مبنای تخصیص‌اند، نه پریمیوم.
- قیمت اجرایی فقط از دفتر سفارش همان لحظه می‌آید.
- قیمت معامله یا پایانی می‌تواند mark مشاهده‌ای باشد، نه آفست اجرایی.
- داده گمشده تا خروجی گمشده می‌ماند؛ عدد جایگزین ساخته نمی‌شود.

## ممیزی داده فاز صفر

| خوراک | مسیر فعلی | وضعیت | کار باقی‌مانده |
|---|---|---|---|
| universe تاریخی | `core/watch-archive.mjs` و `/api/history/universe?date=` | بایگانی همان روز مشاهده‌ای؛ fallback امروز تخمینی، ناکافی و دارای هشدار سوگیری بقاست | کامل |
| روزانه پایه و اختیار | `core/time-gate.mjs` و `ui/bereket-data.mjs` | ردیف آینده بریده و کیفیت، منبع و `asOf` حمل می‌شود | کامل |
| ریزمعامله | `core/time-gate.mjs` و `core/intraday-mark.mjs` | فقط تا ثانیه جاری پذیرفته و کفایت و آخرین مشاهده ثبت می‌شود | کامل |
| دفتر سفارش تاریخی | `core/book-history.mjs` و `core/bereket-exec.mjs` | دفتر لحظه، قابلیت اجرا، کفایت پنج سطح، کهنگی و سلامت جدا ثبت می‌شوند | کامل |
| وجه تضمین | `core/margin.mjs` و `core/bereket-value.mjs` | قرارداد `estimated` برای حمل برآورد تا snapshot و Excel آماده است | کامل در ممیزی؛ اتصال عددی در فاز حسابداری |
| تسویه | مسیر مقرراتی کامل ندارد | ناقص | بدون متن رسمی عدد مالی تازه ساخته نشود |

## وضعیت وزن‌دار

| فاز | وزن | پذیرفته | سهم محقق‌شده از کل | وضعیت |
|---:|---:|---:|---:|---|
| ۰ — ممیزی و اعتماد داده | ۶٫۴٪ | ۵ از ۵ | ۶٫۴٪ | کامل |
| ۱ — جلسه، سرمایه و دفتر رویداد | ۸٪ | ۶ از ۶ | ۸٪ | کامل |
| ۲ — فرم مأموریت و تخصیص | ۹٫۶٪ | ۷ از ۷ | ۹٫۶٪ | کامل — یک محدودیت راستی‌آزمایی ثبت شده |
| ۳ — پیشنهاد و اجراپذیری | ۱۴٫۴٪ | ۱۰ از ۱۰ | ۱۴٫۴٪ | کامل — طرح انتخاب‌شده در دفتر رویداد ثبت می‌شود |
| ۴ — تشکیل سبد و حسابداری | ۹٫۶٪ | ۷ از ۷ | ۹٫۶٪ | کامل — ورود، خروج و حسابداری جلسه |
| ۵ — بازپخش و معامله در مسیر | ۱۴٫۴٪ | ۱۳ از ۱۳ | ۱۴٫۴٪ | کامل — بازپخش، معامله در مسیر و پروندهٔ پایان |
| ۶ — پرونده و تحلیل ضعف | ۸٪ | ۹ از ۹ | ۸٪ | کامل — دو پرونده پیاپی با تغییرهای مستند مقایسه می‌شوند |
| ۷ — Excel و تداوم سرمایه | ۵٫۶٪ | ۷ از ۷ | ۵٫۶٪ | کامل — کارت و Excel یک روند قطعی چندجلسه‌ای دارند |
| ۸ — پذیرش نهایی | ۴٪ | ۹ از ۹ | ۴٪ | کامل — سفر یکپارچه و پذیرش مرورگر واقعی دسکتاپ انجام شد |
| ۹ — تایم‌لاین معامله‌پذیر و تب‌های تحلیل | ۲۰٪ | ۱ از ۷ | ۲٫۹٪ | در جریان — موتور سری زمانی سبز است |

**پیشرفت کل: ۸۲٫۹٪ — باقی‌مانده: ۱۷٫۱٪.**

درصد بر اساس وظیفه پذیرفته‌شده و آزمون سبز است، نه حجم کد. هر عامل پس از
push همین جدول و درصدها را به‌روز می‌کند.

## آنچه در برش نخست ساخته شد

`core/portfolio-session.mjs` قرارداد خالص زیر را فراهم می‌کند:

- ساخت جلسه با سرمایه، ذخیره، بازه و نماد؛
- تخصیص خانواده‌ها با جمع حداکثر صد درصد؛
- قفل snapshot شروع؛
- شناسه پایدار Session، Portfolio، Position، Transaction، Execution و Lot؛
- دفتر رویداد immutable؛
- ورود، افزایش، کاهش، آفست، رول، تسویه و اعمال؛
- مصرف FIFO lotها؛
- بازسازی وضعیت فقط از رویدادها؛
- پایداری کامل پس از JSON round-trip.

آزمون گروه ۱۱۲ در `tests/run.mjs` این قرارداد را با سناریوی یک میلیارد
تومان، ذخیره بیست درصد و تخصیص ۳۰/۴۰/۳۰ قفل می‌کند.

## آنچه در برش کیفیت داده ساخته شد

`core/data-quality.mjs` چهار نوع انحصاری `observed`، `executable`،
`estimated` و `missing` را همراه `source`، `asOf`، کفایت، کهنگی، دلیل و
جزئیات فراهم می‌کند. تجمیع snapshot همیشه ضعیف‌ترین خوراک را آشکار نگه
می‌دارد. این قرارداد به universe تاریخی، daily، intraday و دفتر سفارش
وصل شده و `activatePortfolioSession` نسخه مستقل آن و هشدارها را در عکس
شروع قفل می‌کند. هیچ قیمت یا عدد مالی در این برش تغییر نکرده است.

آزمون گروه ۱۱۳ با ۲۳ پذیرش، پرچم‌ها، تجمیع، بایگانی و fallback، کهنگی
دفتر، اتصال واقعی دروازه زمان و ماندگاری پس از JSON round-trip را قفل
می‌کند.

## آنچه در برش قرارداد مأموریت ساخته شد

`core/portfolio-mission.mjs` ورودی‌های هدف، مبنای بازده، حداکثر نگهداری،
تایم‌فریم بازپخش، دید جهت و تلاطم، قیمت هدف/بازه، اطمینان و دلیل کاربر،
سقف زیان و افت، سرمایه آزاد، مصرف وجه تضمین و قیود نقدشوندگی را یک‌جا
اعتبارسنجی می‌کند. سرمایه، نماد و بازه فقط از session می‌آیند و فرم حق
جعل آن‌ها را ندارد. درصد نامعتبر clamp نمی‌شود و قید گمشده با پیش‌فرض
مالی پر نمی‌شود.

`setPortfolioMission` فقط در پیش‌نویس اجازه تغییر می‌دهد و فعال‌سازی جلسه
بدون مأموریت رد می‌شود. نسخه قفل‌شده مستقل از نسخه کاری و پس از JSON
round-trip بازتولیدپذیر است. گروه آزمون ۱۱۴ با ۳۵ پذیرش این قرارداد را
قفل می‌کند.

## آنچه در برش پوسته رابط ساخته شد

تب lazy و مستقل «استودیوی سفر زمانی سبد» مرحله نخست مسیر را در یک نمای
بصری و واکنش‌گرا باز می‌کند. کاربر سرمایه و ذخیره را به تومان، نماد پایه،
روز و ساعت شروع و پایان و تایم‌فریم فیلم سبد را می‌بیند؛ خلاصه سرمایه
کل، ذخیره و قابل تخصیص و «گذرنامه سفر» نیز هم‌زمان تازه می‌شوند. تاریخ‌ها
از همان تاریخچه نماد و تقویم جلالی مشترک برنامه می‌آیند و نام نماد و زمان
واقعی پنهان نمی‌شوند.

`ui/portfolio-mission-form.mjs` فقط مرز نمایش تومان را به قرارداد ریالی
session وصل می‌کند؛ منطق مالی موازی ندارد. ثبت مرحله، draft واقعی و
درون‌حافظه‌ای می‌سازد اما مأموریت را فعال نمی‌کند، چیزی ذخیره نمی‌کند و
هنوز پیشنهاد استراتژی نمی‌سازد. گروه آزمون ۱۱۵ با ۲۳ پذیرش و آزمون واقعی
دسکتاپ و موبایل، پیام خطای کنار ورودی، تاریخ جلالی، اتصال داده و نبود نشت
به مراحل آینده را قفل می‌کند.

## آنچه در برش انتظار بازار ساخته شد

مرحله دوم پس از ثبت معتبر مرحله نخست باز می‌شود و چهار کارت جهت بازار،
قیمت هدف و بازه به تومان، سه دید تلاطم، درصد تلاطم مورد انتظار، اسلایدر
اطمینان و متن دلیل کاربر را می‌گیرد. خط سناریوی کوچک پایین/هدف/بالا را
همان‌طور که کاربر نوشته نمایش می‌دهد و صریح می‌گوید این اعداد مشاهده یا
پیش‌بینی سیستم نیستند.

`validateMissionOutlook` از قرارداد کامل جدا و در همان هسته export شده تا
فرم برای اعتبارسنجی مرحله‌ای مجبور به ساخت هدف، ریسک یا نقدشوندگی پنهان
نباشد. آداپتر فقط مرز تومان و درصد فارسی را تبدیل و outlook معتبر را به
همان draft مرحله نخست اضافه می‌کند. تغییر هر ورودی مرحله نخست، draft
outlook را باطل و مرحله دوم را دوباره قفل می‌کند. گروه آزمون ۱۱۶ با ۲۰
پذیرش و مرورگر واقعی دسکتاپ و موبایل این مرزها را قفل کرده است.

## آنچه در برش ریسک و نقدشوندگی ساخته شد

مرحله سوم فقط پس از ثبت انتظار بازار باز می‌شود و همه مرزها را صریح از
کاربر می‌گیرد: سقف زیان معامله و افت کل، حداقل سرمایه آزاد، سقف مصرف وجه
تضمین، اجازه ریسک نامحدود و شش قید اجرای واقعی. هیچ radio یا عدد مالی
پیش‌فرض نیست. نقشه درگیری سرمایه، حداقل آزاد، سقف تضمین و بخش انعطاف‌پذیر
را به درصد و تومان از سرمایه قابل تخصیص نشان می‌دهد؛ این تصویر مرز کاربر
است، نه پیشنهاد تخصیص سیستم.

`validateMissionRisk` و `validateMissionLiquidity` از قرارداد کامل جدا و
در همان هسته export شده‌اند. `portfolioMissionRiskBudget` نیز فقط اعداد
معتبر هسته را به بودجه ریالی تبدیل می‌کند. آداپتر رابط رقم فارسی و تومان
را تبدیل، draft قبلی را حفظ و فقط risk/liquidity معتبر را اضافه می‌کند؛
فعال‌سازی، snapshot، پیشنهاد و ذخیره مرورگر همچنان بیرون این برش‌اند.
تغییر مرحله اول یا outlook، draft ریسک کهنه را باطل می‌کند. گروه آزمون
۱۱۷ با ۲۲ پذیرش و مرورگر واقعی در ۱۴۴۰×۱۰۰۰ و ۳۹۰×۸۴۴ این مسیر را قفل
کرده است.

## آنچه در برش تخصیص خانواده‌ها ساخته شد

مرحله چهارم فقط پس از ثبت مرزهای ریسک و اجرا باز می‌شود. هر ردیف یک
خانواده صریح از کاتالوگ و درصد سرمایه قابل تخصیص می‌گیرد و افزودن، حذف و
ویرایش ردیف‌ها بدون خانواده یا درصد پیش‌فرض ممکن است. جمع درصدها، فاصله تا
صد، بودجه هر ردیف، بودجه تخصیص‌یافته و سرمایه تخصیص‌نیافته زنده دیده
می‌شوند؛ باقیمانده بی‌صدا پخش نمی‌شود و جمع بیشتر از صد صریح رد می‌شود.

`previewPortfolioAllocations` و `createPortfolioAllocationDraft` فقط مرز
رقم فارسی و نمایش را به `setFamilyAllocations` و `portfolioCapitalPlan`
وصل می‌کنند؛ محاسبه مالی موازی در رابط وجود ندارد. تغییر setup، outlook یا
risk/liquidity تخصیص کهنه را پاک و مرحله چهارم را دوباره قفل می‌کند.
مأموریت، snapshot، پیشنهاد، سبد و localStorage همچنان بیرون این برش‌اند.
گروه آزمون ۱۱۸ با ۱۷ پذیرش و مرورگر داخلی واقعی در ۱۴۴۰×۱۰۰۰ و ۳۹۰×۸۴۴،
بودجه ۶۰۰/۳۰۰ میلیون تومان، مانده ۱۰۰ میلیون، رد ۱۱۰٪، ابطال بالادستی و
کنسول بدون هشدار و خطا را قفل کرده است.

## آنچه در برش مرور و قفل شروع ساخته شد

مرحله پنجم همه تصمیم‌ها را در پنج ایستگاه زمان/سرمایه، انتظار، ریسک/اجرا،
تخصیص و هدف/افق کنار هم می‌گذارد و برای هر ایستگاه مسیر ویرایش دارد. هدف
مأموریت، مبنای بازده، درصد بازده هدف و حداکثر روز نگهداری هیچ مقدار
پیش‌فرضی ندارند. تبدیل رقم فارسی فقط ورودی خام هسته را می‌سازد؛ ترتیب و
متن خطاها از `createPortfolioMission` می‌آید و نسخه معتبر با
`setPortfolioMission` قفل می‌شود.

عکس شروع از `createTimeGate` و بارگذارهای تاریخی daily، intraday و book و
universe تاریخ‌دار ساخته می‌شود. `activatePortfolioSession` لحظه، مدرک
کیفیت و دلیل هر خوراک ناقص را مستقل قفل می‌کند. نبود بایگانی همان روز،
ریزمعامله یا دفتر سفارش با قیمت دیگری پر نمی‌شود و به‌صورت «ناکافی/فاقد
داده» دیده می‌شود. پس از قفل، کنترل‌ها غیرفعال‌اند و پیشنهاد یا معامله‌ای
هنوز ساخته نشده است. گروه آزمون ۱۱۹ با ۲۰ پذیرش و جریان واقعی دسکتاپ و
موبایل، خطای کنار فیلد، مانده نقد، قفل immutable، چهار دلیل کیفیت، نبود
اسکرول افقی و کنسول پاک را قفل کرده است.

### هفت پذیرش فاز ۲

| ردیف | پذیرش | وضعیت |
|---:|---|---|
| ۱ | قرارداد خالص و immutable مأموریت و اعتبارسنجی همه ورودی‌ها | کامل |
| ۲ | پوسته تب مستقل و مرحله سرمایه، نماد، شروع، پایان و تایم‌فریم | کامل |
| ۳ | مرحله بصری انتظار بازار، قیمت/بازه، تلاطم، اطمینان و دلیل | کامل |
| ۴ | مرحله قیود ریسک، وجه تضمین، سرمایه آزاد و نقدشوندگی | کامل |
| ۵ | ویرایشگر تخصیص خانواده‌ها با جمع و بودجه ریالی زنده | کامل |
| ۶ | مرور نهایی، نمایش خطاها و قفل مأموریت و snapshot شروع | کامل |
| ۷ | ذخیره/ادامه پیش‌نویس و پذیرش کامل در مرورگر واقعی | کامل، با محدودیت زیر |

ردیف ۷ ساخته و آزموده شد: هر پنج مرحله روی سرور ثبت می‌شوند،
`ui/portfolio-mission-resume.mjs` ورودی‌های صریح را از رکورد ذخیره‌شده
بازمی‌سازد و دادهٔ ناقص یا نسخهٔ ناشناخته را صریح رد می‌کند، قفل خوش‌بینانه
تعارض دو تب را می‌گیرد، و جلسهٔ فعال فقط‌خواندنی باز می‌شود. دستهٔ آزمون
`tests/suites/122-portfolio-mission-resume.mjs` با ۵۲ ادعا این‌ها را
می‌بندد، و رفت‌وبرگشت کامل PUT/GET/تعارض روی سرور واقعی اجرا شد.

**محدودیت باز.** پیمودن هر پنج مرحله در مرورگر با دادهٔ واقعی در این
محیط ممکن نشد: `data/` تاریخچه‌ای ندارد، پس انتخابگر نماد و تقویم روزهای
معاملاتی خالی‌اند و wizard از مرحلهٔ یک جلوتر نمی‌رود. آنچه در مرورگر
واقعی (Chromium، دسکتاپ ۱۴۰۰×۹۰۰ و موبایل ۳۹۰×۸۴۴) دیده شد: پنل ادامه
سوار می‌شود، فهرست جلسه‌ها از سرور خوانده و با رقم فارسی نشان داده
می‌شود، سرریز افقی ندارد و کنسول خطا نمی‌دهد. پیمودن پنج مرحله و
refresh وسط کار، روی نصبی با تاریخچهٔ واقعی باید یک بار تأیید شود.

## آنچه در برش ذخیره‌سازی سرور ساخته شد

`server/portfolio-mission-store.mjs` رکورد هر مأموریت را با نسخه ساختار،
شناسه جلسه، زمان ثبت سرور و آخرین مرحله معتبر نگه می‌دارد. هر مرحله پیش
از نوشتن دوباره از قراردادهای هسته ساخته و اعتبارسنجی می‌شود؛ داده ناقص،
نسخه ناشناخته، طرح سرمایه دستکاری‌شده و snapshot ناسازگار به فایل سالم
تبدیل نمی‌شوند. نوشتن اتمیک است و فایل خراب در فهرست پنهان نمی‌شود.

`/api/portfolio/sessions` فهرست و `/api/portfolio/session?id=...` بازیابی
و ذخیره را ارائه می‌دهند. `expectedSavedAt` نوشتن کهنه را با تعارض صریح
متوقف می‌کند. پس از فعال‌شدن، جلسه به draft برنمی‌گردد و مأموریت، تخصیص و
عکس شروع قفل‌شده قابل بازنویسی نیستند. حذف جلسه و `localStorage` منبع حقیقت
در این قرارداد وجود ندارند. گروه آزمون ۱۲۰ با ۲۱ پذیرش round-trip فایل،
تعارض و immutability را قفل کرده است.

## آنچه در برش دروازه اجراپذیری ساخته شد

`core/portfolio-eligible.mjs` نخستین قلم فاز ۳ است و پیش از ساخت هر
پیشنهاد، هر قرارداد را با مأموریت قفل‌شده حکم می‌دهد: پذیرفته، یا ردشده با
کد و علت فارسی. کف ارزش روزانه پایه و اختیار، موقعیت باز، سقف اسپرد، سقف
مصرف عمق و الزام پنج سطح همگی مستقل اعمال می‌شوند. کیفیت گمشده، تخمینی یا
کهنه تا حکم می‌ماند و داده یا قرارداد بعد از لحظه جاری پیش از خواندن عدد
بازار رد می‌شود.

حجم قابل اجرا فقط با `bookCapacity` و `walkBook` از دفتر واقعی ساخته
می‌شود. درصد مصرف عمق روی تک‌تک سطوح می‌نشیند؛ دفتر نبود یا نامعتبر حجم
`null` می‌دهد، نه صفر یا تخمین. دسته آزمون ۱۲۳ با ۲۰ ادعا این مرز و نبود
DOM/شبکه را قفل کرده است. این برش رابط، رتبه‌بندی و پیشنهاد استراتژی ندارد.

## آنچه در برش مدرک اجراپذیری رابط ساخته شد

`ui/portfolio-eligibility.mjs` فقط جلسهٔ فعال، مأموریت قفل‌شده و
`startSnapshot` دقیقاً هم‌لحظه با شروع را می‌پذیرد. ردیف universe برای دو
سمت صریح خرید و فروش به موتور داده می‌شود؛ داده مالی، کیفیت و دفتر گمشده
پر نمی‌شوند و صفرهای ساختاری بایگانی نیز مشاهدهٔ بازار حساب نمی‌شوند.

تب سفر زمانی حکم هر قرارداد را با نام، سمت، همه علت‌های رد، کیفیت و سقف
اجرای واقعی نشان می‌دهد. حجم `null` خط تیره می‌ماند و فیلتر
همه/پذیرفته/ردشده فقط ردیف‌های نمایشی را عوض می‌کند. گروه آزمون ۱۲۴ با ۱۲
پذیرش و مرورگر داخلی در دو viewport این مرز را قفل کرده است. ساخت ترکیب،
قیمت‌گذاری و رتبه‌بندی همچنان بیرون این برش‌اند.

## آنچه در برش ترکیب خام قابل اجرا ساخته شد

`core/portfolio-candidates.mjs` فقط جلسه فعال، عکس قفل‌شده هم‌لحظه،
تعریف‌های واقعی کاتالوگ و خانواده‌های دارای بودجه مثبت را می‌پذیرد. هویت
قرارداد شامل شناسه، نوع، اعمال، سررسید و اندازه فقط از snapshot می‌آید؛
ردیف ناقص حذف و در ممیزی نام برده می‌شود. هر پای اختیار نیز باید برای همان
شناسه و همان سمت حکم پذیرفته داشته باشد.

ترکیب‌سازی همچنان از `generateCandidates` مشترک می‌آید. callback عمومی
`contractAllowed` اجازهٔ هر قرارداد/سمت را پیش از کامل‌شدن پاها می‌سنجد،
پس سقف هر تعریف، سقف کل و شناسه پایدار حفظ می‌شوند و الگوریتم موازی ساخته
نشده است. خروجی شناسه تعریف، خانواده، بودجه، پاها و ارجاع حکم هر پا را
نگه می‌دارد؛ تعریف بی‌بودجه، قرارداد ناقص، پای ردشده و نبود ترکیب ساختاری
علت ممیزی مستقل دارند. گروه آزمون ۱۲۵ با ۱۷ پذیرش این مرز را قفل کرده است.
قیمت‌گذاری، امتیاز، رتبه و تشکیل سبد هنوز بیرون این برش‌اند.

## آنچه در برش ظرفیت و قیمت اجرایی ورود ساخته شد

`core/portfolio-entry.mjs` فقط شناسهٔ ترکیبی را می‌پذیرد که در خروجی
نسخه‌دار `portfolioCandidates` همان جلسه و همان لحظه موجود باشد. هویت،
نوع، اعمال، سررسید و اندازه هر پا دوباره با snapshot تطبیق داده می‌شوند و
حکم پذیرفتهٔ همان قرارداد و سمت باید هنوز با ظرفیت دفتر snapshot یکسان
باشد؛ ترکیب دست‌ساز یا حکم ناهم‌لحظه قیمت نمی‌گیرد.

ظرفیت کل ساختار کمینهٔ ظرفیت واقعی پاها پس از تقسیم بر نسبت است. دفتر هر
پا برای همان حجم مشترک و سقف مصرف عمق مأموریت دوباره پیموده می‌شود؛ خرید
فقط ask و فروش فقط bid را مصرف می‌کند. خروجی filled، VWAP، top، شمار سطح،
کیفیت و ارجاع حکم را نگه می‌دارد و جمع نقد ورود از `grossCash` مشترک در
ریال می‌آید. نبود دفتر یا ظرفیت با آخرین، پایانی یا میانه پر نمی‌شود.

بودجه قفل‌شده خانواده حجم دفتر را پنهانی کوچک نمی‌کند: نیاز نقدی ظرفیت
کامل، عبور از بودجه و سقف بودجه بر مبنای همان VWAP جدا گزارش می‌شوند.
گروه آزمون ۱۲۶ با ۲۳ پذیرش این مرز را قفل کرده است؛ امتیاز، رتبه، UI و ثبت
موقعیت همچنان بیرون‌اند.

## آنچه در برش سرمایه لازم طرح ورود ساخته شد

`core/portfolio-capital.mjs` طرح ورود را صرفاً با اعتماد به شکل JSON
نمی‌پذیرد: همان شناسه را از جلسه، خروجی ترکیب و حکم هم‌لحظه دوباره با
`portfolioEntryPlan` می‌سازد و نتیجه باید عین طرح ورودی باشد. بنابراین
طرح دست‌ساز، ناهم‌لحظه، متعلق به جلسه دیگر یا دارای پای ناقص سرمایه
نمی‌گیرد.

بدهکار واقعی، وجه تضمین و کارمزد ورود سه جزء مستقل ریالی‌اند. کارمزد فقط
از نرخ صریح `capitalInputs.fees` همان snapshot و `entryFees` مشترک می‌آید؛
نبود نرخ با صفر پر نمی‌شود. پای فروش اختیار نیز قیمت پایانی، پارامترهای
مقرراتی و سیاست صریح همان snapshot را به `strategyMargin` می‌دهد. سرمایه
ساختار بستانکار از وجه تضمین ناخالص می‌آید تا پریمیوم دریافتی آن را تا
صفر محو نکند.

کیفیت اجرای پاها، نرخ کارمزد، پارامتر تضمین و پایانی پاهای فروش تجمیع
می‌شود؛ `estimated` و علتش تا خروجی می‌ماند و خوراک گمشده جمع سرمایه را
`null` نگه می‌دارد. بودجه خانواده بر جمع سرمایه سنجیده می‌شود و سقف حجم
بودجه جداست، بی‌آنکه `executableQty` دفتر کوچک شود. گروه آزمون ۱۲۷ با ۱۹
پذیرش این قرارداد را قفل کرده است.

## آنچه در مقایسهٔ دو پروندهٔ پیاپی ساخته شد

`core/portfolio-dossier-compare.mjs` دو پرونده معتبر را فقط با ترتیب
قدیمی به جدید می‌پذیرد و تحلیل سرمایه و یافته‌های مستند موجود را مصرف
می‌کند. بازده تحقق‌یافته، فاصله درصدی هدف و شمار هر شدت برای هر طرف و
دلتا جدا می‌مانند؛ نبود عدد در هر طرف دلتا را `null` می‌کند و صفر جای آن
نمی‌نشیند.

یافته‌های افزوده، دیگر ثبت‌نشده و مشترک فقط با کد پایدار مقایسه می‌شوند؛
تغییر عنوان یا ترتیب، یافته تازه نمی‌سازد. تب نزدیک‌ترین خلاصه قدیمی‌تر
را پیدا و پرونده کاملش را از مرز داده می‌خواند. نبود یا خطای آن پیام مستقل
دارد و پرونده فعلی را پس نمی‌زند. کارت رقم فارسی، درصد، هویت هر دو پرونده
و چیدمان یک‌ستونه موبایل دارد. گروه آزمون ۱۶۱ با ۲۲ پذیرش این مرز را قفل
کرده است.

## آنچه در قرارداد دادهٔ Excel پرونده ساخته شد

`ui/portfolio-dossier-export.mjs` جلسه بسته و پرونده هم‌هویت را به توصیف
نسخه‌دار برگ‌های `xlsx` تبدیل می‌کند، بی‌آنکه فایل دانلود یا DOM لمس شود.
سرشناسه، مأموریت و تخصیص قفل‌شده، سرمایه، حسابداری، تحقق‌یافته‌ها، تعهدهای
باز، هشدارها و یافته‌های مستند برگ‌های جدا دارند. پول عدد خام ریال و درصد
عدد خام است؛ نبود داده خانه `NaN` می‌سازد تا نویسنده xlsx آن را خالی
بگذارد و علت در ستون مجاور بماند. ردیف‌های بلند از `sheetParts` موجود
می‌گذرند و کدهای ماشینی حفظ می‌شوند. گروه آزمون ۱۶۲ با ۱۷ پذیرش این
قرارداد را قفل کرده است.

## آنچه در گزارش چندجلسه‌ای Excel ساخته شد

پرونده‌ای که continuity کامل دارد یک برگ «روند سرمایه» از مدل مشترک
`portfolioCapitalGrowth` می‌گیرد. هر ردیف همان ترتیب lineage را با نسخه
مدل، شناسه جلسه/سبد/نماد، لحظه بستن، سرمایه شروع/تحقق‌یافته/نهایی، تغییر
سفر و تجمعی و کد پایدار `growth`/`decline`/`flat` نگه می‌دارد. درصد
نامعلوم خالی و علت سرمایه شروع صفر در ستون مجاور است. exporter هیچ سرمایه
یا بازدهی را دوباره حساب نمی‌کند؛ زنجیره خراب یا متعلق به پرونده دیگر پیش
از ساخت blob رد می‌شود و نبود قرارداد، خروجی قبلی را معتبر نگه می‌دارد.
گروه آزمون ۱۶۹ با ۸ پذیرش و خواندن فایل واقعی xlsx این قرارداد را قفل کرده
است.

## نقطه دقیق ادامه برای عامل بعدی

**کارِ بعدی در `NEXT.md` است، نه اینجا.** از ۱۴۰۵/۰۶/۰۴ آن فایل تنها
مرجع کار بعدی است و همان‌جا شاخه، فایل‌ها، دستهٔ آزمون و بندهای پذیرش
نوشته می‌شوند.

چرا عوض شد: کارِ بعدی در دو جا نوشته می‌شد و هر بار باید هر دو به‌روز
می‌شدند. یکی‌شان جا می‌ماند و عامل بعدی نمی‌دانست کدام معتبر است. این
فایل از این به بعد نقشهٔ فازهاست — چه چیزی باید ساخته شود و به چه ترتیب —
و `NEXT.md` می‌گوید همین حالا کدام قلم روی میز است.

کار همچنان **ترتیبی** است: در هر لحظه یک عامل، یک قلم. عاملی که قلمی را
تمام می‌کند، `NEXT.md` را برای نفر بعد بازمی‌نویسد.

فاز جاری: **۸ — پذیرش نهایی** (۹ از ۹، کامل). آزمون یکپارچهٔ ۱۷۸ سفر را
از سرمایه یک میلیارد تومان و تخصیص ۳۰/۴۰/۳۰ تا تشکیل موقعیت، تغییر حجم،
آفست FIFO، رتبه نهایی، Excel فشرده و انتقال سرمایه قطعی به جلسه بعد قفل
می‌کند. پذیرش مرورگر واقعی نیز در نمای دسکتاپ انجام شد: پنج مرحله فرم تا
قفل مأموریت پیموده شد، چیدمان پس از جمع‌کردن پنل ناوبری سرریز افقی نداشت،
ورودی بی‌برچسب و خطای کنسول دیده نشد. خوراک واقعی لحظه انتخاب ۲٬۵۹۲ حکم
ردشده و صفر گزینه پذیرفته داشت؛ رابط همین کمبود را بدون ساختن پیشنهاد یا
عدد جایگزین اعلام کرد. با دستور صاحب پروژه، موبایل و تبلت در این پذیرش
نهایی سنجیده نشدند.

## قالب گزارش اجباری هر push

```markdown
### تحویل push

- شاخه و کامیت: ...
- وظایف پذیرفته‌شده این برش: ...
- پیشرفت کل: ...٪
- باقی‌مانده: ...٪
- آزمون‌ها: ...
- مرورگر واقعی: ...
- محدودیت‌های باز: ...
- اولین کار دقیق عامل بعدی: ...
```
